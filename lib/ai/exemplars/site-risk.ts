// lib/ai/exemplars/site-risk.ts
// 888 Safety Solutions — Phase 7 AI Report Pipeline
//
// STUB — blocked on completed Site Risk example from Matt
// (07-CONTEXT §deferred "Site Risk variant exemplar"). Populate when
// available; the consumer in lib/ai/prompt-builder.ts will pick the correct
// exemplar by template type. Stub keeps the import surface stable so the
// next pass only needs to replace `null` with a JSON-stringified literal
// matching `reportSchema` (executiveSummary / hazards[] / complianceStatus).
//
// Provenance: REPORT-04 (deferred per 07-CONTEXT §decisions D-02).
// Pure data module, no imports, no side effects.

export const SITE_RISK_EXEMPLAR: string | null = null;
