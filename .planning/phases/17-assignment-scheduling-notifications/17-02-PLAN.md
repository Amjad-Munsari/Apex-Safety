---
phase: 17
slug: assignment-scheduling-notifications
plan: "02"
plan_id: 17-02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - lib/scheduler/generate-next-occurrence.ts
  - lib/scheduler/send-reminder.ts
  - tests/scheduler/recurrence-generator.spec.ts
  - tests/scheduler/send-reminder.spec.ts
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
tags: [scheduler, recurrence, n8n, vitest, multi-tenancy, pure-functions]
must_haves:
  truths:
    - "lib/scheduler/generate-next-occurrence.ts exports an async generateNextOccurrence(supabase, src) returning a discriminated result type ({ok:true, newAssignmentId} | {ok:false, reason})."
    - "generateNextOccurrence re-pins the new occurrence to the LATEST PUBLISHED template_versions row of the source template_id (RESEARCH §Q5 — same chain as app/client/templates/[id]/fill/page.tsx:51-58)."
    - "generateNextOccurrence inherits client_id, template_id, assigned_by, instructions, recurrence_rule from the source row — server-side carryover only, never from a payload (T-16-04 inheritance, RESEARCH §Pattern 2 + §Example 2)."
    - "generateNextOccurrence computes the new due_date by adding the frequency vocab unit to the SOURCE row's due_date (not today) — weekly=+7d, monthly=+1M, quarterly=+3M, annually=+1Y."
    - "lib/scheduler/send-reminder.ts exports sendAssignmentReminder(args) building an absolute assignment_url from NEXT_PUBLIC_SITE_URL → VERCEL_URL → empty-string fallback (RESEARCH §Pattern 5)."
    - "sendAssignmentReminder calls dispatchNotification with type:'assignment_reminder' and returns the dispatcher's result verbatim — never swallows ok:false."
    - "tests/scheduler/recurrence-generator.spec.ts has 6 passing assertions covering all behaviors named in Plan 17-01 Task 3 scaffold."
    - "tests/scheduler/send-reminder.spec.ts has 3 passing assertions covering all behaviors named in Plan 17-01 Task 3 scaffold."
  artifacts:
    - path: lib/scheduler/generate-next-occurrence.ts
      provides: "Pure async generator for the next recurrence occurrence; consumed by the cron handler (Plan 17-04 PASS B) AND the inline trigger inside submitAssignedFillByIdAction (also Plan 17-04)"
      exports: ["generateNextOccurrence", "Frequency", "RecurrenceRule"]
    - path: lib/scheduler/send-reminder.ts
      provides: "Thin wrapper around dispatchNotification for the assignment_reminder cadence; consumed by the cron handler PASS A (Plan 17-04)"
      exports: ["sendAssignmentReminder"]
    - path: tests/scheduler/recurrence-generator.spec.ts
      provides: "Filled spec replacing Plan 17-01 scaffold; 6 assertions"
      contains: "generateNextOccurrence"
    - path: tests/scheduler/send-reminder.spec.ts
      provides: "Filled spec replacing Plan 17-01 scaffold; 3 assertions"
      contains: "sendAssignmentReminder"
  key_links:
    - from: lib/scheduler/generate-next-occurrence.ts
      to: "supabase.from('template_versions') — latest published lookup"
      via: ".eq('template_id', src.template_id).not('published_at','is',null).order('version_number',{ascending:false}).limit(1).maybeSingle()"
      pattern: "maybeSingle"
    - from: lib/scheduler/send-reminder.ts
      to: lib/notifications/n8n-dispatch.ts dispatchNotification
      via: "import { dispatchNotification } from '@/lib/notifications/n8n-dispatch'"
      pattern: "from\\s+[\"']@/lib/notifications/n8n-dispatch[\"']"
    - from: tests/scheduler/recurrence-generator.spec.ts
      to: lib/scheduler/generate-next-occurrence.ts
      via: "import { generateNextOccurrence } from '@/lib/scheduler/generate-next-occurrence'"
      pattern: "from\\s+[\"']@/lib/scheduler/generate-next-occurrence[\"']"
---

<objective>
Implement the two pure scheduler functions Phase 17's cron handler depends on, and fill the Plan 17-01 scaffolds with real assertions. This plan delivers:

1. `lib/scheduler/generate-next-occurrence.ts` — pure async function taking a Supabase client + a completed-assignment source row, returning either `{ok:true, newAssignmentId}` or `{ok:false, reason}`. Used by Plan 17-04's cron PASS B AND the inline trigger inside `submitAssignedFillByIdAction`. RESEARCH §Example 2 is the byte-faithful reference implementation.
2. `lib/scheduler/send-reminder.ts` — thin wrapper around `dispatchNotification` that builds the absolute `assignment_url` (per RESEARCH §Pattern 5) and forwards the cadence payload. Returns the dispatcher's result verbatim so the cron can detect ok:false and NOT update `last_reminder_sent` (RESEARCH §Pattern 4).
3. Filled `tests/scheduler/recurrence-generator.spec.ts` — 6 assertions replacing the 6 todos from Plan 17-01.
4. Filled `tests/scheduler/send-reminder.spec.ts` — 3 assertions replacing the 3 todos from Plan 17-01.

Purpose: separating these from the cron route handler keeps them unit-testable without an HTTP harness (RESEARCH §Pattern 2). Plan 17-04 imports both functions and glues them into the GET handler.

Output: Two new lib files + two filled specs. Zero changes to migrations, n8n-dispatch, the cron route, or any UI surface.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/17-assignment-scheduling-notifications/17-CONTEXT.md
@.planning/phases/17-assignment-scheduling-notifications/17-RESEARCH.md
@.planning/phases/17-assignment-scheduling-notifications/17-01-PLAN.md

<interfaces>
Existing references this plan consumes (from Plan 17-01 + the codebase):

From lib/notifications/n8n-dispatch.ts (after Plan 17-01 union extension), the new variant is:
- type: "assignment_reminder"
- cadence: "7d" | "1d" | "overdue"
- client_email: string, client_name: string
- template_name: string
- due_date: string (ISO yyyy-mm-dd)
- assignment_url: string (absolute)
- instructions: string | null

The dispatcher signature is unchanged: `dispatchNotification(payload: NotificationPayload): Promise<DispatchResult>` where DispatchResult is `{ ok: boolean; status?: number; error?: string }`.

Latest-published lookup canonical chain (RESEARCH §Q5, verified against `app/client/templates/[id]/fill/page.tsx:52-59`):
- `.from("template_versions").select("id").eq("template_id", X).not("published_at", "is", null).order("version_number", { ascending: false }).limit(1).maybeSingle()`
- For Plan 17-02 we only need `id` — simplify select to `"id"`.

form_assignments columns the generator reads + writes (post-Plan 17-01 migration 015 file; the file isn't pushed yet — types are regenerated in Plan 17-06):
- Read: id, client_id, template_id, assigned_by, instructions, due_date, recurrence_rule
- Write (INSERT): client_id, template_id, template_version_id, assigned_by, due_date, instructions, status='pending', recurrence_rule, last_reminder_sent=null. Database defaults handle created_at/updated_at/deleted_at.

Vitest 3 stubbing idioms (proven in Phase 16 tests):
- generateNextOccurrence accepts SupabaseClient as a parameter → tests pass a manually-built chainable stub object, no vi.mock needed for Supabase.
- sendAssignmentReminder imports dispatchNotification → `vi.mock("@/lib/notifications/n8n-dispatch", () => ({ dispatchNotification: (...args) => dispatchSpy(...args) }))` where `dispatchSpy = vi.fn()` is declared BEFORE the `vi.mock` (the hoisting fix from Plan 16-04 Task 2b SUMMARY).

Frequency vocab (RESEARCH §Q7, locked CONTEXT — vocab is fixed at 4 values for v1):
- "weekly" → +7 days via `d.setDate(d.getDate() + 7)`
- "monthly" → +1 calendar month via `d.setMonth(d.getMonth() + 1)`
- "quarterly" → +3 calendar months via `d.setMonth(d.getMonth() + 3)`
- "annually" → +1 calendar year via `d.setFullYear(d.getFullYear() + 1)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement generateNextOccurrence + fill recurrence-generator.spec.ts</name>
  <read_first>
    - .planning/phases/17-assignment-scheduling-notifications/17-RESEARCH.md §Example 2 (the byte-faithful reference for the generator — copy the structure)
    - .planning/phases/17-assignment-scheduling-notifications/17-RESEARCH.md §Q3 + §Q5 (recurrence storage shape + latest-published lookup contract)
    - .planning/phases/17-assignment-scheduling-notifications/17-RESEARCH.md §Pattern 2 (pure-function discipline — no Next.js request context, no module-level env reads)
    - app/client/templates/[id]/fill/page.tsx (lines 51-59 — canonical latest-published lookup chain to mirror)
    - tests/scheduler/recurrence-generator.spec.ts (Plan 17-01 scaffold — read the it.todo strings; the filled spec must satisfy each behavior the todos described)
    - tests/form-builder/assignment-status-transitions.test.ts (Phase 16 idiom for spy-on-Supabase via a chainable stub; the `.update().eq().eq()` chain spy is the template for Plan 17-02's `.insert().select().single()` and `.eq().not().order().limit().maybeSingle()` chain spies)
  </read_first>
  <behavior>
    - File `lib/scheduler/generate-next-occurrence.ts` exists and exports:
      - `type Frequency = "weekly" | "monthly" | "quarterly" | "annually"`
      - `interface RecurrenceRule { frequency: Frequency }`
      - `interface SourceAssignment` with the seven fields named in `<interfaces>` Read columns
      - `type GenerateResult = { ok: true; newAssignmentId: string } | { ok: false; reason: string }`
      - `async function generateNextOccurrence(supabase: SupabaseClient, src: SourceAssignment): Promise<GenerateResult>`
    - Short-circuit with `ok:false, reason:"no_recurrence_rule"` when `src.recurrence_rule` is null OR `(rule as RecurrenceRule).frequency` is missing or not one of the four vocab values.
    - Short-circuit with `ok:false, reason:"no_due_date"` when `src.due_date` is null.
    - Short-circuit with `ok:false, reason:"no_published_version"` when the latest-published lookup returns null.
    - On insert failure, return `ok:false, reason: error?.message ?? "insert_failed"`.
    - The new due_date is computed by adding the frequency unit to the SOURCE row's due_date (NOT today). Format as `yyyy-mm-dd` via `.toISOString().slice(0,10)`.
    - The INSERT row includes `last_reminder_sent: null`, `status: "pending"`, the inherited `recurrence_rule` (chain continues), and the re-pinned `template_version_id` from the latest-published lookup. Does NOT set `deleted_at` (defaults to NULL).
    - Module-level invariants: NO `process.env` reads, NO `next/*` imports, NO React imports — pure module that runs identically from the cron handler and from Vitest.
    - `tests/scheduler/recurrence-generator.spec.ts` has 6 passing `it()` blocks satisfying:
      1. Returns `{ok:false, reason:"no_recurrence_rule"}` when `recurrence_rule` is null.
      2. Returns `{ok:false, reason:"no_due_date"}` when `due_date` is null and `recurrence_rule` is valid.
      3. Returns `{ok:false, reason:"no_published_version"}` when the template_versions lookup returns `{data: null}`.
      4. Computes new due_date by adding the frequency unit to PRIOR due_date — example: `recurrence_rule: { frequency: "weekly" }` + `due_date: "2026-06-01"` → insert payload `.due_date === "2026-06-08"`.
      5. New row inherits client_id, template_id, assigned_by, instructions, recurrence_rule from src (assert via `expect(insertSpy).toHaveBeenCalledWith(expect.objectContaining({ ... }))`).
      6. New row's template_version_id is the id returned by the latest-published lookup stub (assert via the same `expect.objectContaining`).
  </behavior>
  <files>
    - lib/scheduler/generate-next-occurrence.ts (NEW)
    - tests/scheduler/recurrence-generator.spec.ts (REWRITE — replace Plan 17-01 scaffold)
  </files>
  <action>
    Create `lib/scheduler/generate-next-occurrence.ts` with the single import `import type { SupabaseClient } from "@supabase/supabase-js";`. No React, no Next.js, no env reads.

    Define and export `Frequency`, `RecurrenceRule`, `SourceAssignment`, and `GenerateResult` as named exports so tests and Plan 17-04 can share the typing.

    Implement a private `addFrequency(date: Date, freq: Frequency): Date` returning a NEW Date with the appropriate setDate/setMonth/setFullYear applied — do not mutate the input.

    The `generateNextOccurrence` body, in order:
    1. Coerce `src.recurrence_rule` to `RecurrenceRule | null`. If null, or `rule.frequency` is not in `["weekly","monthly","quarterly","annually"]`, return `{ok:false, reason:"no_recurrence_rule"}`.
    2. If `!src.due_date`, return `{ok:false, reason:"no_due_date"}`.
    3. Latest-published lookup against `template_versions` with the EXACT filter chain: `.select("id").eq("template_id", src.template_id).not("published_at", "is", null).order("version_number", { ascending: false }).limit(1).maybeSingle()`. If `data` is falsy, return `{ok:false, reason:"no_published_version"}`.
    4. Compute `newDue = addFrequency(new Date(src.due_date), rule.frequency).toISOString().slice(0,10)`.
    5. Insert the 9-column payload listed in `<behavior>` via `.insert(payload).select("id").single()`.
    6. On error or null data, return `{ok:false, reason: error?.message ?? "insert_failed"}`.
    7. Otherwise return `{ok:true, newAssignmentId: created.id}`.

    Then rewrite `tests/scheduler/recurrence-generator.spec.ts`. Top-level imports:
    ```
    import { describe, it, expect, vi } from "vitest";
    import { generateNextOccurrence } from "@/lib/scheduler/generate-next-occurrence";
    ```

    Build a `makeStubClient(opts)` helper inside the spec that returns a SupabaseClient-shaped stub:
    - `.from("template_versions")` returns a chainable object whose `.select().eq().not().order().limit().maybeSingle()` chain resolves to `opts.versionsResult` (e.g. `{ data: { id: "tv-new" } }` or `{ data: null }`).
    - `.from("form_assignments")` returns a chainable object whose `.insert(payload)` calls `opts.insertSpy(payload)` then returns a chainable whose `.select().single()` resolves to `opts.insertResult` (e.g. `{ data: { id: "asg-new" }, error: null }`).

    Implement the chain via small builder objects — each method returns `this` until the terminal method returns a Promise (`.maybeSingle()` / `.single()` / direct await of the chain).

    Sample source row used across tests:
    ```
    const src = {
      id: "asg-1", client_id: "client-A", template_id: "tpl-1",
      assigned_by: "admin-1", instructions: "Please complete by Friday",
      due_date: "2026-06-01",
      recurrence_rule: { frequency: "weekly" as const },
    };
    ```

    Write 6 `it()` blocks satisfying the 6 behaviors. For tests 4–6, use `insertSpy` to capture the INSERT payload and `expect(insertSpy).toHaveBeenCalledWith(expect.objectContaining({ due_date: "2026-06-08", client_id: "client-A", template_version_id: "tv-new", ... }))`.

    Run `npm test -- tests/scheduler/recurrence-generator.spec.ts --run` and confirm 6 tests pass.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readFileSync('lib/scheduler/generate-next-occurrence.ts','utf8');for (const s of ['export type Frequency','export interface RecurrenceRule','export async function generateNextOccurrence','no_recurrence_rule','no_due_date','no_published_version','template_versions','maybeSingle']) { if(!f.includes(s)) { console.error('Missing:',s); process.exit(1); } } if(/from\\s+[\"']next/.test(f)) { console.error('Forbidden next/* import'); process.exit(2); } if(/process\\.env/.test(f)) { console.error('Forbidden process.env'); process.exit(3); } const t=fs.readFileSync('tests/scheduler/recurrence-generator.spec.ts','utf8'); const itMatches=t.match(/\\bit\\(/g); if(!itMatches||itMatches.length<6){console.error('Spec needs ≥6 it() blocks, found',itMatches?itMatches.length:0);process.exit(4);} if(/\\bit\\.todo\\b/.test(t)){console.error('Spec still has it.todo entries');process.exit(5);} console.log('OK');" && npm test -- tests/scheduler/recurrence-generator.spec.ts --run --reporter=basic 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/scheduler/generate-next-occurrence.ts` exists.
    - `grep -c 'export async function generateNextOccurrence' lib/scheduler/generate-next-occurrence.ts` = 1.
    - `grep -c 'export type Frequency' lib/scheduler/generate-next-occurrence.ts` = 1.
    - `grep -c 'export interface RecurrenceRule' lib/scheduler/generate-next-occurrence.ts` = 1.
    - File contains all four reason strings as substrings: `"no_recurrence_rule"`, `"no_due_date"`, `"no_published_version"`, `"insert_failed"`.
    - File contains `template_versions` and `.maybeSingle(` and `.not(` (3 substring matches).
    - `grep -cE 'from\s+"next' lib/scheduler/generate-next-occurrence.ts` = 0 (no next/* imports).
    - `grep -c 'process.env' lib/scheduler/generate-next-occurrence.ts` = 0 (pure module).
    - `tests/scheduler/recurrence-generator.spec.ts` has at least 6 `it(` matches and zero `it.todo` matches.
    - `npm test -- tests/scheduler/recurrence-generator.spec.ts --run --reporter=basic` exits 0 and reports 6 passing tests.
  </acceptance_criteria>
  <done>
    The recurrence generator is a pure function unit-tested against a stub Supabase client. Plan 17-04's cron handler AND the inline trigger inside submitAssignedFillByIdAction can both import and call it without HTTP harness scaffolding.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sendAssignmentReminder + fill send-reminder.spec.ts</name>
  <read_first>
    - .planning/phases/17-assignment-scheduling-notifications/17-RESEARCH.md §Example 3 (the byte-faithful reference for the wrapper)
    - .planning/phases/17-assignment-scheduling-notifications/17-RESEARCH.md §Pattern 5 (absolute URL building rule — NEXT_PUBLIC_SITE_URL → VERCEL_URL → empty fallback)
    - lib/notifications/n8n-dispatch.ts (post-Plan 17-01 — confirm the assignment_reminder variant shape)
    - tests/scheduler/send-reminder.spec.ts (Plan 17-01 scaffold — read the it.todo strings)
    - tests/form-builder/assignment-status-transitions.test.ts (Phase 16 vi.mock idiom — the hoisting fix pattern: declare `redirectSpy` BEFORE the vi.mock factory, then have the factory return a wrapper that calls the spy)
  </read_first>
  <behavior>
    - File `lib/scheduler/send-reminder.ts` exists and exports `async function sendAssignmentReminder(args)` where args is `{ cadence: "7d" | "1d" | "overdue"; client_email: string; client_name: string; template_name: string; due_date: string; assignmentId: string; instructions: string | null }`.
    - The function builds `base = process.env.NEXT_PUBLIC_SITE_URL ?? (process.env.VERCEL_URL ? "https://" + process.env.VERCEL_URL : "")` and forms `assignment_url = base + "/client/assignments/" + assignmentId`.
    - It calls `dispatchNotification({ type: "assignment_reminder", cadence, client_email, client_name, template_name, due_date, assignment_url, instructions })` and `return`s the result verbatim — no try/catch, no result mutation, no `.ok` short-circuit.
    - The function imports only `dispatchNotification` from `@/lib/notifications/n8n-dispatch` — no Supabase, no other env vars beyond the two URL vars.
    - `tests/scheduler/send-reminder.spec.ts` has 3 passing `it()` blocks satisfying:
      1. Builds absolute assignment_url from NEXT_PUBLIC_SITE_URL when set: set `process.env.NEXT_PUBLIC_SITE_URL = "https://test.example.com"`, call `sendAssignmentReminder({...assignmentId:"abc-123"})`, assert the dispatched payload's `assignment_url === "https://test.example.com/client/assignments/abc-123"`.
      2. Passes cadence + client_email + client_name + template_name + due_date + instructions through unchanged to dispatchNotification (assert via `expect.objectContaining`).
      3. Returns dispatchNotification's result verbatim — when the stub resolves to `{ok:false, error:"webhook 500"}`, the function's awaited return deep-equals the same object.
  </behavior>
  <files>
    - lib/scheduler/send-reminder.ts (NEW)
    - tests/scheduler/send-reminder.spec.ts (REWRITE — replace Plan 17-01 scaffold)
  </files>
  <action>
    Create `lib/scheduler/send-reminder.ts`. Single import: `import { dispatchNotification } from "@/lib/notifications/n8n-dispatch";`. No "use server" directive (the module is imported from a server route in Plan 17-04 BUT also from Vitest — keep it runtime-agnostic).

    Body: a single `export async function sendAssignmentReminder(args)` matching the args shape in `<behavior>`. Build `base` per RESEARCH §Pattern 5. The exact nullish-coalescing precedence: NEXT_PUBLIC_SITE_URL → VERCEL_URL (prefixed with `https://`) → empty string. Concatenate `assignment_url = `${base}/client/assignments/${args.assignmentId}``. Pass the full payload object to `dispatchNotification` and `return` its awaited result directly.

    Then rewrite `tests/scheduler/send-reminder.spec.ts`. The Vitest hoisting-safe pattern (mirror Plan 16-04 Task 2b Summary §Deviation 3):
    ```
    import { describe, it, expect, vi, beforeEach, afterEach } from "vitest";

    const dispatchSpy = vi.fn();
    vi.mock("@/lib/notifications/n8n-dispatch", () => ({
      dispatchNotification: (...args) => dispatchSpy(...args),
    }));

    import { sendAssignmentReminder } from "@/lib/scheduler/send-reminder";
    ```

    Use a beforeEach/afterEach pair to snapshot+restore `process.env.NEXT_PUBLIC_SITE_URL` and `process.env.VERCEL_URL` so tests don't bleed env state into each other or into other specs.

    Write 3 `it()` blocks:
    - Test 1: set `process.env.NEXT_PUBLIC_SITE_URL = "https://test.example.com"`, delete `process.env.VERCEL_URL`, `dispatchSpy.mockResolvedValueOnce({ok:true,status:200})`, call sendAssignmentReminder with `assignmentId:"abc-123"` and other fields filled in, then assert `dispatchSpy` was called with an object whose `assignment_url === "https://test.example.com/client/assignments/abc-123"`.
    - Test 2: use the same env setup, assert `expect(dispatchSpy).toHaveBeenCalledWith(expect.objectContaining({ type: "assignment_reminder", cadence: <expected>, client_email: <expected>, client_name: <expected>, template_name: <expected>, due_date: <expected>, instructions: <expected> }))`.
    - Test 3: `dispatchSpy.mockResolvedValueOnce({ok:false, error:"webhook 500"})`, call the function, assert the awaited result deep-equals `{ok:false, error:"webhook 500"}`.

    Run `npm test -- tests/scheduler/send-reminder.spec.ts --run` and confirm 3 tests pass.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readFileSync('lib/scheduler/send-reminder.ts','utf8');for (const s of ['export async function sendAssignmentReminder','dispatchNotification','NEXT_PUBLIC_SITE_URL','VERCEL_URL','/client/assignments/','assignment_reminder']) { if(!f.includes(s)) { console.error('Missing:',s); process.exit(1); } } if(/[\"']use server[\"']/.test(f)){console.error('use server forbidden');process.exit(2);} const t=fs.readFileSync('tests/scheduler/send-reminder.spec.ts','utf8'); const itMatches=t.match(/\\bit\\(/g); if(!itMatches||itMatches.length<3){console.error('Spec needs ≥3 it() blocks, found',itMatches?itMatches.length:0);process.exit(3);} if(/\\bit\\.todo\\b/.test(t)){console.error('Spec still has it.todo entries');process.exit(4);} if(!t.includes('vi.mock')){console.error('Spec must mock dispatchNotification via vi.mock');process.exit(5);} console.log('OK');" && npm test -- tests/scheduler/send-reminder.spec.ts --run --reporter=basic 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/scheduler/send-reminder.ts` exists.
    - `grep -c 'export async function sendAssignmentReminder' lib/scheduler/send-reminder.ts` = 1.
    - File contains the substring `dispatchNotification` (the import) AND `assignment_reminder` (the type literal) AND `/client/assignments/` (the URL template).
    - File contains both `NEXT_PUBLIC_SITE_URL` and `VERCEL_URL` substrings.
    - `grep -c '"use server"' lib/scheduler/send-reminder.ts` = 0 AND `grep -c "'use server'" lib/scheduler/send-reminder.ts` = 0.
    - `tests/scheduler/send-reminder.spec.ts` has at least 3 `it(` matches, zero `it.todo` matches, and at least one `vi.mock(` invocation.
    - `npm test -- tests/scheduler/send-reminder.spec.ts --run --reporter=basic` exits 0 and reports 3 passing tests.
  </acceptance_criteria>
  <done>
    The reminder sender is a thin, unit-tested wrapper. Plan 17-04's cron PASS A imports it and treats `ok:false` as the signal to insert workflow_errors + skip the dedup write — that idempotency contract is asserted in Plan 17-05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron handler → generateNextOccurrence | The cron calls the generator with a SupabaseClient. The generator NEVER reads `client_id` from a request payload — only from the source row (server-side carryover). This inherits Phase 16's T-16-04 invariant. |
| sendAssignmentReminder → n8n webhook | The webhook URL/secret are read inside dispatchNotification (Plan 17-01 untouched). sendAssignmentReminder never touches those env vars — it only reads NEXT_PUBLIC_SITE_URL / VERCEL_URL to build the public-facing assignment_url. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 (inherits Phase 16 T-16-04) | Tampering | generateNextOccurrence could be called with a forged `src.client_id` if a future code path reads it from a request payload | mitigate | The function signature accepts `SourceAssignment` typed strictly. Plan 17-04's call sites (cron PASS B + inline trigger) populate `src` from a SELECT on the prior occurrence row — never from a payload. Task 1 spec asserts the inherited fields are passed through, but the function itself does not validate them; the gate is at the call site. |
| T-17-05 (inherits Phase 16 T-16-03) | Information Disclosure | Latest-published lookup could expose a fork's published version cross-org if RLS misconfigured on template_versions | accept | template_versions RLS was proven by Phase 16's `tests/rls/multi-tenancy.spec.ts` (4 negative + 1 positive control). The generator uses an RLS-aware SupabaseClient when called from the inline trigger; the cron uses the service-role client, but the cron's `src` row was already filtered through service-role for the completed-recurring SELECT — same trust boundary as Phase 5's expiry cron. |
| T-17-06 | Tampering | A future caller could pass an invalid frequency string in recurrence_rule, bypassing the CHECK on last_reminder_sent (which only constrains the dedup column) | mitigate | The generator validates `rule.frequency` against the 4-value vocab at runtime and returns `{ok:false, reason:"no_recurrence_rule"}` for anything else. Test 1 covers this. CONTEXT decision: vocab is fixed for v1; expansion requires explicit migration + code change. |
| T-17-07 | Information Disclosure | sendAssignmentReminder constructs an `assignment_url` that goes out via email — a malformed base (e.g. empty) could yield a relative URL that fails when clicked | mitigate | RESEARCH §Pattern 5 fallback chain ensures `base` is either a full origin or empty string. Test 1 asserts the correct construction. In production NEXT_PUBLIC_SITE_URL is set; if both env vars are missing the URL becomes `/client/assignments/[id]` (relative), which n8n's outbound template can detect and fail loud rather than send a broken link. |
</threat_model>

<verification>
After both tasks:
- `node -e "require('fs').readFileSync('lib/scheduler/generate-next-occurrence.ts','utf8').includes('generateNextOccurrence') || process.exit(1)"` exits 0.
- `node -e "require('fs').readFileSync('lib/scheduler/send-reminder.ts','utf8').includes('sendAssignmentReminder') || process.exit(1)"` exits 0.
- `npm test -- tests/scheduler/ --run --reporter=basic` exits 0; reports 4 + 6 + 3 = 13 passing tests across the three filled specs (recurrence-generator, send-reminder, is-overdue from Plan 17-01), plus the still-scaffold cron-reminder-decision todos.
- Existing Phase 13–16 Vitest suites still pass (no regression — Plan 17-02 adds two pure modules and rewrites two existing spec files; no shared imports change).
- `npx tsc --noEmit lib/scheduler/generate-next-occurrence.ts lib/scheduler/send-reminder.ts` exits 0 (zero TS errors against the @supabase/supabase-js types and the lib/notifications import).
</verification>

<success_criteria>
- `lib/scheduler/generate-next-occurrence.ts` exists, exports the four typed members, and is pure (no env, no next/*).
- `lib/scheduler/send-reminder.ts` exists, exports `sendAssignmentReminder`, builds the URL per Pattern 5, and returns dispatchNotification's result verbatim.
- `tests/scheduler/recurrence-generator.spec.ts` is fully filled (6 assertions, 0 todos) and passes.
- `tests/scheduler/send-reminder.spec.ts` is fully filled (3 assertions, 0 todos) and passes.
- The third scheduler scaffold (`tests/scheduler/cron-reminder-decision.spec.ts`) remains a scaffold for Plan 17-05.
- No regressions in Phase 13/14/15/16 test suites.
</success_criteria>

<output>
After completion, create `.planning/phases/17-assignment-scheduling-notifications/17-02-SUMMARY.md` recording: the two lib files' exported surface, the SupabaseClient-stub idiom used in the recurrence spec, the vi.mock hoisting pattern used in the send-reminder spec, the full Vitest output line count for tests/scheduler/, and a callout that Plan 17-04 will import these two functions into the cron handler and the inline trigger in `submitAssignedFillByIdAction`.
</output>
