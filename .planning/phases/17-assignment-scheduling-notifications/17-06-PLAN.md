---
phase: 17
slug: assignment-scheduling-notifications
plan: "06"
plan_id: 17-06
type: execute
wave: 4
depends_on: [17-01, 17-02, 17-03, 17-04, 17-05]
files_modified:
  - lib/supabase/database.types.ts
  - .planning/phases/17-assignment-scheduling-notifications/17-UAT.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
tags: [supabase, db-push, types-regen, verification, uat, roadmap, BLOCKING, wcag]
must_haves:
  truths:
    - "Migration 015 is pushed to the live Supabase project — form_assignments has three new columns (recurrence_rule JSONB, last_reminder_sent TEXT, recurrence_generated_at TIMESTAMPTZ) AND the CHECK constraint on last_reminder_sent."
    - "lib/supabase/database.types.ts is regenerated and reflects the three new columns under the form_assignments row interface."
    - "Full Vitest sweep (npm test --run) is green — all Phase 16 specs + the 6 Phase 17 specs (is-overdue, recurrence-generator, send-reminder, cron-reminder-decision, overdue-pill, admin-assignments-order-by) pass."
    - "npm run build exits 0 against the regenerated types."
    - "WCAG AA contrast for #a14a2a on cream #faf9f6 + 10% overlay AND on dark #1c1b24 + 10% overlay is verified to ≥ 4.5:1 — fallback to /15 overlay applied if either fails (UI-SPEC BLOCKING #3)."
    - "17-UAT.md exists with three manual walkthrough sections: A (overdue badge on /client/assignments + tooltip-free touch surface), B (overdue badge + tooltip on /admin/clients/[id] Assigned Forms tab + ORDER BY surfaces overdue first), C (recurrence: complete a recurring assignment and observe the next occurrence appear)."
    - "17-UAT.md includes a §D 'Cron smoke' section with the curl command to manually trigger /api/cron/assignment-scheduler in dev or against a preview deploy."
    - "ROADMAP.md Phase 17 entry has its Plans line updated to '6 plans' and a Plans enumeration covering 17-01 through 17-06 with wave labels."
  artifacts:
    - path: lib/supabase/database.types.ts
      provides: "Regenerated Supabase types reflecting migration 015 (recurrence_rule + last_reminder_sent + recurrence_generated_at on form_assignments)"
      contains: "recurrence_rule"
    - path: .planning/phases/17-assignment-scheduling-notifications/17-UAT.md
      provides: "Manual walkthrough doc for Phase 17 — 4 sections (overdue client, overdue admin, recurrence, cron smoke)"
      contains: "OVERDUE"
    - path: .planning/ROADMAP.md
      provides: "Updated Phase 17 entry with 6 plans enumerated across 5 waves"
      contains: "17-06-PLAN.md"
  key_links:
    - from: lib/supabase/database.types.ts
      to: supabase/migrations/015_phase17_assignment_recurrence_reminders.sql
      via: "supabase gen types typescript --linked"
      pattern: "recurrence_rule"
    - from: .planning/ROADMAP.md Phase 17 entry
      to: ".planning/phases/17-assignment-scheduling-notifications/17-*-PLAN.md"
      via: "Plans enumeration table"
      pattern: "17-01-PLAN.md"
---

<objective>
[BLOCKING] Push migration 015 to live Supabase, regenerate TypeScript types, verify the full Vitest sweep + production build are green, compute WCAG AA contrast for the OverduePill rust on both painted backgrounds, author 17-UAT.md, and update ROADMAP.md with the Phase 17 plan enumeration.

This plan is gated on completion of Plans 17-01 through 17-05 because:
1. Migration 015 file (Plan 17-01) MUST NOT push before the application code that reads the new columns (Plans 17-03 + 17-04) is in place — otherwise the deployed app would crash on missing columns the moment the DB updates ahead of the deploy.
2. The cron handler (Plan 17-04) references column names that don't exist on the regenerated types yet — until Plan 17-06 runs `supabase gen types typescript`, those references are type-loose. `npm run build` only goes green AFTER the regen.
3. The OverduePill markup (Plan 17-03) uses `#a14a2a` text/10%-bg — UI-SPEC BLOCKING #3 requires explicit WCAG AA verification on both surfaces before phase close-out.
4. The UAT walkthrough requires all UI + cron + DB pieces to be live.

Output: Live DB updated; types regenerated; full sweep green; build green; WCAG verified or fallback applied; 17-UAT.md authored; ROADMAP.md Phase 17 entry finalised.

Plan is `autonomous: false` because Task 1a requires the human to run `supabase db push` (interactive prompts may appear; production-DB side effect). Tasks 1b through 4 are autonomous.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/17-assignment-scheduling-notifications/17-CONTEXT.md
@.planning/phases/17-assignment-scheduling-notifications/17-RESEARCH.md
@.planning/phases/17-assignment-scheduling-notifications/17-UI-SPEC.md
@.planning/phases/17-assignment-scheduling-notifications/17-01-PLAN.md
@.planning/phases/17-assignment-scheduling-notifications/17-04-PLAN.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
Supabase CLI commands (verified working in Phase 16 Plan 07):
- `supabase db push` — applies pending migrations from `supabase/migrations/` to the linked project. May prompt for confirmation on destructive operations (15 is additive: 3 ADD COLUMNs + 1 ADD CONSTRAINT — non-destructive).
- `npx supabase gen types typescript --linked > lib/supabase/database.types.ts` — regenerates types from the linked project schema.

Phase 16 Plan 07 precedent (the structural analog for this plan):
- Task 1a: human-action checkpoint for the two CLI commands.
- Task 1b: programmatic verification — types-reflect-schema regex grep + npm test + npm run build.
- Task 1c: dependency-pin regression check.
- Task 2: UAT.md + ROADMAP update.

Plan 17-06 omits the equivalent of Phase 16 Plan 07 Task 1c because Phase 17 introduces no new dependencies (no new npm packages — only schema, lib code, UI code, and tests). package.json is unchanged.

WCAG AA contrast formula:
- Required ratio: ≥ 4.5:1 for normal text (9px is "small text"; AA threshold is 4.5:1).
- The pill background is `bg-[#a14a2a]/10` — a 10% rust overlay on whatever surface paints behind it.
- On cream client surface (background `#faf9f6` per Phase 16 UI-SPEC): painted bg ≈ blend(#faf9f6, #a14a2a, 0.1) ≈ #ECE5DB-ish.
- On dark admin surface (background `#1c1b24` per Phase 16 UI-SPEC): painted bg ≈ blend(#1c1b24, #a14a2a, 0.1) ≈ #2C2426-ish.
- Text colour: `#a14a2a`.
- Computation: use the relative-luminance formula from WCAG 2.1 § 1.4.3 — or use the npm package `wcag-contrast` (NOT installed; use a manual computation in node).

UI-SPEC fallback rule: if contrast fails on either surface, bump the overlay opacity to `/15` (15% instead of 10%) — keeps the pill family geometry consistent.

ROADMAP.md current Phase 17 entry (lines 382-391):
```
### Phase 17: Assignment Scheduling + Notifications
**Goal**: Recurring form assignments auto-generate on schedule and clients receive automated reminders.
**Depends on**: Phase 16
**Requirements**: SCHED-01..03 (v2 cluster — re-quote pending; decompose at plan-phase)
**Success Criteria** (what must be TRUE):
  1. Recurring assignments auto-generate when the prior occurrence is completed, referencing the latest published version.
  2. Overdue assignments are flagged in both admin and client dashboards.
  3. A daily cron processes recurrences and overdue marking.
  4. Reminder notifications send at 7 days, 1 day, and on overdue, deduped via `last_reminder_sent`.
**Plans**: Not yet planned — run `/gsd:plan-phase 17`.
```
The `**Plans**:` line plus the placeholder need replacement with a Wave-grouped enumeration analogous to Phase 16's structure (Plan 07 line 252-269 in 16-07-PLAN.md).

Also the Progress Table at ROADMAP line 100-119 has Phase 17 as `0/0 | Not planned | -`. After Plan 17-06 completes, this updates to `6/6 | In Progress` (waiting on UAT, mirroring Phase 14/15 pattern) or `6/6 | Complete` (if UAT is signed off during this plan — unlikely since UAT is manual).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1a [BLOCKING]: Human runs `supabase db push` + `gen types typescript`</name>
  <read_first>
    - supabase/migrations/015_phase17_assignment_recurrence_reminders.sql (Plan 17-01 Task 1 — confirm content before pushing)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-07-PLAN.md Task 1a (Phase 16 precedent — same shape, same commands)
  </read_first>
  <what-built>
    Plans 17-01 through 17-05 are complete. Migration 015 exists as a file but is NOT yet pushed. The cron handler (Plan 17-04 Task 1), the inline trigger in `submitAssignedFillByIdAction` (Plan 17-04 Task 3), and the UI surfaces (Plan 17-03) all reference the three new columns — but TypeScript types (`lib/supabase/database.types.ts`) still reflect the pre-push schema. This task pushes the migration and regenerates the types. Claude cannot run `supabase db push` autonomously (interactive prompt + production-DB side effect), so a human runs the two commands and reports outputs.
  </what-built>
  <how-to-verify>
    Run the following commands locally, in order, and paste the exit codes + tail of output back:

    1. **Verify migration file is present:**
       ```
       ls supabase/migrations/015_phase17_assignment_recurrence_reminders.sql
       ```
       File must exist.

    2. **Push migration to the linked Supabase project:**
       ```
       supabase db push
       ```
       Expected CLI output mentions applying `015_phase17_assignment_recurrence_reminders.sql`, followed by "Finished supabase db push." Migration 015 is non-destructive (3 ADD COLUMN + 1 ADD CONSTRAINT). If the CLI prompts for confirmation, type `y`. Capture full stdout/stderr and exit code.

    3. **Regenerate TypeScript types:**
       ```
       npx supabase gen types typescript --linked > lib/supabase/database.types.ts
       ```
       Capture exit code. File should be rewritten in-place.

    4. **(Optional smoke via Supabase Studio or psql):**
       - `\d form_assignments` should show the three new columns: `recurrence_rule jsonb`, `last_reminder_sent text`, `recurrence_generated_at timestamp with time zone`.
       - `\d+ form_assignments` should show the CHECK constraint `form_assignments_last_reminder_sent_check` with definition `((last_reminder_sent IS NULL) OR (last_reminder_sent = ANY (ARRAY['7d'::text, '1d'::text, 'overdue'::text])))` or equivalent canonicalised form.

    Acceptance gate for this manual task:
    - `supabase db push` exit code = 0.
    - `npx supabase gen types typescript --linked` exit code = 0.
    - `lib/supabase/database.types.ts` was modified (file mtime updated; non-empty; the regen wrote new content).

    Paste the exit codes + the last ~20 lines of `supabase db push` output back. If either command failed, do NOT mark verified — share the failure mode and we will diagnose before continuing to Task 1b.
  </how-to-verify>
  <resume-signal>
    Type `verified` and paste the captured exit codes + tail of `supabase db push` output. If either command failed, paste the full error instead — do NOT mark verified.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1b: Programmatic verification — types reflect migration, build green, suite green</name>
  <read_first>
    - lib/supabase/database.types.ts (just regenerated in Task 1a — confirm the file exists and was modified)
    - Plan 17-01 Task 1 (the migration the regen must reflect)
    - Plans 17-02/03/04/05 (the application code that compiles against the regenerated types)
  </read_first>
  <action>
    Claude runs the following commands and asserts their exit codes / output. Do NOT proceed to Task 2 if any assertion fails — report back to the user and stop.

    1. Assert `lib/supabase/database.types.ts` reflects migration 015 — three new columns on form_assignments:
       ```
       node -e "const c=require('fs').readFileSync('lib/supabase/database.types.ts','utf8'); const idx=c.indexOf('form_assignments'); if(idx<0){console.error('form_assignments table missing');process.exit(1);} const region=c.slice(idx, idx+6000); const checks=[['recurrence_rule', /recurrence_rule\\s*:\\s*Json\\s*\\|\\s*null/], ['last_reminder_sent', /last_reminder_sent\\s*:\\s*string\\s*\\|\\s*null/], ['recurrence_generated_at', /recurrence_generated_at\\s*:\\s*string\\s*\\|\\s*null/]]; for (const [n,re] of checks) { if(!re.test(region)){console.error('Column missing from types region:',n);process.exit(2);} } console.log('OK: all three Phase 17 columns present on form_assignments')"
       ```
       NOTE on type for JSONB: Supabase's type generator outputs JSONB columns as `Json | null` (where `Json` is a type alias defined at the top of the file). Adjust the first regex if the actual output uses a different shape — accept any of `Json | null`, `unknown | null`, or `Database['public']['CompositeTypes']['*']`. If the literal name `recurrence_rule` is present within ~6000 chars after `form_assignments`, that's the gate.

    2. Run the full Vitest sweep: `npm test --run`. Assert exit code 0. Tail the output and verify:
       - The 4 scheduler specs (is-overdue, recurrence-generator, send-reminder, cron-reminder-decision) report passing.
       - The 2 new form-builder specs (overdue-pill, admin-assignments-order-by) report passing.
       - Phase 16's tests/rls/multi-tenancy.spec.ts is either passing (env vars present) or skipped via describe.skipIf (env vars absent) — either is acceptable.
       - Total test count is ≥ the pre-Phase-17 count + 24 (the new Phase 17 tests counted in Plan 17-05 verification).

    3. Run the production build: `npm run build`. Assert exit code 0. If it fails on a Phase-17-introduced TypeScript error, diagnose; if it fails on a pre-existing Phase 16 §Deviation 3 issue (react-leaflet or @react-pdf/renderer warnings), confirm those are pre-existing by checking `git stash; npm run build; git stash pop` — they should fail identically on the pre-Phase-17 commit and are out of scope. The acceptance gate is: no new TypeScript errors introduced by Phase 17.

    4. Compute WCAG AA contrast for the OverduePill (UI-SPEC BLOCKING #3). Write a small inline node script:
       ```
       node -e "
       function relativeLuminance(rgb) { const [r,g,b] = rgb.map(v => { const s = v/255; return s <= 0.03928 ? s/12.92 : Math.pow((s+0.055)/1.055, 2.4); }); return 0.2126*r + 0.7152*g + 0.0722*b; }
       function contrastRatio(rgb1, rgb2) { const L1 = relativeLuminance(rgb1); const L2 = relativeLuminance(rgb2); const [hi, lo] = L1 > L2 ? [L1, L2] : [L2, L1]; return (hi + 0.05) / (lo + 0.05); }
       function hexToRgb(hex) { const m = hex.replace('#','').match(/.{2}/g); return m.map(h => parseInt(h, 16)); }
       function blend(bg, fg, alpha) { return bg.map((b, i) => Math.round(b * (1 - alpha) + fg[i] * alpha)); }
       const text = hexToRgb('#a14a2a');
       const cream = hexToRgb('#faf9f6');
       const dark = hexToRgb('#1c1b24');
       for (const overlay of [0.1, 0.15]) {
         const creamBg = blend(cream, text, overlay);
         const darkBg = blend(dark, text, overlay);
         const cCream = contrastRatio(text, creamBg);
         const cDark = contrastRatio(text, darkBg);
         console.log('overlay ' + (overlay * 100) + '%: cream contrast = ' + cCream.toFixed(2) + ':1, dark contrast = ' + cDark.toFixed(2) + ':1');
       }
       "
       ```

       Read the output. If `/10` overlay shows both cream AND dark contrast ≥ 4.5:1, the original UI-SPEC tokens stand — no action. Record the computed numbers in the Plan 17-06 summary.

       If EITHER surface falls below 4.5:1, apply the UI-SPEC §BLOCKING #3 fallback: edit BOTH `app/_components/overdue-pill.tsx` (the className `bg-[#a14a2a]/10` → `bg-[#a14a2a]/15`) AND `app/admin/clients/[id]/client-tabs.tsx` (the inlined `bg-[#a14a2a]/10` on the TooltipTrigger → `bg-[#a14a2a]/15`). Re-run the contrast check at `/15`. If it now passes, document the fallback decision in the summary. If it still fails, STOP and report — do not change the hue (UI-SPEC explicitly forbids that fallback).

    If all four assertions (types reflect, suite green, build green, WCAG ≥ 4.5:1 on both surfaces with EITHER /10 or /15) pass, proceed to Task 2. Otherwise STOP and report.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('lib/supabase/database.types.ts','utf8'); const idx=c.indexOf('form_assignments'); if(idx<0)process.exit(1); const region=c.slice(idx, idx+6000); for (const name of ['recurrence_rule','last_reminder_sent','recurrence_generated_at']) { if(!region.includes(name)){console.error('Column missing:',name);process.exit(2);} } console.log('OK: types regenerated with all three Phase 17 columns');" && npm test --run 2>&1 | tail -30 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --run` exits 0.
    - `npm run build` exits 0.
    - `lib/supabase/database.types.ts` contains the substring `recurrence_rule` within 6000 characters following the first `form_assignments` occurrence.
    - `lib/supabase/database.types.ts` contains the substring `last_reminder_sent` within 6000 characters following the first `form_assignments` occurrence.
    - `lib/supabase/database.types.ts` contains the substring `recurrence_generated_at` within 6000 characters following the first `form_assignments` occurrence.
    - The four new scheduler spec files all report passing in the Vitest output (`tests/scheduler/is-overdue.test.ts`, `tests/scheduler/recurrence-generator.spec.ts`, `tests/scheduler/send-reminder.spec.ts`, `tests/scheduler/cron-reminder-decision.spec.ts`).
    - The two new form-builder spec files report passing (`tests/form-builder/overdue-pill.test.ts`, `tests/form-builder/admin-assignments-order-by.test.ts`).
    - WCAG AA contrast computed; record the numbers in the summary; if fallback to `/15` was needed, both `app/_components/overdue-pill.tsx` AND `app/admin/clients/[id]/client-tabs.tsx` are edited.
    - All Phase 16 specs still pass (no regression).
  </acceptance_criteria>
  <done>
    Live schema, regenerated types, application code, test suite, production build, and WCAG verification are all aligned. The phase-level gates are green programmatically.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 17-UAT.md</name>
  <read_first>
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md (Phase 16 — the structural analog and most recent UAT file)
    - .planning/phases/14-custom-field-types/14-UAT.md (earlier UAT analog)
    - .planning/phases/17-assignment-scheduling-notifications/17-UI-SPEC.md §State Matrix (the visible-state-by-state table the UAT walks through)
    - .planning/phases/17-assignment-scheduling-notifications/17-CONTEXT.md §Success Criteria (the 4 ROADMAP criteria UAT confirms)
  </read_first>
  <behavior>
    - File `.planning/phases/17-assignment-scheduling-notifications/17-UAT.md` exists with four sections (A, B, C, D).
    - Section A — "Overdue badge on /client/assignments (client surface)": prereqs, steps, expected pill rendering, aria-label verification, absence-of-tooltip confirmation.
    - Section B — "Overdue badge + tooltip on /admin/clients/[id] Assigned Forms tab": prereqs, steps, ORDER BY behavior (overdue rows first), tooltip hover copy verification.
    - Section C — "Recurrence: complete a recurring assignment and observe the next occurrence": prereqs (a recurring assignment seeded via the SQL fixture below), steps (complete the assignment as the customer), expected (a new pending assignment appears with due_date = original + frequency unit, template_version_id re-pinned to latest published).
    - Section D — "Cron smoke (manual trigger)": curl command to hit `/api/cron/assignment-scheduler` in dev OR against a preview deploy with the CRON_SECRET; expected JSON response shape.
    - Each section has explicit pass/fail checkboxes.
    - A "Pre-UAT seed SQL" appendix near the top of the file: a SQL block Matt or the developer can paste into Supabase Studio to seed one recurring assignment for Section C testing (`UPDATE form_assignments SET recurrence_rule = '{"frequency":"weekly"}'::jsonb WHERE id = '<some-pending-assignment-id>'`).
  </behavior>
  <files>
    - .planning/phases/17-assignment-scheduling-notifications/17-UAT.md (NEW)
  </files>
  <action>
    Read `.planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md` for the structural analog — Phase 16's UAT layout (Section heading → Prerequisites → Steps → Expected → Pass/Fail).

    Create `.planning/phases/17-assignment-scheduling-notifications/17-UAT.md` with the following sections (full content — write the file with the bodies, not just the headings):

    Top of file: metadata block (phase, depends-on, date), then the seed SQL appendix:

    ```sql
    -- Pre-UAT seed: convert one pending assignment to recurring weekly.
    -- Replace <ASSIGNMENT_ID> with a real pending assignment's id from form_assignments.
    UPDATE public.form_assignments
    SET recurrence_rule = '{"frequency":"weekly"}'::jsonb
    WHERE id = '<ASSIGNMENT_ID>' AND status != 'completed' AND deleted_at IS NULL;
    ```

    **Section A — Overdue badge on `/client/assignments`** (D-overdue-client):
    1. Prereqs: client_user signed in (incognito); at least one assignment for that org with `due_date < today` AND `status != 'completed'`; if none exists, run: `UPDATE form_assignments SET due_date = (CURRENT_DATE - INTERVAL '3 days')::date WHERE id = '<id>' AND status != 'completed'`.
    2. Navigate to `/client/assignments`.
    3. Active tab — observe the overdue assignment row. Expected: `OVERDUE` pill rendered AFTER the status pill on the metadata row. Pill colour: rust on cream-with-rust-overlay. Pill is uppercase mono.
    4. Hover the pill (desktop). Expected: NO tooltip appears (UI-SPEC BLOCKING #4 — client surface is tooltip-free).
    5. Use a screen reader (VoiceOver / NVDA). Expected aria-label reads "Overdue — was due 3 days ago" (or appropriate N).
    6. Switch to Completed tab (if there are completed assignments). Expected: NO OVERDUE pill on any completed-tab card, even if its due_date is in the past (UI-SPEC State Matrix row "Completed regardless of due").

    Checkboxes: [ ] OVERDUE pill renders on active overdue row. [ ] No tooltip on hover. [ ] aria-label is correctly pluralised. [ ] Completed tab shows no overdue pill.

    **Section B — Overdue badge + tooltip on `/admin/clients/[id]` Assigned Forms tab** (D-overdue-admin + D-order-by):
    1. Prereqs: admin signed in; client with multiple assignments where at least one is overdue and at least one is upcoming (due_date in the future).
    2. Navigate to `/admin/clients/<that-client-id>`. Open the "Assigned Forms" tab.
    3. Expected ORDER: the overdue row appears FIRST in the list (due_date ASC NULLS LAST surfaces overdue earliest), upcoming rows below it.
    4. On the overdue row, observe the OVERDUE pill AFTER the status pill, BEFORE the Revoke button. Same colour token as Section A.
    5. Hover the OVERDUE pill. Expected tooltip text: "Was due N days ago" (or "1 day" for N=1) — base-ui Tooltip primitive.
    6. Compare visual contrast against the dark admin surface — should be clearly legible (Plan 17-06 Task 1b confirmed ≥ 4.5:1).

    Checkboxes: [ ] Overdue rows surface first via the ORDER BY swap. [ ] Pill renders in correct position. [ ] Tooltip appears with the locked copy on hover. [ ] Contrast feels legible.

    **Section C — Recurrence end-to-end** (success criteria 1):
    1. Prereqs: run the seed SQL above to set `recurrence_rule = '{"frequency":"weekly"}'` on a pending assignment.
    2. As the client_user for that org, navigate to `/client/assignments`, open the assignment, fill it, and submit (use "Fill as is" → fill any required fields → Submit).
    3. After redirect to the assignment landing page, refresh `/client/assignments`.
    4. Expected: a NEW pending assignment appears in the Active tab with:
       - Same template name as the just-completed one.
       - Due date = the just-completed assignment's due_date + 7 days.
       - Status: PENDING.
       - In Supabase Studio: the just-completed row's `recurrence_generated_at` is now set (not null).
    5. Inspect the new row's `template_version_id` — it should be the latest published version of that template (which may equal the prior occurrence's version if no new publish happened in between, OR a newer version if it did).

    Checkboxes: [ ] New pending occurrence appears. [ ] Due date is +7d from prior. [ ] template_version_id is latest published. [ ] Original row's recurrence_generated_at is set.

    **Section D — Cron smoke (manual trigger)** (success criteria 3 + 4):
    1. Prereqs: a preview deploy OR `npm run dev`. CRON_SECRET set in `.env.local` (or use a dev preview where it's `dev`).
    2. Trigger the cron manually:
       ```
       curl -H "Authorization: Bearer ${CRON_SECRET}" https://<preview>.vercel.app/api/cron/assignment-scheduler
       ```
       (Or against dev: `curl http://localhost:3000/api/cron/assignment-scheduler` — works when CRON_SECRET is unset.)
    3. Expected JSON shape: `{"success":true,"remindersSent":N,"recurrencesGenerated":M,"remindersProcessed":X,"recurrencesProcessed":Y}` where the numbers depend on what's seeded.
    4. To test the 7d reminder cadence: seed an assignment with `due_date = (CURRENT_DATE + INTERVAL '7 days')::date`, run the curl, observe `remindersSent: 1` AND check Proton inbox for the email. Confirm n8n routed via the `assignment_reminder` branch (Matt/Finley confirm the n8n workflow side).
    5. To test idempotency: re-run the curl immediately. Expected: `remindersSent: 0` (the row's `last_reminder_sent` is now `7d` and the cron skips it).
    6. To test failure handling: temporarily set `N8N_WEBHOOK_URL` to an invalid URL. Re-run with a fresh seed. Expected: a `workflow_errors` row appears AND the assignment's `last_reminder_sent` remains null (cron will retry on next tick).

    Checkboxes: [ ] Authed curl returns 200 + JSON summary. [ ] 7d reminder fires once and emails reach Proton. [ ] Re-run is idempotent (no double-send). [ ] Failure path inserts workflow_errors AND leaves dedup column unchanged.

    Footer of the UAT:
    - **Approval:** Matt sign-off date: ______. Notes:
    - **Known follow-ups:** Wire the n8n `assignment_reminder` branch on the n8n side (out-of-repo; Matt/Finley own).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('.planning/phases/17-assignment-scheduling-notifications/17-UAT.md')){console.error('UAT missing');process.exit(1);} const uat=fs.readFileSync('.planning/phases/17-assignment-scheduling-notifications/17-UAT.md','utf8');for (const s of ['Section A','Section B','Section C','Section D','OVERDUE','Was due','assignment-scheduler','recurrence_rule','workflow_errors','aria-label','tooltip','last_reminder_sent']) {if(!uat.includes(s) && !uat.toLowerCase().includes(s.toLowerCase())){console.error('UAT missing keyword:',s);process.exit(2);}} const checkboxCount=(uat.match(/\\[\\s*\\]/g)||[]).length; if(checkboxCount<10){console.error('UAT needs ≥10 checkbox items, found',checkboxCount);process.exit(3);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-assignment-scheduling-notifications/17-UAT.md` exists.
    - File contains the four section headings (substring match for "Section A", "Section B", "Section C", "Section D" — case-insensitive acceptable).
    - File contains the literal "OVERDUE" (pill copy), "Was due" (admin tooltip copy prefix), "assignment-scheduler" (cron path), "recurrence_rule" (DB column for Section C seed), "workflow_errors" (Section D idempotency test), "aria-label" (Section A SR check), "last_reminder_sent" (Section D idempotency).
    - File contains at least 10 checkbox items (`[ ]` markdown checkboxes).
    - File contains the seed SQL block for converting an assignment to recurring (`UPDATE public.form_assignments SET recurrence_rule = ...`).
  </acceptance_criteria>
  <done>
    Matt has a single-document walkthrough for the four ROADMAP success criteria. Each section is independently runnable; Section C requires a one-line SQL seed (provided inline).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md Phase 17 entry + Progress Table</name>
  <read_first>
    - .planning/ROADMAP.md (lines 117-119: Progress Table row for Phase 17; lines 382-391: Phase 17 entry; lines 348-380: Phase 16 entry — the structural analog with full Plans enumeration to mirror)
    - .planning/phases/17-assignment-scheduling-notifications/17-01-PLAN.md through 17-06-PLAN.md (read the objectives to write the Plans enumeration tersely)
  </read_first>
  <behavior>
    - ROADMAP.md Phase 17 entry replaces the line `**Plans**: Not yet planned — run \`/gsd:plan-phase 17\`.` with a Wave-grouped Plans enumeration listing 17-01 through 17-06 (analogous to Phase 16's enumeration at lines 358-378).
    - The Goal line stays unchanged (already accurate: "Recurring form assignments auto-generate on schedule and clients receive automated reminders").
    - The Depends on / Requirements / Success Criteria stay unchanged.
    - ROADMAP.md Progress Table row for Phase 17 (line 118) changes from `| 17. Assignment Scheduling + Notifications | 0/0 | Not planned | - |` to `| 17. Assignment Scheduling + Notifications | 6/6 | In Progress | |` (matching the Phase 14/15 "In Progress" pattern — phase code is shipped; UAT is the open item).
    - No other ROADMAP changes. Specifically: do NOT touch the Phase 18 entry, do NOT touch the v2 Requirements section, do NOT touch the Coverage Audit table.
  </behavior>
  <files>
    - .planning/ROADMAP.md (MODIFY)
  </files>
  <action>
    Edit `.planning/ROADMAP.md`. Apply two changes:

    1. Locate the Phase 17 entry (search for `### Phase 17: Assignment Scheduling + Notifications`). Replace the line `**Plans**: Not yet planned — run \`/gsd:plan-phase 17\`.` with:

       ```
       **Plans**: 6 plans

       Plans:

       **Wave 0** *(schema + shared lib + test scaffolds)*
       - [ ] 17-01-PLAN.md — Migration 015 (recurrence_rule + last_reminder_sent + recurrence_generated_at) + lib/assignments/is-overdue.ts extraction + n8n NotificationPayload union extension + tests/scheduler scaffolds (wave 0)

       **Wave 1** *(pure scheduler functions)*
       - [ ] 17-02-PLAN.md — lib/scheduler/generate-next-occurrence.ts + lib/scheduler/send-reminder.ts + filled specs (wave 1)

       **Wave 2** *(UI + cron — fully parallel)*
       - [ ] 17-03-PLAN.md — Shared OverduePill + mount on both surfaces + admin ORDER BY swap + WCAG-pending markup (wave 2)
       - [ ] 17-04-PLAN.md — Cron route handler + vercel.json entry + inline recurrence trigger in submitAssignedFillByIdAction (wave 2)

       **Wave 3** *(cron regression spec)*
       - [ ] 17-05-PLAN.md — Filled tests/scheduler/cron-reminder-decision.spec.ts (auth gate + filters + cadence ladder + idempotency) (wave 3)

       **Wave 4** *(BLOCKING: db push + types regen + WCAG + UAT + ROADMAP)*
       - [ ] 17-06-PLAN.md — supabase db push migration 015 + types regen + full sweep + WCAG AA contrast verification + 17-UAT.md + this ROADMAP update (wave 4)
       ```

       Preserve the surrounding context lines (Goal, Depends on, Requirements, Success Criteria).

    2. Locate the Progress Table (search for `## Progress Table` or the `| Phase | Plans Complete | Status | Completed |` header). Find the Phase 17 row `| 17. Assignment Scheduling + Notifications | 0/0 | Not planned | - |` and replace it with `| 17. Assignment Scheduling + Notifications | 6/6 | In Progress | |` (six slashes-six plans; status "In Progress" because UAT is pending; trailing pipe with empty completed-date cell).

    Do NOT add any other changes — no Phase 18 updates, no v2 Requirements rewrites, no Coverage Audit edits. Phase 18 stays "Not planned" until its own plan-phase pass.

    After the edits, run `node -e "const r=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); for (const s of ['17-01-PLAN.md','17-02-PLAN.md','17-03-PLAN.md','17-04-PLAN.md','17-05-PLAN.md','17-06-PLAN.md','6 plans','| 6/6 |']) { if(!r.includes(s)) { console.error('ROADMAP missing:',s); process.exit(1); } } console.log('OK')"`.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); for (const s of ['17-01-PLAN.md','17-02-PLAN.md','17-03-PLAN.md','17-04-PLAN.md','17-05-PLAN.md','17-06-PLAN.md','6 plans']) { if(!r.includes(s)) { console.error('ROADMAP missing:',s); process.exit(1); } } if(!/\\|\\s*17\\.\\s*Assignment Scheduling \\+ Notifications\\s*\\|\\s*6\\/6\\s*\\|/.test(r)){console.error('Progress Table row not updated to 6/6');process.exit(2);} if(r.includes('Not yet planned — run `/gsd:plan-phase 17`.')){console.error('Old placeholder line still present');process.exit(3);} if(!r.includes('Goal**: Recurring form assignments')){console.error('Phase 17 Goal accidentally deleted');process.exit(4);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md contains references to 17-01-PLAN.md through 17-06-PLAN.md.
    - ROADMAP.md Phase 17 Plans line reads `6 plans`.
    - The old placeholder `Not yet planned — run \`/gsd:plan-phase 17\`.` is REMOVED from the Phase 17 entry.
    - ROADMAP.md Progress Table row for Phase 17 reads `6/6` plans and status `In Progress` (regex match against `/\|\s*17\.\s*Assignment Scheduling \+ Notifications\s*\|\s*6\/6\s*\|/`).
    - Phase 17 Goal, Depends on, Requirements, Success Criteria are PRESERVED byte-for-byte (verifiable by grep for the existing phrases like "Recurring form assignments auto-generate on schedule").
    - Phase 18 entry is UNCHANGED (Phase 18 stays "Not planned").
  </acceptance_criteria>
  <done>
    ROADMAP.md reflects the Phase 17 plan structure. Phase 17 status reads "In Progress" until UAT sign-off, mirroring the Phase 14/15 lifecycle pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → live Supabase project | `supabase db push` writes DDL via the linked-project auth. Migration 015 is additive (3 ADD COLUMN + 1 ADD CONSTRAINT) — no destructive operations. |
| regenerated types → application compile | TypeScript compile gate (`npm run build`) confirms types and live schema agree. |
| WCAG verification → production UX | Failing contrast on a shipped pill creates an accessibility regression. Task 1b's contrast computation gates the markup before merging to main. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-PUSH-01 | Tampering | Migration 015 push misfires and a table gets dropped | accept | 015 is purely additive (3 ADD COLUMN IF NOT EXISTS + 1 ADD CONSTRAINT). Nothing deletes data. Supabase has nightly backups as the disaster fallback. |
| T-17-PUSH-02 | Tampering | Types regen produces a malformed file (e.g. partial output) | mitigate | Task 1b regex grep verifies the three new column names are present in the form_assignments region of the generated file. `npm run build` independently confirms the file is valid TS. |
| Phase 17 cron + UI inheritance | (multiple, see Plans 17-03 / 17-04 threat models) | (see prior plans) | mitigate | Task 1b's full sweep re-runs every cron + UI regression test; any new failure mode would surface here. |
| T-17-WCAG | Information Disclosure (accessibility) | A shipped overdue pill with sub-AA contrast invisible to low-vision users | mitigate | Task 1b's contrast computation gates the markup; fallback to `/15` overlay is automatic if `/10` fails on either surface; UI-SPEC explicitly forbids hue-change fallback. |
| T-17-N8N-UAT | Operational | The n8n side has not been told to route `assignment_reminder` — emails would fail until Matt/Finley update n8n | accept | UAT Section D explicitly notes "Matt/Finley own the n8n workflow JSON" and includes a curl smoke that exercises the cron-side path. UAT sign-off is the gate that confirms n8n was updated. |
</threat_model>

<verification>
After all three tasks:
- Live Supabase schema reflects migration 015 (three new columns + CHECK constraint).
- `lib/supabase/database.types.ts` regenerated.
- `npm test --run` and `npm run build` both exit 0.
- WCAG AA contrast for `#a14a2a` on both painted backgrounds is ≥ 4.5:1 (with `/10` overlay OR with `/15` fallback applied).
- `17-UAT.md` exists with 4 sections + ≥10 checkboxes.
- `ROADMAP.md` Phase 17 entry enumerates 6 plans and the Progress Table row reads `6/6 | In Progress`.
- All six Phase-17 plan files (17-01 through 17-06) are referenced from ROADMAP.md.
</verification>

<success_criteria>
- Migration 015 applied to live DB.
- TypeScript types regenerated and reflect all three new columns.
- Full Vitest sweep passes (Phase 16 + Phase 17, 24 new tests added).
- Production build passes.
- WCAG AA verified or `/15` fallback applied to both surfaces.
- 17-UAT.md authored with sections A/B/C/D + seed SQL appendix.
- ROADMAP.md updated: Phase 17 enumeration + Progress Table row.
- Phase 17 ready for the user to walk through UAT and mark complete via the standard verification gate.
</success_criteria>

<output>
After completion, create `.planning/phases/17-assignment-scheduling-notifications/17-06-SUMMARY.md` recording: which migration pushed (015), the types-regen diff signature (line count before/after), the four assertion results from Task 1b (types regex, npm test exit, npm run build exit, WCAG contrast numbers for both surfaces at /10 and /15 if computed), the UAT file path + section count, the ROADMAP diff (Phase 17 entry replacement + Progress Table row update). Also include a callout listing all six Phase 17 plans + their SUMMARY paths so the user can trace the full implementation history.
</output>
