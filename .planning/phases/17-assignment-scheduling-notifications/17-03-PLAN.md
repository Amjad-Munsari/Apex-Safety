---
phase: 17
slug: assignment-scheduling-notifications
plan: "03"
plan_id: 17-03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - app/_components/overdue-pill.tsx
  - app/client/assignments/_components/assignment-card.tsx
  - app/admin/clients/[id]/page.tsx
  - app/admin/clients/[id]/client-tabs.tsx
  - tests/form-builder/overdue-pill.test.ts
  - tests/form-builder/admin-assignments-order-by.test.ts
autonomous: true
requirements:
  - SCHED-03
tags: [ui, overdue, shared-component, wcag, multi-tenancy, vitest]
must_haves:
  truths:
    - "OverduePill shared component exists at app/_components/overdue-pill.tsx and is rendered by BOTH /client/assignments cards (via assignment-card.tsx) AND the admin Assigned Forms tab (via client-tabs.tsx) — UI-SPEC §Component Inventory + §Mount A/B."
    - "OverduePill renders the locked markup from UI-SPEC §Visual Spec: span with classes 'inline-flex items-center px-2 py-0.5 rounded-sm font-mono text-[9px] uppercase tracking-[0.25em] text-[#a14a2a] bg-[#a14a2a]/10' and label text 'OVERDUE'."
    - "OverduePill aria-label uses UI-SPEC locked copy: 'Overdue — was due {N} day{s} ago' for N≥1 OR 'Overdue — was due today' for N=0."
    - "assignment-card.tsx no longer defines an inline isOverdue() — both branches import from @/lib/assignments/is-overdue (the BLOCKING extraction from Plan 17-01 is consumed here, UI-SPEC BLOCKING #2)."
    - "app/admin/clients/[id]/page.tsx ORDER BY for the assignments fetch is changed from `created_at DESC` to `due_date ASC NULLS LAST, created_at DESC` (UI-SPEC BLOCKING #1)."
    - "Tooltip is added ONLY on the admin surface; client surface gets aria-label only (UI-SPEC BLOCKING #4)."
    - "OverduePill returns null when daysOverdue < 1 OR status === 'completed' (UI-SPEC State Matrix — completed never shows overdue)."
    - "tests/form-builder/overdue-pill.test.ts asserts: (a) renders when daysOverdue >= 1, (b) returns null when daysOverdue < 1, (c) returns null when status === 'completed', (d) aria-label pluralisation."
    - "tests/form-builder/admin-assignments-order-by.test.ts asserts the page query chain includes the new ORDER BY clauses with the exact arguments."
  artifacts:
    - path: app/_components/overdue-pill.tsx
      provides: "Shared OverduePill presentational component used on both /client/assignments and /admin/clients/[id]"
      exports: ["OverduePill"]
    - path: app/client/assignments/_components/assignment-card.tsx
      provides: "Modified card: inline isOverdue removed; imports from @/lib/assignments/is-overdue; mounts OverduePill on active variant only"
      contains: "from \"@/lib/assignments/is-overdue\""
    - path: app/admin/clients/[id]/page.tsx
      provides: "Modified RSC: ORDER BY for form_assignments select swapped to due_date ASC NULLS LAST, created_at DESC"
      contains: "due_date"
    - path: app/admin/clients/[id]/client-tabs.tsx
      provides: "Modified tab: OverduePill mounted in the row metadata block, after StatusPill, before RevokeAssignmentButton"
      contains: "OverduePill"
    - path: tests/form-builder/overdue-pill.test.ts
      provides: "4 passing assertions covering OverduePill render/null/aria-label cases"
      contains: "OverduePill"
    - path: tests/form-builder/admin-assignments-order-by.test.ts
      provides: "Spec proving the admin page ORDER BY swap"
      contains: "due_date"
  key_links:
    - from: app/_components/overdue-pill.tsx
      to: lib/assignments/is-overdue.ts
      via: "import { daysOverdue } from '@/lib/assignments/is-overdue'"
      pattern: "from\\s+[\"']@/lib/assignments/is-overdue[\"']"
    - from: app/client/assignments/_components/assignment-card.tsx
      to: app/_components/overdue-pill.tsx
      via: "import { OverduePill } from '@/app/_components/overdue-pill'"
      pattern: "from\\s+[\"']@/app/_components/overdue-pill[\"']"
    - from: app/admin/clients/[id]/client-tabs.tsx
      to: app/_components/overdue-pill.tsx
      via: "import { OverduePill } from '@/app/_components/overdue-pill'"
      pattern: "OverduePill"
---

<objective>
Mount the overdue UI on both surfaces with a single shared component, swap the admin Assigned Forms tab ORDER BY to surface overdue rows first, and finalise the Plan 17-01 inline-helper extraction by deleting the duplicated isOverdue from `assignment-card.tsx`. This plan delivers:

1. `app/_components/overdue-pill.tsx` — new shared, purely-presentational component. Accepts `daysOverdue: number` (and an optional `status` for the completed-never-shows-overdue rule). Renders the locked markup from UI-SPEC §Visual Spec or returns null. The same component is consumed by both surfaces — no per-surface variant.
2. `app/client/assignments/_components/assignment-card.tsx` — delete the inline `isOverdue` function (lines 39-42), import from `@/lib/assignments/is-overdue` and the new `OverduePill`. Mount the pill on the metadata row, after the existing StatusPill, only when `variant === "active"` and the assignment is overdue and not completed (UI-SPEC §Mount A).
3. `app/admin/clients/[id]/client-tabs.tsx` — mount the OverduePill in the assignments tab row, after the existing status pill, before the `<RevokeAssignmentButton>`. Mount via the new shared component, NOT a duplicated helper (UI-SPEC §Mount B).
4. `app/admin/clients/[id]/page.tsx` — swap the form_assignments ORDER BY from `created_at DESC` to `due_date ASC NULLS LAST, created_at DESC` (UI-SPEC BLOCKING #1).
5. Tooltip: the admin row gets a Tooltip wrapper around the pill (using the existing base-ui Tooltip already wired by Plan 16-03 `active-pill.tsx`); the client card does NOT (UI-SPEC BLOCKING #4 — touch surface, aria-label-only).
6. Two Vitest specs: one for OverduePill rendering rules, one for the admin page ORDER BY change.

Purpose: this is the entire UI surface of Phase 17. Recall UI-SPEC §Scope: "one new badge" rendered on two already-shipped surfaces — but doing it right means a shared library, a single component, an ORDER BY swap, and proving the BLOCKING #2 extraction is consumed (not duplicated). No new routes, no new layouts, no new shadcn installs.

Output: One new shared component, three modified production files, two new Vitest specs. Zero migration changes, zero scheduler-module changes.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/17-assignment-scheduling-notifications/17-UI-SPEC.md
@.planning/phases/17-assignment-scheduling-notifications/17-CONTEXT.md
@.planning/phases/17-assignment-scheduling-notifications/17-RESEARCH.md
@.planning/phases/17-assignment-scheduling-notifications/17-01-PLAN.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-03-SUMMARY.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-04-SUMMARY.md

<interfaces>
Existing surfaces this plan modifies (read these files FIRST before any edit):

From `app/client/assignments/_components/assignment-card.tsx` (current state, post-Plan 17-01 which left this file untouched):
- Lines 39-42: inline `isOverdue(dateStr)` — TO DELETE in this plan.
- Lines 68-106: `AssignmentCard({ assignment, variant })` — the JSX to extend. The metadata row at lines 80-89 contains `DUE · {date}` + `<StatusPill>` — insert `<OverduePill>` after `<StatusPill>` (line 88).
- Lines 92-96: completed-variant branch — MUST NOT render OverduePill.

From `app/admin/clients/[id]/client-tabs.tsx` (current state):
- Lines 477-510: the `assignments.map((assignment) => ...)` block. The metadata row at lines 486-495 contains `DUE · {date}` + the inline status pill — insert `<OverduePill>` after the status pill at line 494.
- Line 504-506: `{assignment.status !== "completed" && (<RevokeAssignmentButton ... />)}` — the OverduePill mounts BEFORE this conditional, inside the metadata block.

From `app/admin/clients/[id]/page.tsx` (current state):
- Lines 74-79: the form_assignments fetch — `.from("form_assignments").select(...).eq("client_id", id).is("deleted_at", null).order("created_at", { ascending: false })`. The ORDER BY chain on line 79 is what changes to dual-order: `.order("due_date", { ascending: true, nullsFirst: false }).order("created_at", { ascending: false })`.

From `app/_components/`: directory may not exist yet. Verify with `ls app/_components/ 2>/dev/null` — if it doesn't exist, the Write tool will create the parent directory automatically. There are no existing `app/_components/*` files in Phase 16 (Phase 16's shared components live in `app/admin/clients/_components/` and `app/client/assignments/_components/`). Phase 17 introduces `app/_components/` as the first cross-surface shared location, per UI-SPEC §Component Inventory.

From `lib/assignments/is-overdue.ts` (created in Plan 17-01):
- `export function isOverdue(dateStr: string | null): boolean`
- `export function daysOverdue(dateStr: string | null): number`

Existing Tooltip primitive (used on admin in Phase 16):
- From `app/admin/clients/_components/active-pill.tsx` (Plan 16-03): the pattern wraps `<TooltipProvider><Tooltip><TooltipTrigger className=...><span>...</span></TooltipTrigger><TooltipContent>...</TooltipContent></Tooltip></TooltipProvider>`.
- Base-ui Tooltip primitive: imported from `@/components/ui/tooltip` (the shadcn install path proven by Phase 16). Confirm by reading `app/admin/clients/_components/active-pill.tsx`.
- Constraint: base-ui TooltipTrigger does NOT support `asChild` (Phase 16 Plan 03 SUMMARY §Deviation 2). Style the trigger element directly via `className`.

Test idioms from Phase 16:
- `tests/form-builder/assignments-query.test.ts` (Plan 16-04): the "spy on Supabase query chain" pattern for the admin ORDER BY test — mirrors the assignments-query approach but with `app/admin/clients/[id]/page.tsx` as the SUT.
- `tests/form-builder/save-draft.test.ts` lines 19-66: Vitest mock skeleton for `@/lib/supabase/admin`, `next/cache`, `next/navigation` (the admin RSC imports `adminClient` from `@/lib/supabase/admin`).
- For React component tests: jsdom is already configured in vitest.config.ts. A minimal component test renders `<OverduePill ... />` via React's test-renderer or direct invocation (it's a pure function component returning JSX) and inspects the result. The repo does NOT have @testing-library/react installed — use plain React renderer assertions OR inspect the returned element tree via `OverduePill({ daysOverdue: 5 })` as a function call (which is what a "presentational component" allows — it's just a function that returns a ReactElement; no hooks).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/_components/overdue-pill.tsx + Vitest spec</name>
  <read_first>
    - .planning/phases/17-assignment-scheduling-notifications/17-UI-SPEC.md §"The Overdue Badge" (locked copy + colour tokens), §Visual Spec (exact className string), §Accessibility (aria-label patterns), §State Matrix (gating conditions)
    - lib/assignments/is-overdue.ts (created in Plan 17-01 — the daysOverdue helper this component consumes)
    - app/admin/clients/_components/active-pill.tsx (Phase 16 Plan 03 — the closest analog: returns null when count===0, base-ui Tooltip pattern with no asChild)
    - app/client/assignments/_components/assignment-card.tsx (lines 44-66 — the StatusPill markup that establishes the pill geometry vocabulary)
  </read_first>
  <behavior>
    - File `app/_components/overdue-pill.tsx` exists and starts with the `"use client"` directive ONLY if the component uses Tooltip (which is client-only in base-ui). Since Plan 17-03 mounts the Tooltip on the admin surface — and the same component is used on client — the simplest path: the OverduePill itself is a server-compatible function (no hooks, no client state), and the admin tab wraps it in a Tooltip wrapper at the call site. This keeps OverduePill usable from RSCs.
    - Component signature: `export function OverduePill({ daysOverdue, status }: { daysOverdue: number; status?: string })`.
    - Returns `null` when `daysOverdue < 1` (catches negative and zero — defensive even though callers should gate).
    - Returns `null` when `status === "completed"` (UI-SPEC State Matrix — completed never overdue).
    - When rendering, returns exactly:
      ```tsx
      <span
        className="inline-flex items-center px-2 py-0.5 rounded-sm font-mono text-[9px] uppercase tracking-[0.25em] text-[#a14a2a] bg-[#a14a2a]/10"
        aria-label={daysOverdue === 0
          ? "Overdue — was due today"
          : `Overdue — was due ${daysOverdue} day${daysOverdue === 1 ? "" : "s"} ago`}
      >
        OVERDUE
      </span>
      ```
    - NOTE on the `daysOverdue === 0` aria-label: per UI-SPEC §"Locked copy", N=0 is the same-day cron-transition case. But the component returns null when `daysOverdue < 1`. Resolve the contradiction: render path is entered only when `daysOverdue >= 1`. The `=== 0` branch in the aria-label is technically unreachable but kept defensively for future caller flexibility (e.g. if the caller uses `Math.floor` that rounds down to 0 within the same day). The condition `daysOverdue < 1` (strict less than 1) — adjust to `daysOverdue <= 0` to match the State Matrix wording: "due == today is not yet overdue". Then `=== 0` literally never enters the render path. The `=== 0` branch in the aria-label is dead code; keep it for safety but the active branch is the pluralised one.
    - No icon. No border. No Tooltip — Tooltip wrapping is the caller's responsibility (admin only).
    - No "use client" directive (the component is server-compatible).
    - The component imports nothing from the codebase except React (implicit via tsx). No `daysOverdue` recomputation inside the component — it accepts the precomputed number.
    - File `tests/form-builder/overdue-pill.test.ts` exists and has 4 passing `it()` blocks:
      1. Renders when `daysOverdue: 5` (not null, returns a ReactElement whose props include the className substring "text-[#a14a2a]").
      2. Returns null when `daysOverdue: 0`.
      3. Returns null when `daysOverdue: 5, status: "completed"`.
      4. aria-label pluralisation: `daysOverdue: 1` → aria-label contains "1 day ago"; `daysOverdue: 2` → aria-label contains "2 days ago".
  </behavior>
  <files>
    - app/_components/overdue-pill.tsx (NEW)
    - tests/form-builder/overdue-pill.test.ts (NEW)
  </files>
  <action>
    Create directory `app/_components/` if it does not exist (Write tool creates parents). Create `app/_components/overdue-pill.tsx`:

    The component is a pure function — no hooks, no state, no effects. Body:
    1. Type the props: `{ daysOverdue: number; status?: string }`.
    2. Early-return null when `daysOverdue <= 0` OR `status === "completed"`.
    3. Compute the aria-label: `daysOverdue === 0 ? "Overdue — was due today" : ` followed by template literal with pluralised "day"/"days".
    4. Return the `<span>` with the exact className string from UI-SPEC §Visual Spec (do NOT modify any class — it's load-bearing for the WCAG verification in Plan 17-06 acceptance).
    5. Pill text content: literal `OVERDUE` (uppercase per UI-SPEC).

    Do NOT add `"use client"`. Do NOT import Tooltip. Do NOT import `daysOverdue` from `@/lib/assignments/is-overdue` — the value is passed in by the caller (which is responsible for calling `daysOverdue(assignment.due_date)`). This keeps the component free of date-math and trivially unit-testable.

    Then create `tests/form-builder/overdue-pill.test.ts`. Imports:
    ```
    import { describe, it, expect } from "vitest";
    import { OverduePill } from "@/app/_components/overdue-pill";
    ```

    Component-as-function inspection idiom: call `OverduePill({ daysOverdue: 5 })` directly and inspect the returned React element via `result.props.className`, `result.props["aria-label"]`, `result.props.children`. This is a Vitest 3 + React 19 valid pattern for pure functional components — no @testing-library required.

    Write 4 `it()` blocks per `<behavior>`. For Test 1 assert `result !== null && result.props.className.includes('text-[#a14a2a]') && result.props.children === 'OVERDUE'`. For Test 2 + 3 assert `result === null`. For Test 4 invoke twice with `daysOverdue: 1` and `daysOverdue: 2` and assert the `aria-label` substring for each.

    Run `npm test -- tests/form-builder/overdue-pill.test.ts --run` and confirm 4 tests pass.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readFileSync('app/_components/overdue-pill.tsx','utf8');for (const s of ['export function OverduePill','text-[#a14a2a]','bg-[#a14a2a]/10','OVERDUE','aria-label','Overdue — was due']) { if(!f.includes(s)) { console.error('Missing:',s); process.exit(1); } } if(/[\"']use client[\"']/.test(f)){console.error('OverduePill must NOT be a client component — Tooltip wrapping is the caller responsibility');process.exit(2);} const t=fs.readFileSync('tests/form-builder/overdue-pill.test.ts','utf8'); const itMatches=t.match(/\\bit\\(/g); if(!itMatches||itMatches.length<4){console.error('Spec needs ≥4 it() blocks, found',itMatches?itMatches.length:0);process.exit(3);} if(!t.includes('OverduePill')){console.error('Spec must import OverduePill');process.exit(4);} console.log('OK');" && npm test -- tests/form-builder/overdue-pill.test.ts --run --reporter=basic 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `app/_components/overdue-pill.tsx` exists.
    - `grep -c 'export function OverduePill' app/_components/overdue-pill.tsx` = 1.
    - File contains the substrings: `text-[#a14a2a]`, `bg-[#a14a2a]/10`, `OVERDUE`, `aria-label`, `Overdue — was due`.
    - `grep -c '"use client"' app/_components/overdue-pill.tsx` = 0 AND `grep -c "'use client'" app/_components/overdue-pill.tsx` = 0 (component is server-compatible).
    - File does NOT import Tooltip (grep `Tooltip` → 0 matches inside this file).
    - File does NOT import from `@/lib/assignments/is-overdue` (daysOverdue is a prop, not a re-computation).
    - `tests/form-builder/overdue-pill.test.ts` exists, has at least 4 `it(` matches, and zero `it.todo`.
    - `npm test -- tests/form-builder/overdue-pill.test.ts --run --reporter=basic` exits 0 and reports 4 passing tests.
  </acceptance_criteria>
  <done>
    OverduePill is a single, server-compatible, unit-tested component shared across both surfaces. Tooltip wrapping is opt-in at the call site (admin only) — keeps the client touch surface tooltip-free per UI-SPEC BLOCKING #4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire OverduePill into assignment-card.tsx + finalise is-overdue extraction</name>
  <read_first>
    - app/client/assignments/_components/assignment-card.tsx (full file — Plan 17-01 left it untouched; this task modifies it)
    - app/_components/overdue-pill.tsx (just created in Task 1)
    - lib/assignments/is-overdue.ts (Plan 17-01)
    - .planning/phases/17-assignment-scheduling-notifications/17-UI-SPEC.md §Mount A (the exact insertion point: after StatusPill on the metadata row), §State Matrix (gating: variant==="active" AND overdue AND status!=="completed")
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-04-SUMMARY.md §Task 1 (the card's current shape — line 88 is where the new pill mounts; the existing `overdue` boolean at line 70 can be replaced with the imported `isOverdue` call)
  </read_first>
  <behavior>
    - The inline `isOverdue` function definition at lines 39-42 of `app/client/assignments/_components/assignment-card.tsx` is REMOVED. There must be ZERO `function isOverdue` declarations remaining in that file after this task — grep proves it.
    - The file imports `isOverdue` and `daysOverdue` from `@/lib/assignments/is-overdue` at the top of the file.
    - The file imports `OverduePill` from `@/app/_components/overdue-pill`.
    - The `AssignmentCard` body keeps the existing local `const overdue = isOverdue(assignment.due_date);` (line 70 equivalent) — just sourced from the import, not the inline definition.
    - The metadata row (currently lines 80-89) renders `<OverduePill daysOverdue={daysOverdue(assignment.due_date)} status={assignment.status} />` AFTER the existing `<StatusPill>` (line 88), ONLY when `variant === "active"`. The completed branch (variant === "completed") still does NOT render OverduePill (the existing "View submission →" link at line 100-102 stays unchanged; no overdue pill on completed cards per UI-SPEC State Matrix).
    - The existing `text-[#e55a3a]` overdue colouring on the date string (lines 82-85) is PRESERVED — the OverduePill is additive, not a replacement (UI-SPEC §Mount A "The pill is additive, not a replacement").
    - No other lines in `assignment-card.tsx` change. The `getTemplateName`, `formatDate`, `StatusPill`, and `AssignmentCard` function definitions all retain their current bodies modulo the two import additions and the one inline-function deletion and the one new JSX line.
  </behavior>
  <files>
    - app/client/assignments/_components/assignment-card.tsx (MODIFY)
  </files>
  <action>
    Open `app/client/assignments/_components/assignment-card.tsx`. Apply four ordered edits:

    1. At the top of the file (before the existing interface declarations), add the two imports:
       ```
       import { isOverdue, daysOverdue } from "@/lib/assignments/is-overdue";
       import { OverduePill } from "@/app/_components/overdue-pill";
       ```

    2. Delete the inline `isOverdue` function declaration currently at lines 39-42 (the four-line block: `function isOverdue(dateStr: string | null): boolean { ... }`). After deletion the `formatDate` function (lines 30-37) is immediately followed by the `StatusPill` function (now line 39).

    3. In the `AssignmentCard` body, the existing `const overdue = isOverdue(assignment.due_date);` (was line 70) now resolves to the imported helper — no edit needed beyond the import. Confirm by reading the surrounding lines after edits 1 and 2 are applied.

    4. In the JSX metadata row (was lines 80-89), insert the OverduePill AFTER the `<StatusPill status={assignment.status} />` and ONLY for `variant === "active"`:
       ```tsx
       <div className="flex items-center gap-3 flex-wrap">
         <span
           className={`font-mono text-[9px] uppercase tracking-[0.2em] ${
             overdue ? "text-[#e55a3a]" : "text-[#8a857f]"
           }`}
         >
           DUE · {dueDateText}
         </span>
         <StatusPill status={assignment.status} />
         {variant === "active" && (
           <OverduePill
             daysOverdue={daysOverdue(assignment.due_date)}
             status={assignment.status}
           />
         )}
       </div>
       ```

    Do NOT modify: `getTemplateName`, `formatDate`, `StatusPill`, the completed-variant branch (the "View submission →" anchor), the article wrapper className, the Row 3 instructions block, the interface declarations (Assignment, AssignmentCardProps).

    Run `npx tsc --noEmit app/client/assignments/_components/assignment-card.tsx` and confirm zero TS errors. Then run `npm test -- tests/form-builder/ --run` (full form-builder sweep) and confirm no regressions in Phase 16 specs (assignments-query, assignment-status-transitions, customer-self-fill-submission, fork-assigned-template) — they all should still pass because they assert on the page query shape, not the card markup.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readFileSync('app/client/assignments/_components/assignment-card.tsx','utf8');if(/function isOverdue/.test(f)){console.error('Inline isOverdue function still present — Plan 17-01 BLOCKING #2 extraction not finalised');process.exit(1);} for (const s of ['from \"@/lib/assignments/is-overdue\"','from \"@/app/_components/overdue-pill\"','OverduePill','daysOverdue(','isOverdue(']) { if(!f.includes(s)) { console.error('Missing:',s); process.exit(2); } } if(!/variant === [\"']active[\"']\\s*&&\\s*\\(\\s*<OverduePill/.test(f)){console.error('OverduePill must be gated on variant === active');process.exit(3);} console.log('OK');" && npx tsc --noEmit 2>&1 | grep -E "assignment-card\\.tsx.*error TS" | wc -l | grep -E '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'function isOverdue' app/client/assignments/_components/assignment-card.tsx` = 0 (the inline declaration is gone — Plan 17-01 BLOCKING #2 finalised).
    - `grep -c 'from "@/lib/assignments/is-overdue"' app/client/assignments/_components/assignment-card.tsx` = 1.
    - `grep -c 'from "@/app/_components/overdue-pill"' app/client/assignments/_components/assignment-card.tsx` = 1.
    - `grep -c '<OverduePill' app/client/assignments/_components/assignment-card.tsx` = 1 (single mount, on the active variant branch).
    - The file contains the gating regex `variant === "active"` somewhere on or above the OverduePill JSX line (regex: `/variant === ["']active["']\s*&&\s*\(\s*<OverduePill/`).
    - The file STILL contains the existing destructive-red date colouring (`text-[#e55a3a]`) on the DUE date span (UI-SPEC requirement: the pill is additive).
    - `npx tsc --noEmit` reports zero TS errors specifically for `assignment-card.tsx`.
    - `npm test -- tests/form-builder/ --run --reporter=basic` exits 0 (Phase 16 specs unaffected).
  </acceptance_criteria>
  <done>
    The client card consumes the shared isOverdue + OverduePill. The duplicated inline helper is gone — single source of truth for overdue derivation is now `@/lib/assignments/is-overdue`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire OverduePill + Tooltip into admin client-tabs.tsx + swap admin page ORDER BY + ORDER BY spec</name>
  <read_first>
    - app/admin/clients/[id]/client-tabs.tsx (lines 477-510 — the assignments tab row block; lines 486-495 — the metadata row; line 504 — the RevokeAssignmentButton gating)
    - app/admin/clients/[id]/page.tsx (lines 74-79 — the form_assignments fetch; line 79 has `.order("created_at", { ascending: false })` — this changes)
    - app/_components/overdue-pill.tsx (Task 1 output)
    - lib/assignments/is-overdue.ts (Plan 17-01 — `daysOverdue` is consumed at the call site since the tab is a client component that already accepts `assignments` prop with `due_date` field)
    - app/admin/clients/_components/active-pill.tsx (Phase 16 Plan 03 — the Tooltip pattern to mirror; SUMMARY §Deviation 2 — no asChild on base-ui Tooltip)
    - .planning/phases/17-assignment-scheduling-notifications/17-UI-SPEC.md §Mount B (admin), §BLOCKING #1 (ORDER BY swap), §"Tooltip text (admin row, hover)" (locked copy: "Was due {N} day{s} ago")
    - tests/form-builder/assignments-query.test.ts (Plan 16-04 — the spy-on-Supabase-chain pattern for the admin page ORDER BY spec in this task)
  </read_first>
  <behavior>
    - In `app/admin/clients/[id]/page.tsx`, the `form_assignments` fetch's `.order()` chain (currently a single `.order("created_at", { ascending: false })`) is replaced with TWO orders in this exact precedence: `.order("due_date", { ascending: true, nullsFirst: false }).order("created_at", { ascending: false })`. No other change to the page.
    - In `app/admin/clients/[id]/client-tabs.tsx`, imports are extended with `import { OverduePill } from "@/app/_components/overdue-pill";` and `import { daysOverdue } from "@/lib/assignments/is-overdue";` and `import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip";` (mirror the import pattern from `active-pill.tsx`).
    - In the assignments tab row block (the `assignments.map` body), after the existing inline status pill `<span ... className=... assignmentStatusClass(assignment.status) ...>{assignmentStatusLabel(...)}</span>` and BEFORE the RevokeAssignmentButton, insert the OverduePill wrapped in a Tooltip. The Tooltip content is the locked copy from UI-SPEC: `Was due ${daysOverdue(assignment.due_date)} day${daysOverdue(assignment.due_date) === 1 ? "" : "s"} ago`. The Tooltip is only rendered when `daysOverdue(assignment.due_date) >= 1` AND `assignment.status !== "completed"` — both conditions also gate the pill itself (which returns null on its own, but the Tooltip wrapper should not render an empty trigger).
    - The Tooltip trigger is the OverduePill element directly — base-ui Tooltip does NOT support `asChild`, so the OverduePill `<span>` IS the trigger (compose by passing OverduePill as the TooltipTrigger child or by wrapping the OverduePill output in a TooltipTrigger element styled via className — match Plan 16-03 Plan 03's active-pill pattern).
    - File `tests/form-builder/admin-assignments-order-by.test.ts` exists and asserts that the page's form_assignments fetch chain calls `.order("due_date", { ascending: true, nullsFirst: false })` followed by `.order("created_at", { ascending: false })`. Use the same Vitest mock idiom Plan 16-04 used for `assignments-query.test.ts` — mock `@/lib/supabase/admin`, spy on the chain, render the page (or just invoke the relevant chain by calling the page function as a server component).
  </behavior>
  <files>
    - app/admin/clients/[id]/page.tsx (MODIFY — ORDER BY swap)
    - app/admin/clients/[id]/client-tabs.tsx (MODIFY — OverduePill mount + Tooltip wrapper)
    - tests/form-builder/admin-assignments-order-by.test.ts (NEW)
  </files>
  <action>
    Edit `app/admin/clients/[id]/page.tsx`. The single change is in the form_assignments fetch (currently at lines 74-79). Replace:
    ```
    const { data: assignmentRows } = await adminClient
      .from("form_assignments")
      .select("id, status, due_date, instructions, created_at, template:form_templates(id, name)")
      .eq("client_id", id)
      .is("deleted_at", null)
      .order("created_at", { ascending: false })
    ```
    with:
    ```
    const { data: assignmentRows } = await adminClient
      .from("form_assignments")
      .select("id, status, due_date, instructions, created_at, template:form_templates(id, name)")
      .eq("client_id", id)
      .is("deleted_at", null)
      .order("due_date", { ascending: true, nullsFirst: false })
      .order("created_at", { ascending: false })
    ```
    Do NOT change the SELECT clause, the `.eq("client_id", id)` defense-in-depth filter, or `.is("deleted_at", null)`. Do NOT touch the published-templates fetch (lines 82-87).

    Edit `app/admin/clients/[id]/client-tabs.tsx`. Apply three edits:

    1. Top-of-file imports: append `import { OverduePill } from "@/app/_components/overdue-pill"` and `import { daysOverdue } from "@/lib/assignments/is-overdue"` and `import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@/components/ui/tooltip"`. Preserve the existing imports (Card, Tabs, lucide-react icons, AssignTemplateModal, RevokeAssignmentButton).

    2. In the assignments tab row metadata block (was lines 486-495), keep the existing DUE+status pill and insert the OverduePill wrapped in a Tooltip AFTER the status pill `<span>`. The exact JSX:
       ```tsx
       {(() => {
         const d = daysOverdue(assignment.due_date);
         if (d < 1 || assignment.status === "completed") return null;
         return (
           <TooltipProvider>
             <Tooltip>
               <TooltipTrigger
                 className="inline-flex items-center px-2 py-0.5 rounded-sm font-mono text-[9px] uppercase tracking-[0.25em] text-[#a14a2a] bg-[#a14a2a]/10"
                 aria-label={`Overdue — was due ${d} day${d === 1 ? "" : "s"} ago`}
               >
                 OVERDUE
               </TooltipTrigger>
               <TooltipContent>
                 Was due {d} day{d === 1 ? "" : "s"} ago
               </TooltipContent>
             </Tooltip>
           </TooltipProvider>
         );
       })()}
       ```
       Rationale on inlining: base-ui's TooltipTrigger does NOT support asChild (Plan 16-03 §Deviation 2 — verified). The compromise: the trigger IS the styled pill. The OverduePill component (Task 1) is used as-is on the client card surface (no tooltip); on admin we inline the same className + aria-label on the TooltipTrigger so the visual is identical. Both paths use the same className string from UI-SPEC §Visual Spec — this is the unavoidable cost of the no-asChild constraint.

       Place this block inside the existing metadata `<div className="flex items-center gap-3 mt-1">` (was line 486), AFTER the existing inline status pill `<span>` (was line 490-494).

    3. No other JSX changes. The RevokeAssignmentButton conditional (was line 504) stays where it is — the Tooltip-wrapped pill is logically before it.

    Create `tests/form-builder/admin-assignments-order-by.test.ts`. Use the spy-on-Supabase-chain pattern from `tests/form-builder/assignments-query.test.ts`:
    ```
    import { describe, it, expect, vi } from "vitest";

    const orderSpy = vi.fn();
    // chain stub returning itself for every method until select() returns the order-spy-bearing object

    vi.mock("@/lib/supabase/admin", () => ({
      adminClient: {
        from: vi.fn(() => ({
          select: vi.fn().mockReturnThis(),
          eq: vi.fn().mockReturnThis(),
          is: vi.fn().mockReturnThis(),
          order: orderSpy.mockReturnThis(),
          single: vi.fn(() => Promise.resolve({ data: null })),
        })),
      },
    }));
    ```
    (Mirror tests/form-builder/assignments-query.test.ts lines 1-66 — the dual-call-chainable orderSpy idiom referenced in Plan 16-04 SUMMARY.)

    Then write one `it("orders form_assignments by due_date ASC NULLS LAST then created_at DESC")` test that imports the page module (which will trigger the mocked Supabase chain on module load OR — preferred — calls the default export with a stub `params`). Assert:
    - `orderSpy.mock.calls.some(call => call[0] === "due_date" && call[1]?.ascending === true && call[1]?.nullsFirst === false)` is true.
    - `orderSpy.mock.calls.some(call => call[0] === "created_at" && call[1]?.ascending === false)` is true.
    - The "due_date" call comes before the "created_at" call in `orderSpy.mock.calls` (assert via index comparison).

    Run `npm test -- tests/form-builder/admin-assignments-order-by.test.ts tests/form-builder/overdue-pill.test.ts --run` and confirm both specs pass. Then run `npm test -- tests/form-builder/ --run` and confirm zero Phase 16 regressions.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('app/admin/clients/[id]/page.tsx','utf8');const orderRe=/order\\([\"']due_date[\"']\\s*,\\s*\\{\\s*ascending:\\s*true\\s*,\\s*nullsFirst:\\s*false\\s*\\}\\)/;if(!orderRe.test(p)){console.error('page.tsx ORDER BY swap missing — due_date ASC NULLS FALSE not found');process.exit(1);} const ctTab=fs.readFileSync('app/admin/clients/[id]/client-tabs.tsx','utf8');for (const s of ['from \"@/lib/assignments/is-overdue\"','from \"@/components/ui/tooltip\"','TooltipProvider','TooltipTrigger','TooltipContent','text-[#a14a2a]','Was due ','daysOverdue(']) { if(!ctTab.includes(s)) { console.error('client-tabs missing:',s); process.exit(2); } } const spec=fs.readFileSync('tests/form-builder/admin-assignments-order-by.test.ts','utf8');if(!spec.includes('due_date')){console.error('Spec must assert due_date order');process.exit(3);} if(!spec.includes('orderSpy')&&!spec.includes('order:')){console.error('Spec must spy on .order calls');process.exit(4);} console.log('OK');" && npm test -- tests/form-builder/admin-assignments-order-by.test.ts tests/form-builder/overdue-pill.test.ts --run --reporter=basic 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/clients/[id]/page.tsx` contains a substring matching the regex `/order\(["']due_date["']\s*,\s*\{\s*ascending:\s*true\s*,\s*nullsFirst:\s*false\s*\}\)/`.
    - `app/admin/clients/[id]/page.tsx` still contains `.order("created_at"` (the second order remains).
    - `app/admin/clients/[id]/client-tabs.tsx` contains all three import substrings: `from "@/lib/assignments/is-overdue"`, `from "@/components/ui/tooltip"`, AND `daysOverdue` referenced in JSX (not just import).
    - `app/admin/clients/[id]/client-tabs.tsx` contains the substrings `TooltipProvider`, `TooltipTrigger`, `TooltipContent`, `text-[#a14a2a]`, `Was due ` (the locked tooltip copy prefix).
    - `app/admin/clients/[id]/client-tabs.tsx` ALSO contains the literal `OVERDUE` (the pill text).
    - `tests/form-builder/admin-assignments-order-by.test.ts` exists, contains `due_date` and a spy on `.order` (substring `order:` or `orderSpy`), and has at least 1 `it(` block.
    - `npm test -- tests/form-builder/admin-assignments-order-by.test.ts tests/form-builder/overdue-pill.test.ts --run` exits 0 and reports passing tests for both specs.
    - `npm test -- tests/form-builder/ --run --reporter=basic` exits 0 (full form-builder sweep — no regression on Phase 16 specs).
    - `npx tsc --noEmit` reports zero TS errors specifically for `client-tabs.tsx` and `page.tsx`.
  </acceptance_criteria>
  <done>
    Admin Assigned Forms tab surfaces overdue first (BLOCKING #1 resolved); the OverduePill (or its inlined Tooltip-trigger equivalent due to base-ui's no-asChild constraint) renders with the locked aria-label + tooltip copy; the client surface remains tooltip-free per BLOCKING #4. The ORDER BY swap is regression-asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC server query → admin tab UI | `app/admin/clients/[id]/page.tsx` runs as an admin RSC using `adminClient` (service-role). The new ORDER BY swap does not change the trust gate — the page is still admin-only via the existing admin proxy. |
| Client component → user-displayed overdue state | `assignment-card.tsx` and `client-tabs.tsx` derive overdue from the row's `due_date` and `status` (both already enforced by RLS to be the calling client's own data on the client surface, and by admin-proxy on the admin surface). No new data crosses a trust boundary. |
| Phase 16 invariants (carried forward) | T-16-01 (RLS gate on form_assignments), T-16-04 (`client_id` from server context only), T-16-08 (`.is("deleted_at", null)` filter), T-16-06 (no service-role on client surface). This plan does not weaken any of them — the changes are presentational + a query-order swap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 (inherits T-16-01) | Information Disclosure | The OverduePill is rendered server-side on `/client/assignments` (RSC). If a stale or wrong assignment is passed to the card, the pill would label the wrong row as overdue — but RLS is the actual gate; this is a derived display state | accept | The card receives `assignment` props from `app/client/assignments/page.tsx` which fetches with `.eq("client_id", ctx.client_id).is("deleted_at", null)` (Plan 16-04 — confirmed in the existing page.tsx). No change to that fetch in this plan. |
| T-17-09 (inherits T-16-08) | Information Disclosure | The admin ORDER BY swap could surface a soft-deleted row if `.is("deleted_at", null)` were dropped during the edit | mitigate | Task 3 acceptance criteria includes `grep -c 'is("deleted_at", null)' app/admin/clients/[id]/page.tsx` ≥ 1 — verifying the deleted_at filter survived the ORDER BY swap. Plan 17-06 full sweep re-confirms via `npm run build` + tests. |
| T-17-10 | Tampering | Inline TooltipTrigger className duplication (admin) vs the OverduePill className (client) could drift if UI-SPEC changes the colour token | accept | The duplication is a known cost of base-ui's no-asChild constraint (Phase 16 §Deviation 2). Plan 17-06's WCAG AA check covers both surfaces, and any future colour token change requires editing both sites. Logged here so a future planner is aware. |
| T-17-11 | Tampering | A regression test bypass — if `tests/form-builder/admin-assignments-order-by.test.ts` is silently deleted, a future ORDER BY change could go uncaught | mitigate | Plan 17-06's full Vitest sweep + the must_haves contract here both reference the spec. Phase verification gate (Plan 17-06 Task 1b) re-confirms suite count. |
| T-17-12 (Phase 17 specific) | Information Disclosure | OverduePill aria-label leaks N days overdue to screen readers — this is intentional context, not a privacy leak (the user already sees the row + the due date). Documented to close the threat-modelling loop. | accept | Same data is visibly displayed via the DUE date text. Accessibility win > tiny incremental disclosure (none, really). |
</threat_model>

<verification>
After all three tasks:
- `node -e "require('fs').readFileSync('app/_components/overdue-pill.tsx','utf8').includes('export function OverduePill') || process.exit(1)"` exits 0.
- `node -e "const f=require('fs').readFileSync('app/client/assignments/_components/assignment-card.tsx','utf8'); if(/function isOverdue/.test(f)) process.exit(1); if(!f.includes('@/lib/assignments/is-overdue')) process.exit(2); if(!f.includes('OverduePill')) process.exit(3);"` exits 0.
- `node -e "const f=require('fs').readFileSync('app/admin/clients/[id]/page.tsx','utf8'); if(!/order\\([\"']due_date[\"']\\s*,\\s*\\{\\s*ascending:\\s*true/.test(f)) process.exit(1);"` exits 0.
- `npm test -- tests/form-builder/ --run --reporter=basic` exits 0 (all Phase 16 + Phase 17 form-builder specs pass).
- `npx tsc --noEmit` reports zero new TS errors on the three modified production files.
- WCAG AA contrast verification on `#a14a2a` over cream `#faf9f6` + 10% overlay AND over dark `#1c1b24` + 10% overlay is DEFERRED to Plan 17-06 Task 1b (the BLOCKING gate where the build + full sweep run). UI-SPEC BLOCKING #3 explicitly nominates that as the verification point; Plan 17-03 ships the markup, Plan 17-06 confirms the contrast.
</verification>

<success_criteria>
- `app/_components/overdue-pill.tsx` exists, exports a server-compatible `OverduePill` with the locked className + aria-label.
- `app/client/assignments/_components/assignment-card.tsx` no longer contains an inline `isOverdue` function (BLOCKING #2 finalised) and mounts OverduePill on the active variant only.
- `app/admin/clients/[id]/client-tabs.tsx` mounts an OverduePill-equivalent (TooltipTrigger with the same className) wrapped in a Tooltip carrying the locked "Was due {N} day{s} ago" copy, gated on overdue AND status !== "completed".
- `app/admin/clients/[id]/page.tsx` ORDER BY is `due_date ASC NULLS LAST` followed by `created_at DESC` (BLOCKING #1 resolved).
- Client surface has NO Tooltip on the overdue pill (BLOCKING #4 honoured).
- Two new Vitest specs (`overdue-pill.test.ts`, `admin-assignments-order-by.test.ts`) prove the rendering rules and the ORDER BY swap.
- Phase 16 specs unaffected; npm test green across the full form-builder + form-interpreter + rls + scheduler directories.
</success_criteria>

<output>
After completion, create `.planning/phases/17-assignment-scheduling-notifications/17-03-SUMMARY.md` recording: the OverduePill exported surface and rendering rules, the inline-helper deletion diff signature on `assignment-card.tsx`, the ORDER BY swap diff signature on `page.tsx`, the Tooltip wrapper pattern used on `client-tabs.tsx` (and the no-asChild compromise that inlines the className), the two new spec files + their assertion counts, and a callout that Plan 17-06 still owes WCAG AA verification on `#a14a2a` over both painted backgrounds.
</output>
