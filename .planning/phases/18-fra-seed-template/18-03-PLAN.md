---
phase: 18
slug: fra-seed-template
plan: "03"
plan_id: 18-03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - lib/forms/fra-template.ts
  - .planning/phases/18-fra-seed-template/18-UAT.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - TMPL-FRA-01
  - TMPL-FRA-02
  - TMPL-FRA-03
  - TMPL-FRA-04
  - TMPL-FRA-05
tags: [BLOCKING, supabase, db-push, types-regen, verification, uat, roadmap, cleanup]
must_haves:
  truths:
    - "Migration 016 is pushed to the live Supabase project — one form_templates row exists with id='00000000-0000-4000-a000-000000000018', name='Fire Risk Assessment (Type 3) — Single Premises', template_type='fra', owner_type='admin', is_published=true; one template_versions row exists with id='00000000-0000-4000-a000-000000000118', version_number=1, published_at not null."
    - "lib/supabase/database.types.ts is regenerated and is byte-comparable to its prior content (Phase 18 added zero columns — the regen is a no-op for type signatures but is run for parity with the Phase 17 closeout pattern; if the file IS unchanged that's acceptable and recorded in the SUMMARY)."
    - "lib/forms/fra-template.ts is DELETED — pre-deletion grep confirms zero remaining references to `HARDCODED_FRA_TEMPLATE` or `@/lib/forms/fra-template` from `app/`, `components/`, `lib/` (excluding the file itself), `tests/`, and non-planning markdown (Pitfall P4 — RESEARCH §Q9)."
    - "Full Vitest sweep (npm test --run) is green — all Phase 13/14/15/16/17 specs + the 2 Phase 18 specs (fra-seed-schema + n8n-assessment-webhook) pass."
    - "npm run build exits 0 — confirms the deletion did not orphan any consumer."
    - "18-UAT.md exists with at least 5 sections (A: admin opens seeded FRA in builder; B: admin starts an assessment and fills the form end-to-end with conditional reveals + photos + signature + PAS 79 + Action Plan; C: admin submits and observes AI draft generation; D: n8n webhook smoke via the live workflow inbox or curl; E: customer-side assignment + fork-on-fill smoke per Phase 16 D-08) and ≥ 12 checkbox items."
    - "ROADMAP.md Phase 18 entry has its Plans line updated to '3 plans' with a Wave-grouped enumeration covering 18-01 / 18-02 / 18-03; the Progress Table row for Phase 18 changes from `0/0 | Not planned | -` to `3/3 | In Progress | ` (status In Progress until UAT signoff, mirroring Phase 14/15/17 lifecycle)."
  artifacts:
    - path: lib/supabase/database.types.ts
      provides: "Regenerated Supabase types — Phase 18 is data-only so the file is expected to be unchanged or byte-equivalent; regenerated for parity with closeout convention"
      contains: "form_templates"
    - path: .planning/phases/18-fra-seed-template/18-UAT.md
      provides: "Manual walkthrough doc for Phase 18 — 5 sections (admin builder open, end-to-end fill, AI draft, n8n smoke, customer fork-on-fill smoke)"
      contains: "Risk score (PAS 79)"
    - path: .planning/ROADMAP.md
      provides: "Updated Phase 18 entry with 3 plans enumerated across 3 waves + Progress Table row updated to 3/3 In Progress"
      contains: "18-03-PLAN.md"
  key_links:
    - from: live Supabase form_templates row
      to: supabase/migrations/016_phase18_fra_seed.sql
      via: "supabase db push"
      pattern: "00000000-0000-4000-a000-000000000018"
    - from: .planning/ROADMAP.md Phase 18 entry
      to: ".planning/phases/18-fra-seed-template/18-*-PLAN.md"
      via: "Plans enumeration table"
      pattern: "18-01-PLAN.md"
---

<objective>
[BLOCKING] Push migration 016 to live Supabase, regenerate TypeScript types, verify the full Vitest sweep + production build are green, delete the legacy `lib/forms/fra-template.ts` after verifying zero references, walk through UAT manually with Matt's eyes (or alone with a screen capture for him to review), and update ROADMAP.md with the Phase 18 plan enumeration.

This plan is gated on completion of Plans 18-01 (migration file authored + static-analysis spec) and 18-02 (n8n webhook wired in `submitAssessmentAction` + regression spec). It's `autonomous: false` because:

1. Task 1a requires the human to run `supabase db push` (interactive prompt may appear; production-DB side effect).
2. Task 4 is a manual UAT walkthrough — the seeded template's end-to-end behavior (render in builder, fill, conditional reveals, PAS 79 reactivity, Action Plan repeating-section, signature, submit, AI draft) can only be confirmed by a real browser session.

Why this is BLOCKING and not autonomous: per the Phase 14 / 16 / 17 closeout precedent, the db-push command is human-initiated, and the UAT smoke against the live DB cannot be automated without spinning up a full e2e harness (out of scope for Phase 18). The structural invariants of the migration are already pinned by Plan 18-01's static-analysis spec; the live-DB smoke is purely to catch deployment-time anomalies (admin-user table empty, prior conflicting UUID, RLS misconfiguration).

Tasks:
- Task 1a — Human runs `supabase db push` + `npx supabase gen types typescript --linked > lib/supabase/database.types.ts`. Returns exit codes and tail of output. [BLOCKING checkpoint]
- Task 1b — Claude verifies: types file mtime updated; the live row exists (programmatic check via the regenerated types or a follow-up read); `npm test --run` exits 0; `npm run build` exits 0. [autonomous]
- Task 2 — Claude deletes `lib/forms/fra-template.ts` after grep-verifying zero remaining references. [autonomous]
- Task 3 — Claude authors `18-UAT.md` with 5 sections covering admin-builder render, end-to-end fill, AI draft, n8n smoke, customer fork-on-fill smoke. [autonomous]
- Task 4 — Claude updates `.planning/ROADMAP.md` Phase 18 entry + Progress Table row. [autonomous]

Output: live DB updated; types regenerated; legacy fra-template.ts deleted; full sweep + build green; 18-UAT.md authored; ROADMAP.md Phase 18 entry finalized.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/18-fra-seed-template/18-CONTEXT.md
@.planning/phases/18-fra-seed-template/18-RESEARCH.md
@.planning/phases/18-fra-seed-template/18-UI-SPEC.md
@.planning/phases/18-fra-seed-template/18-01-PLAN.md
@.planning/phases/18-fra-seed-template/18-02-PLAN.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
Supabase CLI commands (verified working in Phase 14 / 16 / 17 closeouts):
- `supabase db push` — applies pending migrations from `supabase/migrations/` to the linked project. Migration 016 is additive (1 INSERT into form_templates + 1 INSERT into template_versions, both with ON CONFLICT DO NOTHING) — non-destructive.
- `npx supabase gen types typescript --linked > lib/supabase/database.types.ts` — regenerates types from the linked project schema. Phase 18 added ZERO columns; the regen output is expected to be byte-equivalent to the pre-push file, but is run for closeout-convention parity.

Phase 17 Plan 17-06 precedent (the structural analog for THIS plan):
- Task 1a: human-action checkpoint for the two CLI commands.
- Task 1b: programmatic verification — types-reflect-schema regex grep + npm test + npm run build.
- Task 2: cleanup or follow-up task (Phase 17 had a WCAG contrast computation here).
- Task 3 / Task 4: UAT.md + ROADMAP update.

ROADMAP.md current Phase 18 entry (lines 411-421):
```
### Phase 18: FRA Seed Template
**Goal**: Matt's actual Fire Risk Assessment form (Blank FRA, Type 3) is built with the form builder and seeded as the first real template.
**Depends on**: Phases 14 and 15 (custom fields + conditional logic)
**Requirements**: FRA seed — reuses TMPL-01..06 patterns against the new builder (v2)
**Success Criteria** (what must be TRUE):
  1. The Blank FRA is built using the form builder, matching the Yellow Broom FRA structure across all sections.
  2. Conditional sub-sections work inside the FRA (Yes/No → show/hide).
  3. Per-field photo attachment and speech-to-text are enabled on all FRA text fields.
  4. The risk matrix auto-calculates from the two input fields, and the Action Plan uses repeating sections.
  5. A submission fires the n8n webhook for the AI report pipeline (Module 1 bridge). Site Risk template stays BLOCKED until Matt provides the blank.
**Plans**: Not yet planned — run `/gsd:plan-phase 18`.
```
The `**Plans**:` line plus the placeholder need replacement with a 3-plan Wave-grouped enumeration analogous to Phase 17's enumeration at ROADMAP.md lines 392-409 (Wave 0 / Wave 1 / Wave 2 BLOCKING).

ROADMAP.md current Phase 18 Progress Table row (line 119):
```
| 18. FRA Seed Template | 0/0 | Not planned | - |
```
After Plan 18-03 completes, this becomes `| 18. FRA Seed Template | 3/3 | In Progress |  |` (3-slashes-3-plans, status In Progress until UAT signoff, trailing pipe with empty completed-date cell).

Pre-deletion verification pattern (RESEARCH §Code Examples §Example 3):
```
grep -rn "HARDCODED_FRA_TEMPLATE\|@/lib/forms/fra-template" \
  --include="*.ts" --include="*.tsx" --include="*.md" \
  . | grep -v "lib/forms/fra-template.ts" | grep -v ".planning/"
```
Expected output: empty (excluding planning docs). If any hits remain in `app/`, `components/`, `lib/`, or `tests/`, those consumers must be migrated to read from the seeded DB row first — but per the existing repo grep at planning time, no such consumers exist (confirmed via `grep -rln HARDCODED_FRA_TEMPLATE` returning only the 5 files: 3 planning docs + Phase 13 CONTEXT + the file itself).

n8n smoke (UAT §D): the curl shape for Matt's existing n8n workflow is the same one used during legacy `submitAssessment`. Matt's n8n workflow listens at `$N8N_ASSESSMENT_WEBHOOK_URL` and routes the `{ submissionId }` payload to whatever downstream he has configured (Proton Mail / Drive backup / etc.). The UAT smoke confirms (a) the cron is wiring the right URL via Vercel env vars, and (b) the n8n side has not been changed since Phase 7 / 17. If n8n has been re-wired since Phase 7, UAT §D documents that as a follow-up.

Fork-on-fill smoke (UAT §E, per Phase 16 D-08 / AGENTS.md): customer fills the seeded FRA → if they add or remove fields, a forked template (owned by their org) is created with `parent_template_id` pointing at the seeded master. The master `is_published` row in Phase 18 must be visible to client_users via RLS `form_templates_client_published` (Pitfall P2) for this to work. UAT §E confirms.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1a [BLOCKING]: Human runs `supabase db push` + `gen types typescript`</name>
  <read_first>
    - supabase/migrations/016_phase18_fra_seed.sql (Plan 18-01 Task 1 — confirm content before pushing; specifically the two fixed UUID literals and the ON CONFLICT DO NOTHING idempotency)
    - .planning/phases/17-assignment-scheduling-notifications/17-06-PLAN.md Task 1a (Phase 17 precedent — same shape, same commands)
    - app/admin/assessments/actions.ts (just modified in Plan 18-02 — confirm the n8n fire is present in submitAssessmentAction; if not, STOP and report — Plan 18-02 didn't complete)
  </read_first>
  <what-built>
    Plans 18-01 and 18-02 are complete. Migration 016 exists at `supabase/migrations/016_phase18_fra_seed.sql` and inserts the FRA Type 3 admin master + its v1 published template_versions row, both with fixed UUIDs and ON CONFLICT (id) DO NOTHING. `app/admin/assessments/actions.ts` `submitAssessmentAction` now fires the n8n assessment webhook from inside an `after(...)` callback (Phase 18 SC#5). Vitest specs at `tests/scheduler/fra-seed-schema.test.ts` and `tests/scheduler/n8n-assessment-webhook.test.ts` cover the structural and behavioral regressions.

    This task pushes migration 016 to the live Supabase project and regenerates `lib/supabase/database.types.ts`. Claude cannot run `supabase db push` autonomously (interactive prompt + production-DB side effect), so a human runs the two commands and reports outputs.
  </what-built>
  <how-to-verify>
    Run the following commands locally, in order, and paste the exit codes + tail of output back:

    1. **Verify migration file is present and the n8n wire landed:**
       ```
       ls supabase/migrations/016_phase18_fra_seed.sql
       grep -c "N8N_ASSESSMENT_WEBHOOK_URL" app/admin/assessments/actions.ts
       ```
       The migration file must exist. The grep count must be ≥ 2 (legacy `submitAssessment` keeps its existing reference + the new one in `submitAssessmentAction`).

    2. **Push migration to the linked Supabase project:**
       ```
       supabase db push
       ```
       Expected CLI output mentions applying `016_phase18_fra_seed.sql`, followed by "Finished supabase db push." Migration 016 is purely additive (2 INSERTs with ON CONFLICT DO NOTHING). If the CLI prompts for confirmation, type `y`. Capture full stdout/stderr and exit code.

       **If the CLI complains the migration is already applied** (e.g., a previous attempt partial-succeeded), confirm the seed row exists via the smoke query in step 4 below; if it does, this is a no-op success — proceed.

    3. **Regenerate TypeScript types:**
       ```
       npx supabase gen types typescript --linked > lib/supabase/database.types.ts
       ```
       Capture exit code. Phase 18 added zero columns; the file content is expected to be byte-equivalent to the pre-push version, but the regen is run for parity with the Phase 17 closeout pattern.

    4. **Smoke query (in Supabase Studio SQL Editor or via psql):**
       ```sql
       SELECT id, name, template_type, owner_type, is_published
       FROM public.form_templates
       WHERE id = '00000000-0000-4000-a000-000000000018';
       ```
       Expected ONE row: id matches, name = 'Fire Risk Assessment (Type 3) — Single Premises', template_type = 'fra', owner_type = 'admin', is_published = true.

       Then:
       ```sql
       SELECT id, template_id, version_number, published_at IS NOT NULL AS is_published_v
       FROM public.template_versions
       WHERE id = '00000000-0000-4000-a000-000000000118';
       ```
       Expected ONE row: id matches, template_id = '00000000-0000-4000-a000-000000000018', version_number = 1, is_published_v = true (published_at is non-null per Pitfall P2).

       Then verify the schema_json is well-formed:
       ```sql
       SELECT jsonb_typeof(schema_json) AS top, jsonb_array_length(schema_json -> 'root') AS root_len,
              (SELECT COUNT(*) FROM jsonb_object_keys(schema_json -> 'entities')) AS entity_count
       FROM public.template_versions
       WHERE id = '00000000-0000-4000-a000-000000000118';
       ```
       Expected: top = 'object'; root_len = 6 (six top-level sectionGroups); entity_count ≥ 25 (the full FRA entity graph).

    Acceptance gate for this manual task:
    - `supabase db push` exit code = 0.
    - `npx supabase gen types typescript --linked` exit code = 0.
    - `lib/supabase/database.types.ts` was rewritten (file mtime updated; non-empty).
    - The form_templates smoke query returns exactly 1 row with the expected fields.
    - The template_versions smoke query returns exactly 1 row with `published_at` non-null.
    - The schema_json shape query returns `root_len = 6` and `entity_count >= 25`.

    Paste the exit codes + the smoke query outputs back. If any of these fail, do NOT mark verified — share the failure mode and we will diagnose before continuing to Task 1b.
  </how-to-verify>
  <resume-signal>
    Type `verified` and paste the captured exit codes + smoke query outputs. If any step failed, paste the full error / unexpected row count instead — do NOT mark verified.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1b: Programmatic verification — types regenerated, build green, suite green</name>
  <read_first>
    - lib/supabase/database.types.ts (just regenerated in Task 1a — confirm the file exists and was modified, or note that it's byte-identical which is acceptable for a data-only migration)
    - Plan 18-01 Task 1 (the migration that pushed)
    - Plan 18-02 Task 1 (the n8n wire that compiles against the regenerated types)
  </read_first>
  <action>
    Claude runs the following commands and asserts their exit codes / output. Do NOT proceed to Task 2 if any assertion fails — report back to the user and stop.

    1. Assert `lib/supabase/database.types.ts` exists and still has the `form_templates` table interface (sanity check the regen didn't produce an empty file):
       ```
       node -e "const c=require('fs').readFileSync('lib/supabase/database.types.ts','utf8'); if(!c.includes('form_templates')){console.error('form_templates table missing from regenerated types');process.exit(1);} if(c.length < 5000){console.error('Types file suspiciously small ('+c.length+' bytes) — regen may have errored');process.exit(2);} console.log('OK: types file contains form_templates and is '+c.length+' bytes');"
       ```

    2. Run the full Vitest sweep: `npm test --run`. Assert exit code 0. Tail the output and verify:
       - `tests/scheduler/fra-seed-schema.test.ts` reports passing (Plan 18-01 Task 2 — 10 assertions).
       - `tests/scheduler/n8n-assessment-webhook.test.ts` reports passing (Plan 18-02 Task 2 — 4 assertions).
       - All Phase 13 / 14 / 15 / 16 / 17 specs still pass.
       - Total test count is ≥ the pre-Phase-18 count + 14 (10 + 4 new Phase 18 tests).

    3. Run the production build: `npm run build`. Assert exit code 0. Phase 18 added zero columns and zero new files in app/ or components/ (the Phase 18 changes were one file edit in actions.ts + the migration); a clean build proves the deletion in Task 2 (next step) does not orphan any consumer either — but if the deletion happens AFTER this build, run a second build at the end of Task 2 to confirm.

    If all three assertions pass, proceed to Task 2. Otherwise STOP and report.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('lib/supabase/database.types.ts','utf8'); if(!c.includes('form_templates')){console.error('form_templates table missing');process.exit(1);} if(c.length<5000){console.error('Types file suspiciously small');process.exit(2);} console.log('OK: types regenerated');" && npm test --run 2>&1 | tail -30 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --run` exits 0 with the Vitest tally showing the two new Phase 18 spec files passing.
    - `npm run build` exits 0.
    - `lib/supabase/database.types.ts` exists, is > 5000 bytes, and contains the substring `form_templates`.
    - No Phase 13 / 14 / 15 / 16 / 17 spec regressed.
  </acceptance_criteria>
  <done>
    Live schema, regenerated types, application code, test suite, and production build are aligned. Programmatic gates are green for the Phase 18 ship.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy lib/forms/fra-template.ts after grep-verifying zero references (Pitfall P4)</name>
  <read_first>
    - .planning/phases/18-fra-seed-template/18-RESEARCH.md §Q9 Pitfall P4 (the cleanup rationale + the verification grep shape)
    - .planning/phases/18-fra-seed-template/18-RESEARCH.md §Code Examples §Example 3 (the literal grep command to run)
    - lib/forms/fra-template.ts (the file to delete — confirm its contents are the structural baseline only; no exports beyond `HARDCODED_FRA_TEMPLATE`)
    - .planning/phases/18-fra-seed-template/18-CONTEXT.md §"Migration / cleanup" — the deferred-but-in-scope cleanup of this file once the DB-backed version is canonical
  </read_first>
  <action>
    1. Run the pre-deletion grep (RESEARCH §Code Examples §Example 3 — adapted for Windows / cross-platform via node + readdir):
       ```
       node -e "
       const fs=require('fs'),path=require('path');
       const skipDirs=new Set(['node_modules','.next','.git','dist','build','.planning']);
       const targetExts=new Set(['.ts','.tsx','.md','.mdx']);
       const skipFile='lib/forms/fra-template.ts';
       const hits=[];
       function walk(dir){
         for(const ent of fs.readdirSync(dir,{withFileTypes:true})){
           const p=path.join(dir,ent.name);
           if(ent.isDirectory()){ if(!skipDirs.has(ent.name)) walk(p); }
           else if(targetExts.has(path.extname(ent.name))){
             const rel=path.relative('.',p).replace(/\\\\/g,'/');
             if(rel===skipFile) continue;
             const c=fs.readFileSync(p,'utf8');
             if(/HARDCODED_FRA_TEMPLATE|@\\/lib\\/forms\\/fra-template/.test(c)) hits.push(rel);
           }
         }
       }
       walk('.');
       if(hits.length===0){ console.log('OK: zero non-self references to HARDCODED_FRA_TEMPLATE or @/lib/forms/fra-template — safe to delete'); }
       else { console.error('STOP — references still exist:'); for(const h of hits) console.error('  '+h); process.exit(1); }
       "
       ```
       If the grep returns ANY hits outside `lib/forms/fra-template.ts` itself AND outside `.planning/`, STOP and report. Phase 18 does not migrate those consumers — that was out-of-scope per CONTEXT (the cleanup is "deferred but in-scope" only insofar as the file has zero remaining consumers; if there ARE consumers, they belong to a follow-up phase).

    2. Once the grep returns zero hits, DELETE `lib/forms/fra-template.ts`:
       ```
       node -e "require('fs').unlinkSync('lib/forms/fra-template.ts'); console.log('Deleted lib/forms/fra-template.ts');"
       ```

    3. Re-run `npm run build` to confirm the deletion did not orphan any consumer. Assert exit code 0.

    4. Re-run `npm test --run` to confirm no test depends on the deleted file. Assert exit code 0.

    If either re-check fails (build or test red), the grep was insufficient — restore the file (`git checkout HEAD -- lib/forms/fra-template.ts`), investigate the failing consumer, and stop to report. Do NOT push the deletion forward if it broke anything.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(fs.existsSync('lib/forms/fra-template.ts')){console.error('lib/forms/fra-template.ts still exists — deletion did not run');process.exit(1);}const path=require('path');const skipDirs=new Set(['node_modules','.next','.git','dist','build','.planning']);const targetExts=new Set(['.ts','.tsx','.md','.mdx']);const hits=[];function walk(dir){for(const ent of fs.readdirSync(dir,{withFileTypes:true})){const p=path.join(dir,ent.name);if(ent.isDirectory()){if(!skipDirs.has(ent.name))walk(p);}else if(targetExts.has(path.extname(ent.name))){const c=fs.readFileSync(p,'utf8');if(/HARDCODED_FRA_TEMPLATE|@\\/lib\\/forms\\/fra-template/.test(c))hits.push(path.relative('.',p).replace(/\\\\/g,'/'));}}}walk('.');if(hits.length>0){console.error('Post-deletion references found:',hits);process.exit(2);}console.log('OK: file deleted and zero references remain');" && npm run build 2>&1 | tail -10 && npm test --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `lib/forms/fra-template.ts` no longer exists (`test -f` returns false).
    - Zero remaining references to `HARDCODED_FRA_TEMPLATE` or `@/lib/forms/fra-template` in `app/`, `components/`, `lib/`, `tests/` (or any non-planning file under the repo root).
    - `npm run build` exits 0 (no orphaned consumer).
    - `npm test --run` exits 0 (no test depends on the deleted file).
  </acceptance_criteria>
  <done>
    The legacy hardcoded FRA template is removed from the codebase. The DB-backed master at form_templates UUID `00000000-0000-4000-a000-000000000018` is now the single source of truth — AGENTS.md MEMORY.md "no demo mocks in shipped code" + "DB as source of truth for seed data" are both honoured.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 18-UAT.md</name>
  <read_first>
    - .planning/phases/17-assignment-scheduling-notifications/17-UAT.md (Phase 17 — the most recent UAT analog; same Section heading → Prereqs → Steps → Expected → Pass/Fail layout)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md (Phase 16 — the fork-on-fill walkthrough is the analog for §E of this UAT)
    - .planning/phases/18-fra-seed-template/18-UI-SPEC.md (Copywriting Contract section — UAT walks through each content-locked label to confirm it renders as written)
    - .planning/phases/18-fra-seed-template/18-CONTEXT.md §Success Criteria (the 5 ROADMAP SCs UAT confirms)
    - .planning/phases/18-fra-seed-template/18-RESEARCH.md §Q3 (the three conditional sub-section mappings — UAT §B tests each reveal)
  </read_first>
  <behavior>
    - File `.planning/phases/18-fra-seed-template/18-UAT.md` exists with five sections (A, B, C, D, E).
    - Section A — "Seeded FRA opens in the admin builder": admin navigates to `/admin/templates`, finds the new "Fire Risk Assessment (Type 3) — Single Premises" row, opens it in the builder, confirms the 6 sections + entities render in the entity tree without errors.
    - Section B — "Admin starts and fills an assessment end-to-end": admin starts an assessment from the seeded template, walks through all six sections, fires the conditional reveals (policy_in_place = 'no' → Policy remediation sub-section appears; escape_routes_clear = 'partial' → Obstruction details appears with required photos; detection_type = 'none' → Detection upgrade plan appears), enters Likelihood=4 + Consequence=4 → PAS 79 badge shows AMBER "Substantial — Score: 16" (or RED — depends on band lookup; the live computation is the assertion), uploads at least one photo per required multiPhotoField, adds 2 Action Plan rows, signs the responsible-person signatureField at the end of Section 06.
    - Section C — "Submit fires AI draft": admin clicks Submit; redirect goes through; check `form_submissions.status='submitted'`; within ~30s `draft_report_json` is populated by `runReportDraftGeneration` (the existing AI after() callback unchanged by Phase 18). The draft includes hazards extracted from the Action Plan rows (via `expandRepeatingSections`).
    - Section D — "n8n webhook smoke (SC#5)": curl-based smoke OR observation that Matt's n8n inbox / downstream received the `{ submissionId }` payload. Includes the curl command for manual replay if needed: `curl -X POST $N8N_ASSESSMENT_WEBHOOK_URL -H "Content-Type: application/json" -d '{"submissionId":"<id>"}'`. Includes the workflow_errors check: `SELECT * FROM workflow_errors WHERE workflow_name='assessment-submission-webhook' ORDER BY created_at DESC LIMIT 5;` — expected empty unless the env var was unset or n8n was down.
    - Section E — "Customer fork-on-fill smoke (Phase 16 D-08)": admin assigns the seeded FRA to a customer org via the existing Phase 16 admin assign flow; customer logs in (incognito), opens the assignment, fills a few fields, modifies the structure (e.g., adds a textField via the customer-side builder if that surface is wired; otherwise skips this step with a note), submits. Confirm: a forked template row exists in form_templates with `owner_type='customer'`, `parent_template_id='00000000-0000-4000-a000-000000000018'`. The master row is unchanged.
    - Each section has explicit pass/fail checkboxes.
    - A "Pre-UAT smoke SQL" appendix near the top: queries to confirm the seeded rows exist before walking through the UAT (the same queries from Task 1a step 4).
    - File contains ≥ 12 checkbox items (`[ ]` markdown checkboxes) across the five sections.
    - File mentions the literal content-locked phrases from UI-SPEC to confirm what the tester is looking at: `Risk score (PAS 79)`, `Responsible Person signature`, `Action Plan`, `Site location`, at least one of `Policy remediation` / `Obstruction details` / `Detection upgrade plan`.
  </behavior>
  <files>
    - .planning/phases/18-fra-seed-template/18-UAT.md (NEW)
  </files>
  <action>
    Read `.planning/phases/17-assignment-scheduling-notifications/17-UAT.md` for the structural analog. Create `.planning/phases/18-fra-seed-template/18-UAT.md` with the following sections (full content — write the body of each section, not just the headings):

    Top of file: metadata block (`phase: 18`, `depends-on: Plans 18-01, 18-02`, `date: 2026-05-27`), then the smoke SQL appendix:

    ```sql
    -- Pre-UAT smoke — confirm the seeded rows exist
    SELECT id, name, template_type, owner_type, is_published
    FROM public.form_templates
    WHERE id = '00000000-0000-4000-a000-000000000018';
    -- Expected: 1 row, name='Fire Risk Assessment (Type 3) — Single Premises', is_published=true.

    SELECT id, version_number, published_at IS NOT NULL AS is_published_v,
           jsonb_array_length(schema_json -> 'root') AS root_len,
           (SELECT COUNT(*) FROM jsonb_object_keys(schema_json -> 'entities')) AS entity_count
    FROM public.template_versions
    WHERE id = '00000000-0000-4000-a000-000000000118';
    -- Expected: 1 row, version_number=1, is_published_v=true, root_len=6, entity_count >= 25.
    ```

    **Section A — Seeded FRA opens in the admin builder** (D-builder-render):
    1. Prereqs: admin signed in; seeded rows confirmed via smoke SQL above.
    2. Navigate to `/admin/templates`.
    3. Find the row "Fire Risk Assessment (Type 3) — Single Premises" (template_type: fra; status: Published).
    4. Click into the template → open the builder.
    5. Expected: builder loads without console errors. The entity tree shows 6 top-level sectionGroups (01 — Premises Details … 06 — Sign-Off). Each section expands to its child fields. The Action Plan repeatingSection in Section 05 expands to 4 children (Action description / Responsible person / Target completion date / Priority). The PAS 79 computedField in Section 05 shows its formula = 'pas79' and links to Likelihood + Consequence inputs.

    Checkboxes: [ ] Template appears in /admin/templates. [ ] Builder loads with no console errors. [ ] All 6 sections render. [ ] PAS 79 computedField references the two number inputs. [ ] Action Plan repeating section shows 4 children only.

    **Section B — Admin fills an assessment end-to-end** (SC#1, SC#2, SC#3, SC#4):
    1. Prereqs: a client with at least one client_user; admin starts a new assessment against the seeded template for that client.
    2. Section 01 — Premises Details: geolocationField "Site location" captures GPS on mount → pin appears on Leaflet map. Fill premises_name, site_address, responsible_person, occupancy_type.
    3. Section 02 — Fire Safety Management: select `policy_in_place = 'No'` → "Policy remediation" sub-section appears (Phase 15 visibility — abrupt unmount → mount; no animation). Fill the two new fields (last review date + what needs updating).
    4. Section 03 — Means of Escape: select `escape_routes_clear = 'Partially'` → "Obstruction details" sub-section appears with a textareaField + a required multiPhotoField. Upload at least one photo to the main escape_route_photos field AND at least one photo to the conditional obstruction photos field.
    5. Section 04 — Fire Protection Measures: select `detection_type = 'None / domestic only'` → "Detection upgrade plan" sub-section appears. Fill the two new fields. Upload a fire_protection photo.
    6. Section 05 — Findings & Action Plan: enter Likelihood = 4 and Consequence = 4 → "Risk score (PAS 79)" computedField updates LIVE to show a banded badge (per Phase 14: score=16 falls in the RED Substantial band). Add 2 Action Plan rows: each row carries action description + responsible person + target date + priority. The "Add Action Plan" dashed button appears.
    7. Section 06 — Sign-Off: sign the responsible-person signatureField on the canvas → Redraw / Remove controls appear; "Signed" pill shows after upload.
    8. STT smoke: open any textField / textareaField → the mic button is visible bottom-right (Phase 14 D-14). Click it on the "Describe obstructions" textarea → speak → text appears (browser permission popup may show first).

    Checkboxes: [ ] Geolocation pin captures on mount. [ ] All 3 conditional reveals fire as specified (no animation; abrupt mount). [ ] Required multiPhotoFields enforce ≥1 photo on submit. [ ] PAS 79 badge updates LIVE on Likelihood/Consequence change. [ ] Action Plan repeating-section "Add" button adds rows; rows can be removed with no confirmation. [ ] Signature canvas + redraw works. [ ] STT mic button visible on text/textarea fields.

    **Section C — Submit fires AI draft** (Phase 7 pipeline integration):
    1. Submit the assessment. The page redirects to the assessment landing page or `/admin/assessments/[id]/review`.
    2. In Supabase Studio:
       ```sql
       SELECT id, status, submitted_at, draft_report_json IS NOT NULL AS has_draft
       FROM public.form_submissions ORDER BY submitted_at DESC LIMIT 1;
       ```
       Expected: status='submitted', submitted_at recent, has_draft=false initially.
    3. Wait ~30 seconds (the `after(...)` callback runs `runReportDraftGeneration` post-response). Re-run the query.
    4. Expected: has_draft=true. Open `/admin/assessments/[id]/review` and observe the draft sections (Site Overview, Identified Hazards, Risk Assessment, Recommendations, Conclusion). The Hazards array should reference at least one Action Plan row's description.

    Checkboxes: [ ] Submit redirects without error. [ ] form_submissions.status flips to 'submitted'. [ ] draft_report_json populates within ~30s. [ ] /review page renders the draft sections.

    **Section D — n8n webhook smoke (SC#5)**:
    1. Prereqs: `N8N_ASSESSMENT_WEBHOOK_URL` is set in the deployed environment (Vercel project settings → Environment Variables). If unset, this section is moot — the fire silently skips.
    2. After Section C submit, check Matt's n8n inbox / downstream consumer. Expected: a record / message arrives mentioning the submissionId from Section C.
    3. If n8n's UI is not accessible, manual replay:
       ```
       curl -X POST $N8N_ASSESSMENT_WEBHOOK_URL \
         -H "Content-Type: application/json" \
         -d '{"submissionId":"<the-submission-id-from-section-c>"}'
       ```
       Expected response: 200 OK or 204 No Content from n8n.
    4. Verify no failure was logged:
       ```sql
       SELECT * FROM public.workflow_errors
       WHERE workflow_name='assessment-submission-webhook'
       ORDER BY created_at DESC LIMIT 5;
       ```
       Expected: empty (or only historical rows from before this UAT).

    Checkboxes: [ ] n8n received the submission (Matt or Finley confirms). [ ] Manual curl replay returns 2xx (optional fallback). [ ] workflow_errors has no new rows for this submission.

    **Section E — Customer fork-on-fill smoke (Phase 16 D-08 inheritance)**:
    1. Prereqs: a customer org with at least one client_user. From the admin UI, assign the seeded FRA template to that org (existing Phase 16 admin assign flow).
    2. Log out and sign in as the client_user (incognito).
    3. Navigate to `/client/assignments` → open the assignment → click "Fill as is".
    4. Fill a few fields and submit.
    5. Verify the customer can READ the master:
       ```sql
       -- Run as the client_user via Supabase RLS; or check via the admin client to confirm visibility.
       SELECT id, owner_type FROM public.form_templates
       WHERE id='00000000-0000-4000-a000-000000000018' AND is_published=true;
       ```
       Expected: 1 row visible.
    6. (Optional, if the customer-side fork-on-fill UI is wired) Modify the structure (add/remove a field) before submitting → a forked template appears in `form_templates` with `owner_type='customer'`, `parent_template_id='00000000-0000-4000-a000-000000000018'`. If the customer-side fork UI is NOT yet wired (AGENTS.md "What's NOT done yet (deferred): Fork-on-fill UI"), note in the section that this step is a future-UAT marker, not a Phase 18 gate.

    Checkboxes: [ ] Admin can assign the seeded FRA to a customer. [ ] Customer can open the assignment and fill it. [ ] Customer submit completes without RLS errors. [ ] Master row is unchanged after customer submit.

    Footer of the UAT:
    - **Approval:** Matt sign-off date: ______. Notes:
    - **Known follow-ups:** (a) PAS 79 band boundaries are practitioner convention (RESEARCH A1) — Matt can request boundary edits in `lib/form-builder/risk/pas79.ts` as a follow-up. (b) Customer-side fork-on-fill UI is deferred per AGENTS.md — Section E step 6 is a placeholder until that surface ships.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('.planning/phases/18-fra-seed-template/18-UAT.md')){console.error('UAT missing');process.exit(1);}const uat=fs.readFileSync('.planning/phases/18-fra-seed-template/18-UAT.md','utf8');for (const s of ['Section A','Section B','Section C','Section D','Section E','Risk score (PAS 79)','Responsible Person signature','Action Plan','Site location','runReportDraftGeneration','N8N_ASSESSMENT_WEBHOOK_URL','workflow_errors','00000000-0000-4000-a000-000000000018','parent_template_id']) {if(!uat.includes(s)){console.error('UAT missing keyword:',s);process.exit(2);}}const checkboxCount=(uat.match(/\\[\\s*\\]/g)||[]).length;if(checkboxCount<12){console.error('UAT needs >=12 checkbox items, found',checkboxCount);process.exit(3);}const subRevealMentions=['Policy remediation','Obstruction details','Detection upgrade plan'].filter(s=>uat.includes(s));if(subRevealMentions.length<1){console.error('UAT must mention at least one conditional sub-section by name');process.exit(4);}console.log('OK: UAT structure valid');"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/18-fra-seed-template/18-UAT.md` exists.
    - File contains the five section headings (Section A / B / C / D / E — case-insensitive acceptable).
    - File contains the literal content-lock phrases `Risk score (PAS 79)`, `Responsible Person signature`, `Action Plan`, `Site location`.
    - File mentions at least one of `Policy remediation`, `Obstruction details`, `Detection upgrade plan` (the three Phase 15 conditional sub-sections).
    - File contains the literal `N8N_ASSESSMENT_WEBHOOK_URL` (Section D smoke).
    - File contains the literal `workflow_errors` (Section D failure check).
    - File contains the literal `00000000-0000-4000-a000-000000000018` (the seeded master UUID).
    - File contains the literal `parent_template_id` (Section E fork verification).
    - File contains ≥ 12 markdown checkbox items (`[ ]`).
    - File contains the literal `runReportDraftGeneration` (Section C AI draft confirmation).
  </acceptance_criteria>
  <done>
    Matt has a single-document walkthrough for the five ROADMAP success criteria + the Phase 16 fork-on-fill inheritance check. Each section is independently runnable.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md Phase 18 entry + Progress Table</name>
  <read_first>
    - .planning/ROADMAP.md (line 119: Progress Table row for Phase 18; lines 411-421: Phase 18 entry; lines 382-409: Phase 17 entry — the structural analog with full 6-plan Wave-grouped enumeration to mirror)
    - .planning/phases/18-fra-seed-template/18-01-PLAN.md (read the objective to write the enumeration tersely)
    - .planning/phases/18-fra-seed-template/18-02-PLAN.md
    - .planning/phases/18-fra-seed-template/(this file)
  </read_first>
  <behavior>
    - ROADMAP.md Phase 18 entry replaces the line `**Plans**: Not yet planned — run \`/gsd:plan-phase 18\`.` with a Wave-grouped Plans enumeration listing 18-01 / 18-02 / 18-03 (analogous to Phase 17's enumeration at lines 382-409, scaled down to 3 plans / 3 waves).
    - The Goal line stays unchanged ("Matt's actual Fire Risk Assessment form (Blank FRA, Type 3) is built with the form builder and seeded as the first real template").
    - The Depends on / Requirements / Success Criteria stay unchanged.
    - ROADMAP.md Progress Table row for Phase 18 (line 119) changes from `| 18. FRA Seed Template | 0/0 | Not planned | - |` to `| 18. FRA Seed Template | 3/3 | In Progress |  |` (matching the Phase 14/15/17 "In Progress" pattern — phase code is shipped, UAT signoff is the open item; trailing pipe with empty completed-date cell).
    - No other ROADMAP changes. Specifically: do NOT touch other phase entries (especially do not advance Phase 17 status or alter the Phase 17 plan list); do NOT touch the v2 Requirements section; do NOT touch the Coverage Audit table.
  </behavior>
  <files>
    - .planning/ROADMAP.md (MODIFY — two edits: Phase 18 entry + Progress Table row)
  </files>
  <action>
    Edit `.planning/ROADMAP.md`. Apply two changes:

    1. Locate the Phase 18 entry (search for `### Phase 18: FRA Seed Template`). Replace the line `**Plans**: Not yet planned — run \`/gsd:plan-phase 18\`.` with:

       ```
       **Plans**: 3 plans

       Plans:

       **Wave 0** *(migration authoring + static-analysis spec)*
       - [ ] 18-01-PLAN.md — Migration 016 (FRA Type 3 admin master seed + published v1 template_versions row with the full coltorapps FormBuilderSchema) + tests/scheduler/fra-seed-schema.test.ts static-analysis regression (wave 0)

       **Wave 1** *(n8n webhook port for SC#5)*
       - [ ] 18-02-PLAN.md — Inline N8N_ASSESSMENT_WEBHOOK_URL fire-and-forget POST inside submitAssessmentAction via after() + tests/scheduler/n8n-assessment-webhook.test.ts regression (wave 1)

       **Wave 2** *(BLOCKING: db push + types regen + legacy cleanup + UAT + ROADMAP)*
       - [ ] 18-03-PLAN.md — supabase db push migration 016 + types regen + full sweep + delete lib/forms/fra-template.ts + 18-UAT.md + this ROADMAP update (wave 2)
       ```

       Preserve the surrounding context lines (Goal, Depends on, Requirements, Success Criteria).

    2. Locate the Progress Table (search for `## Progress Table` or the `| Phase | Plans Complete | Status | Completed |` header). Find the Phase 18 row `| 18. FRA Seed Template | 0/0 | Not planned | - |` (line 119) and replace it with `| 18. FRA Seed Template | 3/3 | In Progress |  |` (three slashes-three plans; status "In Progress" because UAT signoff is the open item; trailing pipe with empty completed-date cell).

    Do NOT add any other changes — no other phase updates, no v2 Requirements rewrites, no Coverage Audit edits.

    After the edits, run:
    ```
    node -e "const r=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); for (const s of ['18-01-PLAN.md','18-02-PLAN.md','18-03-PLAN.md','3 plans']) { if(!r.includes(s)) { console.error('ROADMAP missing:',s); process.exit(1); } } if(!/\\|\\s*18\\.\\s*FRA Seed Template\\s*\\|\\s*3\\/3\\s*\\|/.test(r)){console.error('Progress Table row not updated to 3/3');process.exit(2);} if(r.includes('Not yet planned — run `/gsd:plan-phase 18`.')){console.error('Old placeholder line still present');process.exit(3);} if(!r.includes(\"Goal**: Matt's actual Fire Risk Assessment\")){console.error('Phase 18 Goal accidentally deleted');process.exit(4);} console.log('OK')"
    ```
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); for (const s of ['18-01-PLAN.md','18-02-PLAN.md','18-03-PLAN.md','3 plans']) { if(!r.includes(s)) { console.error('ROADMAP missing:',s); process.exit(1); } } if(!/\\|\\s*18\\.\\s*FRA Seed Template\\s*\\|\\s*3\\/3\\s*\\|/.test(r)){console.error('Progress Table row not updated to 3/3');process.exit(2);} if(r.includes('Not yet planned — run `/gsd:plan-phase 18`.')){console.error('Old placeholder line still present');process.exit(3);} if(!r.includes(\"Goal**: Matt's actual Fire Risk Assessment\")){console.error('Phase 18 Goal accidentally deleted');process.exit(4);} console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md contains references to 18-01-PLAN.md, 18-02-PLAN.md, 18-03-PLAN.md.
    - ROADMAP.md Phase 18 Plans line reads `3 plans`.
    - The old placeholder `Not yet planned — run \`/gsd:plan-phase 18\`.` is REMOVED from the Phase 18 entry.
    - ROADMAP.md Progress Table row for Phase 18 reads `3/3` plans and status `In Progress` (regex match against `/\|\s*18\.\s*FRA Seed Template\s*\|\s*3\/3\s*\|/`).
    - Phase 18 Goal, Depends on, Requirements, Success Criteria are PRESERVED byte-for-byte.
    - Phase 17 entry is UNCHANGED (no advancement, no Plans-line edits).
  </acceptance_criteria>
  <done>
    ROADMAP.md reflects the Phase 18 plan structure. Phase 18 status reads "In Progress" until UAT signoff, mirroring the Phase 14/15/17 lifecycle pattern. The phase is ready for the user to walk through 18-UAT.md and mark complete via the standard verification gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → live Supabase project | `supabase db push` writes DML via the linked-project auth. Migration 016 is purely additive (2 INSERTs with ON CONFLICT DO NOTHING). |
| regenerated types → application compile | TypeScript compile gate (`npm run build`) confirms types and live schema agree. |
| pre-deletion grep → repo integrity | The cleanup of `lib/forms/fra-template.ts` is gated by a grep that must return zero non-self references. Any false negative (grep missed a consumer) is caught by the post-deletion `npm run build` and `npm test --run` re-runs in Task 2. |
| UAT walkthrough → production UX | UAT signoff confirms the seeded template behaves as Matt expects against real-browser rendering, real-DB persistence, and real n8n. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-PUSH-01 | Tampering | Migration 016 push misfires and a previously-seeded master is overwritten | accept | Both INSERTs carry `ON CONFLICT (id) DO NOTHING`. Re-running a push that previously succeeded is a no-op. Concurrent pushes from CI / dev / blue-green are safe. |
| T-18-03-PUSH-02 | Tampering | Types regen produces a malformed file (e.g., partial output, network glitch) | mitigate | Task 1b sanity-checks the regenerated file contains `form_templates` AND is > 5000 bytes. `npm run build` independently confirms the file is valid TypeScript. |
| T-18-03-CLEANUP | Tampering | Deleting `lib/forms/fra-template.ts` orphans a hidden consumer (e.g., a dynamic import or string interpolation grep missed) | mitigate | Task 2 grep walks the entire repo (excluding node_modules / .next / .planning) for both `HARDCODED_FRA_TEMPLATE` AND `@/lib/forms/fra-template`. If hits exist, the task STOPS without deleting. Post-deletion, `npm run build` + `npm test --run` are re-run as a second confirmation gate. If either reds, the deletion is reverted via `git checkout HEAD -- lib/forms/fra-template.ts`. |
| T-18-03-RLS | Information Disclosure (Phase 16 inheritance) | The seeded admin master is readable by all client_users via RLS `form_templates_client_published`. If `is_published=false` was accidentally seeded (Pitfall P2 inverse), customers wouldn't see it → assignments would 404 | mitigate | UAT §E step 5 includes a smoke query confirming customer-side visibility. The Plan 18-01 spec already gates `is_published=TRUE` via static analysis. |
| T-18-03-UAT-N8N | Operational | The n8n side may have been re-wired since Phase 7 — the assessment-submission webhook URL might point at a workflow that no longer exists | accept | UAT §D explicitly asks Matt / Finley to confirm n8n receipt. If broken, the fix is on the n8n side (Matt's territory), not the app. Workflow_errors logs serve as the canary. |
| T-18-03-UAT-PAS79 | Repudiation (RESEARCH A1) | PAS 79 band boundaries are practitioner convention. Matt may want to shift cutoffs after seeing them live | accept | UAT footer flags this as a known follow-up. Boundary shifts are a single-file edit to `lib/form-builder/risk/pas79.ts` — no schema change, no migration. |
| T-18-03-UAT-FORK | Operational | The customer-side fork-on-fill UI is deferred per AGENTS.md. Section E step 6 is a placeholder | accept | UAT §E step 6 is explicitly marked as a future-UAT marker, NOT a Phase 18 gate. The Phase 16 D-08 contract (schema + RLS) is the gate Phase 18 inherits; the UI deferral is documented in AGENTS.md as out-of-scope. |
| T-18-03-CONTENT | Repudiation | If the seeded content diverges from Matt's Blank FRA Type 3 doc materially, UAT may fail on copy alignment | mitigate | UI-SPEC §Copywriting Contract content-locks the Phase-18-specific labels (PAS 79 axis labels, signature copy, geolocation copy, Action Plan column labels). Plan 18-01 acceptance criteria pin these. Other labels are inherited from `lib/forms/fra-template.ts` baseline (now deleted post-seed) — material divergence is a follow-up content edit, not a Phase 18 architectural fix per CONTEXT §"FRA content source". |
</threat_model>

<verification>
After all four tasks:
- Live Supabase has 1 form_templates row at UUID `00000000-0000-4000-a000-000000000018` and 1 template_versions row at UUID `00000000-0000-4000-a000-000000000118` — both seeded, both published.
- `lib/supabase/database.types.ts` regenerated.
- `lib/forms/fra-template.ts` deleted; zero remaining references.
- `npm test --run` and `npm run build` both exit 0.
- `18-UAT.md` exists with 5 sections + ≥ 12 checkboxes + the locked content phrases.
- `ROADMAP.md` Phase 18 entry enumerates 3 plans and the Progress Table row reads `3/3 | In Progress`.
- All three Phase-18 plan files (18-01, 18-02, 18-03) are referenced from ROADMAP.md.
- Phase 17 entry is unchanged.
</verification>

<success_criteria>
- Migration 016 applied to live DB; admin master + v1 published row exist.
- TypeScript types regenerated (Phase 18 added no columns; file is byte-equivalent OR cleanly regenerated).
- `lib/forms/fra-template.ts` deleted; zero remaining references.
- Full Vitest sweep passes (Phase 13–18, 14 new tests added: 10 from Plan 18-01 + 4 from Plan 18-02).
- Production build passes.
- `18-UAT.md` authored with sections A/B/C/D/E + smoke SQL appendix.
- `ROADMAP.md` updated: Phase 18 enumeration + Progress Table row at 3/3 In Progress.
- Phase 18 ready for the user to walk through UAT and mark complete via the standard verification gate.
</success_criteria>

<output>
After completion, create `.planning/phases/18-fra-seed-template/18-03-SUMMARY.md` recording: which migration pushed (016), the types-regen diff signature (byte count before/after — expected near-zero diff for data-only migration), the four assertion results from Task 1b (types sanity check, npm test exit, npm run build exit), the cleanup result (file deleted, post-deletion build + test re-runs), the UAT file path + section count, the ROADMAP diff (Phase 18 entry replacement + Progress Table row update). Also include a callout listing all three Phase 18 plans + their SUMMARY paths so the user can trace the full implementation history. Note any deviations: in particular, if the customer fork-on-fill UI deferral surfaces during UAT §E step 6, document that as a placeholder for a future phase.
</output>
