---
phase: 18
slug: fra-seed-template
plan: "01"
plan_id: 18-01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/016_phase18_fra_seed.sql
  - tests/scheduler/fra-seed-schema.test.ts
autonomous: true
requirements:
  - TMPL-FRA-01
  - TMPL-FRA-02
  - TMPL-FRA-03
  - TMPL-FRA-04
tags: [supabase, schema-migration, seed-data, coltorapps, pas79, repeating-section, conditional-logic, vitest]
must_haves:
  truths:
    - "supabase/migrations/016_phase18_fra_seed.sql exists and inserts EXACTLY two rows: one form_templates (id='00000000-0000-4000-a000-000000000018', owner_type='admin', is_published=true, template_type='fra') and one template_versions (id='00000000-0000-4000-a000-000000000118', version_number=1, published_at=NOW())."
    - "Migration is idempotent: uses fixed UUID literals + ON CONFLICT (id) DO NOTHING on BOTH inserts so re-applying on a deployed DB never duplicates the seed (RESEARCH §Q4 Pitfall P2)."
    - "Migration resolves owner via `SELECT id INTO v_admin_id FROM admin_users LIMIT 1` and RAISEs EXCEPTION if no admin exists (Pitfall P3 — never NULL owner_id when owner_type='admin')."
    - "schema_json contains 6 top-level sectionGroup entities (the FRA's six sections: Premises Details, Fire Safety Management, Means of Escape, Fire Protection Measures, Findings & Action Plan, Sign-Off) + 3 conditional sub-section sectionGroups (RESEARCH §Q3 mappings) + 1 repeatingSection (Action Plan) + 1 computedField with formula='pas79' (Pitfall P1 — NEVER a fictional `riskMatrixField` entity)."
    - "PAS 79 computedField wires its computedInputs.likelihood and computedInputs.consequence to two numberField entity IDs with min:1 max:5 required:true — labels 'Likelihood (1–5)' and 'Consequence (1–5)' with PAS 79 help text per UI-SPEC Delta 2."
    - "Action Plan repeatingSection has minInstances:0 maxInstances:50 and EXACTLY four child entities (textareaField 'Action description', textField 'Responsible person', dateField 'Target completion date', selectField 'Priority' with options High/Medium/Low) — NO specialty fields nested inside instances (UI-SPEC constraint, RESEARCH §Q2, Phase 14 RepeatingSectionRenderer JSDoc lines 36-43)."
    - "Three conditional sub-section sectionGroups carry visibilityRules whose `rules` array uses operator:'equals' against option VALUE strings (e.g. value:'no', value:'out_of_date', value:'none', value:'partial') and logic:'or' for multi-trigger drivers — never a `yesNoField` (Pitfall P6) and never `contains` for enum disjunction (RESEARCH §Q3)."
    - "Migration contains zero CREATE TRIGGER (project convention), zero DROP/TRUNCATE/DELETE FROM clauses, and zero references to form_submissions (Pitfall P5 — template seed only)."
    - "tests/scheduler/fra-seed-schema.test.ts loads the raw SQL file as a string, strips SQL line comments before asserting, and contains at least 10 passing `it()` blocks covering all structural invariants above."
  artifacts:
    - path: supabase/migrations/016_phase18_fra_seed.sql
      provides: "Phase 18 FRA Type 3 admin master seed — one form_templates row + one published template_versions row with the full coltorapps FormBuilderSchema (sections, PAS 79 computedField, Action Plan repeatingSection, signature, geolocation, multi-photo evidence, conditional sub-sections)"
      contains: "'formula', 'pas79'"
    - path: tests/scheduler/fra-seed-schema.test.ts
      provides: "Static-analysis spec that parses the migration's schema_json literal and asserts the entity-graph invariants pinned by 18-CONTEXT + 18-UI-SPEC + 18-RESEARCH §Q2/Q3"
      contains: "fra-seed"
  key_links:
    - from: supabase/migrations/016_phase18_fra_seed.sql
      to: lib/form-builder/risk/pas79.ts
      via: "computedField.attributes.formula = 'pas79' string enum"
      pattern: "'formula',\\s*'pas79'"
    - from: supabase/migrations/016_phase18_fra_seed.sql
      to: lib/form-builder/entities/repeating-section.ts
      via: "repeatingSection container with children array + child parentId backrefs (coltorapps EntityParentMismatch rule)"
      pattern: "'type',\\s*'repeatingSection'"
    - from: tests/scheduler/fra-seed-schema.test.ts
      to: supabase/migrations/016_phase18_fra_seed.sql
      via: "fs.readFileSync of the SQL path + regex assertions over the body"
      pattern: "016_phase18_fra_seed"
---

<objective>
Author migration `supabase/migrations/016_phase18_fra_seed.sql` — the Phase 18 FRA Type 3 admin master seed — and a Vitest spec that statically verifies its schema_json invariants. This plan delivers the entire FRA template content:

1. ONE `form_templates` row: fixed `id='00000000-0000-4000-a000-000000000018'`, `name='Fire Risk Assessment (Type 3) — Single Premises'`, `template_type='fra'`, `owner_id=<resolved admin>`, `owner_type='admin'`, `is_published=true`.
2. ONE published `template_versions` row: fixed `id='00000000-0000-4000-a000-000000000118'`, `template_id=<form_templates.id>`, `version_number=1`, `schema_json=<full coltorapps FormBuilderSchema>`, `created_by=<admin>`, `published_at=NOW()`.
3. schema_json contains the full FRA: 6 top-level sectionGroups (01 Premises Details, 02 Fire Safety Management, 03 Means of Escape, 04 Fire Protection Measures, 05 Findings & Action Plan, 06 Sign-Off) + 3 conditional sub-section sectionGroups (Policy remediation, Obstruction details, Detection upgrade plan) + 3 multiPhotoFields (escape route + fire protection + conditional obstruction) + 1 geolocationField (site location at Section 01 start) + 1 PAS 79 computedField wired to two numberFields + 1 Action Plan repeatingSection with 4 child entities (textarea + text + date + select) + 1 signatureField (responsible-person sign-off in Section 06).
4. Idempotency: both INSERTs use fixed UUID literals + `ON CONFLICT (id) DO NOTHING` — re-running the migration on prod is a no-op (RESEARCH §Q4 Pitfall P2).
5. Static-analysis Vitest spec at `tests/scheduler/fra-seed-schema.test.ts` reads the SQL file, strips line comments, and asserts ≥10 invariants: form_templates carries `is_published := TRUE`; template_versions carries `published_at`; schema contains `'formula', 'pas79'` exactly once; Action Plan repeating section has the locked 4-column shape; no specialty fields are nested inside the repeating section; ≥3 visibilityRules blocks with `operator: 'equals'`; no `riskMatrixField` / `yesNoField` / `form_submissions` / `CREATE TRIGGER` occurrences.

This plan does NOT push the migration. Push lives in Plan 18-03 (BLOCKING wave 2). This plan does NOT touch any application code beyond writing the SQL + the spec.

Purpose: every architectural piece the FRA needs is already implemented (Phase 14 specialty entities, Phase 15 conditional visibility, PAS 79 banding in `lib/form-builder/risk/pas79.ts`, Phase 16 admin-master + RLS). Phase 18's job is purely to seed the row — the migration body IS the deliverable.

Output: one new SQL migration (not pushed), one new Vitest spec, zero changes to application code.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-fra-seed-template/18-CONTEXT.md
@.planning/phases/18-fra-seed-template/18-RESEARCH.md
@.planning/phases/18-fra-seed-template/18-UI-SPEC.md

<interfaces>
Existing infrastructure consumed by this seed (do NOT re-implement; reference by exact shape):

From `lib/form-builder/index.ts:19-35` — the 11 entity types this seed uses are ALL already registered:
- `textField`, `textareaField`, `numberField`, `selectField`, `dateField` (basic types)
- `signatureField`, `multiPhotoField`, `geolocationField`, `computedField` (Phase 14 specialty)
- `sectionGroup`, `repeatingSection` (containers — Phase 14)

From `lib/form-builder/risk/pas79.ts` — the PAS 79 5×5 matrix is already implemented and consumed by `components/form-interpreter/computed-field-renderer.tsx`. The seed wires `formula: 'pas79'` and the renderer + helper handle the rest. **Do NOT modify pas79.ts** — band boundaries are practitioner convention; Matt confirms in UAT.

From `lib/form-builder/attributes/visibility-rules.ts:18-29` — Phase 15 rule shape:
```
{
  rules: [{
    sourceEntityId: "<driver-entity-id>",
    operator: "equals" | "notEquals" | "contains",
    value: "<option-value-string>",
    action: "show"
  }],
  logic: "or" | "and"
}
```
Default for unconditional fields: `{ rules: [], logic: "and" }`. Include this on EVERY entity for consistency (RESEARCH §Library Notes — migration 012 line 145-148 is the precedent; migration 011 omitted because it predates Phase 15).

From `supabase/migrations/011_specialty_smoke_test_template.sql` (the canonical specialty-seed pattern) — DO $$ ... $$ wrapper, admin resolution, `gen_random_uuid()` for entity UUIDs, jsonb_build_object construction. **Two divergences for Phase 18** (Pitfall P2):
- `is_published := TRUE` on form_templates (smoke test was false)
- `published_at := NOW()` on template_versions (smoke test omitted)
- Fixed UUID literals on the two rows + ON CONFLICT (id) DO NOTHING (smoke test used gen_random_uuid()).

From `supabase/migrations/012_phase15_conditional_smoke_test.sql:198-246` — the canonical `visibilityRules` JSON shape for sectionGroup sub-section reveals. Use this pattern verbatim for the three conditional sub-sections.

From `lib/forms/fra-template.ts` — the structural baseline (DO NOT import; this file is to be deleted in Plan 18-03 cleanup). The five sections + 17 fields are the content source. Map:

| Hardcoded baseline field      | Coltorapps entity                                | Section     |
|-------------------------------|--------------------------------------------------|-------------|
| `premises_name`               | textField                                        | 01          |
| `site_address`                | textareaField                                    | 01          |
| `responsible_person`          | textField                                        | 01          |
| `occupancy_type`              | selectField (6 options)                          | 01          |
| (NEW per CONTEXT)             | geolocationField "Site location" — FIRST field   | 01          |
| `policy_in_place`             | selectField (3 options — drives sub-section)     | 02          |
| `fire_warden_count`           | numberField                                      | 02          |
| `policy_observations`         | textareaField                                    | 02          |
| (NEW conditional 02a)         | sectionGroup "Policy remediation"                | 02 — child  |
| `escape_routes_clear`         | selectField (3 options — drives sub-section)     | 03          |
| `exit_signage_findings`       | textareaField                                    | 03          |
| `escape_route_photos`         | multiPhotoField (required, maxPhotos:8)          | 03          |
| (NEW conditional 03a)         | sectionGroup "Obstruction details"               | 03 — child  |
| `detection_type`              | selectField (5 options — drives sub-section)     | 04          |
| `extinguisher_servicing_date` | dateField (was text/date string in baseline)     | 04          |
| `fire_door_findings`          | textareaField                                    | 04          |
| `fire_protection_photos`      | multiPhotoField (required, maxPhotos:8)          | 04          |
| (NEW conditional 04a)         | sectionGroup "Detection upgrade plan"            | 04 — child  |
| (REPLACES overall_risk_rating) | numberField "Likelihood" + numberField "Consequence" + computedField "Risk score (PAS 79)" | 05 |
| (REPLACES priority_actions)   | repeatingSection "Action Plan" with 4 children   | 05          |
| `general_observations`        | textareaField                                    | 05          |
| (NEW Section 06)              | signatureField "Responsible Person signature"    | 06 — last   |

UI-SPEC content-locked copy (Delta 2 / Delta 3 / Copywriting Contract):
- Likelihood label: `Likelihood (1–5)`, helpText: `1 = Very Low, 2 = Low, 3 = Medium, 4 = High, 5 = Very High`
- Consequence label: `Consequence (1–5)`, helpText: `1 = Insignificant, 2 = Minor, 3 = Moderate, 4 = Major, 5 = Catastrophic`
- Computed field label: `Risk score (PAS 79)`, helpText: `Auto-calculated from Likelihood × Consequence above.`
- Signature label: `Responsible Person signature`, helpText: `Sign to certify this assessment is complete and accurate.`
- Geolocation label: `Site location`, helpText: `Captured automatically when you start the assessment. Tap the map to correct the pin if needed.`
- Action Plan section title: `Action Plan`, description: `One row per remedial action. Add as many as required.`
- Action Plan children: `Action description` (textareaField required), `Responsible person` (textField required), `Target completion date` (dateField required), `Priority` (selectField required, options High/Medium/Low, allowMultiple:false)
- Section titles: `01 — Premises Details`, `02 — Fire Safety Management`, `03 — Means of Escape`, `04 — Fire Protection Measures`, `05 — Findings & Action Plan`, `06 — Sign-Off`

RESEARCH §Q3 conditional sub-section mappings (the three Yes/No → reveal pairs):

| Driver field         | Driver values that fire reveal     | Sub-section sectionGroup title | Sub-section children                                                                                          |
|----------------------|------------------------------------|--------------------------------|---------------------------------------------------------------------------------------------------------------|
| policy_in_place      | `out_of_date` OR `no`              | Policy remediation             | dateField "When was the last review?" + textareaField "What needs updating?"                                  |
| escape_routes_clear  | `partial` OR `no`                  | Obstruction details            | textareaField "Describe obstructions" + multiPhotoField "Obstruction photos" (required, maxPhotos:5)          |
| detection_type       | `none`                             | Detection upgrade plan         | textareaField "Recommended detection level" + selectField "Recommended grade" (L1/L2/L3/L4, allowMultiple:false) |

The driver `selectField` carries the default empty `visibilityRules`; the sub-section sectionGroup carries the populated visibilityRules pointing back at the driver. Rule shape for the `policy_in_place` driver (two equals rules, logic 'or'):
```
'visibilityRules', jsonb_build_object(
  'rules', jsonb_build_array(
    jsonb_build_object('sourceEntityId', e_policy_in_place::text, 'operator', 'equals', 'value', 'out_of_date', 'action', 'show'),
    jsonb_build_object('sourceEntityId', e_policy_in_place::text, 'operator', 'equals', 'value', 'no',          'action', 'show')
  ),
  'logic', 'or'
)
```

Vitest scheduler/ glob is already wired (Plan 17-01 Task 1 added `tests/scheduler/**/*.{test,spec}.{ts,tsx}` to vitest.config.ts include array — confirmed at `vitest.config.ts`). Place the new spec under `tests/scheduler/` for that reason — it will be discovered without any vitest.config.ts change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author migration 016 (FRA seed) — full DO $$ block with idempotent inserts</name>
  <read_first>
    - supabase/migrations/011_specialty_smoke_test_template.sql (lines 1-343 — canonical specialty-seed pattern: DO $$ wrapper, admin resolution, jsonb_build_object, computedField wiring at lines 224-242, repeatingSection children with parentId at lines 249-313, INSERT form_templates + template_versions)
    - supabase/migrations/012_phase15_conditional_smoke_test.sql (the visibilityRules JSON shape across sectionGroup reveals — copy the exact rule structure for the three conditional sub-sections)
    - supabase/migrations/015_phase17_assignment_recurrence_reminders.sql (most recent migration — confirm filename pattern `0NN_phaseN_*.sql`, header-comment style with phase number + one-line purpose + decision citation)
    - lib/forms/fra-template.ts (lines 1-186 — structural baseline; do NOT import, only mirror the field labels + options into the SQL per the mapping table in <interfaces>)
    - lib/form-builder/risk/pas79.ts (lines 22-72 — confirm `formula: 'pas79'` is the wire, axes are integer 1–5; the seed wires the inputs, the helper does the math)
    - lib/form-builder/index.ts (lines 19-35 — entity registry; confirms every entity type the seed uses is registered)
    - lib/form-builder/attributes/visibility-rules.ts (lines 18-29 — the rule shape and default `{rules:[], logic:'and'}`)
    - .planning/phases/18-fra-seed-template/18-CONTEXT.md (locked decisions — admin master, FRA Type 3 only, the 5 specialty entities to wire, the conditional logic mandate)
    - .planning/phases/18-fra-seed-template/18-UI-SPEC.md §Copywriting Contract (FRA-specific labels are content-locked; mirror them verbatim into the SQL)
    - .planning/phases/18-fra-seed-template/18-RESEARCH.md §Q1-§Q9 + §Pitfalls + §Code Examples §Example 1 (the 140-line SQL excerpt — use as the body skeleton; expand to the full ~30 entities)
  </read_first>
  <behavior>
    - File `supabase/migrations/016_phase18_fra_seed.sql` exists.
    - File contains a header comment block (mirror 011's lines 1-47 style) naming the phase, the seed purpose, the divergences from 011/012 (is_published=true, published_at=NOW(), fixed UUIDs + ON CONFLICT), and a one-line citation of UI-SPEC + RESEARCH.
    - File contains a single `DO $$ ... $$ LANGUAGE plpgsql;` block.
    - DECLARE block contains:
      - `v_admin_id    UUID;`
      - `v_template_id UUID := '00000000-0000-4000-a000-000000000018'::uuid;` (fixed for idempotency)
      - `v_version_id  UUID := '00000000-0000-4000-a000-000000000118'::uuid;` (fixed for idempotency)
      - At minimum 29 entity UUID declarations using `gen_random_uuid()` (6 sectionGroups + 3 conditional sub-section sectionGroups + 1 geolocationField + 1 signatureField + 2 numberFields for PAS 79 + 1 computedField + 1 repeatingSection + 4 Action Plan children + 3 multiPhotoFields + 9–11 ordinary fields drawn from the baseline mapping table).
      - `v_schema JSONB;`
    - Body block:
      - `SELECT id INTO v_admin_id FROM admin_users LIMIT 1;`
      - `IF v_admin_id IS NULL THEN RAISE EXCEPTION 'Phase 18 seed requires at least one admin_users row. Seed admin first.'; END IF;`
      - `v_schema := jsonb_build_object('entities', jsonb_build_object(...), 'root', jsonb_build_array(...));` with the full entity graph.
    - Every entity in `entities` carries a `visibilityRules` attribute. For unconditional fields: `jsonb_build_object('rules', jsonb_build_array(), 'logic', 'and')`. For the three conditional sub-section sectionGroups: the populated rule shape with `operator: 'equals'` against driver option VALUE strings (Pitfall P6 — VALUE not LABEL; never a `yesNoField`).
    - `formula` literal `'pas79'` appears EXACTLY once in the file (Pitfall P1 — single computedField, never a fictional `riskMatrixField` entity).
    - Action Plan `repeatingSection` declares `minInstances:0, maxInstances:50` and a `children` array with EXACTLY four child UUIDs. The four child entities each carry `parentId, e_action_plan::text` (coltorapps EntityParentMismatch rule). NO child of the repeating section is a signatureField / multiPhotoField / geolocationField / computedField / ratingField / repeatingSection (UI-SPEC constraint + Phase 14 §RepeatingSectionRenderer JSDoc lines 36-43).
    - Three `multiPhotoField` entities exist: two top-level required (escape route photos in Section 03 + fire protection photos in Section 04, both `required: true, maxPhotos: 8`) and one nested inside the Obstruction details conditional sub-section (`required: true, maxPhotos: 5`). RESEARCH §Q7.
    - `root` array contains EXACTLY the 6 top-level sectionGroup UUIDs in section order (01 → 02 → 03 → 04 → 05 → 06). All other entities are nested via parent/children relationships (this mirrors the migration 012 pattern, not the migration 011 flat-root pattern).
    - INSERT statements:
      - `INSERT INTO form_templates (id, name, template_type, owner_id, owner_type, is_published) VALUES (v_template_id, 'Fire Risk Assessment (Type 3) — Single Premises', 'fra', v_admin_id, 'admin', TRUE) ON CONFLICT (id) DO NOTHING;`
      - `INSERT INTO template_versions (id, template_id, version_number, schema_json, created_by, published_at) VALUES (v_version_id, v_template_id, 1, v_schema, v_admin_id, NOW()) ON CONFLICT (id) DO NOTHING;`
    - Final `RAISE NOTICE 'Phase 18: FRA Type 3 seed installed — template_id %, version_id %', v_template_id, v_version_id;`.
    - File does NOT contain `CREATE TRIGGER` (project convention — zero triggers).
    - File does NOT contain `DROP COLUMN`, `DROP TABLE`, `TRUNCATE`, `DELETE FROM form_templates`, `DELETE FROM template_versions`.
    - File does NOT modify any RLS policy (Phase 16 RLS is the trust gate; admin master is read-visible to all client_users via the existing `form_templates_client_published` policy).
    - File does NOT touch `form_submissions` or any submission-side table (Pitfall P5).
  </behavior>
  <files>
    - supabase/migrations/016_phase18_fra_seed.sql (NEW)
  </files>
  <action>
    Create `supabase/migrations/016_phase18_fra_seed.sql`. Mirror the file-header style of migration 011 lines 1-47 — start with `-- 016_phase18_fra_seed.sql` + project header + a Purpose block citing AGENTS.md "DB is source of truth" + UI-SPEC + RESEARCH §Q4. Cite the two divergences from 011/012 explicitly (is_published=true so customer surfaces see it per RLS `form_templates_client_published`; fixed UUIDs + ON CONFLICT for prod-deploy idempotency per RESEARCH Pitfall P2).

    Write a single `DO $$ ... $$ LANGUAGE plpgsql;` block. Use `RESEARCH §Example 1` as the structural skeleton — expand the truncated comment "// ... remaining ~20 entities ..." into the full entity graph by walking the mapping table in `<interfaces>` above.

    Critical content rules — cite each pitfall in adjacent SQL comments so a reader sees the rationale:
    - **Pitfall P1** (RESEARCH §Q9): the PAS 79 risk matrix is `computedField` with `formula: 'pas79'`, NEVER a fictional `riskMatrixField`. Add a comment above the computedField definition: `-- Pitfall P1: PAS 79 is a computedField with formula='pas79' — no riskMatrixField entity exists.`
    - **Pitfall P2** (RESEARCH §Q9): set `is_published := TRUE` on form_templates + `published_at := NOW()` on template_versions. Add adjacent comment: `-- Pitfall P2: diverges from smoke-tests 011/012 — this seed is a real master, customers must see it (RLS form_templates_client_published).`
    - **Pitfall P3** (RESEARCH §Q9): resolve `v_admin_id` and RAISE EXCEPTION if NULL. Add comment: `-- Pitfall P3: owner_type='admin' requires a real admin_users.id — never NULL.`
    - **Pitfall P5** (RESEARCH §Q9): the seed inserts EXACTLY two rows (form_templates + template_versions). NEVER touch form_submissions. Add comment near the inserts: `-- Pitfall P5: this seed creates a TEMPLATE — submissions happen at fill time via existing actions.`
    - **Pitfall P6** (RESEARCH §Q9): every Yes/No-style driver is a `selectField`; visibilityRules compare the option VALUE string. Add comment above the first visibilityRules block: `-- Pitfall P6: no yesNoField entity exists — drivers are selectField, rules compare option VALUE strings.`
    - **Phase 14 RepeatingSectionRenderer constraint** (UI-SPEC + JSDoc lines 36-43): only textField/textareaField/numberField/dateField/selectField/checkboxField may be children of `e_action_plan`. Add comment above the Action Plan repeatingSection: `-- Phase 14 §RepeatingSectionRenderer constraint: Action Plan children must be basic types only — no signature/photo/geolocation/computed/rating/nested-repeating inside instances.`

    Wire the PAS 79 computedField verbatim from migration 011 lines 224-242, replacing label with the UI-SPEC content lock `Risk score (PAS 79)` and adding `helpText: 'Auto-calculated from Likelihood × Consequence above.'`. Use `attachPhotos: false` (computed value, not user input). Include the default empty `visibilityRules` block.

    Wire the two PAS 79 inputs as `numberField` with `min: 1, max: 5, required: true, unit: ''` and the labels/helpText from UI-SPEC Delta 2 (Likelihood / Consequence with the integer-meaning help strings).

    Wire the Action Plan as `repeatingSection` per RESEARCH §Example 1 lines 478-535. Carry `title: 'Action Plan'`, `description: 'One row per remedial action. Add as many as required.'`, `minInstances: 0`, `maxInstances: 50`, default empty `visibilityRules`. The four children are: `textareaField` (label `Action description`, required:true, placeholder `What needs doing?`, attachPhotos:false); `textField` (label `Responsible person`, required:true, placeholder `Who owns this?`, helpText:'', prefillSource:'', attachPhotos:false); `dateField` (label `Target completion date`, required:true, attachPhotos:false); `selectField` (label `Priority`, required:true, options `[{label:'High',value:'High'},{label:'Medium',value:'Medium'},{label:'Low',value:'Low'}]`, allowMultiple:false, attachPhotos:false). Each child carries `parentId, e_action_plan::text`.

    Wire the three conditional sub-section sectionGroups (Policy remediation / Obstruction details / Detection upgrade plan). Each is a `sectionGroup` entity whose `attributes.visibilityRules` carries the populated rule shape pointing at its driver field's `e_*::text` UUID. Use `logic: 'or'` when there are two driver-value triggers (policy_in_place fires on `out_of_date` OR `no`; escape_routes_clear fires on `partial` OR `no`); `logic: 'or'` is also fine for the single-trigger case (detection_type → `none`) — keep the shape consistent across the three reveals. Each sub-section sectionGroup carries `title`, `description`, and a `children` array; each child carries `parentId` back to the sub-section UUID.

    Wire the geolocationField with `label: 'Site location'`, `required: true`, `helpText: 'Captured automatically when you start the assessment. Tap the map to correct the pin if needed.'`, `attachPhotos: false`. Place it FIRST in Section 01 (its UUID appears in Section 01's sectionGroup `children` array as the first element).

    Wire the responsible-person signatureField with `label: 'Responsible Person signature'`, `required: true`, `helpText: 'Sign to certify this assessment is complete and accurate.'`, `attachPhotos: false`. Place it as the LAST field of Section 06 — Sign-Off (its UUID is the last element of Section 06's `children` array).

    Wire the two main photo evidence multiPhotoFields: `escape_route_photos` (Section 03, label `Photographic evidence — Means of Escape`, required:true, maxPhotos:8, helpText:'Capture each protected route, including final exits.'); `fire_protection_photos` (Section 04, label `Photographic evidence — Fire Protection Measures`, required:true, maxPhotos:8, helpText:'Photograph any defects on doors, alarms, or extinguishers.'). The third multiPhotoField (obstruction photos in conditional sub-section 03a) carries `label: 'Obstruction photos'`, required:true, maxPhotos:5.

    The 6 top-level sectionGroups carry:
    - Section 01: `title: '01 — Premises Details'`, `description: 'Identify the premises and the responsible person.'`
    - Section 02: `title: '02 — Fire Safety Management'`, `description: 'Policies, procedures, and accountability.'`
    - Section 03: `title: '03 — Means of Escape'`, `description: 'Travel distances, exits, signage, and emergency lighting.'`
    - Section 04: `title: '04 — Fire Protection Measures'`, `description: 'Detection, suppression, and compartmentation systems.'`
    - Section 05: `title: '05 — Findings & Action Plan'`, `description: 'Overall risk rating via PAS 79 + remedial actions.'`
    - Section 06: `title: '06 — Sign-Off'`, `description: 'Confirm completion. Your signature certifies the findings and actions recorded above.'`

    Each top-level sectionGroup carries a `children` array listing its child entity UUIDs (including any conditional sub-section sectionGroup UUIDs that belong to it). Children carry `parentId, e_section_NN::text`.

    The `root` array contains ONLY the 6 top-level sectionGroup UUIDs. All other entities are nested via parent/children relationships. Cross-reference: this matches migration 012's sectionGroup-rooted pattern (NOT migration 011's flat-root pattern).

    End the DO block with `INSERT INTO form_templates ... ON CONFLICT (id) DO NOTHING;` then `INSERT INTO template_versions ... ON CONFLICT (id) DO NOTHING;` then `RAISE NOTICE 'Phase 18: FRA Type 3 seed installed — template_id %, version_id %', v_template_id, v_version_id;`. Close `END; $$ LANGUAGE plpgsql;`.

    Do NOT run `supabase db push` — push is Plan 18-03 Task 1. Do NOT regenerate types — types regen is Plan 18-03 Task 1 too. This task ends after the file is written.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('supabase/migrations/016_phase18_fra_seed.sql','utf8');const stripped=m.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n');const checks=[['fixed template UUID',/v_template_id\s+UUID\s*:=\s*'00000000-0000-4000-a000-000000000018'::uuid/],['fixed version UUID',/v_version_id\s+UUID\s*:=\s*'00000000-0000-4000-a000-000000000118'::uuid/],['admin resolution',/SELECT\s+id\s+INTO\s+v_admin_id\s+FROM\s+admin_users\s+LIMIT\s+1/i],['admin not null guard',/IF\s+v_admin_id\s+IS\s+NULL\s+THEN\s+RAISE\s+EXCEPTION/i],['is_published TRUE',/'fra',\s*v_admin_id,\s*'admin',\s*TRUE/],['published_at NOW',/published_at[\s\S]{0,200}NOW\(\)/],['ON CONFLICT form_templates',/INSERT INTO form_templates[\s\S]+?ON CONFLICT \(id\) DO NOTHING/],['ON CONFLICT template_versions',/INSERT INTO template_versions[\s\S]+?ON CONFLICT \(id\) DO NOTHING/],['pas79 formula present',/'formula',\s*'pas79'/],['Action Plan repeatingSection',/'type',\s*'repeatingSection'[\s\S]{0,400}'title',\s*'Action Plan'/],['signatureField',/'type',\s*'signatureField'/],['geolocationField',/'type',\s*'geolocationField'/]];for (const [label,re] of checks){ if(!re.test(stripped)){console.error('Missing/wrong:',label);process.exit(1);} } const ruleBlocks=(stripped.match(/'operator',\s*'equals'/g)||[]).length; if(ruleBlocks<3){console.error('Need >=3 equals-operator visibilityRules, found',ruleBlocks);process.exit(2);} const pas79Count=(stripped.match(/'formula',\s*'pas79'/g)||[]).length; if(pas79Count!==1){console.error('formula:pas79 must appear exactly once, found',pas79Count);process.exit(3);} const triggerCount=(stripped.match(/CREATE\s+TRIGGER/gi)||[]).length; if(triggerCount!==0){console.error('CREATE TRIGGER forbidden');process.exit(4);} const dropCount=(m.match(/\bDROP\s+TABLE\b|\bDROP\s+COLUMN\b|\bTRUNCATE\b|\bDELETE\s+FROM\s+(form_templates|template_versions|form_submissions)/gi)||[]).length; if(dropCount!==0){console.error('Destructive DDL forbidden');process.exit(5);} const submTouches=(stripped.match(/form_submissions/gi)||[]).length; if(submTouches!==0){console.error('Pitfall P5 — must NOT touch form_submissions');process.exit(6);} const forbiddenChildren=stripped.match(/'parentId',\s*e_action_plan::text[\s\S]{0,400}'type',\s*'(signatureField|multiPhotoField|geolocationField|computedField|ratingField|repeatingSection)'/g)||stripped.match(/'type',\s*'(signatureField|multiPhotoField|geolocationField|computedField|ratingField|repeatingSection)'[\s\S]{0,400}'parentId',\s*e_action_plan::text/g); if(forbiddenChildren&&forbiddenChildren.length>0){console.error('Specialty/repeating field nested inside Action Plan — forbidden by Phase 14 RepeatingSectionRenderer');process.exit(7);} const photoCount=(stripped.match(/'type',\s*'multiPhotoField'/g)||[]).length; if(photoCount<3){console.error('Need >=3 multiPhotoField entities (escape route + fire protection + obstruction conditional), found',photoCount);process.exit(8);} console.log('OK: migration 016 structure valid');"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/016_phase18_fra_seed.sql` exists.
    - `grep -c "'00000000-0000-4000-a000-000000000018'" supabase/migrations/016_phase18_fra_seed.sql` ≥ 1.
    - `grep -c "'00000000-0000-4000-a000-000000000118'" supabase/migrations/016_phase18_fra_seed.sql` ≥ 1.
    - `grep -c 'ON CONFLICT (id) DO NOTHING' supabase/migrations/016_phase18_fra_seed.sql` = 2.
    - `grep -c "SELECT id INTO v_admin_id FROM admin_users LIMIT 1" supabase/migrations/016_phase18_fra_seed.sql` ≥ 1.
    - `grep -c "RAISE EXCEPTION" supabase/migrations/016_phase18_fra_seed.sql` ≥ 1 (admin-null guard).
    - `grep -cE "'fra',\\s*v_admin_id,\\s*'admin',\\s*TRUE" supabase/migrations/016_phase18_fra_seed.sql` = 1.
    - `grep -c "published_at" supabase/migrations/016_phase18_fra_seed.sql` ≥ 1.
    - `grep -cE "'formula',\\s*'pas79'" supabase/migrations/016_phase18_fra_seed.sql` = 1.
    - `grep -c "riskMatrixField" supabase/migrations/016_phase18_fra_seed.sql` = 0.
    - `grep -c "yesNoField" supabase/migrations/016_phase18_fra_seed.sql` = 0.
    - `grep -v '^--' supabase/migrations/016_phase18_fra_seed.sql | grep -cE "'operator',\\s*'equals'"` ≥ 3.
    - `grep -cE "'type',\\s*'repeatingSection'" supabase/migrations/016_phase18_fra_seed.sql` = 1.
    - `grep -cE "'type',\\s*'signatureField'" supabase/migrations/016_phase18_fra_seed.sql` = 1.
    - `grep -cE "'type',\\s*'geolocationField'" supabase/migrations/016_phase18_fra_seed.sql` = 1.
    - `grep -cE "'type',\\s*'multiPhotoField'" supabase/migrations/016_phase18_fra_seed.sql` ≥ 3.
    - `grep -v '^--' supabase/migrations/016_phase18_fra_seed.sql | grep -cE "CREATE\\s+TRIGGER"` = 0.
    - `grep -cE "\\bDROP\\s+TABLE\\b|\\bDROP\\s+COLUMN\\b|\\bTRUNCATE\\b" supabase/migrations/016_phase18_fra_seed.sql` = 0.
    - `grep -v '^--' supabase/migrations/016_phase18_fra_seed.sql | grep -c "form_submissions"` = 0.
  </acceptance_criteria>
  <done>
    Migration 016 exists as a file with the full FRA schema_json embedded. It's idempotent, additive, and pinned to Phase 16's admin-master contract. Not yet pushed (push is Plan 18-03).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/scheduler/fra-seed-schema.test.ts — static analysis of migration 016</name>
  <read_first>
    - supabase/migrations/016_phase18_fra_seed.sql (just created in Task 1 — the file under static analysis)
    - tests/scheduler/is-overdue.test.ts (Phase 17 — the most recent test file under tests/scheduler/; same describe/it idiom; pure node fs imports)
    - vitest.config.ts (confirms `tests/scheduler/**/*.{test,spec}.{ts,tsx}` is in the include glob — wired in Plan 17-01)
    - .planning/phases/18-fra-seed-template/18-RESEARCH.md §Q1-§Q3 + §Pitfalls (the invariants the spec asserts)
    - .planning/phases/18-fra-seed-template/18-UI-SPEC.md §"What the existing renderers already handle" → Action Plan children constraint (the no-specialty-inside-repeating assertion)
  </read_first>
  <behavior>
    - File `tests/scheduler/fra-seed-schema.test.ts` exists.
    - It reads `supabase/migrations/016_phase18_fra_seed.sql` via `fs.readFileSync` at module scope (no network, no DB connection — pure static analysis).
    - It strips SQL line comments (`-- ...` lines) before counting structural occurrences so descriptive header comments cannot satisfy assertions (grep-gate hygiene per planner rules).
    - The spec contains AT LEAST 10 passing `it()` blocks asserting:
      1. `it("inserts the form_templates row with the fixed Phase 18 UUID and is_published=TRUE")` — regex matches for `'00000000-0000-4000-a000-000000000018'` and `'fra', v_admin_id, 'admin', TRUE` on the form_templates INSERT.
      2. `it("inserts the template_versions row with published_at and the fixed version UUID")` — regex matches for `'00000000-0000-4000-a000-000000000118'` and `published_at` near the template_versions INSERT.
      3. `it("is idempotent — both inserts carry ON CONFLICT (id) DO NOTHING")` — count of `ON CONFLICT (id) DO NOTHING` >= 2.
      4. `it("declares exactly one PAS 79 computedField — never a fictional riskMatrixField")` — count of `'formula', 'pas79'` === 1 AND no `riskMatrixField` (Pitfall P1).
      5. `it("declares exactly one Action Plan repeatingSection with the locked 4-column child contract")` — count of `'type', 'repeatingSection'` === 1; presence of `e_action_desc`, `e_action_owner`, `e_action_due`, `e_action_priority`; each child appears within 800 chars of `parentId, e_action_plan::text` (either order).
      6. `it("forbids specialty fields inside Action Plan repeating instances")` — scans for the forbidden colocation in both directions (parentId→type and type→parentId) within 400 chars; expects zero matches for signatureField/multiPhotoField/geolocationField/computedField/ratingField/repeatingSection nested under `e_action_plan`.
      7. `it("wires three conditional sub-section visibilityRules with operator equals against option VALUE strings")` — count of `'operator', 'equals'` >= 3 AND no `yesNoField` (Pitfall P6).
      8. `it("declares the three Phase 14 specialty entities — signatureField (1) + geolocationField (1) + multiPhotoField (>=3 instances)")` — exact counts per the migration spec.
      9. `it("does not touch form_submissions or any submission-side table — template seed only")` — non-comment occurrences of `form_submissions` === 0 (Pitfall P5).
      10. `it("does not create database triggers — project convention")` — non-comment occurrences of `CREATE TRIGGER` === 0.
    - `npm test -- tests/scheduler/fra-seed-schema --run` exits 0 with all assertions passing.
    - The spec does NOT import anything from `lib/`, `app/`, or `@/lib/forms/fra-template` (pure file-reader; importing application code would couple this test to runtime dependencies we don't need).
  </behavior>
  <files>
    - tests/scheduler/fra-seed-schema.test.ts (NEW)
  </files>
  <action>
    Create `tests/scheduler/fra-seed-schema.test.ts`. Top of file:

    File header comment:
    ```
    // Static-analysis spec for supabase/migrations/016_phase18_fra_seed.sql.
    // Phase 18 — Plan 18-01. The migration body IS the deliverable; this spec
    // asserts the structural invariants the seed must satisfy without booting
    // a Postgres instance. SQL line comments are stripped before counting so
    // header prose cannot self-satisfy a grep-style assertion (planner grep-
    // gate hygiene).
    ```

    Imports:
    ```
    import { readFileSync } from "node:fs";
    import { resolve } from "node:path";
    import { describe, it, expect } from "vitest";
    ```

    Module-scope loaders:
    ```
    const MIGRATION_PATH = resolve(__dirname, "../../supabase/migrations/016_phase18_fra_seed.sql");
    const rawSql = readFileSync(MIGRATION_PATH, "utf8");
    const sql = rawSql
      .split("\n")
      .filter((line) => !line.trim().startsWith("--"))
      .join("\n");
    ```

    Inside `describe("Phase 18 FRA seed migration (016)", () => { ... })`, write the 10 `it()` blocks. Implementation notes per test (use exact regex shapes — `expect(sql).toMatch(...)` or counted via `String.prototype.match`):

    - Test 1 — assert `expect(sql).toMatch(/v_template_id\s+UUID\s*:=\s*'00000000-0000-4000-a000-000000000018'::uuid/)` AND `expect(sql).toMatch(/'fra',\s*v_admin_id,\s*'admin',\s*TRUE/)`.
    - Test 2 — assert `expect(sql).toMatch(/v_version_id\s+UUID\s*:=\s*'00000000-0000-4000-a000-000000000118'::uuid/)` AND `expect(sql).toMatch(/INSERT INTO template_versions[\s\S]+?published_at/m)`.
    - Test 3 — `const conflicts = (sql.match(/ON CONFLICT \(id\) DO NOTHING/g) || []); expect(conflicts.length).toBeGreaterThanOrEqual(2);`.
    - Test 4 — `const pas79 = (sql.match(/'formula',\s*'pas79'/g) || []); expect(pas79.length).toBe(1);` AND `expect(sql).not.toMatch(/riskMatrixField/);`.
    - Test 5 — `const reps = (sql.match(/'type',\s*'repeatingSection'/g) || []); expect(reps.length).toBe(1);`. For each child symbol in `['e_action_desc', 'e_action_owner', 'e_action_due', 'e_action_priority']`, assert `expect(sql).toContain(symbol)` AND `expect(sql).toMatch(new RegExp(symbol + "::text[\\s\\S]{0,800}parentId',\\s*e_action_plan::text|parentId',\\s*e_action_plan::text[\\s\\S]{0,800}" + symbol + "::text"))`.
    - Test 6 — for each forbidden type in `['signatureField','multiPhotoField','geolocationField','computedField','ratingField','repeatingSection']`, assert `expect(sql.match(new RegExp("'parentId',\\s*e_action_plan::text[\\s\\S]{0,400}'type',\\s*'" + t + "'", "g")) || []).toHaveLength(0)` AND assert the inverse `expect(sql.match(new RegExp("'type',\\s*'" + t + "'[\\s\\S]{0,400}'parentId',\\s*e_action_plan::text", "g")) || []).toHaveLength(0)`.
    - Test 7 — `const eq = (sql.match(/'operator',\s*'equals'/g) || []); expect(eq.length).toBeGreaterThanOrEqual(3);` AND `expect(sql).not.toMatch(/yesNoField/);`.
    - Test 8 — `expect((sql.match(/'type',\s*'signatureField'/g) || []).length).toBe(1);`, `expect((sql.match(/'type',\s*'geolocationField'/g) || []).length).toBe(1);`, `expect((sql.match(/'type',\s*'multiPhotoField'/g) || []).length).toBeGreaterThanOrEqual(3);`.
    - Test 9 — `expect(sql).not.toMatch(/form_submissions/);`.
    - Test 10 — `expect(sql).not.toMatch(/CREATE\s+TRIGGER/i);`.

    Do NOT mock Date or use any timers. Do NOT touch the network or any DB connection. The spec is pure file IO + regex.

    After writing the spec, run `npm test -- tests/scheduler/fra-seed-schema --run --reporter=basic`. Expected: all 10 assertions pass.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('tests/scheduler/fra-seed-schema.test.ts')){console.error('Spec missing');process.exit(1);}const c=fs.readFileSync('tests/scheduler/fra-seed-schema.test.ts','utf8');const itCount=(c.match(/\bit\(/g)||[]).length;if(itCount<10){console.error('Need >=10 it() blocks, found',itCount);process.exit(2);}for (const sym of ['016_phase18_fra_seed','readFileSync','formula','pas79','repeatingSection','e_action_plan','operator','equals','riskMatrixField','yesNoField','form_submissions','CREATE','TRIGGER','signatureField','geolocationField','multiPhotoField']) {if(!c.includes(sym)){console.error('Spec missing keyword/identifier:',sym);process.exit(3);}}if(c.includes('@/lib/forms/fra-template')||c.includes(\"from '@/lib\")||c.includes('from \"@/lib')){console.error('Spec must not import application code from @/lib');process.exit(4);}console.log('OK: spec structure valid');" && npm test -- tests/scheduler/fra-seed-schema --run --reporter=basic 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/scheduler/fra-seed-schema.test.ts` exists.
    - `grep -cE "\\bit\\(" tests/scheduler/fra-seed-schema.test.ts` ≥ 10.
    - `grep -c "readFileSync" tests/scheduler/fra-seed-schema.test.ts` ≥ 1 (spec reads the SQL via fs).
    - `grep -c "016_phase18_fra_seed" tests/scheduler/fra-seed-schema.test.ts` ≥ 1 (spec references the migration path).
    - `grep -c "@/lib" tests/scheduler/fra-seed-schema.test.ts` = 0 (no application-code imports).
    - `npm test -- tests/scheduler/fra-seed-schema --run --reporter=basic` exits 0.
    - The full Vitest sweep `npm test --run` still exits 0 (no Phase 13/14/15/16/17 regression).
  </acceptance_criteria>
  <done>
    The migration's structural invariants are pinned by a fast (<2s) spec that runs as part of the standard Vitest sweep. The spec uses static analysis only — no DB, no network — so it is reliable in CI and on every developer machine.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → migration file | Migration file is authored only; push to live Supabase is Plan 18-03. No DB writes in this plan. |
| seeded admin master → all client tenants | The FRA template is an admin master with `owner_type='admin'`. Phase 16 RLS policy `form_templates_client_published` makes it readable by every client_user. The seed must NOT set `is_published=false` or no customer would see it (Pitfall P2). |
| schema_json content → form interpreter | The interpreter trusts the schema_json shape it loads. If the seed embeds malformed entity graphs (missing parentId backrefs, fictional entity types, wrong attribute names), the interpreter will throw at runtime. Plan 18-01 Task 2 spec catches this via static analysis BEFORE the push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | A future migration could re-INSERT the same fixed UUID and silently overwrite the seed, drifting the master from Matt's intent | mitigate | `ON CONFLICT (id) DO NOTHING` on BOTH inserts (verified by Plan 18-01 Task 1 acceptance criterion count=2). Re-running this migration on a populated DB is a no-op. A different migration trying to write the same UUID would also no-op. |
| T-18-01-02 | Tampering | A planner / executor could invent a `riskMatrixField` entity not registered in `lib/form-builder/index.ts`, breaking schema validation at runtime | mitigate | Pitfall P1 is cited inline in the SQL with a `-- Pitfall P1:` comment, AND tested by Plan 18-01 Task 2 Test 4 (`expect(sql).not.toMatch(/riskMatrixField/)`). |
| T-18-01-03 | Information Disclosure (inherits T-16-01 from Phase 16) | The seeded admin master is readable by all client_users via RLS `form_templates_client_published` — that is the design intent. Risk would be if the seed leaked customer-PII or org-specific content into the master | accept | The master is a generic Type 3 FRA blank — zero customer-PII baked in. Customer forks (Phase 16 fork-on-fill) materialize per-org copies; the master itself stays clean. |
| T-18-01-04 | Tampering | A future migration could relax the Phase 14 RepeatingSectionRenderer constraint by nesting a specialty field inside an Action Plan instance, causing the interpreter to crash at fill time | mitigate | Plan 18-01 Task 2 Test 6 scans bi-directionally for forbidden colocation (`parentId, e_action_plan::text` ↔ specialty `type`) within 400 chars; spec is part of the standard Vitest sweep so any future migration that violates the constraint would fail CI. |
| T-18-01-05 | Denial of Service | An admin-null deployment (e.g. fresh Supabase project before seed-admin runs) would crash the migration with an unhelpful error | mitigate | `IF v_admin_id IS NULL THEN RAISE EXCEPTION 'Phase 18 seed requires at least one admin_users row. Seed admin first.';` (Pitfall P3). The error message names the fix. |
| T-18-01-06 | Repudiation | If the seed's content drifts from UI-SPEC labels (typos, missing helpText, wrong PAS 79 axis labels), Matt's UAT signoff would be against a different document than intended | mitigate | UI-SPEC §Copywriting Contract content-locks every FRA-specific label; Plan 18-01 Task 1 acceptance criteria pin the key labels (`Risk score (PAS 79)`, `Responsible Person signature`, etc.). Plan 18-03 UAT confirms with Matt. |
</threat_model>

<verification>
After both tasks:
- `node -e "require('fs').readFileSync('supabase/migrations/016_phase18_fra_seed.sql','utf8').includes('formula') || process.exit(1)"` exits 0.
- `node -e "require('fs').readFileSync('supabase/migrations/016_phase18_fra_seed.sql','utf8').includes('ON CONFLICT (id) DO NOTHING') || process.exit(1)"` exits 0.
- `npm test -- tests/scheduler/fra-seed-schema --run` exits 0 with all 10 assertions passing.
- `npm test --run` exits 0 (full sweep — Phase 13/14/15/16/17 specs still green, no regression).
- Migration 016 is NOT yet pushed; live `lib/supabase/database.types.ts` is NOT yet regenerated. Both happen in Plan 18-03.
</verification>

<success_criteria>
- Migration 016 file authored (not pushed) — full FRA schema_json embedded with 6 sectionGroups + 3 conditional sub-sections + 1 PAS 79 computedField + 1 Action Plan repeatingSection + 1 signatureField + 1 geolocationField + 3 multiPhotoFields + supporting basic-type fields drawn from the baseline mapping.
- Both INSERTs idempotent via fixed UUIDs + ON CONFLICT (id) DO NOTHING.
- `is_published=TRUE` + `published_at=NOW()` so customer surfaces see the master via Phase 16 RLS.
- `tests/scheduler/fra-seed-schema.test.ts` passes ≥ 10 static-analysis assertions.
- All 7 RESEARCH §Pitfalls (P1–P7) cited inline in the SQL OR enforced by spec assertions where the pitfall is a runtime/data-shape concern (P1, P2, P3, P5, P6 — directly; P4, P7 — handled by Plans 18-03 and 18-02 respectively, not this plan).
- Existing Phase 13/14/15/16/17 Vitest suites still green.
</success_criteria>

<output>
After completion, create `.planning/phases/18-fra-seed-template/18-01-SUMMARY.md` recording: migration 016 file path + line count, the entity-type count breakdown (sectionGroups, conditional sub-sections, specialty entities, PAS 79 wiring, Action Plan children), the visibilityRules-mapping decisions for the three conditional reveals (driver field UUIDs + driver-value strings + sub-section UUIDs), the spec file path + assertion list, and a note that Plans 18-02 (n8n port) and 18-03 (BLOCKING push + UAT + cleanup) are the remaining Phase 18 work.
</output>
