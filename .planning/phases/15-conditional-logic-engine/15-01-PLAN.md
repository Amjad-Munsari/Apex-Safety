---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: [15-00]
files_modified:
  - lib/form-builder/attributes/visibility-rules.ts
  - lib/form-builder/entities/text-field.ts
  - lib/form-builder/entities/number-field.ts
  - lib/form-builder/entities/date-field.ts
  - lib/form-builder/entities/select-field.ts
  - lib/form-builder/entities/textarea-field.ts
  - lib/form-builder/entities/checkbox-field.ts
  - lib/form-builder/entities/section-group.ts
  - lib/form-builder/entities/signature-field.ts
  - lib/form-builder/entities/rating-field.ts
  - lib/form-builder/entities/multi-photo-field.ts
  - lib/form-builder/entities/geolocation-field.ts
  - lib/form-builder/entities/computed-field.ts
  - lib/form-builder/entities/repeating-section.ts
  - tests/form-builder/visibility/visibility-rules-attribute.test.ts
  - tests/form-builder/visibility/backcompat.test.ts
autonomous: true
requirements: [COND-01, BUILDER-02]
requirements_addressed: [COND-01, BUILDER-02]

must_haves:
  truths:
    - "Every one of the 13 registered entities accepts a `visibilityRules` attribute and a shared `shouldBeProcessed` hook (D-05)"
    - "`visibilityRulesAttribute.validate(undefined|null|missing)` returns `{ rules: [], logic: 'and' }` — never throws, so pre-Phase-15 `template_versions.schema_json` rows load cleanly (Pitfall 1, A4)"
    - "`validate()` REJECTS unknown operators, unknown actions, empty `sourceEntityId`, and non-object rule entries with `Rule #i: ...` messages"
    - "Migration 011's seed schema, loaded through `validateSchema(seed, formBuilder)`, returns `result.success === true` and every entity has the default visibilityRules injected post-validation"
  artifacts:
    - path: lib/form-builder/attributes/visibility-rules.ts
      provides: "visibilityRulesAttribute factory + exported types VisibilityRule, VisibilityRules"
      exports: ["visibilityRulesAttribute", "VisibilityRule", "VisibilityRules", "VALID_OPERATORS", "VALID_ACTIONS"]
    - path: lib/form-builder/entities/*.ts (13 files)
      provides: "Each entity now declares visibilityRulesAttribute in attrs[] AND `shouldBeProcessed: makeShouldBeProcessed()` option"
      contains: "visibilityRulesAttribute"
  key_links:
    - from: lib/form-builder/entities/*.ts (all 13)
      to: lib/form-builder/attributes/visibility-rules.ts
      via: "import { visibilityRulesAttribute }"
      pattern: "import.*visibility-rules"
    - from: lib/form-builder/entities/*.ts (all 13)
      to: lib/form-builder/visibility/should-be-processed.ts (SHIPPED by plan 15-00 in Wave 0)
      via: "import { makeShouldBeProcessed }; shouldBeProcessed: makeShouldBeProcessed()"
      pattern: "shouldBeProcessed.*makeShouldBeProcessed"
---

<objective>
Implement the `visibilityRulesAttribute` factory per D-05 and attach it (plus the shared `shouldBeProcessed` hook shipped by plan 15-00 at `lib/form-builder/visibility/should-be-processed.ts`) to all 13 entity definitions. The attribute MUST default-coerce missing/undefined values so every pre-Phase-15 `template_versions.schema_json` row continues to validate cleanly (Pitfall 1, A4). This plan depends on plan 15-00 (which owns the real `makeShouldBeProcessed` body) — there is no stub bootstrap, no temporary re-export. Plans 15-01 and 15-02 run in parallel in Wave 1 with zero `files_modified` overlap on `should-be-processed.ts`.

Purpose: COND-01 (rules can be declared per field) + BUILDER-02 (properties panel surfaces visibility).
Output: One new attribute file, 13 entity files modified with two-line patch (import + attrs entry + option), real `visibility-rules` Wave-0 test populated, backcompat test populated.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-conditional-logic-engine/15-CONTEXT.md
@.planning/phases/15-conditional-logic-engine/15-RESEARCH.md
@.planning/phases/15-conditional-logic-engine/15-PATTERNS.md
@lib/form-builder/attributes/computed-inputs.ts
@lib/form-builder/attributes/attach-photos.ts
@lib/form-builder/entities/text-field.ts
@lib/form-builder/entities/computed-field.ts
@lib/form-builder/entities/repeating-section.ts
@lib/form-builder/index.ts
@supabase/migrations/011_specialty_smoke_test_template.sql
</context>

<task type="auto" tdd="true">
  <name>Task 1: Implement visibilityRulesAttribute factory</name>
  <files>
    lib/form-builder/attributes/visibility-rules.ts,
    tests/form-builder/visibility/visibility-rules-attribute.test.ts
  </files>
  <read_first>
    lib/form-builder/attributes/computed-inputs.ts (the closest analog — verbatim default-coerce shape lines 14-32),
    lib/form-builder/attributes/attach-photos.ts (second analog, simpler shape lines 1-25),
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Pattern 2 (lines 358-414),
    .planning/phases/15-conditional-logic-engine/15-PATTERNS.md §"lib/form-builder/attributes/visibility-rules.ts" + §"Default-coerce attribute validate()"
  </read_first>
  <behavior>
    - undefined/null input → return `{ rules: [], logic: "and" }` (no throw).
    - non-object root (string, array, number) → throw with message starting `visibilityRules must be an object`.
    - logic === "or" → preserved; any other value (including missing) → coerced to "and".
    - rules not an array → throw.
    - each rule entry: non-object → throw `Rule #${i} is not an object`; missing/non-string sourceEntityId → throw `Rule #${i}: sourceEntityId must be a non-empty string`; bad operator → throw `Rule #${i}: operator must be one of equals, notEquals, contains, greaterThan, lessThan, isEmpty, isNotEmpty`; bad action → throw `Rule #${i}: action must be show, hide, or require`.
    - value defaults to null when missing.
  </behavior>
  <action>
    Create `lib/form-builder/attributes/visibility-rules.ts` exporting:
    - `VALID_OPERATORS: Set<string>` with the seven D-06 operators.
    - `VALID_ACTIONS: Set<string>` = `new Set(["show","hide","require"])` per D-07.
    - `interface VisibilityRule { sourceEntityId: string; operator: ...; value: string|number|boolean|null; action: ... }` per D-05.
    - `interface VisibilityRules { rules: VisibilityRule[]; logic: "and"|"or" }` per D-05.
    - `const DEFAULT: VisibilityRules = { rules: [], logic: "and" }`.
    - `visibilityRulesAttribute = createAttribute({ name: "visibilityRules", validate(value) { ... } })` matching the shape in 15-RESEARCH.md §Pattern 2.
    Populate `tests/form-builder/visibility/visibility-rules-attribute.test.ts` by replacing the Wave-0 `it.todo` blocks with real `it()` assertions using `await import("@/lib/form-builder/attributes/visibility-rules")`. Cover all 5 cases listed in the behavior block.
  </action>
  <verify>
    <automated>npx vitest run tests/form-builder/visibility/visibility-rules-attribute.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/form-builder/attributes/visibility-rules.ts` exists and exports `visibilityRulesAttribute`, `VisibilityRule`, `VisibilityRules`, `VALID_OPERATORS`, `VALID_ACTIONS`.
    - `npx vitest run tests/form-builder/visibility/visibility-rules-attribute.test.ts` reports all 5 tests passing.
    - Running `npx tsc --noEmit` reports no new errors traceable to the new file.
  </acceptance_criteria>
  <done>visibilityRulesAttribute factory is shipped, default-coerces undefined per Pitfall 1, and rejects malformed shapes with Rule #i: messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach attribute + shouldBeProcessed to all 13 entities and backcompat test</name>
  <files>
    lib/form-builder/entities/text-field.ts,
    lib/form-builder/entities/number-field.ts,
    lib/form-builder/entities/date-field.ts,
    lib/form-builder/entities/select-field.ts,
    lib/form-builder/entities/textarea-field.ts,
    lib/form-builder/entities/checkbox-field.ts,
    lib/form-builder/entities/section-group.ts,
    lib/form-builder/entities/signature-field.ts,
    lib/form-builder/entities/rating-field.ts,
    lib/form-builder/entities/multi-photo-field.ts,
    lib/form-builder/entities/geolocation-field.ts,
    lib/form-builder/entities/computed-field.ts,
    lib/form-builder/entities/repeating-section.ts,
    tests/form-builder/visibility/backcompat.test.ts
  </files>
  <read_first>
    lib/form-builder/entities/text-field.ts (lines 1-40 — current attrs[] shape),
    lib/form-builder/entities/computed-field.ts (computedField special case — read-only, never accepts `require`),
    lib/form-builder/entities/repeating-section.ts (container shape, `{instances:[]}` value),
    lib/form-builder/entities/section-group.ts (container),
    .planning/phases/15-conditional-logic-engine/15-PATTERNS.md §"lib/form-builder/entities/*.ts (MODIFY — all 13 entities)" (lines 190-237 — exact BEFORE/AFTER diff),
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Pattern 3 (lines 417-450),
    supabase/migrations/011_specialty_smoke_test_template.sql (the seed schema the backcompat test loads),
    lib/form-builder/index.ts (confirm no separate registration call is needed)
  </read_first>
  <behavior>
    - Every one of the 13 entity files imports `visibilityRulesAttribute` from `../attributes/visibility-rules`.
    - Every one of the 13 entity files imports `makeShouldBeProcessed` from `../visibility/should-be-processed` (file already exists — shipped by plan 15-00 in Wave 0; do NOT create a stub here and do NOT modify the file).
    - Every one of the 13 entity definitions adds `visibilityRulesAttribute` as the LAST entry in `attributes[]` and adds `shouldBeProcessed: makeShouldBeProcessed(),` as a new option key.
    - Container entities (`sectionGroup`, `repeatingSection`) ALSO get both changes — engine uniformity per 15-PATTERNS §"Container entities" (UI hides editor for them; engine doesn't care).
    - `computedField` gets both changes — UI filters `require` from action dropdown later in plan 15-06 (not here).
    - The backcompat test loads migration 011's published seed schema via `validateSchema(seed, formBuilder)`, asserts `result.success === true`, AND asserts every entity in `result.data.entities` has `attributes.visibilityRules` equal to `{ rules: [], logic: "and" }`.
  </behavior>
  <action>
    For each of the 13 entity files, apply the BEFORE/AFTER diff shown in 15-PATTERNS.md lines 195-229: add the two import lines, add `visibilityRulesAttribute` to the `attributes` array, and add the `shouldBeProcessed: makeShouldBeProcessed()` option. Do NOT change any `validate(value, context)` body or any other attribute. `lib/form-builder/visibility/should-be-processed.ts` is guaranteed to exist (shipped by plan 15-00 Task 3) — do NOT create a stub and do NOT modify the file. Confirm by reading `lib/form-builder/index.ts` lines 1-38 that no additional `createBuilder({ entities: [...] })` registration is needed beyond the existing entity-export list. Then populate `tests/form-builder/visibility/backcompat.test.ts`: use `await import("@coltorapps/builder")` for `validateSchema`, `await import("@/lib/form-builder")` for `formBuilder`, and embed a minimal JSON snapshot of the migration 011 seed schema (extracted by reading the migration file) as the test fixture. Assert success + post-validation default-injection per the behavior block.
  </action>
  <verify>
    <automated>npx vitest run tests/form-builder/visibility/backcompat.test.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "lib/form-builder/(entities|attributes|visibility)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 13 entity files contain the line `visibilityRulesAttribute,` inside their `attributes` array (`grep -l "visibilityRulesAttribute" lib/form-builder/entities/*.ts` returns 13 paths).
    - All 13 entity files contain `shouldBeProcessed: makeShouldBeProcessed()` (`grep -l "makeShouldBeProcessed" lib/form-builder/entities/*.ts` returns 13 paths).
    - `npx vitest run tests/form-builder/visibility/backcompat.test.ts` passes — confirms Pitfall 1 / A4 resolved.
    - `npx tsc --noEmit` reports zero NEW errors in `lib/form-builder/entities/`, `lib/form-builder/attributes/`, or `lib/form-builder/visibility/`.
    - `lib/form-builder/index.ts` is UNCHANGED (no new entity registration needed — confirmed during read_first).
  </acceptance_criteria>
  <done>All 13 entities accept the new attribute + hook (which was shipped in Wave 0 by plan 15-00 — no stub here), and the backcompat test confirms legacy schemas validate cleanly with the default-injected rule object.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client request body → server `saveDraftAction` / `publishTemplateAction` | Schema JSON crosses here. `visibilityRulesAttribute.validate` runs server-side via coltorapps `validateSchema`. |
| Stored `template_versions.schema_json` → interpreter render | Pre-Phase-15 rows lack the new attribute. Default-coerce is the migration boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | visibilityRulesAttribute.validate | mitigate | Whitelist VALID_OPERATORS + VALID_ACTIONS sets reject any value not in the fixed D-06/D-07 lists. validateSchema runs server-side; client tampering cannot smuggle an unknown operator past INSERT. |
| T-15-01-02 | Denial of Service | visibilityRulesAttribute.validate | accept | A schema with thousands of rules is bounded by overall coltorapps validateSchema cost — pre-existing accepted-risk class. Cycle detection (plan 15-03) covers the runtime resource-exhaustion path. |
| T-15-01-03 | Information Disclosure | Backward-compat path | mitigate | Pitfall 1 — default-coerce ensures legacy schemas validate; without it, pinned versions would fail to render and admin would see empty forms, NOT a leak. Backcompat test guards regression. |
| T-15-01-04 | Tampering | Per-rule shape | mitigate | Per-rule object is hand-validated key-by-key (sourceEntityId non-empty string; operator + action whitelisted). Throws with indexed `Rule #i:` message so the source of the malformed entry is identifiable in server logs. |
</threat_model>

<verification>
- All 13 entity files import and use `visibilityRulesAttribute` and `makeShouldBeProcessed`.
- `npx vitest run tests/form-builder/visibility/visibility-rules-attribute.test.ts tests/form-builder/visibility/backcompat.test.ts` passes.
- `npx tsc --noEmit` reports zero new errors in `lib/form-builder/`.
- Legacy seed schema (migration 011) loads cleanly through validateSchema with default visibilityRules injected on every entity (A4 verified).
</verification>

<success_criteria>
COND-01 attribute layer is live: every entity carries the new attribute, validate() default-coerces missing values, malformed shapes throw with admin-readable indexed messages, and pre-Phase-15 stored schemas continue to render without breakage.
</success_criteria>

<output>
After completion, create `.planning/phases/15-conditional-logic-engine/15-01-SUMMARY.md` documenting: the attribute factory implementation choices, the 13 entity files modified (one-line summary each), confirmation that `should-be-processed.ts` was imported (not authored — plan 15-00 owns it), and the backcompat test outcome.
</output>
