---
phase: 15
plan: 07
type: execute
wave: 3
depends_on: [15-03, 15-05, 15-06]
files_modified:
  - components/form-builder/cycle-error-banner.tsx
  - components/form-builder/conditional-logic-section.tsx
  - components/form-builder/properties-panel.tsx
  - app/admin/templates/[id]/builder-client.tsx
  - tests/form-builder/cycle-error-banner.test.tsx
autonomous: true
requirements: [COND-03, BUILDER-02]
requirements_addressed: [COND-03, BUILDER-02]

must_haves:
  truths:
    - "When the server save/publish action throws a `RuleGraphInvalid` JSON Error (from plan 15-05), the builder surface catches, JSON-parses, and renders: (a) a Sonner toast `Circular rule detected` with the cycle entity labels, AND (b) an inline `CycleErrorBanner` inside the PropertiesPanel for the selected entity if it is part of any reported cycle (UI-SPEC §2)"
    - "Scope errors from validateRuleGraph (cross-instance / root→inside-repeating) surface as similarly-styled inline banners with the appropriate copy"
    - "The Publish button shows a disabled-state tooltip `Fix circular rules before publishing` when the most recent server save returned a RuleGraphInvalid error (UI-SPEC §Copywriting)"
    - "The banner uses the destructive token palette per UI-SPEC §2: `rounded-[3px] border border-[#8b2b21]/40 bg-[#8b2b21]/10 px-3 py-2`, AlertTriangle icon 12px, text-destructive copy"
  artifacts:
    - path: components/form-builder/cycle-error-banner.tsx
      provides: "CycleErrorBanner — inline warning with entity labels + actionable second line"
      exports: ["CycleErrorBanner"]
    - path: components/form-builder/conditional-logic-section.tsx
      provides: "Section now conditionally renders CycleErrorBanner above the + Add condition button when selected entity is part of a cycle"
    - path: app/admin/templates/page.tsx
      provides: "Save / Publish click handlers catch the RuleGraphInvalid Error, JSON.parse, and pipe to (toast + builder store cycle-state)"
    - path: app/client/templates/page.tsx
      provides: "Same handler change on customer surface (parity per plan 15-05 Open Question #3)"
  key_links:
    - from: app/admin/templates/[id]/builder-client.tsx (handleSave + handlePublish + builderStore.subscribe)
      to: components/form-builder/cycle-error-banner.tsx
      via: "Local cycleState useState slice carries the parsed RuleGraphInvalid payload; cleared via builderStore.subscribe(EntityAttributeUpdated where attributeName==='visibilityRules'); passed down through PropertiesPanel"
      pattern: "RuleGraphInvalid|cycleState|builderStore.subscribe"
    - from: components/form-builder/conditional-logic-section.tsx
      to: components/form-builder/cycle-error-banner.tsx
      via: "Renders the banner above the + Add condition button when entity.id appears in cycleState.cycles[*].entityIds"
      pattern: "CycleErrorBanner"
---

<objective>
Surface plan 15-05's structured `RuleGraphInvalid` Error to the builder UI per UI-SPEC §2. Two presentation layers: (1) Sonner toast for immediate feedback on Save/Publish click, (2) inline `CycleErrorBanner` in the PropertiesPanel that persists until the admin removes a rule from the cycle. Both surfaces (admin + customer) receive the same treatment per the Open Question #3 resolution from plan 15-05.

Purpose: COND-03 (cycle detection must be visible to the admin, not just silently rejected), BUILDER-02 (rule editor surfaces errors in-place).
Output: 1 new component, 1 small extension to ConditionalLogicSection, 2 page-handler modifications, 1 test file.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-conditional-logic-engine/15-CONTEXT.md
@.planning/phases/15-conditional-logic-engine/15-UI-SPEC.md
@.planning/phases/15-conditional-logic-engine/15-PATTERNS.md
@.planning/phases/15-conditional-logic-engine/15-05-SUMMARY.md
@app/admin/templates/page.tsx
@app/client/templates/page.tsx
@components/form-builder/conditional-logic-section.tsx
@components/form-builder/properties-panel.tsx
@lib/form-builder/visibility/validate-rule-graph.ts
</context>

<task type="auto" tdd="true">
  <name>Task 1: CycleErrorBanner component + ConditionalLogicSection integration</name>
  <files>
    components/form-builder/cycle-error-banner.tsx,
    components/form-builder/conditional-logic-section.tsx,
    tests/form-builder/cycle-error-banner.test.tsx
  </files>
  <read_first>
    .planning/phases/15-conditional-logic-engine/15-UI-SPEC.md §2 (the inline banner spec — exact tokens and copy),
    .planning/phases/15-conditional-logic-engine/15-PATTERNS.md §"components/form-builder/cycle-error-banner.tsx",
    components/form-builder/conditional-logic-section.tsx (from plan 15-06 — the integration site),
    lib/form-builder/visibility/validate-rule-graph.ts (the CycleError and ScopeError shapes — banner needs to render either)
  </read_first>
  <behavior>
    CycleErrorBanner props: `{ cycles?: Array<{ entityIds: string[]; labels: string[] }>; scopeErrors?: Array<{ consumerId: string; sourceId: string; consumerLabel: string; sourceLabel: string; reason: "cross-instance" | "root-references-inside-repeating" | "orphan-source"; severity?: "advisory" }>; selectedEntityId: string; surface: "dark" | "cream" }`.
    - Renders nothing when neither cycles nor scopeErrors contain the selected entity.
    - For cycles: render one banner per cycle that includes `selectedEntityId` in its `entityIds`. Copy: line 1 `Circular rule: [Label A] → [Label B]` (join labels with `→`, truncate at 3 hops + `…`); line 2 `Remove a rule above to break the cycle.`.
    - For scopeErrors: render one banner per scopeError where `consumerId === selectedEntityId`. Copy varies by reason — `cross-instance`: `Cross-instance rule rejected: [sourceLabel] is in a different instance` + line 2 `Pick a sibling in this instance or an ancestor field.`; `root-references-inside-repeating`: `Cannot reference inside a repeating section from root: [sourceLabel]` + line 2 `Pick a root field or move this rule into the repeating section.`; `orphan-source` (advisory): `Source field deleted: [sourceLabel]` + line 2 `Remove this rule or pick a new source.`.
    - Visual: outer container `rounded-[3px] border border-[#8b2b21]/40 bg-[#8b2b21]/10 px-3 py-2` (dark) — UI-SPEC §2 explicit; cream surface uses the same destructive palette (CSS variables resolve per surface). Icon: AlertTriangle 12px. Text size `text-[10px]` with `text-destructive`.
    ConditionalLogicSection integration:
    - Section accepts a new optional prop: `cycleState?: { cycles: ..., scopeErrors: ... }`.
    - When `cycleState` is present, render `<CycleErrorBanner cycles={cycleState.cycles} scopeErrors={cycleState.scopeErrors} selectedEntityId={entity.id} surface={surface} />` ABOVE the `+ Add condition` button.
    - When `cycleState` is undefined or empty, render nothing (no banner area).
  </behavior>
  <action>
    Create `components/form-builder/cycle-error-banner.tsx` implementing the component per the behavior block. Use AlertTriangle from lucide-react. Implement label-list truncation per UI-SPEC §2 (toast joins labels with `→`, truncates at 3 hops + `…`; inline banner uses the same shape).
    Modify `components/form-builder/conditional-logic-section.tsx` (from plan 15-06): add the optional `cycleState` prop; render `<CycleErrorBanner ...>` above the `+ Add condition` button when cycleState is provided. Keep the existing behaviour identical when cycleState is absent.
    Create `tests/form-builder/cycle-error-banner.test.tsx` with cases: (1) renders nothing when selectedEntityId not in any cycle/scope; (2) renders cycle banner with truncated labels for a 4-hop cycle; (3) renders cross-instance scope banner with the correct copy; (4) renders root-references-inside-repeating banner; (5) renders orphan-source advisory banner with the advisory copy.
  </action>
  <verify>
    <automated>npx vitest run tests/form-builder/cycle-error-banner.test.tsx tests/form-builder/conditional-logic-section.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `components/form-builder/cycle-error-banner.tsx` exists with `CycleErrorBanner` named export.
    - `components/form-builder/conditional-logic-section.tsx` imports and conditionally renders `CycleErrorBanner` above the `+ Add condition` button.
    - All 5 cycle-error-banner.test.tsx assertions pass.
    - Existing 7 conditional-logic-section.test.tsx assertions (from plan 15-06) still pass.
    - Banner uses the exact destructive token palette per UI-SPEC §2 (verified by test assertion on rendered className substring).
  </acceptance_criteria>
  <done>The inline banner is shipped and integrated into the section; plan 15-07 Task 2 wires the page-level error catch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save/Publish handlers — catch, parse, toast, and pipe cycleState</name>
  <files>
    app/admin/templates/[id]/builder-client.tsx,
    components/form-builder/properties-panel.tsx
  </files>
  <read_first>
    app/admin/templates/page.tsx (FULL FILE — find the Save and Publish click handlers; they call saveDraftAction / publishTemplateAction; also confirms the parent state surface the cycleState slice will live next to — added per checker Warning 11 to inform the banner's prop shape),
    app/client/templates/page.tsx (FULL FILE — same find: saveClientDraftAction / publishClientTemplateAction; included for symmetric parent-state inspection per Warning 11),
    app/admin/templates/[id]/builder-client.tsx (FULL FILE — this is the ACTUAL client component that hosts the builder store via `useBuilderStore(formBuilder, {initialData})` from `@coltorapps/builder-react` and wires the Save/Publish handlers. The cycleState slice lives here, NOT in the route-level `page.tsx`. Verified at file lines 102-104. Customer surface uses the same `TemplateBuilderClient` component — same wiring path.),
    components/form-builder/properties-panel.tsx (FULL FILE — where ConditionalLogicSection is rendered; needs the cycleState passthrough; the `setAttr` helper at lines 230-232 is where attribute writes flow but is NOT the right hook for cycleState clearing — see behavior block),
    node_modules/@coltorapps/builder/dist/index.d.ts lines 309-347 (the `builderStoreEventsNames` const + `BuilderStoreEvent` discriminated union — exposes the `EntityAttributeUpdated` event with payload `{entity, attributeName}` that this task subscribes to for clear-on-edit behaviour),
    .planning/phases/15-conditional-logic-engine/15-UI-SPEC.md §2 (Toast specification) + §Copywriting Contract (Save blocked tooltip text),
    .planning/phases/15-conditional-logic-engine/15-05-SUMMARY.md (the structured error shape from server),
    components/form-builder/cycle-error-banner.tsx (the banner just built — needs cycleState shape)
  </read_first>
  <behavior>
    Page-level handler changes (apply to BOTH admin and customer templates pages):
    - Wrap Save and Publish click handlers in try/catch.
    - On catch, attempt `JSON.parse(error.message)`. If parsed shape has `kind === "RuleGraphInvalid"`:
      - Compute cycles + scopeErrors + selectedEntityId.
      - Call `toast.error("Circular rule detected", { description: cycles.map(c => c.labels.slice(0,3).join(" → ") + (c.labels.length > 3 ? " …" : "")).join("; ") })` from `sonner`. If no cycles but scopeErrors exist, title becomes the first scopeError's reason-specific copy.
      - Set local component state `cycleState = { cycles, scopeErrors }` and pass it down to the PropertiesPanel via a new optional prop.
      - For Publish: also set a `publishBlocked = true` flag that disables the publish button with tooltip `Fix circular rules before publishing` per UI-SPEC §Copywriting.
    - On other errors (non-JSON or different `kind`), fall through to existing error handling.
    - When the admin edits any rule, CLEAR `cycleState` and `publishBlocked` so the next save attempt is unblocked-by-default. Use the coltorapps builder-store subscription API (verified in `node_modules/@coltorapps/builder/dist/index.d.ts` lines 309-347): `builderStore.subscribe((events) => { ... })` receives a batched array of `BuilderStoreEvent` objects. Filter for `event.name === "EntityAttributeUpdated" && event.payload.attributeName === "visibilityRules"`; on match, call the parent's `setCycleState(null)` + `setPublishBlocked(false)`. Wire this subscription inside a `useEffect(() => { const unsub = builderStore.subscribe(handler); return unsub; }, [builderStore, setCycleState, setPublishBlocked])` in `TemplateBuilderClient` (the host file). DO NOT intercept at the PropertiesPanel `setAttr` boundary — the subscription pattern correctly catches every code path that writes the attribute (paste, undo, programmatic edits) and matches existing repo patterns (`useBuilderStoreData(builderStore, () => true)` at builder-client.tsx line 107 uses the same subscription substrate).
    PropertiesPanel passthrough:
    - PropertiesPanel accepts a new optional `cycleState` prop; passes it to ConditionalLogicSection unchanged.
    - No other PropertiesPanel render branch changes.
  </behavior>
  <action>
    Modify `app/admin/templates/[id]/builder-client.tsx` (the actual host of the builder store, used by BOTH admin and customer surfaces — confirmed at file lines 102-104 + the customer surface's reuse of `TemplateBuilderClient`). In `handleSave` (currently lines 132-145) and `handlePublish` (currently lines 147-166), wrap the inner `await saveDraftAction(...)` / `await publishTemplateAction(...)` calls with a try/catch that attempts `JSON.parse(err.message)`; if the parsed payload has `kind === "RuleGraphInvalid"`, call `toast.error("Circular rule detected", { description: ... })` from `sonner` (already imported in the project), set local `cycleState` state (`useState<{cycles, scopeErrors} | null>(null)`), and for the publish path also set `publishBlocked` state. Add a `useEffect` immediately after the `useBuilderStore` call: `useEffect(() => { const unsub = builderStore.subscribe((events) => { for (const e of events) { if (e.name === "EntityAttributeUpdated" && e.payload.attributeName === "visibilityRules") { setCycleState(null); setPublishBlocked(false); return; } } }); return unsub; }, [builderStore]);`. Pass `cycleState` down to `<PropertiesPanel cycleState={cycleState} ... />` and `publishBlocked` to the Publish `<Button>` (disable + render tooltip `Fix circular rules before publishing` when true). Modify `components/form-builder/properties-panel.tsx` to accept the optional `cycleState` prop and forward it to `<ConditionalLogicSection cycleState={cycleState} ... />`. Reuse the existing surfaceTokens map; no new visual changes outside the new banner conditional render. No edits to `app/admin/templates/page.tsx` or `app/client/templates/page.tsx` are needed — both are server-component route handlers that delegate to `TemplateBuilderClient`. Verify this by re-reading both page files for the `<TemplateBuilderClient ... />` JSX before committing.
  </action>
  <verify>
    <automated>npx vitest run tests/form-builder/ tests/form-interpreter/ &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "app/(admin|client)/templates|components/form-builder" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/templates/[id]/builder-client.tsx` contains `JSON.parse` of the caught error message inside BOTH the `handleSave` and `handlePublish` paths (grep for `JSON.parse.*message` returns ≥2 matches in the file).
    - The file imports `toast` from `sonner` and calls `toast.error(...)`.
    - The file contains exactly one `builderStore.subscribe(` call (the EntityAttributeUpdated handler) — `grep -c "builderStore.subscribe(" app/admin/templates/[id]/builder-client.tsx` returns `1`.
    - The subscribe handler filters on `attributeName === "visibilityRules"` (grep for the literal string returns ≥1).
    - `components/form-builder/properties-panel.tsx` accepts and forwards `cycleState` to ConditionalLogicSection.
    - When publishBlocked is true, the publish button is disabled (verified manually or by adding a smoke test if time allows).
    - No edits made to `app/admin/templates/page.tsx` or `app/client/templates/page.tsx` (verified by `git diff --stat` showing zero lines changed for those two files).
    - All existing tests across `tests/form-builder/` and `tests/form-interpreter/` still pass — zero regression.
    - `npx tsc --noEmit` reports zero new errors.
  </acceptance_criteria>
  <done>Server-side RuleGraphInvalid errors are caught, parsed, and surfaced as toast + inline banner on both surfaces. Publish button disables on cycle until admin edits a rule.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server thrown Error.message → client JSON.parse | The Error message is constructed server-side by plan 15-05 with a known shape. JSON.parse on the client may fail for non-RuleGraphInvalid errors — code path falls back to existing handlers in that case. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Tampering | Toast/banner copy | accept | All copy comes from server-supplied entity LABELS (not IDs alone) and from a fixed string set per UI-SPEC. Labels are admin-authored strings already in the schema — no injection vector beyond what already exists for the labels themselves. |
| T-15-07-02 | Cross-Site Scripting | Banner label rendering | mitigate | Labels render as React text children (not dangerouslySetInnerHTML); React auto-escapes. Verified by the cycle-error-banner.test.tsx render assertion checking the text content matches the input label verbatim. |
| T-15-07-03 | Information Disclosure | Cycle payload content | mitigate | Payload contains entity LABELS and IDS only — no field values, no PII. Already validated server-side in plan 15-03 (T-15-03-03). |
| T-15-07-04 | Spoofing | Publish-blocked tooltip | accept | Tooltip is purely advisory; server-side enforcement remains the authoritative gate. A user who somehow re-enables the button still hits the server-side validateRuleGraph reject. |
| T-15-07-05 | Repudiation | Save/Publish failures | accept | The existing Next.js error pipeline already logs server-action throws; this plan adds a UI surface, not a new audit boundary. |
</threat_model>

<verification>
- `components/form-builder/cycle-error-banner.tsx` exists with the named export.
- Both template page files contain JSON.parse + toast.error + cycleState wiring.
- PropertiesPanel forwards cycleState prop.
- `npx vitest run tests/form-builder/cycle-error-banner.test.tsx tests/form-builder/conditional-logic-section.test.tsx` passes.
- Existing Phase 14 + Phase 15 tests across `tests/form-builder/` and `tests/form-interpreter/` all pass.
</verification>

<success_criteria>
COND-03 cycle errors are now visible to the admin: toast on save/publish click, inline banner inside the rule editor for the selected offending entity, publish button disabled with explanatory tooltip until the admin edits a rule. Both surfaces (admin + customer) parity-matched.
</success_criteria>

<output>
After completion, create `.planning/phases/15-conditional-logic-engine/15-07-SUMMARY.md` documenting: the cycleState lifecycle (set on server error inside `handleSave`/`handlePublish` in `builder-client.tsx`; cleared via `builderStore.subscribe(EntityAttributeUpdated)` filter on `attributeName === "visibilityRules"`), the toast vs inline banner split, the publish-blocked tooltip wiring, and confirmation that the route-level `page.tsx` files were NOT modified (host of state is the shared `TemplateBuilderClient`).
</output>
