---
phase: 15
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - components/form-interpreter/interpreter-renderer.tsx
  - components/form-interpreter/repeating-section-renderer.tsx
  - lib/form-builder/progress.ts
  - tests/form-builder/progress-with-visibility.test.ts
  - tests/form-interpreter/visibility-renderer.test.tsx
autonomous: true
requirements: [COND-01, COND-02, COND-04, BUILDER-02]
requirements_addressed: [COND-01, COND-02, COND-04, BUILDER-02]

must_haves:
  truths:
    - "Renderer integrates `evaluateVisibility(schema, values)` via `propsRef` so each per-entity renderer receives a primitive `dynamicRequired` boolean (Phase 14-06 focus-loss invariant preserved — useMemo deps stay `[surface]`)"
    - "Show/hide is NOT threaded through props — coltorapps `shouldBeProcessed` (already attached to every entity in plan 15-01) handles render-skipping natively"
    - "`computeFormProgress(schema, values, visibility?)` excludes hidden entities from both numerator and denominator when visibility is supplied; default behaviour (visibility=undefined) is byte-identical to pre-Phase-15"
    - "Hidden trumps required at the progress layer: a static-required field that is hidden does NOT count toward the denominator (D-07)"
    - "Renderer visibility-renderer.test.tsx confirms focus retention on a neighbouring text input across a hide/show flip (Pitfall 5 / Phase 14-06 carry-forward)"
  artifacts:
    - path: components/form-interpreter/interpreter-renderer.tsx
      provides: "propsRef extended with `visibility`; per-renderer wrapper threads primitive `dynamicRequired`; visibility recomputed on every value change without invalidating the components useMemo"
    - path: lib/form-builder/progress.ts
      provides: "computeFormProgress signature accepts optional `visibility` arg; hidden ids drop from numerator+denominator"
      exports: ["computeFormProgress"]
  key_links:
    - from: components/form-interpreter/interpreter-renderer.tsx
      to: lib/form-builder/visibility/evaluate-visibility.ts
      via: "Dynamic import inside the propsRef useEffect; recomputed on each value change"
      pattern: "evaluateVisibility"
    - from: components/form-interpreter/interpreter-renderer.tsx
      to: lib/form-builder/progress.ts
      via: "onProgressChange callback now passes visibility map alongside values"
      pattern: "computeFormProgress\\(schema, values, visibility\\)"
---

<objective>
Wire the runtime visibility evaluator into the interpreter renderer and progress calculator. coltorapps `shouldBeProcessed` (attached in plan 15-01) already skips render+validation for hidden entities — this plan only handles the two things it does NOT cover: (a) dynamic-required state per renderer (coltorapps has no `shouldBeRequired` hook), and (b) progress bar denominator. Both must integrate WITHOUT breaking the Phase 14-06 focus-loss fix: the `components` useMemo deps MUST stay `[surface]`; visibility threads via `propsRef.current.visibility` (Pitfall 5).

Purpose: COND-02 (dynamic required), COND-04 (live evaluation), BUILDER-02 (properties side already in plan 15-06, but this plan owns the runtime).
Output: Two file modifications + two Wave-0 tests filled in.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-conditional-logic-engine/15-CONTEXT.md
@.planning/phases/15-conditional-logic-engine/15-RESEARCH.md
@.planning/phases/15-conditional-logic-engine/15-PATTERNS.md
@.planning/phases/15-conditional-logic-engine/15-UI-SPEC.md
@.planning/phases/14-custom-field-types/14-06-SUMMARY.md
@components/form-interpreter/interpreter-renderer.tsx
@components/form-interpreter/repeating-section-renderer.tsx
@lib/form-builder/progress.ts
@lib/form-builder/visibility/evaluate-visibility.ts
@lib/form-builder/visibility/should-be-processed.ts
@.planning/phases/15-conditional-logic-engine/15-02-SUMMARY.md
</context>

<task type="auto" tdd="true">
  <name>Task 1: Extend interpreter-renderer with visibility via propsRef</name>
  <files>
    components/form-interpreter/interpreter-renderer.tsx,
    tests/form-interpreter/visibility-renderer.test.tsx
  </files>
  <read_first>
    components/form-interpreter/interpreter-renderer.tsx (FULL FILE — lines 1-200; the propsRef/useMemo pattern at lines 84-158 is the load-bearing invariant),
    components/form-interpreter/repeating-section-renderer.tsx (FULL FILE — confirms the per-instance value binding from the A3 spike outcome in plan 15-02),
    .planning/phases/14-custom-field-types/14-06-SUMMARY.md (the focus-loss-fix history),
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Pattern 6 lines 644-665 + Pitfall 5,
    .planning/phases/15-conditional-logic-engine/15-PATTERNS.md §"components/form-interpreter/interpreter-renderer.tsx",
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Open Questions (RESOLVED) item #1 (A3 spike outcome locked: `entitiesValues` is root-only AND coltorapps does NOT invoke `shouldBeProcessed` for repeatingSection template children because they aren't coltorapps entities. Per-instance visibility for child entities inside a repSection instance is owned by `RepeatingSectionRenderer` calling `evaluateVisibilityForInstance` from plan 15-02. This task MUST add the bridge — it is no longer conditional.),
    .planning/phases/15-conditional-logic-engine/15-UI-SPEC.md §3 + §4 (hidden DOM unmounting, required asterisk)
  </read_first>
  <behavior>
    propsRef extension:
    - `propsRef` now carries `visibility: Record<entityId, VisibilityState>` alongside the existing `{ clientId, submissionId, schema, interpreterStore }`.
    - The propsRef-syncing useEffect computes `const values = interpreterStore.getEntitiesValues()` then `visibility = evaluateVisibility(schema, values)` on every render. evaluateVisibility is imported statically at the top of the file (small pure module — no dynamic import needed inside hot path).
    - The `components` useMemo deps stay `[surface]` — DO NOT add `visibility`. Per-renderer wrappers read `propsRef.current.visibility[entity.id]?.required ?? false` at call time.
    - Each per-renderer wrapper that can be `required` (text, number, date, select, textarea, checkbox, signature, rating, multi-photo, geolocation, repeatingSection) passes a primitive `dynamicRequired` boolean prop. computedField wrapper SKIPS this prop (no requiredAttribute).
    - sectionGroup/repeatingSection wrappers do NOT receive `dynamicRequired` (containers; cascade is handled by coltorapps shouldBeProcessed already).
    - **REQUIRED (per RESOLVED A3 spike)** — modify `components/form-interpreter/repeating-section-renderer.tsx` so that for each instance and each child, the `ChildInput` render is gated on `evaluateVisibilityForInstance(schema, allValues, repSectionId, instanceIndex)[childId]?.visible !== false`. Compute the per-instance visibility map once per render using `propsRef.current.interpreterStore.getEntitiesValues()`. For dynamic required, pass the per-instance `.required` boolean into `ChildInput` (extend `ChildInput` props with `dynamicRequired?: boolean`). The base `interpreter-renderer.tsx` propsRef changes do NOT cover repeating-section template children — they aren't coltorapps entities — so this renderer-level bridge is the ONLY enforcement path for per-instance visibility + required.
    progress wiring (in this same file — small adjacent change):
    - The `onProgressChange` call inside the existing value-update handler now passes the freshly computed visibility: `onProgressChange?.(computeFormProgress(schema, values, visibility))`. computeFormProgress signature extension lives in Task 2.
    Renderer dynamic-required UX:
    - Each renderer that accepts `dynamicRequired` MUST render the asterisk `after:content-['*'] after:text-destructive after:ml-0.5` whenever `dynamicRequired === true`, regardless of static `entity.attributes.required`. Per UI-SPEC §4.
    - This task does NOT modify the 10 renderer component files individually — instead, it threads the prop and adds the asterisk logic to a single shared `RendererLabel` helper component OR to each renderer's label call site. Choose whichever is cleaner against the current renderer shapes; document the choice in 15-04-SUMMARY.md.
  </behavior>
  <action>
    Modify `components/form-interpreter/interpreter-renderer.tsx`: import `evaluateVisibility` from `@/lib/form-builder/visibility/evaluate-visibility` at the top; extend the propsRef declaration to include `visibility`; extend the propsRef-syncing useEffect to compute visibility from interpreterStore values; for each renderer in the `components` useMemo, add `dynamicRequired={propsRef.current.visibility[entity.id]?.required ?? false}` (only for the 10 renderers listed in the behavior block; SKIP computedField, sectionGroup, repeatingSection). DO NOT alter the `[surface]` dep array of the components useMemo — verify the comment `// eslint-disable-next-line react-hooks/exhaustive-deps` above it remains. Update `onProgressChange` callsite to pass the new visibility arg. ALSO modify `components/form-interpreter/repeating-section-renderer.tsx`: import `evaluateVisibilityForInstance` from `@/lib/form-builder/visibility/evaluate-visibility`; in the per-instance render loop, compute `const instanceVis = evaluateVisibilityForInstance(schema, allValues, entity.id, idx)` once per instance; gate each `<ChildInput key={childId} ... />` on `instanceVis[childId]?.visible !== false`; pass `dynamicRequired={instanceVis[childId]?.required ?? false}` into `ChildInput` (extend its props accordingly so the per-child label asterisk reflects the dynamic state). Populate `tests/form-interpreter/visibility-renderer.test.tsx`: 4 tests per Wave-0 — entity with hide rule fires → DOM unmounts; entity with show rule (default-hidden) appears when rule fires; focus on a neighbouring text input is RETAINED across a hide/show flip of a sibling (the Phase 14-06 invariant); Select source field stays controlled when its dependent's source flips between defined/undefined.
  </action>
  <verify>
    <automated>npx vitest run tests/form-interpreter/visibility-renderer.test.tsx &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "components/form-interpreter" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/form-interpreter/interpreter-renderer.tsx` imports `evaluateVisibility`.
    - propsRef declaration includes `visibility` (grep confirms a `visibility` key in the propsRef literal).
    - `components` useMemo deps line still reads `[surface]` (no `visibility` added — verified by `grep -Fc "}), [surface])" components/form-interpreter/interpreter-renderer.tsx` returning ≥1, where `-F` makes the bracket literal).
    - Each of the 10 named renderer wrappers passes `dynamicRequired={propsRef.current.visibility[entity.id]?.required ?? false}` — primitive boolean, not object reference (Pitfall 5).
    - computedField wrapper does NOT pass `dynamicRequired`.
    - All 4 visibility-renderer.test.tsx assertions pass.
    - The Phase 14-06 focus-retention test in the same file (or in `renderers.test.tsx`) still passes — no regression.
    - `onProgressChange` call now includes the visibility arg.
  </acceptance_criteria>
  <done>Interpreter renderer threads visibility via propsRef without breaking the focus-loss invariant; hidden DOM unmounts via coltorapps shouldBeProcessed; dynamic required is live per UI-SPEC §4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend computeFormProgress with optional visibility</name>
  <files>
    lib/form-builder/progress.ts,
    tests/form-builder/progress-with-visibility.test.ts
  </files>
  <read_first>
    lib/form-builder/progress.ts (FULL FILE — the current signature at line 123 + the flatMap at lines 143-152),
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Pattern 8 (lines 730-758),
    .planning/phases/15-conditional-logic-engine/15-PATTERNS.md §"lib/form-builder/progress.ts (MODIFY)",
    lib/form-builder/visibility/types.ts (the VisibilityState shape),
    tests/form-builder/progress.test.ts (existing — for the test harness shape)
  </read_first>
  <behavior>
    computeFormProgress new signature: `computeFormProgress(schema, values, visibility?: Record<string, VisibilityState>): number`.
    - When `visibility === undefined` → behaviour is byte-identical to current Phase 14 (backward-compat — A4 / Pitfall 1 analogue at this layer).
    - When `visibility` provided:
      - Skip entity entirely if `visibility[id]?.visible === false` (drop from both numerator and denominator — D-07 hidden trumps required).
      - Use `visibility[id]?.required === true` (which already folds static+dynamic+hide-wins per plan 15-02) instead of `entity.attributes?.required === true`.
    - repeatingSection minInstances handling unchanged: if min > 0 AND visible AND has minimum instances, counts as 1 required slot.
  </behavior>
  <action>
    Modify `lib/form-builder/progress.ts`: change the export signature per the behavior block; in the `flatMap` (current lines 143-152), add the two early-exit + dynamic-required branches per RESEARCH §Pattern 8. Import `VisibilityState` from `./visibility/types`. Populate `tests/form-builder/progress-with-visibility.test.ts` with 3 cases per Wave-0: visibility=undefined matches existing computeFormProgress output for the same schema/values (backward-compat); hidden field excluded from denominator (a static-required field with `visibility[id].visible === false` does NOT contribute to the count); dynamic-required (rule-fired) field with `visible === true` IS counted.
  </action>
  <verify>
    <automated>npx vitest run tests/form-builder/progress-with-visibility.test.ts tests/form-builder/progress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/form-builder/progress.ts` exports `computeFormProgress` with the new optional `visibility` parameter.
    - Backward-compat test passes (visibility=undefined gives same output).
    - Hidden-field-excluded test passes.
    - Dynamic-required-counted test passes.
    - Existing `tests/form-builder/progress.test.ts` still passes — zero regression on Phase 14 behaviour.
    - `npx tsc --noEmit` reports zero new errors in `lib/form-builder/progress.ts` or its callers.
  </acceptance_criteria>
  <done>Progress calculation is visibility-aware behind an optional arg; legacy callers see no change; interpreter renderer (Task 1) now passes the arg.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interpreter-renderer ↔ coltorapps store | Visibility map is read-only from React's perspective; mutations flow via setEntityValue. |
| Hidden field DOM ↔ user input | coltorapps unmounts the DOM (UI-SPEC §3); no zombie listeners can accept input for hidden fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | dynamicRequired prop | mitigate | dynamicRequired is a primitive boolean read from propsRef.current.visibility — recomputed each render from the canonical schema+values. Client cannot tamper with the required state by editing DOM; server-side `validateEntitiesValues` (plan 15-05) enforces the schema's required attribute against the FINAL submitted values, and plan 15-05's stripHiddenAnswers ensures hidden fields are dropped before that validation matters. |
| T-15-04-02 | Information Disclosure | Hidden field DOM | mitigate | UI-SPEC §3 mandates DOM unmount (not visibility:hidden). coltorapps shouldBeProcessed returning false skips render entirely. No hidden-field DOM means no devtools-side data leak. |
| T-15-04-03 | Denial of Service | propsRef + visibility recompute | accept | `evaluateVisibility` runs on every render (cheap — pure O(entities × rules)). For a 100-entity form with ~50 rules total, single-digit-millisecond cost. Bounded by validateRuleGraph at save time (plan 15-03) so cycles can never reach this hot path. |
| T-15-04-04 | Tampering | Focus-loss regression | mitigate | useMemo deps remain `[surface]` (Pitfall 5). Acceptance criteria explicitly verifies the dep array did not gain `visibility`. The Phase 14-06 focus-retention test in `renderers.test.tsx` is re-run to confirm zero regression. |
</threat_model>

<verification>
- `npx vitest run tests/form-interpreter/visibility-renderer.test.tsx tests/form-builder/progress-with-visibility.test.ts tests/form-builder/progress.test.ts tests/form-interpreter/renderers.test.tsx` all pass.
- `grep -c "useMemo" components/form-interpreter/interpreter-renderer.tsx` returns the expected count (≥1) AND `grep -Fc "}), [surface])" components/form-interpreter/interpreter-renderer.tsx` returns ≥1 (the literal string `}), [surface])` appears at least once — focus-loss invariant preserved; `-F` makes ripgrep treat brackets literally).
- `grep -n "dynamicRequired" components/form-interpreter/interpreter-renderer.tsx` returns ≥10 matches (one per renderer wrapper threading the prop).
- 15-04-SUMMARY.md records the asterisk-label implementation choice (shared helper vs per-renderer).
</verification>

<success_criteria>
COND-04 live evaluation is wired through the runtime: hidden fields unmount via coltorapps shouldBeProcessed; dynamic required asterisk renders per UI-SPEC §4; progress bar reflects the corrected denominator; focus-loss fix from Phase 14-06 is preserved. Plan 15-05 takes over the server-side strip wiring next.
</success_criteria>

<output>
After completion, create `.planning/phases/15-conditional-logic-engine/15-04-SUMMARY.md` documenting: how visibility threads via propsRef without entering useMemo deps; the dynamicRequired prop wiring per renderer; the per-instance bridge added to `repeating-section-renderer.tsx` (always required per RESOLVED A3 spike — see 15-RESEARCH §Open Questions); progress.ts backward-compat strategy.
</output>
