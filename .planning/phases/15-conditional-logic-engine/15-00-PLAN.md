---
phase: 15
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/form-builder/visibility/evaluate-rule.test.ts
  - tests/form-builder/visibility/combine-rules.test.ts
  - tests/form-builder/visibility/cascade-visibility.test.ts
  - tests/form-builder/visibility/evaluate-visibility.test.ts
  - tests/form-builder/visibility/dependency-map.test.ts
  - tests/form-builder/visibility/validate-rule-graph.test.ts
  - tests/form-builder/visibility/scope.test.ts
  - tests/form-builder/visibility/strip-hidden-answers.test.ts
  - tests/form-builder/visibility/visibility-rules-attribute.test.ts
  - tests/form-builder/visibility/backcompat.test.ts
  - tests/form-builder/progress-with-visibility.test.ts
  - tests/form-interpreter/visibility-renderer.test.tsx
  - lib/form-builder/visibility/should-be-processed.ts
autonomous: true
requirements: [COND-01, COND-02, COND-03, COND-04, BUILDER-02]
requirements_addressed: [COND-01, COND-02, COND-03, COND-04, BUILDER-02]

must_haves:
  truths:
    - "Every Wave-0 test file exists and is discoverable by Vitest before Wave-1 begins"
    - "Each stub contains `it.todo` or skeleton `it()` blocks with the test descriptions named in 15-VALIDATION.md so executors can fill them"
    - "`npx vitest run tests/form-builder/visibility/` exits non-zero (because todos are pending) but does NOT error on parse/import"
    - "`lib/form-builder/visibility/should-be-processed.ts` ships in Wave 0 as the REAL `makeShouldBeProcessed` implementation (not a stub) so both 15-01 (entity attach) and 15-02 (engine logic) can import it in parallel without a file-ownership race"
  artifacts:
    - path: tests/form-builder/visibility/evaluate-rule.test.ts
      provides: "Operator × source-type matrix stub (7 operators × 5 source types ~30 cases)"
    - path: tests/form-builder/visibility/combine-rules.test.ts
      provides: "AND/OR + hide-wins-over-show truth-table stub"
    - path: tests/form-builder/visibility/cascade-visibility.test.ts
      provides: "Parent hide → children hide cascade stub"
    - path: tests/form-builder/visibility/evaluate-visibility.test.ts
      provides: "Integration stub (evaluate + combine + cascade)"
    - path: tests/form-builder/visibility/dependency-map.test.ts
      provides: "Direct + computed edge construction stub"
    - path: tests/form-builder/visibility/validate-rule-graph.test.ts
      provides: "Cycle detection + scope-error stub (D-08/D-09)"
    - path: tests/form-builder/visibility/scope.test.ts
      provides: "resolveScope for root/sectionGroup/repeatingSection child stub"
    - path: tests/form-builder/visibility/strip-hidden-answers.test.ts
      provides: "Server-side scrub stub"
    - path: tests/form-builder/visibility/visibility-rules-attribute.test.ts
      provides: "Attribute default-coerce + shape-validation stub"
    - path: tests/form-builder/visibility/backcompat.test.ts
      provides: "Legacy template_versions.schema_json passes validate() stub"
    - path: tests/form-builder/progress-with-visibility.test.ts
      provides: "Progress extension stub (hidden drops from denominator)"
    - path: tests/form-interpreter/visibility-renderer.test.tsx
      provides: "Renderer focus-retention + controlled-Select stub"
    - path: lib/form-builder/visibility/should-be-processed.ts
      provides: "Real `makeShouldBeProcessed()` shared hook body — imported by all 13 entities in plan 15-01 and re-used (not overwritten) by plan 15-02"
      exports: ["makeShouldBeProcessed"]
  key_links:
    - from: tests/form-builder/visibility/*.test.ts
      to: lib/form-builder/visibility/* (Wave 1 modules)
      via: dynamic `await import("@/lib/form-builder/visibility/...")` (per 15-PATTERNS.md "dynamic await import" rule)
      pattern: "await import.*lib/form-builder/visibility"
    - from: lib/form-builder/entities/*.ts (all 13, via plan 15-01)
      to: lib/form-builder/visibility/should-be-processed.ts (this plan)
      via: "import { makeShouldBeProcessed } from '../visibility/should-be-processed'"
      pattern: "makeShouldBeProcessed"
---

<objective>
Stub all 12 Wave-0 test files (per `15-VALIDATION.md` §Wave 0 Requirements and `15-RESEARCH.md` §Validation Architecture) so every Wave 1–4 task has an `<automated>` verify command that points at an existing file. Stubs MUST be parseable by Vitest (no broken imports), contain `it.todo` or skeleton `it()` blocks with the descriptions named in 15-VALIDATION.md, and use the dynamic `await import()` pattern from `tests/form-builder/attributes.test.ts`.

Additionally, this plan now owns the **real** `lib/form-builder/visibility/should-be-processed.ts` implementation. Both plans 15-01 and 15-02 import it: 15-01 wires `makeShouldBeProcessed()` into all 13 entity definitions; 15-02 builds the surrounding pure-logic engine (evaluate-rule, combine-rules, cascade-visibility, evaluate-visibility, strip-hidden-answers) but does NOT overwrite this file. Moving the helper into Wave 0 closes the `files_modified` race that would otherwise let 15-01 and 15-02 both claim the path in Wave 1.

Purpose: satisfy the Nyquist gate — Wave 1+ tasks reference the 12 test files in `<verify>` blocks; the files MUST exist before those tasks run. Also unblock 15-01 + 15-02 parallel execution by pre-shipping the shared hook body.
Output: 12 test files + 1 production module (`should-be-processed.ts`); `npx vitest run tests/form-builder/visibility/` discovers and reports `pending` for each todo (exit code != 0 is acceptable; parse errors are NOT).
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-conditional-logic-engine/15-CONTEXT.md
@.planning/phases/15-conditional-logic-engine/15-RESEARCH.md
@.planning/phases/15-conditional-logic-engine/15-PATTERNS.md
@.planning/phases/15-conditional-logic-engine/15-VALIDATION.md
@tests/form-builder/attributes.test.ts
@tests/form-builder/save-draft.test.ts
@tests/form-interpreter/renderers.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pure-logic Wave-0 stubs (10 files)</name>
  <files>
    tests/form-builder/visibility/evaluate-rule.test.ts,
    tests/form-builder/visibility/combine-rules.test.ts,
    tests/form-builder/visibility/cascade-visibility.test.ts,
    tests/form-builder/visibility/evaluate-visibility.test.ts,
    tests/form-builder/visibility/dependency-map.test.ts,
    tests/form-builder/visibility/validate-rule-graph.test.ts,
    tests/form-builder/visibility/scope.test.ts,
    tests/form-builder/visibility/strip-hidden-answers.test.ts,
    tests/form-builder/visibility/visibility-rules-attribute.test.ts,
    tests/form-builder/visibility/backcompat.test.ts
  </files>
  <read_first>
    tests/form-builder/attributes.test.ts (lines 1-50 — `import { describe, it, expect } from "vitest"` + dynamic await import pattern),
    .planning/phases/15-conditional-logic-engine/15-VALIDATION.md (§Wave 0 Requirements + §Per-Task Verification Map),
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Validation Architecture (lines 1020-1083)
  </read_first>
  <action>
    Create each test file with: (1) header comment "Phase 15 Wave-0 stub — populated by Wave 1+", (2) `import { describe, it, expect } from "vitest"`, (3) one `describe()` per logical group, (4) `it.todo(...)` for each test case named in 15-VALIDATION.md §Wave 0 / 15-RESEARCH.md §Validation Architecture. DO NOT implement assertions. DO NOT import production modules statically — Wave 1 tasks will add the `await import("@/lib/form-builder/visibility/...")` calls when they implement. Per-file todo lists:
    - evaluate-rule.test.ts: 7 todos for the 7 operators (equals, notEquals, contains, greaterThan, lessThan, isEmpty, isNotEmpty); 5 todos for source types (text, number, select, checkbox, date).
    - combine-rules.test.ts: 4 todos for AND/OR truth-table cases; 1 todo for hide-wins-over-show (per D-07).
    - cascade-visibility.test.ts: 3 todos — hidden parent → children hidden; hidden sectionGroup → grandchildren hidden; repeatingSection per-instance cascade.
    - evaluate-visibility.test.ts: 3 todos — basic integration; require-rule fires; hidden trumps required (D-07).
    - dependency-map.test.ts: 2 todos — direct edges from rules; computed edges from computedField inputs (D-02).
    - validate-rule-graph.test.ts: 7 todos — linear chain passes; direct cycle A→B→A rejected; computed-mediated cycle rejected (Pitfall 8); ancestor-scope passes (D-03); cross-instance rejected with reason="cross-instance"; root→inside-repeating rejected with reason="root-references-inside-repeating"; orphan source emits advisory entry.
    - scope.test.ts: 3 todos — resolveScope returns "root" for root entity; returns sectionGroup parent id for child of sectionGroup; returns repeatingSection id for child of repeatingSection.
    - strip-hidden-answers.test.ts: 4 todos — hidden field stripped; cascade strip on hidden parent; per-instance repeatingSection scrub; hidden-but-required silently dropped without throwing.
    - visibility-rules-attribute.test.ts: 5 todos — undefined coerces to `{rules:[], logic:"and"}`; null coerces to default; wrong-shape root throws; bad operator throws with Rule #i; bad action throws with Rule #i.
    - backcompat.test.ts: 1 todo — load migration 011 seed schema via validateSchema; expect success AND every entity now has `visibilityRules: { rules: [], logic: "and" }` post-validation (Pitfall 1).
  </action>
  <verify>
    <automated>npx vitest run tests/form-builder/visibility/ 2>&amp;1 | grep -E "(pending|todo|Error)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 10 files exist at the listed paths.
    - `npx vitest run tests/form-builder/visibility/` runs without parse errors or import-resolution errors.
    - Output reports "pending" or "todo" counts equal to the sum of todos listed in this task's action.
    - No file contains a static `import` of any `@/lib/form-builder/visibility/...` module (per `tests/form-builder/attributes.test.ts` convention).
  </acceptance_criteria>
  <done>10 Wave-0 pure-logic test files exist as parseable stubs with todo blocks named after the cases in 15-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create renderer + progress Wave-0 stubs (2 files)</name>
  <files>
    tests/form-builder/progress-with-visibility.test.ts,
    tests/form-interpreter/visibility-renderer.test.tsx
  </files>
  <read_first>
    tests/form-builder/progress.test.ts (existing — copy the test-harness import shape),
    tests/form-interpreter/renderers.test.tsx (existing — copy the JSDOM render shape; this is the Phase 14-06 focus-retention test bed),
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Pattern 8 (lines 730-757) for progress signature,
    .planning/phases/15-conditional-logic-engine/15-PATTERNS.md §"propsRef for per-render values" shared pattern
  </read_first>
  <action>
    Create both stubs with the same `import { describe, it, expect } from "vitest"` header + `it.todo` shape as Task 1. Per-file todo lists:
    - progress-with-visibility.test.ts: 3 todos — `computeFormProgress` with visibility=undefined matches pre-Phase-15 behaviour (backward-compat); hidden entity excluded from denominator; dynamic-required (rule-fired) counted in denominator only when visible.
    - visibility-renderer.test.tsx: 4 todos — entity returning `shouldBeProcessed=false` is NOT in DOM after a setEntityValue triggers hide rule; focus retained on neighbouring text input across hide/show flip (the Phase 14-06 invariant); Select field stays controlled when its source flips between defined/undefined; dynamicRequired prop drives the asterisk and submit-block UX. Mark the .tsx file with a top-of-file comment "Phase 14-06 focus-loss invariant — these tests guard `[surface]` useMemo deps".
  </action>
  <verify>
    <automated>npx vitest run tests/form-builder/progress-with-visibility.test.ts tests/form-interpreter/visibility-renderer.test.tsx 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at listed paths.
    - Vitest parses each file (no "SyntaxError" / "Cannot find module" / "Error during collection").
    - Pending/todo count matches: progress = 3, renderer = 4.
    - The renderer file uses `.tsx` extension (TSX/JSX collector picks it up).
  </acceptance_criteria>
  <done>Renderer + progress Wave-0 stubs exist as parseable .ts/.tsx files with the named todos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship real `lib/form-builder/visibility/should-be-processed.ts` (cross-cutting helper)</name>
  <files>
    lib/form-builder/visibility/should-be-processed.ts
  </files>
  <read_first>
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Pattern 1 lines 283-353 (the canonical hook body),
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Open Questions (RESOLVED) item #1 (the A3 spike outcome: root-only `entitiesValues`, instance children NOT coltorapps entities — implication: hook does NOT need a per-instance branch because coltorapps never invokes it for instance template children),
    .planning/phases/15-conditional-logic-engine/15-PATTERNS.md §"lib/form-builder/visibility/should-be-processed.ts",
    node_modules/@coltorapps/builder/dist/index.d.ts lines 223-247 (the `EntityContext` shape — confirms `{entity, entitiesValues}` is the hook's context object)
  </read_first>
  <behavior>
    - Export `makeShouldBeProcessed(): (context: { entity, entitiesValues }) => boolean`.
    - Inside the returned closure, read `context.entity.attributes.visibilityRules` and default to `{ rules: [], logic: "and" }` if undefined (defence-in-depth; the attribute factory in plan 15-01 also default-coerces).
    - If `rules.length === 0` → return `true`.
    - Filter rules to show/hide only (action !== "require"). If filtered list is empty → return `true`.
    - For each show/hide rule, evaluate `evaluateRule(r.operator, context.entitiesValues[r.sourceEntityId], r.value)` and combine with `combineShowHide(...)` honouring `logic` and the D-07 hide-wins-over-show rule.
    - NEVER throws (per Pitfall 3 — orphan-source returns false at the evaluator layer).
    - NEVER reads the schema. The hook is hot-path and is bounded by the rule count on this single entity.
    - **Per-instance bridging is NOT this hook's concern** — per the resolved A3 spike, coltorapps never invokes `shouldBeProcessed` for repeatingSection template children (they aren't coltorapps entities). Plan 15-04's `RepeatingSectionRenderer` modification owns per-instance visibility via `evaluateVisibilityForInstance`.
    - **Dependency on 15-02:** this file imports `evaluateRule` from `./evaluate-rule` and `combineShowHide` from `./combine-rules`. Plan 15-02 ships those modules in Wave 1. To break the cycle, this file uses **dynamic `import()` inside the closure body** so the hook can be defined at Wave 0 without the Wave-1 modules existing yet: the actual evaluation only fires once an interpreter store is constructed (which only happens at runtime, after Wave 1 has shipped). For Wave-0 self-tests, an `it.todo` smoke entry inside the existing `cascade-visibility.test.ts` stub is sufficient — the real assertions land in plan 15-02 Task 2.
    - Alternative compile-only approach if dynamic import is undesirable: ship a placeholder module body that exports the `makeShouldBeProcessed` symbol but throws a `"Wave-1 not yet shipped"` error if called before 15-02 lands. Plan 15-02 Task 2 then overwrites with the real evaluator wiring. **Choose whichever option keeps `npx tsc --noEmit` green when only 15-00 has shipped — document the choice in 15-00-SUMMARY.md.**
  </behavior>
  <action>
    Create `lib/form-builder/visibility/should-be-processed.ts` exporting `makeShouldBeProcessed()` per the behavior block. Use the verbatim baseline from RESEARCH §Pattern 1 lines 321-346 with the following adjustments: (a) inline-comment the resolved A3 outcome (root-only entitiesValues per `repeating-section-renderer.tsx` lines 22-25); (b) wrap the `evaluateRule` / `combineShowHide` calls in either dynamic `await import()` inside an internal helper OR ship a placeholder that throws until 15-02 overwrites. Verify the file compiles cleanly when only Wave 0 modules exist — `npx tsc --noEmit` must report zero errors traceable to this file. Plans 15-01 (entity attach) and 15-02 (engine logic) MUST NOT include this path in their `files_modified`.
  </action>
  <verify>
    <automated>test -f lib/form-builder/visibility/should-be-processed.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep "should-be-processed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/form-builder/visibility/should-be-processed.ts` exists at the listed path.
    - File exports `makeShouldBeProcessed` (verified by a `grep "export.*makeShouldBeProcessed"` returning ≥1 match).
    - `npx tsc --noEmit` reports zero NEW errors traceable to this file (Wave-1 evaluate-rule.ts / combine-rules.ts not required to exist yet — the chosen strategy keeps tsc green).
    - File header comment documents the resolved A3 spike outcome ("root-only entitiesValues; instance children NOT coltorapps entities") and references `components/form-interpreter/repeating-section-renderer.tsx` lines 22-25.
    - File contains the hot-path-cheap invariant note: "iterates only this entity's own visibilityRules; never walks the schema".
  </acceptance_criteria>
  <done>Cross-cutting shared hook body ships in Wave 0 so plans 15-01 and 15-02 can run in parallel without a `files_modified` ownership race. The Wave-1 self-test assertions are owned by plan 15-02 Task 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest runner ↔ filesystem | No untrusted input; test files are project source. No boundary risk in this plan. |
| coltorapps interpreter → makeShouldBeProcessed hook | The hook ships in Wave 0 but is not invoked until the interpreter store is constructed at runtime (after Wave 1 lands). No execution-time exposure from this plan alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-00-01 | Tampering | Test stubs | accept | Stubs ship no production logic; Wave 1+ tasks add real assertions and are gated by code review per project process. |
| T-15-00-02 | Repudiation | Test artefacts | accept | Test files are committed via git with author trail; no auth surface. |
| T-15-00-03 | Denial of Service | makeShouldBeProcessed hot path | mitigate | Hook iterates only the host entity's own `visibilityRules.rules` array (bounded by per-entity rule count). Never recurses into schema. Wave-1 cycle detector (plan 15-03) prevents construction of pathological schemas before they reach the interpreter. |

(Wave 0 is primarily test-infrastructure — the only production code is `should-be-processed.ts`, which has no I/O surface and cannot be invoked until the interpreter store is constructed at runtime. The substantive STRIDE register for Phase 15 lives in Waves 1–4.)
</threat_model>

<verification>
- All 12 Wave-0 test files exist.
- `lib/form-builder/visibility/should-be-processed.ts` exists with `makeShouldBeProcessed` exported.
- `npx vitest run tests/form-builder/visibility/ tests/form-builder/progress-with-visibility.test.ts tests/form-interpreter/visibility-renderer.test.tsx` runs without parse errors.
- Pending/todo count totals match the sums in each task's action.
- `npx tsc --noEmit` reports zero new errors traceable to `lib/form-builder/visibility/should-be-processed.ts`.
</verification>

<success_criteria>
Every later wave's `<automated>` command in its `<verify>` blocks references one of the 12 test files created here. Vitest collects all 12 without error. Plans 15-01 and 15-02 can import `makeShouldBeProcessed` from `lib/form-builder/visibility/should-be-processed.ts` without either claiming ownership in their `files_modified`.
</success_criteria>

<output>
After completion, create `.planning/phases/15-conditional-logic-engine/15-00-SUMMARY.md` listing the 12 test files created, total todo counts per file, the `npx vitest run` output snapshot, AND the chosen strategy for `should-be-processed.ts` (dynamic-import inside the closure vs placeholder-throw vs other) so plan 15-02 Task 2 knows what it inherits.
</output>
