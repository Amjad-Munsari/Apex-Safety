# Phase 2: Form Prerequisites - Discussion Log

**Date:** 2026-04-29
**Mode:** `--auto` (Autonomous selection)

## Areas Resolved

### 1. STT Integration — Native vs Cloud
- **Question:** Should we use browser native Web Speech API or OpenAI Whisper via OpenRouter?
- **Options Presented:** 
  1. OpenAI Whisper via OpenRouter (Recommended)
  2. Native Web Speech API
- **Selection:** [auto] **OpenAI Whisper via OpenRouter**
- **Reasoning:** Ensures high-fidelity transcription and consistent cross-device behavior (especially on iPad Safari standalone), which is critical for the "Matt narrates" core value.

### 2. Processing Pipeline — Client vs Server
- **Question:** Should HEIC-to-JPEG conversion and compression happen in the browser or on the server?
- **Options Presented:**
  1. Client-side (Browser) processing (Recommended)
  2. Server-side processing
- **Selection:** [auto] **Client-side (Browser) processing**
- **Reasoning:** Saves user bandwidth (uploading compressed JPEGs vs raw HEIC) and utilizes client-side hardware for rotation and conversion.

### 3. Draft Persistence — Local vs Cloud Sync
- **Question:** Should drafts be purely browser-local or periodically synced to Supabase?
- **Options Presented:**
  1. Cloud Sync to Supabase (Recommended)
  2. Local Storage only
- **Selection:** [auto] **Cloud Sync to Supabase**
- **Reasoning:** Provides multi-device continuity (iPad to Phone) and more robust recovery than Local Storage alone.

### 4. Renderer Architecture — Schema-driven vs Hardcoded
- **Question:** Should the form renderer be schema-driven or hardcoded?
- **Options Presented:**
  1. JSON Schema-driven (Recommended)
  2. Hardcoded fields
- **Selection:** [auto] **JSON Schema-driven**
- **Reasoning:** Future-proofs the system for the Phase 3 Template System and establishes a clean data contract early.

## Deferred Ideas
- Offline-first IndexedDB sync (Deferred to v2).
- Dynamic form builder UI (Deferred to Phase 3).

---
*Generated by GSD Discuss-Phase (--auto)*
