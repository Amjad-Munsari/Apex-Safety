---
phase: 16-multi-tenancy-fork-on-fill
plan: 03
plan_id: 16-03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - app/admin/assignments/page.tsx
  - app/admin/clients/[id]/page.tsx
  - app/admin/clients/[id]/client-tabs.tsx
  - app/admin/clients/page.tsx
autonomous: true
requirements:
  - BUILDER-01
tags: [admin, queue, tabs, rsc, multi-tenancy]
must_haves:
  truths:
    - "Admin can view a cross-client assignments queue at /admin/assignments sortable by due_date and filterable via searchParams (D-12 global queue)."
    - "Admin clients/[id] page shows an Assigned Forms tab listing that client's active assignments (D-12 per-client tab)."
    - "Admin clients list shows an active-assignment counter pill next to each client name when count > 0; hidden when count === 0 (UI-SPEC Route A)."
    - "All reads filter `.is(\"deleted_at\", null)` and never use `.delete(\"deleted_at\")` (D-10, T-16-08 mitigation)."
    - "/admin/clients/[id] tab mounts <AssignTemplateModal preselectClientId={id}/> for client-first assignment entry (D-01)."
  artifacts:
    - path: app/admin/assignments/page.tsx
      provides: "Cross-client queue RSC page with searchParams filters (status, client, template)"
      contains: "export default async function"
    - path: app/admin/clients/[id]/client-tabs.tsx
      provides: "Existing tabs component extended with new TabsTrigger value='assignments' + TabsContent rendering assignment rows + Revoke button"
      contains: "value=\"assignments\""
    - path: app/admin/clients/page.tsx
      provides: "Clients list with active-assignment counter pill"
      contains: "form_assignments"
  key_links:
    - from: app/admin/assignments/page.tsx
      to: form_assignments table (adminClient)
      via: "select with join to clients + form_templates, .is(deleted_at, null)"
      pattern: "is\\(\\\"deleted_at\\\", null\\)"
    - from: app/admin/clients/[id]/page.tsx
      to: form_assignments table
      via: ".select(...).eq('client_id', id).is('deleted_at', null)"
      pattern: "client_id"
    - from: app/admin/clients/[id]/client-tabs.tsx
      to: components/admin/assign-template-modal.tsx
      via: "<AssignTemplateModal preselectClientId={clientId} ... />"
      pattern: "preselectClientId"
---

<objective>
Build the admin-side viewing surfaces for assignments (D-12). Two render targets:

1. `/admin/assignments` — new top-level cross-client queue (RSC, searchParams filters, table layout) — UI-SPEC Route A.
2. `/admin/clients/[id]` — new "Assigned Forms" tab in the existing client detail tabs — UI-SPEC Route B. Also mounts `<AssignTemplateModal preselectClientId={id} clients={[...]} />` so admins can assign without leaving the client view.
3. `/admin/clients` list — active-assignment counter pill per client row — UI-SPEC Route A.

Purpose: Matt needs both per-client context ("what does this client need next?") and a global "who's overdue this week" view (D-12). All reads must respect soft-delete (`deleted_at IS NULL`) to mitigate T-16-08.

Output: Three pure-RSC reads (queue page, client-detail-page fetch extension, client-list counter aggregation) + one tabs extension.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md
@.planning/phases/16-02-PLAN.md

<interfaces>
From app/admin/assignments/actions.ts (created in Plan 02):
- export async function createAssignments(templateId: string, clientIds: string[], opts?: { dueDate?: string; instructions?: string }): Promise<{ created: number }>
- export async function revokeAssignment(assignmentId: string): Promise<void>

From components/admin/assign-template-modal.tsx (created in Plan 02):
- export function AssignTemplateModal(props: { templateId?: string; preselectClientId?: string; templates?: Array<{ id: string; name: string }>; clients: Array<{ id: string; name: string }>; triggerLabel?: string }): JSX.Element

From lib/supabase/admin.ts:
- export const adminClient: SupabaseClient  // service-role; used in admin RSCs

Table joins available via PostgREST (already used elsewhere in this codebase):
- form_assignments.client → clients(id, name) via FK form_assignments_client_id_fkey
- form_assignments.template → form_templates(id, name) via FK form_assignments_template_id_fkey

Status pill colour contract (UI-SPEC § Status pill spec — applies on both surfaces):
- pending: text-[#666] bg-[#555]/10
- in_progress: text-[#c0a66d] bg-[#c0a66d]/10
- completed: text-[#3b8273] bg-[#3b8273]/10

Existing analog files to read for patterns:
- app/admin/proposals/page.tsx (queue page header + dynamic flag + adminClient read)
- app/admin/clients/page.tsx (table layout: thead bg-[#151515], th text-[10px] font-mono tracking-widest uppercase text-[#555], tr hover:bg-white/[0.02])
- app/admin/clients/[id]/page.tsx (existing tabs fetch + ClientTabs mount pattern)
- app/admin/clients/[id]/client-tabs.tsx (existing tabs structure with TabsTrigger lines 148-165, TabsContent layout lines 168-218, inline status pill ternary lines 284-296)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build /admin/assignments queue page (RSC + searchParams filters)</name>
  <read_first>
    - app/admin/proposals/page.tsx (lines 1-130 — full file: queue header pattern, mono index `06 SALES PIPELINE` → reuse pattern with new index, adminClient read, force-dynamic export)
    - app/admin/clients/page.tsx (lines 52-134 — table primitive: thead, tr hover, mono header style)
    - app/admin/assignments/actions.ts (created in Plan 02 — Revoke action lives here; the queue table will surface a Revoke trigger)
    - lib/supabase/admin.ts (adminClient shape)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route A — /admin/assignments" + § "Status pill spec" + § "Copywriting Contract"
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "app/admin/assignments/page.tsx (route, RSC queue with searchParams)" [File Classification row 1: app/admin/assignments/page.tsx]
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pattern 5: /admin/assignments Queue" lines 593-630
  </read_first>
  <behavior>
    - File app/admin/assignments/page.tsx is a Next.js 16 async server component, `export const dynamic = "force-dynamic"`.
    - Signature: `export default async function AssignmentsQueuePage({ searchParams }: { searchParams: Promise<{ status?: string; client?: string; template?: string }> })`.
    - `await searchParams` unpacking (Next 16 codemod requirement).
    - Reads via adminClient: `from("form_assignments").select("id, status, due_date, instructions, created_at, client:clients(id, name), template:form_templates(id, name)").is("deleted_at", null).order("due_date", { ascending: true, nullsFirst: false })`.
    - Conditionally appends `.eq("status", status)`, `.eq("client_id", client)`, `.eq("template_id", template)` when filter searchParams are present.
    - Page header matches `/admin/proposals` shape: back arrow Link "Back to Dashboard", mono `XX ASSIGNMENT QUEUE` index (pick a fresh number — `07` works; coordinate with existing admin/clients = 03, admin/templates = 04 etc., or use a label without a number if numbering conflicts), serif h2 "Active Assignments", body subhead "All assignments across clients. Sort by due date; filter by status, client, or template.".
    - Filter bar: three `<Select>` dropdowns OR three `<Link>`-wrapped pill groups (since this is a server component, prefer Link-based filters to preserve no-client-state contract). Each filter group includes an "All" reset link. URL shape: `/admin/assignments?status=pending&client=...&template=...`.
    - Table columns per UI-SPEC Route A: Template | Client | Due Date | Status | Instructions (truncated 60 chars or `line-clamp-2`) | Actions. Status pill colour per UI-SPEC. Due-date colour coding: text-[#e55a3a] when overdue (date < today), text-gold within 3 days, text-[#666] otherwise.
    - Empty state when no assignments match: serif h3 + body "No assignments matching the current filters." with a "Clear filters" Link back to `/admin/assignments`.
    - Per-row "Revoke" action: a `<RevokeAssignmentButton assignmentId={row.id} />` client island (NEW small component — write inline in this file's directory). The client component wraps `<AlertDialog>` with title "Revoke this assignment?", body "The client will no longer see this form in their Assigned Forms list.", cancel "Keep assignment", confirm "Revoke assignment" (destructive variant). On confirm: calls `revokeAssignment(assignmentId)` inside startTransition → toast.success "Assignment revoked" → router.refresh() OR rely on the server action's revalidatePath.
  </behavior>
  <files>
    - app/admin/assignments/page.tsx (NEW)
    - app/admin/assignments/revoke-assignment-button.tsx (NEW — small client component sibling)
  </files>
  <action>
    Author app/admin/assignments/page.tsx as a server component. Top-level imports: `import { adminClient } from "@/lib/supabase/admin";`, `import Link from "next/link";`, `import { ArrowLeft } from "lucide-react";`, `import { RevokeAssignmentButton } from "./revoke-assignment-button";`. Export `const dynamic = "force-dynamic"`.

    In the page function, await searchParams, build the query per the `<behavior>` block (adminClient.from("form_assignments")...). Conditionally apply `.eq(...)` only when the filter param is a non-empty string.

    Render: outer container matching `/admin/proposals` layout — back link + mono index + serif h2 + subhead at top, then the filter bar (a row of three `<Link>` groups OR three small filter components — pick the Link approach to keep the page pure RSC). Then a `<Card>` (or just a bordered div following `/admin/clients/page.tsx` table styling) wrapping a `<table>` with columns matching the spec.

    For each row, render the status pill inline (ternary on `row.status`), the due-date with the colour-coding ternary (overdue/3-day/default), instructions truncated via `line-clamp-2 max-w-xs`, and `<RevokeAssignmentButton assignmentId={row.id} />` in the Actions column.

    Empty state inside the table: `{!assignments || assignments.length === 0 && <tr><td colSpan={6} className="text-center py-12 text-[#666]">No assignments matching the current filters. <Link href="/admin/assignments" className="underline">Clear filters</Link></td></tr>}`.

    Create app/admin/assignments/revoke-assignment-button.tsx as `"use client"`. Imports: useState, useTransition, AlertDialog primitives from `@/components/ui/alert-dialog`, Button from `@/components/ui/button`, `toast` from "sonner", `revokeAssignment` from `./actions`, `useRouter` from `next/navigation`. Render an outline Button "Revoke" that triggers an AlertDialog with the locked copy. On confirm: startTransition → try { await revokeAssignment(assignmentId); toast.success("Assignment revoked"); router.refresh(); } catch { toast.error("Revoke failed — please try again"); }.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('app/admin/assignments/page.tsx','utf8');for (const s of ['force-dynamic','await searchParams','from(\"form_assignments\")','is(\"deleted_at\", null)','client:clients(','template:form_templates(','RevokeAssignmentButton']) {if(!p.includes(s)){console.error('Page missing:',s);process.exit(1);}} const r=fs.readFileSync('app/admin/assignments/revoke-assignment-button.tsx','utf8');for (const s of ['use client','revokeAssignment','AlertDialog','Revoke this assignment?','Revoke assignment']) {if(!r.includes(s)){console.error('Button missing:',s);process.exit(2);}} console.log('OK')" && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File app/admin/assignments/page.tsx exists.
    - Page contains `export const dynamic = "force-dynamic"`.
    - Page contains `await searchParams`.
    - Page contains `is("deleted_at", null)` (T-16-08 mitigation).
    - Page contains joins `client:clients(` and `template:form_templates(`.
    - Page contains the status-pill colour literals: `#666`, `#c0a66d`, `#3b8273`.
    - File app/admin/assignments/revoke-assignment-button.tsx exists with `"use client"`.
    - Button file contains literal `Revoke this assignment?` (AlertDialog title) and `Revoke assignment` (confirm copy).
    - Button file contains `useTransition`.
    - `npm run build` completes successfully and creates the route `/admin/assignments`.
  </acceptance_criteria>
  <done>
    Visiting `/admin/assignments` in dev shows the cross-client queue with the seeded smoke assignments (from Phase 14/15 or new Plan 02 inserts). Filters work via URL params. Revoke button soft-deletes via the existing server action.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add "Assigned Forms" tab on /admin/clients/[id] + assign-from-tab entry point</name>
  <read_first>
    - app/admin/clients/[id]/page.tsx (full file — read all fetches around lines 27-126 to understand how documents/proposals/hours/submissions are loaded and passed to ClientTabs)
    - app/admin/clients/[id]/client-tabs.tsx (lines 148-218 — TabsList + TabsTrigger + TabsContent structure; lines 284-296 — inline status pill ternary)
    - app/admin/assignments/actions.ts (revokeAssignment — re-used here for the per-row Revoke in the tab)
    - components/admin/assign-template-modal.tsx (created in Plan 02 — the tab header mounts this with preselectClientId)
    - lib/supabase/admin.ts
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route B — /admin/clients/[id] — Assigned Forms tab"
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "Modify app/admin/clients/[id]/page.tsx + client-tabs.tsx" [File Classification rows 18+19: MODIFY app/admin/clients/[id]/page.tsx + client-tabs.tsx]
  </read_first>
  <behavior>
    - /admin/clients/[id]/page.tsx adds an assignments fetch alongside existing fetches: `adminClient.from("form_assignments").select("id, status, due_date, instructions, created_at, template:form_templates(id, name)").eq("client_id", id).is("deleted_at", null).order("created_at", { ascending: false })`.
    - The fetch result is passed to `<ClientTabs ... assignments={assignmentRows ?? []} clientId={id} clientName={...} />`.
    - client-tabs.tsx receives `assignments` + `clientId` + `clientName` props (extend existing Props interface non-breakingly — add optional fields).
    - A new `<TabsTrigger value="assignments">Assigned Forms{count > 0 ? ` (${count})` : ''}</TabsTrigger>` slots after the existing tabs (count = active count, i.e. `assignments.filter(a => a.status !== 'completed').length`).
    - A new `<TabsContent value="assignments">` renders a flat list of assignment rows (matching the Documents tab visual density). Each row: template name (serif), due date + status pill (mono), instructions truncated, Revoke action.
    - The tab header (above the row list) right-aligns an "Assign template" outline button that opens `<AssignTemplateModal preselectClientId={clientId} clients={[]} templates={publishedTemplates} triggerLabel="Assign template" />`. Since the tab does not have templates pre-fetched, either: (a) fetch published templates inside page.tsx and pass them in via props, OR (b) the tab itself can mount a small client component that lazy-loads templates on open. Pick (a) — simpler and consistent with the rest of the page's RSC pattern.
    - The page.tsx fetches published templates separately: `adminClient.from("form_templates").select("id, name").eq("is_published", true).is("deleted_at", null).order("name")` and passes them as `publishedTemplates` to ClientTabs.
    - The per-row Revoke uses the same RevokeAssignmentButton built in Task 1 (or a sibling component in this directory if cross-route imports feel weird — prefer importing from `app/admin/assignments/revoke-assignment-button` to avoid duplication).
  </behavior>
  <files>
    - app/admin/clients/[id]/page.tsx (MODIFY)
    - app/admin/clients/[id]/client-tabs.tsx (MODIFY)
  </files>
  <action>
    Read app/admin/clients/[id]/page.tsx end-to-end. Identify the section where existing fetches (documents, proposals, hours, submissions) are issued and how the ClientTabs component is mounted. Add two new fetches in that section:

    (a) Assignments fetch — `const { data: assignmentRows } = await adminClient.from("form_assignments").select("id, status, due_date, instructions, created_at, template:form_templates(id, name)").eq("client_id", id).is("deleted_at", null).order("created_at", { ascending: false });`.

    (b) Published templates fetch — `const { data: publishedTemplates } = await adminClient.from("form_templates").select("id, name").eq("is_published", true).is("deleted_at", null).order("name");`.

    Pass both into the existing ClientTabs mount: `<ClientTabs ... assignments={assignmentRows ?? []} publishedTemplates={publishedTemplates ?? []} clientId={id} />` (clientId may already be passed; if so, do not duplicate).

    Read app/admin/clients/[id]/client-tabs.tsx end-to-end. Add to its Props interface: `assignments?: Array<{ id: string; status: string; due_date: string | null; instructions: string | null; created_at: string; template: { id: string; name: string } | null }>;` and `publishedTemplates?: Array<{ id: string; name: string }>;` and `clientId?: string;`. Default each to `[]` / `""` if not provided so existing call-sites do not break.

    Inside the JSX, add a new `<TabsTrigger value="assignments">` alongside the existing triggers. Label is dynamic per `<behavior>`: `Assigned Forms{activeCount > 0 ? ` (${activeCount})` : ''}`.

    Add a new `<TabsContent value="assignments">`. Inside it: a header row with `<h3>Assigned forms</h3>` (or matching the existing tab heading pattern) on the left and `<AssignTemplateModal preselectClientId={clientId} clients={[]} templates={publishedTemplates} triggerLabel="Assign template" />` on the right.

    Below the header: a flat list of assignment rows. For each row, render template name (serif `text-base font-medium`), a mono metadata line showing `DUE · {formatDate(due_date) || "no due date"}` + the status pill (inline ternary using the UI-SPEC colours), a clamped instructions line if present (`<p className="text-sm text-white/60 line-clamp-2">{instructions}</p>` only when instructions truthy), and `<RevokeAssignmentButton assignmentId={row.id} />` on the right.

    Empty state: `{(assignments?.length ?? 0) === 0 && (<div className="text-center py-12 text-[#666]"><p className="font-serif text-lg">No forms assigned to this client yet.</p><p className="text-sm mt-1">Use "Assign template" above to assign a published template.</p></div>)}`.

    Import the modal: `import { AssignTemplateModal } from "@/components/admin/assign-template-modal";`. Import the revoke button: `import { RevokeAssignmentButton } from "@/app/admin/assignments/revoke-assignment-button";`.

    Do NOT modify any other existing tab logic. Diff is additive.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('app/admin/clients/[id]/page.tsx','utf8');for (const s of ['from(\"form_assignments\")','eq(\"client_id\", id)','assignmentRows','publishedTemplates','is_published']) {if(!p.includes(s)){console.error('Page missing:',s);process.exit(1);}} const t=fs.readFileSync('app/admin/clients/[id]/client-tabs.tsx','utf8');for (const s of ['value=\"assignments\"','AssignTemplateModal','preselectClientId','Assigned Forms','RevokeAssignmentButton']) {if(!t.includes(s)){console.error('Tabs missing:',s);process.exit(2);}} console.log('OK')" && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - app/admin/clients/[id]/page.tsx contains `from("form_assignments")` and `eq("client_id", id)` and `is("deleted_at", null)`.
    - app/admin/clients/[id]/page.tsx contains `is_published` (the published-templates fetch).
    - app/admin/clients/[id]/client-tabs.tsx contains literal `value="assignments"` (the new TabsTrigger value).
    - client-tabs.tsx contains literal `Assigned Forms` (UI-SPEC Route B tab label).
    - client-tabs.tsx contains `AssignTemplateModal` and `preselectClientId`.
    - client-tabs.tsx contains `RevokeAssignmentButton`.
    - `npm run build` succeeds with the modified files.
  </acceptance_criteria>
  <done>
    Visiting `/admin/clients/[id]` shows a new "Assigned Forms (N)" tab. Clicking it shows the client's active assignments, allows revoking, and exposes an "Assign template" button that opens the modal pre-selected for this client.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Active-assignment counter pill on /admin/clients list</name>
  <read_first>
    - app/admin/clients/page.tsx (full file — read the existing clients fetch + table render; the counter pill is added as an extra column or inline-next-to-name per UI-SPEC Route A)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route A — counter pill" (`Counter pill on /admin/clients list` paragraph)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pitfall 7: Counter pill performance" — single GROUP BY query, build Map<clientId, count>, pass to row
  </read_first>
  <behavior>
    - Single aggregate query (no N+1): `adminClient.from("form_assignments").select("client_id").in("status", ["pending", "in_progress"]).is("deleted_at", null)` — then group in JS to build `Map<clientId, count>`. (PostgREST GROUP BY is awkward; the codebase precedent is fetch-then-group for ~7-8 clients per Pitfall 7.)
    - The counter pill renders inline next to the client name in the existing clients-list row, using mono `text-[9px] uppercase tracking-[0.25em]` styling with `text-[#c0a66d] bg-[#c0a66d]/10` colours and small `px-1.5 py-0.5 rounded-sm` padding.
    - When count === 0, the pill is NOT rendered (the absence is the affordance per UI-SPEC).
    - On hover, a tooltip shows `{N} active assignment{s}` (use shadcn `Tooltip` per the inventory; matches UI-SPEC § Interaction States > Overdue line).
    - No other behavior on the page changes.
  </behavior>
  <files>
    - app/admin/clients/page.tsx (MODIFY)
  </files>
  <action>
    Read app/admin/clients/page.tsx end-to-end. Identify the existing clients fetch and the row-render loop.

    Add an additional aggregate fetch before the render: `const { data: activeRows } = await adminClient.from("form_assignments").select("client_id").in("status", ["pending", "in_progress"]).is("deleted_at", null);`. Build the count map: `const activeCountByClient = new Map<string, number>(); for (const r of activeRows ?? []) activeCountByClient.set(r.client_id, (activeCountByClient.get(r.client_id) ?? 0) + 1);`.

    In the row-render loop, just after the client name text, conditionally render:
    `{(activeCountByClient.get(client.id) ?? 0) > 0 && (<Tooltip><TooltipTrigger asChild><span className="ml-2 inline-flex items-center px-1.5 py-0.5 rounded-sm font-mono text-[9px] uppercase tracking-[0.25em] text-[#c0a66d] bg-[#c0a66d]/10">{activeCountByClient.get(client.id)}</span></TooltipTrigger><TooltipContent>{activeCountByClient.get(client.id)} active assignment{(activeCountByClient.get(client.id) ?? 0) === 1 ? '' : 's'}</TooltipContent></Tooltip>)}`.

    Import the Tooltip primitives from `@/components/ui/tooltip` if they are not already imported. If the page is a server component and Tooltip requires "use client", wrap only the pill in a small client component sibling (e.g. `./_components/active-pill.tsx`) to avoid converting the entire page.

    No other changes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('app/admin/clients/page.tsx','utf8');for (const s of ['from(\"form_assignments\")','in(\"status\", [\"pending\", \"in_progress\"])','is(\"deleted_at\", null)','activeCountByClient']) {if(!c.includes(s)){console.error('Missing:',s);process.exit(1);}} if(c.includes('count: 0')){console.error('Should not render zero-count pill');process.exit(2);} console.log('OK')" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/admin/clients/page.tsx contains `from("form_assignments")`.
    - app/admin/clients/page.tsx contains `in("status", ["pending", "in_progress"])`.
    - app/admin/clients/page.tsx contains `is("deleted_at", null)`.
    - app/admin/clients/page.tsx contains identifier `activeCountByClient`.
    - app/admin/clients/page.tsx renders the pill conditionally guarded by `> 0` (verifiable by grep for the pill's earth-amber colour `#c0a66d` AND the guard expression).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>
    The clients list shows the active-assignment count pill on every client with ≥ 1 active assignment. Clients with 0 active assignments show no pill (no "0" rendered).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → admin RSC | Untrusted; route gated by admin middleware/proxy already established in Phase 1 |
| RevokeAssignmentButton client → revokeAssignment server action | Same trust boundary as Plan 02 actions; `requireActorUserId('admin')` enforces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information Disclosure | Soft-deleted assignments leaking into admin queue or per-client tab | mitigate | Every fetch in this plan calls `.is("deleted_at", null)`. Verified by grep gates in each acceptance-criteria block. |
| T-16-01 | Information Disclosure | Cross-tenant data exposure on the queue page | mitigate | Admin RSC uses adminClient (service-role) which is the intended trust gate for admin routes (per the architectural responsibility map). The queue is admin-only by virtue of route placement under `app/admin/`, gated by the existing proxy.ts admin check. |
| T-16-07 | Tampering | Direct POST to revokeAssignment without admin claim | mitigate | revokeAssignment already enforces `requireActorUserId("admin")` in Plan 02 — confirmed gate. |
</threat_model>

<verification>
After all three tasks:
- `npm run build` succeeds.
- Manual smoke: (a) `/admin/assignments` renders with the assignments seeded in Plan 02 modal flow (or directly via Studio); (b) `/admin/clients/[id]` shows the new Assigned Forms tab populated for clients with assignments; (c) `/admin/clients` shows the counter pill on rows where the client has ≥ 1 active assignment.
</verification>

<success_criteria>
- /admin/assignments queue page exists, renders, filters via searchParams, shows status pills with correct colours.
- /admin/clients/[id] page has new Assigned Forms tab with Assign template button + per-row Revoke.
- /admin/clients list shows counter pill on clients with active assignments.
- All reads filter `deleted_at IS NULL`.
- No existing admin route is regressed.
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-tenancy-fork-on-fill/16-03-SUMMARY.md` recording: queue page route + filter shape, the ClientTabs Props extension, the counter-pill aggregation strategy (single GROUP BY in JS), and any deviations from the UI-SPEC visual contract.
</output>
