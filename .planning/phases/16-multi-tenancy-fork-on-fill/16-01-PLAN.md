---
phase: 16-multi-tenancy-fork-on-fill
plan: 01
plan_id: 16-01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/013_phase16_assignments_instructions.sql
  - supabase/migrations/014_phase16_customer_submissions.sql
  - vitest.config.ts
  - tests/rls/helpers/seed-two-tenants.ts
  - tests/rls/multi-tenancy.spec.ts
  - tests/form-builder/fork-assigned-template.test.ts
  - tests/form-builder/assignment-status-transitions.test.ts
  - tests/form-builder/customer-self-fill-submission.test.ts
  - tests/form-builder/assignments-query.test.ts
autonomous: true
requirements:
  - BUILDER-01
  - BUILDER-02
  - BUILDER-03
  - BUILDER-04
  - BUILDER-05
tags: [supabase, rls, vitest, multi-tenancy, schema-migration]
must_haves:
  truths:
    - "RLS test proves no cross-org data bleed on form_templates, template_versions, form_submissions, form_assignments (D-15)."
    - "form_assignments has nullable instructions TEXT column (D-04)."
    - "form_submissions.assignment_id is nullable to allow customer self-fill submissions (D-16)."
    - "Wave-0 test scaffolds exist (failing or skipping) for every behavior asserted by D-05, D-06, D-08, D-10, D-11, D-16."
  artifacts:
    - path: supabase/migrations/013_phase16_assignments_instructions.sql
      provides: "ALTER TABLE form_assignments ADD COLUMN instructions TEXT"
      contains: "ADD COLUMN IF NOT EXISTS instructions TEXT"
    - path: supabase/migrations/014_phase16_customer_submissions.sql
      provides: "ALTER TABLE form_submissions ALTER COLUMN assignment_id DROP NOT NULL + comment"
      contains: "ALTER COLUMN assignment_id DROP NOT NULL"
    - path: tests/rls/helpers/seed-two-tenants.ts
      provides: "Programmatic two-tenant fixture (two clients, two client_users, assignments, customer-owned templates, submissions); teardown"
      exports: ["seedTwoTenants", "teardown", "signedInClientFor", "SeedContext"]
    - path: tests/rls/multi-tenancy.spec.ts
      provides: "5-spec RLS isolation suite (4 negative + 1 positive control)"
      contains: "describe.skipIf"
    - path: vitest.config.ts
      provides: "include extended with tests/rls/**/*.{test,spec}.{ts,tsx}"
      contains: "tests/rls"
  key_links:
    - from: tests/rls/multi-tenancy.spec.ts
      to: tests/rls/helpers/seed-two-tenants.ts
      via: "import { seedTwoTenants, teardown, signedInClientFor } from './helpers/seed-two-tenants'"
      pattern: "from\\s+\\\"./helpers/seed-two-tenants\\\""
    - from: tests/rls/multi-tenancy.spec.ts
      to: "@supabase/supabase-js createClient (anon + signInWithPassword)"
      via: "RLS-aware anon client per signed-in user — NEVER service-role"
      pattern: "signInWithPassword"
---

<objective>
Establish the schema changes and test infrastructure that prove Phase 16's multi-tenancy contract before any UI/server-action code lands. This plan delivers:

1. Migration 013: `form_assignments.instructions TEXT NULL` (D-04).
2. Migration 014: `form_submissions.assignment_id` becomes nullable (D-16 / Claude's Discretion option (a)).
3. `vitest.config.ts` extended to discover `tests/rls/**`.
4. Two-tenant seed/teardown harness (`tests/rls/helpers/seed-two-tenants.ts`).
5. Cross-org RLS isolation spec (`tests/rls/multi-tenancy.spec.ts`) — 4 negative + 1 positive control covering `form_templates`, `template_versions`, `form_submissions`, `form_assignments`.
6. Four `tests/form-builder/` Wave-0 spec scaffolds for the behavior contracts later plans must satisfy (`fork-assigned-template`, `assignment-status-transitions`, `customer-self-fill-submission`, `assignments-query`).

Purpose: Schema + tests must land first so subsequent plans (02-07) execute against a verified contract. The schema push runs in Plan 07 (last wave) to avoid a partial-DB state if intervening waves discover schema gaps.

Output: Two new migrations (NOT yet pushed), one config edit, one helpers module, one RLS spec, four Wave-0 unit/integration spec scaffolds.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-VALIDATION.md

<interfaces>
Auth helpers (read-only references — used inside test mocks):

From lib/auth-helpers.ts:
- export async function getClientContext(): Promise<{ user_id: string; client_id: string; role: string } | null>
- export async function requireActorUserId(actorType: "admin" | "client"): Promise<string | null>

Supabase clients:
- import { createClient } from "@/lib/supabase/server"  — RLS-aware
- import { adminClient } from "@/lib/supabase/admin"    — service-role (server-only)
- import { createClient as createAnonClient } from "@supabase/supabase-js" — used in tests/rls only

Existing Vitest mock template (mirror exactly):
- tests/form-builder/save-draft.test.ts lines 19-66 — boilerplate for vi.mock of @/lib/supabase/server, @/lib/auth-helpers, next/cache, next/navigation

Existing RLS pattern source (Playwright → port to Vitest):
- tests/security.spec.ts lines 62-138 — seedTwoTenants, teardown, signedInClientFor (auth.admin.createUser + signInWithPassword)

Schema source of truth:
- supabase/migrations/001_initial_schema.sql lines 72-98 — form_assignments + form_submissions current shape
- supabase/migrations/003_form_template_customer_ownership.sql — polymorphic owner contract (owner_type='customer', owner_id=clients.id)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author migrations 013 + 014 and extend vitest.config.ts</name>
  <read_first>
    - supabase/migrations/001_initial_schema.sql (lines 72-98 — form_assignments + form_submissions current shape; line 86 has assignment_id UUID NOT NULL REFERENCES form_assignments(id))
    - supabase/migrations/002_phase7_draft_report.sql (analog: simplest ALTER TABLE ADD COLUMN migration in the project — file header style)
    - supabase/migrations/003_form_template_customer_ownership.sql (analog for DDL + RLS; CHECK pattern; comment style)
    - supabase/migrations/012_phase15_conditional_smoke_test.sql (most recent migration — confirms numbering 013 / 014 is correct)
    - vitest.config.ts (current include array at line 8; extend it without breaking existing patterns)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Code Examples" lines 736-769 — exact migration text guidance
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "supabase/migrations/013_phase16_assignments_instructions.sql" + "014_phase16_customer_submissions.sql"
  </read_first>
  <behavior>
    - Migration 013 file exists at supabase/migrations/013_phase16_assignments_instructions.sql, contains ALTER TABLE form_assignments ADD COLUMN IF NOT EXISTS instructions TEXT and a COMMENT ON COLUMN documenting D-04.
    - Migration 014 file exists at supabase/migrations/014_phase16_customer_submissions.sql, contains ALTER TABLE form_submissions ALTER COLUMN assignment_id DROP NOT NULL, a COMMENT ON COLUMN noting D-16 application-level invariant, and does NOT attempt a cross-table subquery CHECK (Postgres disallows it).
    - vitest.config.ts include array contains all three entries: tests/form-builder/**, tests/form-interpreter/**, tests/rls/**.
    - No code outside the migration files or vitest.config.ts is modified by this task.
  </behavior>
  <files>
    - supabase/migrations/013_phase16_assignments_instructions.sql (NEW)
    - supabase/migrations/014_phase16_customer_submissions.sql (NEW)
    - vitest.config.ts (MODIFY)
  </files>
  <action>
    Author migration 013 with a header comment citing Phase 16 D-04, then `ALTER TABLE public.form_assignments ADD COLUMN IF NOT EXISTS instructions TEXT;` and a `COMMENT ON COLUMN form_assignments.instructions IS '...'` explaining the optional free-text shown above the form (set at create-time or via edit per D-03; NULL = no instructions block).

    Author migration 014 with a header comment citing Phase 16 D-16. The body must execute `ALTER TABLE public.form_submissions ALTER COLUMN assignment_id DROP NOT NULL;` and add a `COMMENT ON COLUMN form_submissions.assignment_id IS '...'` documenting: NULL only when the submission is against a customer-owned template (owner_type=customer) with no preceding assignment; all admin-assigned and admin-on-site flows MUST set this; enforcement is application-level (Postgres CHECK cannot subquery cross-table). Do NOT add a TRIGGER. Do NOT add a CHECK that attempts to read another table.

    Edit vitest.config.ts (current `include` at line 8 contains two globs for tests/form-builder/** and tests/form-interpreter/**) — add the third entry `"tests/rls/**/*.{test,spec}.{ts,tsx}"` to the array. Preserve existing entries and ordering convention. Do not change `testTimeout` or any other config.

    Do NOT run `supabase db push` in this task — push is reserved for Plan 07. After the file writes, run `npm test -- --run --reporter=basic --passWithNoTests` to confirm Vitest can still bootstrap with the new include glob (no tests exist under tests/rls/ yet, so passWithNoTests is required).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m13=fs.readFileSync('supabase/migrations/013_phase16_assignments_instructions.sql','utf8');const m14=fs.readFileSync('supabase/migrations/014_phase16_customer_submissions.sql','utf8');const vc=fs.readFileSync('vitest.config.ts','utf8');if(!m13.includes('ADD COLUMN IF NOT EXISTS instructions TEXT'))process.exit(1);if(!m14.includes('ALTER COLUMN assignment_id DROP NOT NULL'))process.exit(2);if(!vc.includes('tests/rls'))process.exit(3);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/013_phase16_assignments_instructions.sql exists.
    - File supabase/migrations/013_phase16_assignments_instructions.sql contains string `ADD COLUMN IF NOT EXISTS instructions TEXT`.
    - File supabase/migrations/013_phase16_assignments_instructions.sql contains string `COMMENT ON COLUMN`.
    - File supabase/migrations/014_phase16_customer_submissions.sql exists.
    - File supabase/migrations/014_phase16_customer_submissions.sql contains string `ALTER COLUMN assignment_id DROP NOT NULL`.
    - File supabase/migrations/014_phase16_customer_submissions.sql does NOT contain `CREATE TRIGGER` (anti-pattern per RESEARCH).
    - vitest.config.ts contains string `tests/rls/**/*.{test,spec}.{ts,tsx}` inside the include array.
    - `npm test -- --run --reporter=basic --passWithNoTests` exits 0.
  </acceptance_criteria>
  <done>
    Migrations 013 and 014 exist as files (not yet pushed); vitest discovers tests/rls/** as a target; existing form-builder and form-interpreter Vitest specs still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port two-tenant seed helper from tests/security.spec.ts to tests/rls/helpers/seed-two-tenants.ts</name>
  <read_first>
    - tests/security.spec.ts (lines 7-22 — RLS audit comment; lines 62-138 — seedTwoTenants + teardown + signedInClientFor implementations; lines 141-144 — env-skip pattern)
    - lib/auth-helpers.ts (for the auth shape these tests must NOT rely on — they use createClient(URL, ANON_KEY) directly per RESEARCH Pitfall 4)
    - supabase/migrations/001_initial_schema.sql (lines 72-98 — table shapes the helper must seed against)
    - supabase/migrations/003_form_template_customer_ownership.sql (owner_type='customer' polymorphism the customer template seed rows must respect)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "tests/rls/helpers/seed-two-tenants.ts" + "tests/rls/multi-tenancy.spec.ts"
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pattern 2: Cross-Org RLS Test" + "Pitfall 4: RLS test harness doesn't actually exercise RLS"
    - tests/form-builder/save-draft.test.ts (no direct port, but confirms the project's Vitest 3 idioms — describe/it/expect, beforeAll/afterAll)
  </read_first>
  <behavior>
    - The helper exports: seedTwoTenants(), teardown(ctx), signedInClientFor(user), and types SeedContext / TestUser.
    - seedTwoTenants creates two clients rows, two auth users via auth.admin.createUser (email_confirm: true), two client_users rows linking each user to their client; for each tenant it ALSO seeds: one admin master form_templates row (owner_type='admin') + one published template_versions row, one form_assignments row (status='pending', client_id=tenant's clientId, pinned to the master version), one customer-owned form_templates row (owner_type='customer', owner_id=clients.id) + v1 published template_versions row, one form_submissions row (assignment_id=tenant's assignmentId, status='Draft').
    - signedInClientFor uses createClient(URL, ANON_KEY).auth.signInWithPassword — NEVER service-role.
    - teardown deletes every seeded row in reverse order via service-role + auth.admin.deleteUser.
    - All emails carry a Date.now() stamp so concurrent runs do not collide.
  </behavior>
  <files>
    - tests/rls/helpers/seed-two-tenants.ts (NEW)
  </files>
  <action>
    Create tests/rls/helpers/seed-two-tenants.ts as a TypeScript module. Read tests/security.spec.ts lines 62-138 first — the seedTwoTenants/teardown/signedInClientFor functions there are the structural template; port them to Vitest-compatible TypeScript (no Playwright `test` imports; use `@supabase/supabase-js` `createClient` + `SupabaseClient` types).

    Constants block at top: read SUPABASE_URL from NEXT_PUBLIC_SUPABASE_URL, ANON_KEY from NEXT_PUBLIC_SUPABASE_ANON_KEY, SERVICE_ROLE from SUPABASE_SERVICE_ROLE_KEY. Throw a descriptive error in seedTwoTenants if any are missing (the calling spec uses describe.skipIf to bypass when env is absent).

    Define `interface TestUser { email: string; password: string; authUserId: string; clientId: string; clientUserId: string; adminMasterTemplateId: string; adminMasterVersionId: string; assignmentId: string; customerTemplateId: string; customerTemplateVersionId: string; submissionId: string; }` and `interface SeedContext { userA: TestUser; userB: TestUser; }`.

    seedTwoTenants: instantiate an admin SupabaseClient via createClient(URL, SERVICE_ROLE). For each tenant (A and B): insert a `clients` row (name = `rls-test-${Date.now()}-${A|B}`), create auth user via `admin.auth.admin.createUser({ email, password, email_confirm: true })`, insert a `client_users` row (id = auth_user_id, client_id = clients.id, role = 'client'). Then for each tenant insert an admin master `form_templates` row (owner_type='admin', owner_id=auth_user_id of an admin actor — for test purposes reuse one of the seeded users as nominal admin for the master, OR insert a service-role admin_users entry; pick the approach that matches existing test pattern at tests/security.spec.ts line 96-105). Insert a published template_versions row (version_number=1, schema_json={ entities: {}, root: { children: [] } } as a minimal valid coltorapps schema, published_at=now). Insert a `form_assignments` row (client_id=tenant.clientId, template_id=master.id, template_version_id=master.versionId, assigned_by=auth_user_id, status='pending'). Insert a customer-owned form_templates row (owner_type='customer', owner_id=clients.id, name=`rls-customer-${A|B}`) + v1 template_versions (published). Insert a `form_submissions` row (assignment_id=tenant.assignmentId, template_version_id=master.versionId, client_id=tenant.clientId, status='Draft', answers_json={}).

    Return SeedContext { userA, userB } with every UUID populated.

    teardown(ctx): reverse-order delete via service-role — form_submissions, form_assignments, template_versions (customer-owned + admin master), form_templates (customer-owned + admin master), client_users, clients, then auth.admin.deleteUser for both auth users.

    signedInClientFor(user): instantiate createClient(URL, ANON_KEY, { auth: { persistSession: false, autoRefreshToken: false } }), call signInWithPassword({ email, password }), throw on error, return the client.

    No exports beyond seedTwoTenants, teardown, signedInClientFor, SeedContext, TestUser. No top-level side effects (no module-load env checks — let seedTwoTenants throw).
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/rls/helpers/seed-two-tenants.ts 2>&1 | tee /tmp/tsc-rls-helpers.log; node -e "const c=require('fs').readFileSync('tests/rls/helpers/seed-two-tenants.ts','utf8');for (const sym of ['seedTwoTenants','teardown','signedInClientFor','SeedContext','TestUser']) { if(!c.includes(sym)) { console.error('Missing export:',sym); process.exit(1); } } if(!c.includes('signInWithPassword'))process.exit(2); if(c.includes('adminClient'))process.exit(3); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File tests/rls/helpers/seed-two-tenants.ts exists.
    - File exports identifiers: `seedTwoTenants`, `teardown`, `signedInClientFor`, `SeedContext`, `TestUser` (verifiable via `grep -nE "^export" tests/rls/helpers/seed-two-tenants.ts`).
    - File contains `signInWithPassword`.
    - File contains `auth.admin.createUser`.
    - File does NOT import from `@/lib/supabase/admin` (the helper must use `createClient(URL, SERVICE_ROLE_KEY)` directly via @supabase/supabase-js, not the project's adminClient wrapper, so the test can be inspected in isolation).
    - File does NOT use `getClientContext` or `requireActorUserId` (Pitfall 4 — tests must use raw anon + signInWithPassword JWTs).
    - `npx tsc --noEmit tests/rls/helpers/seed-two-tenants.ts` reports no errors.
  </acceptance_criteria>
  <done>
    Two-tenant fixture is reusable from any spec under tests/rls/; seeds the four tables Phase 16 RLS test must cover; teardown is loud-on-error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write 5-spec multi-tenancy RLS isolation suite + 4 form-builder Wave-0 spec scaffolds</name>
  <read_first>
    - tests/rls/helpers/seed-two-tenants.ts (created in Task 2 — the helper this spec consumes)
    - tests/security.spec.ts (lines 156-192 — the four negative `expect(data).toEqual([])` shape + positive control)
    - tests/form-builder/save-draft.test.ts (lines 1-100 — Vitest mock skeleton: vi.mock for @/lib/supabase/server, @/lib/auth-helpers, next/cache, next/navigation)
    - tests/form-builder/version-pin.test.ts (spy-on-from query-shape assertion pattern)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "Test files under tests/form-builder/ (4 new specs)" + § "tests/rls/multi-tenancy.spec.ts"
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-VALIDATION.md § "Wave 0 Requirements" (definitive list of 5 spec files)
    - app/client/templates/actions.ts (lines 214-283 — forkOnFill prior art; the new fork-assigned-template spec asserts query shape similar to but DIFFERENT from this)
    - app/admin/assessments/actions.ts (lines 232-309 — submitAssessmentAction optimistic guard pattern to mirror in assignment-status-transitions.test.ts)
  </read_first>
  <behavior>
    - tests/rls/multi-tenancy.spec.ts: 5 specs inside one `describe.skipIf(!hasEnv)` block: (a) Client A cannot read Client B's form_templates owned by B's client_id; (b) Client A cannot read Client B's template_versions by id; (c) Client A cannot read Client B's form_submissions by client_id; (d) Client A cannot read Client B's form_assignments by client_id; (e) positive control — Client A CAN read at least one own-org assignment row (`.length >= 1`).
    - tests/form-builder/fork-assigned-template.test.ts: scaffold a describe block with a single skipped/todo test or a "scaffold only" test that asserts a placeholder — the real assertions land in Plan 05 when the implementation arrives. The scaffold MUST import the eventual function name (`forkAssignedTemplate` from `@/app/client/assignments/actions`) inside a `vi.mock` or wrapped in `it.todo` so the test file references the future production module path correctly.
    - tests/form-builder/assignment-status-transitions.test.ts: scaffold mirrors the same shape — describe block with `it.todo('pending → in_progress on first draft create or Fill-as-is click')` and `it.todo('in_progress → completed on submit')` and `it.todo('backwards transition completed → in_progress is rejected by optimistic .eq("status", previous)')`.
    - tests/form-builder/customer-self-fill-submission.test.ts: scaffold with `it.todo('customer-built fill INSERTs form_submissions with assignment_id: null')` and `it.todo('client_id is taken from server context, never client-supplied')`.
    - tests/form-builder/assignments-query.test.ts: scaffold with `it.todo('/client/assignments page query includes .is("deleted_at", null) and .eq("client_id", ctx.client_id)')`.
    - All four form-builder scaffolds use the same Vitest 3 idiom (`describe`, `it.todo`) so `npm test -- tests/form-builder/` reports the todo count without failing.
  </behavior>
  <files>
    - tests/rls/multi-tenancy.spec.ts (NEW)
    - tests/form-builder/fork-assigned-template.test.ts (NEW — scaffold only; full assertions land in Plan 05)
    - tests/form-builder/assignment-status-transitions.test.ts (NEW — scaffold only; full assertions land in Plan 04)
    - tests/form-builder/customer-self-fill-submission.test.ts (NEW — scaffold only; full assertions land in Plan 06)
    - tests/form-builder/assignments-query.test.ts (NEW — scaffold only; full assertions land in Plan 04)
  </files>
  <action>
    Create tests/rls/multi-tenancy.spec.ts. Top-level: `import { describe, it, expect, beforeAll, afterAll } from "vitest";` and `import { seedTwoTenants, teardown, signedInClientFor, type SeedContext } from "./helpers/seed-two-tenants";`. Define `const hasEnv = Boolean(process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY && process.env.SUPABASE_SERVICE_ROLE_KEY);`. Wrap all five specs in `describe.skipIf(!hasEnv)("RLS — cross-org isolation (Phase 16)", () => { ... })`. Use `let ctx: SeedContext;` and `beforeAll(async () => { ctx = await seedTwoTenants(); });` + `afterAll(async () => { if (ctx) await teardown(ctx); });`. The 5 specs follow the names listed in `<behavior>` above; each negative spec asserts `expect(data).toEqual([])` after a signed-in-as-A query that filters on B's IDs (form_templates by owner_id=B.clientId, template_versions by id=B.customerTemplateVersionId, form_submissions by client_id=B.clientId, form_assignments by client_id=B.clientId). The positive control queries form_assignments by client_id=A.clientId and asserts `expect(data?.length).toBeGreaterThan(0)`.

    Create the four tests/form-builder/ scaffolds. Each file begins with `import { describe, it } from "vitest";` and a single describe block containing 1-3 `it.todo` lines for the behaviors that Plans 04/05/06 will implement. Include a header comment in each scaffold: `// Wave-0 scaffold for Phase 16 D-{X} — full assertions land in Plan {NN}` so future executors know where the contract is filled in. Do NOT mock production modules in the scaffolds — `it.todo` does not require it.

    After the writes, run `npm test -- tests/rls/ tests/form-builder/ --reporter=basic` to confirm: (a) the RLS spec is collected and skipped when env vars are absent (Vitest reports the describe.skipIf), (b) the four scaffolds report their todos without failing the suite.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['tests/rls/multi-tenancy.spec.ts','tests/form-builder/fork-assigned-template.test.ts','tests/form-builder/assignment-status-transitions.test.ts','tests/form-builder/customer-self-fill-submission.test.ts','tests/form-builder/assignments-query.test.ts'];for (const f of files) { if(!fs.existsSync(f)) { console.error('Missing:',f); process.exit(1); } } const rls=fs.readFileSync('tests/rls/multi-tenancy.spec.ts','utf8');if(!rls.includes('describe.skipIf'))process.exit(2);if(!rls.includes('signedInClientFor'))process.exit(3);const exp=rls.match(/expect\\(data\\)\\.toEqual\\(\\[\\]\\)/g);if(!exp||exp.length<4){console.error('Need 4 negative expect-empty assertions, found',exp?exp.length:0);process.exit(4);}console.log('OK');" && npm test -- tests/rls/ tests/form-builder/ --reporter=basic --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File tests/rls/multi-tenancy.spec.ts exists.
    - tests/rls/multi-tenancy.spec.ts contains string `describe.skipIf`.
    - tests/rls/multi-tenancy.spec.ts contains string `signedInClientFor` (consumes the helper from Task 2).
    - tests/rls/multi-tenancy.spec.ts contains at least 4 occurrences of `expect(data).toEqual([])` (the four negative cross-org reads).
    - tests/rls/multi-tenancy.spec.ts contains at least one positive control assertion using `toBeGreaterThan(0)` against own-org assignments.
    - All 4 scaffold files exist under tests/form-builder/.
    - Each scaffold file contains at least one `it.todo(` line.
    - `npm test -- tests/rls/ tests/form-builder/ --reporter=basic --run` exits 0 (RLS suite is skipped when env-vars are absent; scaffolds report todos without failing).
  </acceptance_criteria>
  <done>
    The Phase 16 RLS regression gate (multi-tenancy.spec.ts) is live and runs as part of `npm test` whenever Supabase env vars are present; the four form-builder Wave-0 scaffolds reserve the file paths that Plans 04, 05, 06 will fill in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (Supabase via @supabase/ssr) | Untrusted JWT; RLS is the gate |
| test process → live Supabase | Service-role used ONLY in seedTwoTenants / teardown; anon + signInWithPassword used for every assertion read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | Cross-tenant read on form_templates, template_versions, form_submissions, form_assignments | mitigate | 5-spec RLS suite (`tests/rls/multi-tenancy.spec.ts`) — 4 negative + 1 positive control. Vitest config extended so suite runs in CI. RLS policies on each table (migrations 001, 003, 004) are the actual gate; this plan PROVES the gate works. |
| T-16-04 | Tampering | Test harness using service-role for assertion reads (Pitfall 4) — would falsely report "isolated" when broken | mitigate | Hard rule in helper: `signedInClientFor` uses anon + signInWithPassword. Service-role is restricted to seed/teardown. Positive control assertion confirms RLS is filtering rather than returning empty for missing data. |
| T-16-08 | Information Disclosure | RLS suite false-passes by returning empty when seed step silently fails | mitigate | The positive control (`Client A CAN read own org rows`) FAILS loudly if seedTwoTenants left a tenant un-seeded. |
</threat_model>

<verification>
After all three tasks:
- `npm test -- --run --reporter=basic --passWithNoTests` exits 0 (full Vitest sweep including new specs).
- Migrations 013 and 014 exist as files but are NOT pushed (push runs in Plan 07).
- `node -e "require('fs').readFileSync('vitest.config.ts','utf8').includes('tests/rls') || process.exit(1)"` exits 0.
</verification>

<success_criteria>
- Migrations 013 + 014 authored (not pushed).
- vitest.config.ts include extended.
- tests/rls/helpers/seed-two-tenants.ts exports the four helpers + types.
- tests/rls/multi-tenancy.spec.ts collects 5 specs under describe.skipIf.
- Four tests/form-builder/ Wave-0 scaffold files reserve their paths with it.todo entries pointing at later plans.
- Existing form-builder + form-interpreter Vitest specs still pass (no regression).
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-tenancy-fork-on-fill/16-01-SUMMARY.md` recording: migration file paths created, vitest.config.ts diff, the helper module's exported surface, the 5 RLS spec names, the 4 form-builder scaffold paths, and the env-var requirements for the RLS suite to actually run (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY).
</output>
