---
phase: 16-multi-tenancy-fork-on-fill
plan: 06
plan_id: 16-06
type: execute
wave: 3
depends_on: [16-01, 16-04]
files_modified:
  - app/client/templates/page.tsx
  - app/client/templates/[id]/fill/page.tsx
  - app/client/templates/[id]/fill/fill-customer-template-client.tsx
  - app/client/templates/actions.ts
  - lib/forms/schema-diff.ts
  - tests/form-builder/customer-self-fill-submission.test.ts
  - tests/form-builder/save-draft.test.ts
autonomous: true
requirements:
  - BUILDER-03
  - BUILDER-05
tags: [client, customer-templates, self-fill, deleted_at, ui-cleanup, dead-code-removal]
must_haves:
  truths:
    - "/client/templates page no longer shows 'Available admin masters' section; only customer-owned templates remain (D-09)."
    - "/client/templates page heading is 28px Newsreader (UI-SPEC unified client heading size)."
    - "A new /client/templates/[id]/fill route mounts the interpreter against the customer template's latest published version (D-16)."
    - "Customer self-fill submission server action inserts form_submissions with assignment_id=NULL, client_id from server context, template_version_id pinned (D-16, T-16-04)."
    - "Every client_user in the org sees My Templates and Customise first — no new role flag (D-13); the new fill route and the existing CRUD actions both rely on org-level RLS (migration 003) without a per-user gate."
    - "Customer-side soft-delete of templates is org-level (any client_user in the org can soft-delete) (D-14) — pattern carried forward from existing deleteClientTemplate; no change required if already org-scoped."
    - "Wave-0 scaffold tests/form-builder/customer-self-fill-submission.test.ts is filled with real assertions."
    - "forkOnFill (auto-trigger contract) is deleted from app/client/templates/actions.ts; superseded by explicit-button forkAssignedTemplate (D-07). If lib/forms/schema-diff.ts has no remaining consumers after the deletion, that file is also removed."
  artifacts:
    - path: app/client/templates/page.tsx
      provides: "Simplified page — My Templates only; no admin-masters browse list; 28px heading"
      contains: "text-[28px]"
    - path: app/client/templates/[id]/fill/page.tsx
      provides: "Customer-built template fill route (interpreter against latest published version)"
      contains: "template_version"
    - path: app/client/templates/actions.ts
      provides: "submitCustomerTemplateFillAction(templateId, answers) → INSERT form_submissions assignment_id=NULL; forkOnFill removed (D-07 supersedes)"
      exports: ["submitCustomerTemplateFillAction"]
  key_links:
    - from: app/client/templates/[id]/fill/page.tsx
      to: InterpreterRenderer + submitCustomerTemplateFillAction
      via: "FillCustomerTemplateClient (small client component) wraps InterpreterRenderer and calls the new submission action on submit"
      pattern: "submitCustomerTemplateFillAction"
    - from: app/client/templates/actions.ts (submitCustomerTemplateFillAction)
      to: form_submissions table
      via: ".insert({ template_version_id, client_id: ctx.client_id, assignment_id: null, status: 'submitted', answers_json })"
      pattern: "assignment_id: null"
---

<objective>
Deliver the customer-built template fill path (D-16), clean up `/client/templates` per D-09, and decommission the auto-trigger fork predecessor (D-07 dead-code removal).

1. Simplify `/client/templates` — remove the "Available admin masters" section and any associated fetch; retain only My Templates; update heading to 28px Newsreader; drop the section numbering (single section now).
2. Add `/client/templates/[id]/fill` route — interpreter against the customer template's latest published version.
3. Add `submitCustomerTemplateFillAction` to `app/client/templates/actions.ts` — INSERTs form_submissions with `assignment_id=NULL` and `client_id` from server context.
4. Verify org-level soft-delete works for customer templates (D-14) — typically no code change required; document the verification.
5. Fill `tests/form-builder/customer-self-fill-submission.test.ts` scaffold with real assertions.
6. Delete `forkOnFill` from `app/client/templates/actions.ts` (D-07 supersedes — the explicit-button `forkAssignedTemplate` shipped in Plan 05 is the only fork path). Remove any now-orphaned imports (`hasStructuralChanges` / schema-diff helpers) from `app/client/templates/actions.ts`; if `lib/forms/schema-diff.ts` has no remaining consumers, delete that file too. This task runs LAST so its sole-writer ownership of `actions.ts` doesn't collide with Task 2's append.

Purpose: Customer-built templates are fill-only within the org (no assignment scaffolding). D-09 resolves the `TODO(phaseB)` in app/client/templates/page.tsx. D-07 finalises by removing the predecessor fork action.

Output: One modified page (simplification), one new route, one new server action, one filled test scaffold, one dead-function deletion + optional file deletion.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md

<interfaces>
From lib/supabase/server.ts:
- export async function createClient(): Promise<SupabaseClient>  // RLS-aware

From lib/auth-helpers.ts:
- export async function getClientContext(): Promise<{ user_id: string; client_id: string; role: string } | null>

form_submissions columns (after migration 014 — assignment_id is nullable):
- id, client_id, template_version_id, assignment_id (nullable per migration 014), status, answers_json, created_at, deleted_at

Existing patterns to read:
- app/client/templates/page.tsx (current — contains the TODO(phaseB) comment at line 17, the "Available admin masters" section at lines 54-95, the My Templates section, and the assigned fetch at lines 18-23)
- app/admin/assessments/[id]/page.tsx (interpreter mount + two-step fetch pattern — analog for /client/templates/[id]/fill/page.tsx)
- app/admin/assessments/[id]/assessment-client.tsx (InterpreterRenderer wrapper — analog for FillCustomerTemplateClient)
- app/client/templates/actions.ts (BEFORE this plan: still contains forkOnFill at lines 214-283 — Plan 05 no longer touches this file; Task 3 of THIS plan removes it. Existing actions: createClientTemplate, saveClientDraftAction, publishClientTemplateAction, deleteClientTemplate, forkOnFill (to-be-removed))
- tests/form-builder/customer-self-fill-submission.test.ts (Plan 01 scaffold)
- lib/forms/schema-diff.ts (exports hasStructuralChanges — currently consumed by forkOnFill; check for other consumers before deletion in Task 3)

UI-SPEC Route G (the simplification):
- Remove "Available Templates" section + mono divider
- Page heading: 28px Newsreader (update from current 32px)
- "My Templates" section label: drop "02 — " numbering (now just "My Templates")
- "FORKED" mono tag retained on forked cards (already implemented)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Simplify /client/templates page (remove admin masters section, 28px heading, drop section numbering)</name>
  <read_first>
    - app/client/templates/page.tsx (full file — every line; identify the assigned-masters fetch around lines 18-23 + the section render around lines 54-95)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route G — /client/templates (simplified — My Templates only)"
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "Modify app/client/templates/page.tsx" [File Classification row 20: MODIFY app/client/templates/page.tsx]
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md D-09 paragraph
  </read_first>
  <behavior>
    - The fetch that loads `assigned` admin-master templates (around lines 18-23 of the current file) is removed.
    - The "Available admin masters" / "01 — Available Templates" section JSX (around lines 54-95) is removed entirely, including its mono divider.
    - The `TODO(phaseB)` comment at line 17 is removed (D-09 resolves the TODO).
    - The page heading `text-[32px]` is changed to `text-[28px]` (UI-SPEC unified client heading size).
    - The "My Templates" section label changes from `02 — My Templates` (or similar numbered prefix) to plain `My Templates` (single section now).
    - The mono index header preceding the page h2 is renumbered: from `06 · Templates` to `06 · My Templates` (or planner-discretion — the renumbering is needed because `/client/assignments` now uses `05 · Assigned Forms`; keep templates as `06 · My Templates` to avoid collisions and reflect the unified naming).
    - The `<NewClientTemplateButton>` (or equivalent "New template" trigger) is retained.
    - The empty-state copy (when zero customer templates) is updated to UI-SPEC locked copy: heading `No templates yet`, body `Create your own forms or customise an assigned form when it arrives.`.
    - No other behavior on the page changes.
  </behavior>
  <files>
    - app/client/templates/page.tsx (MODIFY)
  </files>
  <action>
    Read app/client/templates/page.tsx end-to-end. Identify and remove:

    1. The `assigned` masters fetch (typically `const { data: assigned } = await supabase.from("form_templates").select(...).eq("owner_type", "admin").eq("is_published", true)...` or similar; precise lines per RESEARCH/PATTERNS reference is lines 18-23).
    2. The `TODO(phaseB)` comment at line 17.
    3. The "Available admin masters" section JSX block (lines 54-95). This includes any mono section index (`01 — Available Templates` or similar) and the divider markup.

    Update the page heading:
    - Change `text-[32px]` to `text-[28px]` on the serif h2.
    - Update the mono section index from `06 · Templates` to `06 · My Templates` (single section).
    - Remove the `02 — ` (or any) numbering prefix from the My Templates section header — the section is now the only one, so the divider/index can be the page heading itself.

    Update the empty-state copy (in the JSX that renders when customerTemplates is empty) to match UI-SPEC locked copy: `<h3 className="font-serif text-xl">No templates yet</h3><p className="text-sm text-[#6b6560] mt-2">Create your own forms or customise an assigned form when it arrives.</p>`.

    Retain `<NewClientTemplateButton>` exactly where it currently lives. Retain the `<ClientTemplateCard>` grid for customerTemplates exactly as is (only the surrounding section header changes).

    Do NOT remove anything outside the scope above. Re-read the diff carefully before saving.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('app/client/templates/page.tsx','utf8');if(p.includes('TODO(phaseB)')){console.error('TODO comment not removed');process.exit(1);} if(p.includes('text-[32px]')){console.error('Heading still 32px — must be 28px');process.exit(2);} if(!p.includes('text-[28px]')){console.error('Heading missing 28px class');process.exit(3);} if(!p.includes('My Templates')){console.error('My Templates label missing');process.exit(4);} if(!p.includes('No templates yet')){console.error('Empty state heading not updated');process.exit(5);} const availMatches=p.match(/Available\\s+(?:admin\\s+masters|Templates)/i);if(availMatches){console.error('Available admin masters section still present:',availMatches[0]);process.exit(6);} console.log('OK')" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/client/templates/page.tsx does NOT contain `TODO(phaseB)`.
    - app/client/templates/page.tsx does NOT contain `text-[32px]`.
    - app/client/templates/page.tsx contains `text-[28px]`.
    - app/client/templates/page.tsx contains `My Templates`.
    - app/client/templates/page.tsx contains `No templates yet` AND `Create your own forms or customise an assigned form when it arrives.`.
    - app/client/templates/page.tsx does NOT contain the substring `Available admin masters` or `Available Templates` (D-09 cleanup).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>
    /client/templates renders a single "My Templates" section with the 28px heading. The admin-masters browse list is gone. Empty state uses locked copy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build /client/templates/[id]/fill route + submitCustomerTemplateFillAction</name>
  <read_first>
    - app/client/templates/[id]/page.tsx (existing customer template builder mount — read to understand how the page resolves templateId + ownership checks; the new fill route reuses requireOwnedTemplate)
    - app/admin/assessments/[id]/page.tsx (lines 1-58 — two-step fetch + UUID guard; the same pattern but reading template_versions by template_id + latest published instead of by submission's template_version_id)
    - app/admin/assessments/[id]/assessment-client.tsx (InterpreterRenderer wrapper — analog for FillCustomerTemplateClient)
    - app/client/assignments/[id]/fill/fill-assignment-client.tsx (created in Plan 04 — closer analog; reuse the same InterpreterRenderer mount shape but call the new customer-submit action instead)
    - app/client/templates/actions.ts (current state still includes forkOnFill — confirm the file's existing exports and the requireOwnedTemplate helper pattern at lines 23-35; this task APPENDS submitCustomerTemplateFillAction. The forkOnFill removal is Task 3 in this same plan and runs AFTER this task.)
    - lib/supabase/server.ts
    - lib/auth-helpers.ts
    - tests/form-builder/customer-self-fill-submission.test.ts (Plan 01 scaffold — fill with real assertions)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "app/client/assignments/[id]/fill/page.tsx" (analog for the new route) [File Classification row 5: app/client/assignments/[id]/fill/page.tsx — reuse as analog for app/client/templates/[id]/fill/page.tsx (no row of its own — sibling of row 20)]
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pitfall 2: form_submissions.assignment_id NOT NULL breaks customer-built fill" + § "Security Domain — Customer self-fill writes with another org's client_id"
  </read_first>
  <behavior>
    - submitCustomerTemplateFillAction is a `"use server"` export with signature `(templateId: string, answers: Record<string, unknown>): Promise<void>` added to app/client/templates/actions.ts.
    - Step 1: `const ctx = await requireClientContext();` (existing helper at the top of the file).
    - Step 2: `const supabase = await createClient();`.
    - Step 3: Verify template ownership via existing `requireOwnedTemplate(templateId, ctx.client_id)` (already in the file).
    - Step 4: Read the latest published template_versions row: `supabase.from("template_versions").select("id").eq("template_id", templateId).not("published_at", "is", null).order("version_number", { ascending: false }).limit(1).maybeSingle()`. Throw "Template has no published version" if maybeSingle returns null.
    - Step 5: INSERT form_submissions with `{ template_version_id: latestVersion.id, client_id: ctx.client_id, assignment_id: null, status: "submitted", answers_json: answers }`. The `client_id` MUST come from `ctx.client_id` (server context), NEVER from any client-supplied parameter (T-16-04 mitigation). The `assignment_id: null` is the D-16 contract enabled by migration 014.
    - Step 6: `revalidatePath("/client/templates")` + revalidate any per-template detail path if relevant.
    - Step 7: `redirect("/client/templates")` (or to a confirmation route — pick the simpler option; redirect to /client/templates).
    - app/client/templates/[id]/fill/page.tsx is RSC. UUID guard. Verify template ownership. Read latest published version's schema_json. Mount FillCustomerTemplateClient with the schema + templateId.
    - FillCustomerTemplateClient (NEW small client component sibling) wraps InterpreterRenderer; on submit, calls submitCustomerTemplateFillAction(templateId, answers).
    - tests/form-builder/customer-self-fill-submission.test.ts asserts:
      - (a) submitCustomerTemplateFillAction reads the latest published version (`.not("published_at", "is", null)` is in the chain).
      - (b) The INSERT into form_submissions has `assignment_id: null` (D-16).
      - (c) The INSERT's `client_id` value equals the mocked `getClientContext().client_id` — NOT any value passed into the function. Verify this by passing a different value as a hypothetical second parameter (or by ensuring no client_id parameter exists in the signature) — concretely: assert the spy's insert payload contains `client_id: "client-org-001"` regardless of any decoy state.
      - (d) Without a published version, the action throws "Template has no published version" (mock maybeSingle to return `{ data: null }`).
  </behavior>
  <files>
    - app/client/templates/actions.ts (MODIFY — append submitCustomerTemplateFillAction; do NOT touch forkOnFill in this task)
    - app/client/templates/[id]/fill/page.tsx (NEW)
    - app/client/templates/[id]/fill/fill-customer-template-client.tsx (NEW — InterpreterRenderer wrapper for the customer-self-fill surface)
    - tests/form-builder/customer-self-fill-submission.test.ts (REPLACE scaffold from Plan 01)
  </files>
  <action>
    Open app/client/templates/actions.ts. Append submitCustomerTemplateFillAction per the `<behavior>` block. Confirm imports already include `createClient`, `requireClientContext` (or equivalent), `requireOwnedTemplate`, `revalidatePath`, `redirect`. Do NOT remove `forkOnFill` or `hasStructuralChanges` in this task — that is Task 3.

    Implementation: top-level async server function (no try/catch wrap around the whole body). The redirect is the LAST statement, outside any try/catch (RESEARCH Pitfall 1).

    Author app/client/templates/[id]/fill/page.tsx as RSC. UUID guard pattern from `app/admin/assessments/[id]/page.tsx`. Steps:
    1. `await params` to get id.
    2. `const ctx = await getClientContext(); if (!ctx) notFound();`.
    3. `const supabase = await createClient();`.
    4. Fetch the template to verify ownership: `.from("form_templates").select("id, name, owner_id, owner_type, deleted_at").eq("id", id).maybeSingle()`. notFound() if null, deleted, or owner_type !== "customer" || owner_id !== ctx.client_id.
    5. Fetch latest published version: `.from("template_versions").select("id, schema_json").eq("template_id", id).not("published_at", "is", null).order("version_number", { ascending: false }).limit(1).maybeSingle()`. notFound() if null (no published version yet).
    6. Render the cream-surface wrapper (mono back link to /client/templates, serif 28px template name) and mount `<FillCustomerTemplateClient templateId={id} templateName={template.name} schemaJson={version.schema_json} />`.

    Author app/client/templates/[id]/fill/fill-customer-template-client.tsx as `"use client"`. Read app/client/assignments/[id]/fill/fill-assignment-client.tsx (from Plan 04) and mirror its structure but call `submitCustomerTemplateFillAction(templateId, answers)` on submit instead of the assignment-fill action. Surface is cream. Submit button "Submit form" black-fill full-width.

    Fill tests/form-builder/customer-self-fill-submission.test.ts. Vitest mock skeleton from save-draft.test.ts. Mock getClientContext to return `{ client_id: "client-org-001", user_id: "user-001", role: "client" }`.

    Build a fake supabase chain where:
    - `.from("form_templates").select(...).eq("id", "tmpl-1").maybeSingle()` returns `{ data: { id: "tmpl-1", owner_type: "customer", owner_id: "client-org-001", deleted_at: null } }` (the ownership check passes).
    - `.from("template_versions").select(...).eq("template_id", "tmpl-1").not("published_at", "is", null).order(...).limit(1).maybeSingle()` returns `{ data: { id: "ver-1" } }`.
    - `.from("form_submissions").insert(<payload>)` records the payload via a spy.

    Tests:
    - "writes form_submissions with assignment_id: null" — call `await submitCustomerTemplateFillAction("tmpl-1", { q1: "answer" })`, then assert the insert spy was called with an object containing `assignment_id: null`.
    - "client_id is taken from server context, not function parameter" — assert the insert payload contains `client_id: "client-org-001"` (the mocked context value). Add a comment: "no client_id parameter in submitCustomerTemplateFillAction signature — T-16-04 mitigation".
    - "throws when no published version exists" — reconfigure maybeSingle for template_versions to return `{ data: null }`; call the action and assert it throws with message "Template has no published version".

    Run `npm test -- tests/form-builder/customer-self-fill-submission.test.ts --run` to confirm green.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for (const f of ['app/client/templates/[id]/fill/page.tsx','app/client/templates/[id]/fill/fill-customer-template-client.tsx']) if(!fs.existsSync(f)){console.error('Missing:',f);process.exit(1);} const a=fs.readFileSync('app/client/templates/actions.ts','utf8');for (const s of ['submitCustomerTemplateFillAction','assignment_id: null','client_id: ctx.client_id','not(\"published_at\", \"is\", null)']) {if(!a.includes(s)){console.error('Actions missing:',s);process.exit(2);}} const t=fs.readFileSync('tests/form-builder/customer-self-fill-submission.test.ts','utf8');if(t.match(/it\\.todo\\(/g)){console.error('Todos remain');process.exit(3);} for (const s of ['assignment_id: null','client-org-001','Template has no published version']) {if(!t.includes(s)){console.error('Test missing:',s);process.exit(4);}} console.log('OK')" && npm test -- tests/form-builder/customer-self-fill-submission.test.ts --run 2>&1 | tail -15 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/client/templates/actions.ts contains `submitCustomerTemplateFillAction`.
    - app/client/templates/actions.ts contains literal `assignment_id: null` (D-16 contract).
    - app/client/templates/actions.ts contains `client_id: ctx.client_id` (T-16-04 mitigation — server-context only).
    - app/client/templates/actions.ts contains `.not("published_at", "is", null)` (latest published version filter).
    - app/client/templates/[id]/fill/page.tsx exists.
    - app/client/templates/[id]/fill/fill-customer-template-client.tsx exists with `"use client"`.
    - tests/form-builder/customer-self-fill-submission.test.ts contains literals `assignment_id: null` and `client-org-001` and `Template has no published version` and zero `it.todo(` calls.
    - `npm test -- tests/form-builder/customer-self-fill-submission.test.ts --run` exits 0.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>
    A customer-built template can be filled and submitted. The resulting form_submissions row has assignment_id=NULL, client_id from server context, and is pinned to the template's latest published version. Test scaffold filled and green. forkOnFill is still in the file (Task 3 removes it).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete forkOnFill from app/client/templates/actions.ts + tidy orphaned schema-diff helper (D-07)</name>
  <read_first>
    - app/client/templates/actions.ts (full file, AFTER Task 2's append — locate forkOnFill at lines 214-283 and any top-of-file import of hasStructuralChanges)
    - lib/forms/schema-diff.ts (if it exists — the file exporting hasStructuralChanges that forkOnFill consumes; check for other consumers before deletion)
    - app/client/assignments/actions.ts (after Plan 05 — confirms forkAssignedTemplate exists as the canonical replacement so the deletion is safe)
    - tests/form-builder/save-draft.test.ts (line 66 area — vi.mock entry for forkOnFill / hasStructuralChanges that may need removal)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md § "Claude's Discretion" → fork action location (D-07 supersedes auto-trigger)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Open Questions" #2 — clean cut recommended
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § File Classification row 21 (prior art: MODIFY app/client/templates/actions.ts — the now-dead forkOnFill)
  </read_first>
  <behavior>
    - The `export async function forkOnFill(...)` block is removed from app/client/templates/actions.ts. No other export in the file is touched (createClientTemplate, saveClientDraftAction, publishClientTemplateAction, deleteClientTemplate, submitCustomerTemplateFillAction all remain intact).
    - Any import of `hasStructuralChanges` (or other helpers from `@/lib/forms/schema-diff`) that was used ONLY by forkOnFill is also removed from app/client/templates/actions.ts. Imports still used by other functions in the file are preserved.
    - Grep confirms no remaining file imports `forkOnFill` (other than possibly test files that mock it). Grep confirms no remaining file outside the deleted function imports `hasStructuralChanges`.
    - If `hasStructuralChanges` has no remaining consumers, `lib/forms/schema-diff.ts` is deleted. If any other file consumes it (e.g., a builder helper somewhere), the file stays but only the import from actions.ts is removed.
    - Any mock entries in tests/form-builder/save-draft.test.ts (or sibling test files) that mock the removed symbols (`forkOnFill`, `hasStructuralChanges`) are removed so the test suite still passes.
    - `npm test -- tests/form-builder/ --run` exits 0 (no regressions from the deletion).
    - `npm run build` succeeds.
    - D-07 invariant: the only fork path in the codebase is the explicit-button `forkAssignedTemplate` in `app/client/assignments/actions.ts` (shipped by Plan 05). The auto-trigger predecessor is gone.
  </behavior>
  <files>
    - app/client/templates/actions.ts (MODIFY — delete forkOnFill function + any solely-used imports)
    - lib/forms/schema-diff.ts (DELETE if no other consumers; otherwise leave)
    - tests/form-builder/save-draft.test.ts (MODIFY only if it currently mocks the removed symbols)
  </files>
  <action>
    Read app/client/templates/actions.ts end-to-end (it now also contains submitCustomerTemplateFillAction appended by Task 2 — preserve it). Locate the `forkOnFill` export (RESEARCH cites lines 214-283 in the pre-append file; the line range will have shifted after Task 2's append). Note any imports it uniquely consumes — typically `hasStructuralChanges` from `@/lib/forms/schema-diff` (or similar path).

    Run greps to discover other consumers (use the Grep tool):
    - Search pattern `forkOnFill` across the repo (excluding node_modules and .planning). Expect hits only in test files after deletion.
    - Search pattern `hasStructuralChanges` across the repo.
    - Search pattern `from "@/lib/forms/schema-diff"` (and the relative variant) to enumerate all importers.

    Delete the forkOnFill function body (the `export async function forkOnFill(...)` block and any helper that exists solely to support it) from app/client/templates/actions.ts. Delete any import lines that are now unused (e.g. `import { hasStructuralChanges } from ...`). Do NOT delete unrelated exports or unrelated imports. Do NOT touch `submitCustomerTemplateFillAction` (Task 2's output).

    If `hasStructuralChanges` (and any other helper in schema-diff.ts) has no other consumers AND `lib/forms/schema-diff.ts` only exists to support forkOnFill, delete that file. If it has other consumers, leave the file untouched and remove only the import from actions.ts.

    Check tests/form-builder/save-draft.test.ts (and any sibling test files referenced by 16-PATTERNS.md "tests/form-builder/save-draft.test.ts line 66") for `vi.mock` entries that mock `hasStructuralChanges` or `forkOnFill` — remove those mock entries if present. Do not remove unrelated mocks.

    Run the form-builder test suite to verify no regressions: `npm test -- tests/form-builder/ --run`. If a test references the removed symbol directly (e.g., a `forkOnFill`-specific test), update or remove only that specific test — preserve unrelated tests.

    Run `npm run build` to confirm the deletion doesn't break compilation.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('app/client/templates/actions.ts','utf8');const noComments=a.split('\n').filter(l=>!l.trim().startsWith('//')).join('\n');if(noComments.includes('forkOnFill')){console.error('forkOnFill still present in actions.ts (non-comment)');process.exit(1);} if(noComments.includes('hasStructuralChanges')){console.error('hasStructuralChanges import still present in actions.ts (non-comment)');process.exit(2);} if(!a.includes('submitCustomerTemplateFillAction')){console.error('submitCustomerTemplateFillAction was accidentally removed — Task 2 output must remain');process.exit(3);} console.log('OK')" && npm test -- tests/form-builder/ --run 2>&1 | tail -15 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/client/templates/actions.ts does NOT contain the substring `forkOnFill` outside of comments (function fully removed).
    - app/client/templates/actions.ts does NOT contain the substring `hasStructuralChanges` outside of comments (import removed if it was solely consumed by forkOnFill).
    - app/client/templates/actions.ts STILL contains `submitCustomerTemplateFillAction` (Task 2's append is preserved).
    - app/client/templates/actions.ts STILL contains the other pre-existing exports: `createClientTemplate`, `saveClientDraftAction`, `publishClientTemplateAction`, `deleteClientTemplate`.
    - If lib/forms/schema-diff.ts was deleted, a grep for `from "@/lib/forms/schema-diff"` and `from "../../lib/forms/schema-diff"` (and similar relative variants) returns 0 hits across `app/` and `lib/` (excluding deleted-file references in git history).
    - `npm test -- tests/form-builder/ --run` exits 0 (no regressions).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>
    Dead code (forkOnFill + schema-diff helper if orphaned) is removed. The explicit-button `forkAssignedTemplate` from Plan 05 is the only fork path. D-07 supersession is complete and the codebase has one fork action, not two.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → submitCustomerTemplateFillAction | Untrusted POST; auth gate via requireClientContext + requireOwnedTemplate |
| RLS @supabase/ssr → form_submissions | Server uses RLS-aware createClient; RLS policy from migration 003 on form_submissions limits insert to caller's org |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | Customer self-fill writes with another org's `client_id` | mitigate | client_id comes from `ctx.client_id` (server context via getClientContext + auth.uid()), NEVER from a function parameter. Test scaffold asserts the insert payload's client_id equals the mocked context value. |
| T-16-06 | Elevation of Privilege | Service-role import on customer surface | mitigate | submitCustomerTemplateFillAction uses `createClient` from `lib/supabase/server.ts` (RLS-aware). Grep gate: `grep -rn "supabase/admin" app/client/templates/` returns 0 hits. |
| T-16-07 | Tampering | Direct POST without client claim | mitigate | requireClientContext throws "Not a client user"; requireOwnedTemplate throws on cross-org template id. |
| T-16-01 | Information Disclosure | Cross-tenant template/version read on /client/templates/[id]/fill | mitigate | Ownership check before fetching schema_json; defense-in-depth alongside RLS. Verified by the Plan 01 RLS test. |
| T-16-08 | Information Disclosure | Soft-deleted customer templates leak into fill route | mitigate | The page.tsx ownership check filters on `deleted_at IS NULL` via maybeSingle on a row that has `deleted_at` set returns notFound. |
| T-16-09 | Tampering | Dead `forkOnFill` path remains callable post-Plan-05, bypasses the AlertDialog confirmation gate (D-07) | mitigate | Task 3 deletes forkOnFill outright; grep gate in `<verification>` asserts `forkOnFill` no longer appears in app/ or lib/. |
</threat_model>

<verification>
After all three tasks:
- `npm run build` succeeds.
- `npm test -- tests/form-builder/customer-self-fill-submission.test.ts --run` exits 0.
- `npm test -- tests/form-builder/ --run` exits 0 (no regressions from the forkOnFill deletion).
- grep gate: `grep -rn "from \"@/lib/supabase/admin\"" app/client/templates/` returns 0 hits (T-16-06).
- grep gate: `grep -rn "forkOnFill" app/ lib/ | grep -v '^[^:]*:\s*//'` returns 0 hits (T-16-09 — dead code removed; comments excluded).
- D-14 verification: `grep -nE "deleteClientTemplate" app/client/templates/actions.ts` — confirm the existing function scopes by `ctx.client_id` (org-level), NOT by the per-user uid. The Plan 01 RLS test already covers cross-org isolation; D-14 is an "any teammate in the org can soft-delete any teammate's fork" assertion. If `grep -nE "ctx\.client_id|client_id.*=.*ctx" app/client/templates/actions.ts` returns at least one match within the deleteClientTemplate body, the org-level scope is in place. If only a per-user uid filter is present, raise a finding for the 16-UAT.md walkthrough to confirm via two teammates in one org.
- Manual smoke (dev, optional): sign in as customer; create a new template via "+ New template"; publish v1; navigate to `/client/templates/[id]/fill`; fill + submit; verify row in form_submissions has `assignment_id IS NULL` AND `client_id` equals the logged-in user's org.
</verification>

<success_criteria>
- /client/templates is simplified (admin-masters section removed; 28px heading; locked empty-state copy).
- /client/templates/[id]/fill route mounts the interpreter and submits via the new server action.
- submitCustomerTemplateFillAction enforces server-context client_id and writes assignment_id=NULL.
- customer-self-fill-submission.test.ts passes with real assertions.
- forkOnFill is deleted from app/client/templates/actions.ts; lib/forms/schema-diff.ts removed if orphaned. The explicit-button forkAssignedTemplate (Plan 05) is the only fork path (D-07).
- No regressions in existing client-side tests.
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-tenancy-fork-on-fill/16-06-SUMMARY.md` recording: the simplified /client/templates layout, the new /client/templates/[id]/fill route + the FillCustomerTemplateClient component path, the submitCustomerTemplateFillAction signature + write contract, the test assertions added, and the dead-code deletion (what was removed from app/client/templates/actions.ts, and whether lib/forms/schema-diff.ts was deleted or retained).
</output>
