---
phase: 19-client-portal-productionization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/auth-helpers.ts
  - tests/auth-helpers/client-context-with-identity.test.ts
autonomous: true
requirements: [D-02]
must_haves:
  decisions: [D-02]
  truths:
    - "A signed-in client's org name resolves from clients.name"
    - "A signed-in client's display name resolves from client_users.name, falling back to email when name is empty"
    - "Demo mode resolves identity by picking the first client_users row without calling getUser()"
    - "Missing session returns null (no crash)"
  artifacts:
    - path: "lib/auth-helpers.ts"
      provides: "getClientContextWithIdentity() sibling export + ClientIdentity interface"
      contains: "getClientContextWithIdentity"
    - path: "tests/auth-helpers/client-context-with-identity.test.ts"
      provides: "Wave 0 unit coverage for the identity helper"
      contains: "getClientContextWithIdentity"
  key_links:
    - from: "lib/auth-helpers.ts getClientContextWithIdentity()"
      to: "client_users + clients(name) join"
      via: "supabase.from('client_users').select('client_id, role, name, email, client:clients(name)')"
      pattern: "client:clients\\(name\\)"
    - from: "demo path"
      to: "isDemoMode() guard"
      via: "early return before getUser()"
      pattern: "isDemoMode"
---

<objective>
Add a `getClientContextWithIdentity()` sibling to `lib/auth-helpers.ts` that resolves the
signed-in client's org name, display name, and role in a single query, and cover it with a
Wave 0 unit test. This is the foundation the layout server shell (Plan 02) consumes to replace
hardcoded identity strings.

Purpose: D-02 — the portal header/footer must show the real org and signed-in person, not
"Hallam House Care Home" / "Sarah Whitfield". The existing `getClientContext()` only returns
`{client_id, role}`; this sibling extends the select without disturbing the existing helper.
Output: One new exported function + interface in `lib/auth-helpers.ts`, one new test file.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-client-portal-productionization/19-CONTEXT.md
@.planning/phases/19-client-portal-productionization/19-RESEARCH.md
@.planning/phases/19-client-portal-productionization/19-PATTERNS.md

<interfaces>
<!-- Existing getClientContext() pattern to mirror (lib/auth-helpers.ts lines 65-94). -->
<!-- getUser() and isDemoMode() are already exported from this file — no new imports. -->
<!-- The new ClientIdentity contract (consumed by Plan 02 layout shell): -->

ClientIdentity {
  client_id: string
  role: string
  orgName: string   // from clients.name, fallback "—"
  userName: string  // from client_users.name, fallback to client_users.email, then "—"
}

Supabase join cardinality normalization (from app/client/assignments/[id]/page.tsx lines 90-94):
  const clientRow = Array.isArray(data.client) ? data.client[0] : data.client
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing unit test for getClientContextWithIdentity()</name>
  <files>tests/auth-helpers/client-context-with-identity.test.ts</files>
  <read_first>
    - lib/auth-helpers.ts (the file being extended; study getClientContext() lines 65-94 and the isDemoMode guard at lines 43-55)
    - vitest.config.ts (test discovery + path aliases)
    - Any existing test under tests/ that mocks @/lib/supabase/server createClient (mirror the mocking style; do not invent a mock shape)
  </read_first>
  <behavior>
    - Test: real-user path — getUser() returns a user, client_users row has name + role + client:{name}; returns { client_id, role, orgName, userName } with orgName from the join and userName from name.
    - Test: demo path — isDemoMode() true; helper does NOT call getUser(); picks the first client_users row (.limit(1).single()); returns identity.
    - Test: name fallback — when client_users.name is empty string, userName falls back to email.
    - Test: org fallback — when the clients join is null, orgName is "—".
    - Test: join-shape normalization — when supabase returns client as an array, orgName still resolves from client[0].name.
    - Test: null session — getUser() returns null in non-demo mode → helper returns null.
  </behavior>
  <action>
    Create tests/auth-helpers/client-context-with-identity.test.ts using Vitest. Mock @/lib/supabase/server's createClient and the cookies()-backed isDemoMode + getUser paths the SAME way existing auth-helper-adjacent tests do (read an existing example first — do not hand-roll a divergent mock). Assert the six behaviors above. The demo-path test MUST assert getUser is NOT invoked (spy/assert call count 0) to lock the demo-mode auth-header invariant. Import the not-yet-written getClientContextWithIdentity from "@/lib/auth-helpers". This test MUST fail to compile/run initially (function does not exist yet) — that is the RED state.
  </action>
  <verify>
    <automated>npx vitest run tests/auth-helpers/client-context-with-identity.test.ts 2>&1 | grep -Eiq "fail|error|is not a function|not exported|cannot find" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - File tests/auth-helpers/client-context-with-identity.test.ts exists and imports getClientContextWithIdentity from "@/lib/auth-helpers".
    - Test run fails (RED) because the function does not yet exist — `npx vitest run tests/auth-helpers/client-context-with-identity.test.ts` exits non-zero.
    - The demo-path test asserts getUser is never called (explicit call-count/spy assertion present in the file).
    - At least 6 distinct `it(`/`test(` cases present (grep -c for the test keyword ≥ 6).
  </acceptance_criteria>
  <done>Test file exists, fails because the function is unimplemented, and locks all six D-02 behaviors including the no-getUser-in-demo invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement getClientContextWithIdentity() to make the test pass</name>
  <files>lib/auth-helpers.ts</files>
  <read_first>
    - lib/auth-helpers.ts (mirror getClientContext() lines 65-94 exactly; reuse the existing isDemoMode + getUser exports; honor the stale-auth-header comment at lines 43-55)
    - tests/auth-helpers/client-context-with-identity.test.ts (the contract just written — implement to satisfy it)
    - lib/supabase/database.types.ts (confirm client_users.Row.name is non-nullable string and proposals/clients column names — do not invent columns)
  </read_first>
  <action>
    Add an exported `interface ClientIdentity { client_id: string; role: string; orgName: string; userName: string }` and an exported `async function getClientContextWithIdentity(): Promise<ClientIdentity | null>` directly below the existing getClientContext(). Both demo and prod paths select the columns `"client_id, role, name, email, client:clients(name)"`. The demo path MUST guard with `if (await isDemoMode())` and early-return WITHOUT calling getUser() (picks first row via `.limit(1).single()`) — this is the stale-auth-header invariant from lines 43-55. The prod path calls getUser(), returns null if no user, then queries `.eq("id", user.id).single()`. In BOTH paths normalize the join with `const clientRow = Array.isArray(data.client) ? data.client[0] : data.client`, then return `orgName: clientRow?.name ?? "—"` and `userName: data.name || data.email || "—"`. Return null on error/no-data. Do NOT modify the existing getClientContext().
  </action>
  <verify>
    <automated>npx vitest run tests/auth-helpers/client-context-with-identity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/auth-helpers/client-context-with-identity.test.ts` exits 0 (all six cases GREEN).
    - `grep -n "getClientContextWithIdentity" lib/auth-helpers.ts` shows an `export` of the function.
    - `grep -n "interface ClientIdentity" lib/auth-helpers.ts` returns a match.
    - Source contains the join normalization: `grep -q "Array.isArray(data.client)" lib/auth-helpers.ts`.
    - The demo branch does not call getUser before its return: confirmed by the GREEN no-getUser test.
    - `npm run build` exits 0 (no broken imports, no type errors).
    - The original getClientContext() body is unchanged (the function still returns only client_id, role).
  </acceptance_criteria>
  <done>getClientContextWithIdentity() returns org name + user name + role, demo-safe, all unit tests green, build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser session → server helper | The authenticated session (auth.uid()) is the only trusted identity input; no client-supplied client_id is accepted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Spoofing | getClientContextWithIdentity() identity resolution | mitigate | Scope the client_users lookup by `.eq("id", user.id)` (auth.uid()); never accept a user-supplied client_id. The clients(name) join is RLS-gated by clients_own_select. |
| T-19-02 | Elevation of Privilege | demo-mode auth path | mitigate | Guard with `isDemoMode()` before any getUser() call; the unit test asserts getUser is never invoked in demo mode, preventing the stale-auth-header poisoning documented at lib/auth-helpers.ts lines 43-55. |
| T-19-03 | Information Disclosure | org/user name exposure | accept | Org name + display name are shown only to the authenticated owner of that client_users row; no cross-org data is selectable because the row is keyed by auth.uid(). |
</threat_model>

<verification>
- `npx vitest run tests/auth-helpers/client-context-with-identity.test.ts` green.
- `npm run build` exits 0.
- `getClientContext()` (the original) is untouched and other portal pages still build.
</verification>

<success_criteria>
- getClientContextWithIdentity() exported from lib/auth-helpers.ts, returns ClientIdentity | null.
- Six unit behaviors covered and green (org/user/role resolution, name→email fallback, org "—" fallback, join-array normalization, demo no-getUser, null session).
- Build clean; existing getClientContext() unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/19-client-portal-productionization/19-01-SUMMARY.md`
</output>
