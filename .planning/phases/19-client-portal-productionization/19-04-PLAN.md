---
phase: 19-client-portal-productionization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/client/contracts/page.tsx
autonomous: true
requirements: [D-09, D-10, D-11]
must_haves:
  decisions: [D-09, D-10, D-11]
  truths:
    - "A client sees their counter-signed contracts derived from the proposals pipeline"
    - "Only proposals at status 'Contract Issued' with a non-null contract_pdf_path appear"
    - "Each contract downloads via a short-lived signed Storage URL (proposals bucket)"
    - "When no counter-signed contracts exist, the existing editorial empty-state card shows"
    - "A client never sees another org's contract (adminClient query scoped by client_id)"
  artifacts:
    - path: "app/client/contracts/page.tsx"
      provides: "Real proposals-derived contracts query + signed-URL download + empty state"
      contains: "Contract Issued"
  key_links:
    - from: "app/client/contracts/page.tsx"
      to: "proposals (adminClient, scoped by client_id, status 'Contract Issued')"
      via: "adminClient.from('proposals') with .eq('client_id', ctx.client_id)"
      pattern: "Contract Issued"
    - from: "contract_pdf_path"
      to: "signed Storage URL"
      via: "adminClient.storage.from('proposals').createSignedUrl(s)"
      pattern: "createSignedUrl"
---

<objective>
Replace the static "No contracts yet" stub at `app/client/contracts/page.tsx` with a real query
that derives counter-signed contracts from the `proposals` table and serves each contract PDF via
a short-lived signed Storage URL, falling back to the existing editorial empty-state card.

Purpose: D-09 (contracts = proposals at the counter-signed stage), D-10 (signed-URL download),
D-11 (RESOLVED — stored proposal status is title-case "Contract Issued", NOT lowercase
"contract_signed"; query with adminClient because the RLS policy uses unreachable lowercase values).
Output: one rewritten server page.

This plan touches only app/client/contracts/page.tsx — fully parallel with Plans 02/03.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-client-portal-productionization/19-CONTEXT.md
@.planning/phases/19-client-portal-productionization/19-RESEARCH.md
@.planning/phases/19-client-portal-productionization/19-PATTERNS.md
@app/client/contracts/page.tsx
@app/client/proposals/page.tsx
@app/client/proposals/[id]/page.tsx

<interfaces>
<!-- D-11 RESOLVED (RESEARCH Critical Finding): stored proposal status values are TITLE-CASE.
     The Contracts query MUST use status = "Contract Issued", NOT "contract_signed". -->
<!-- adminClient is REQUIRED (RLS proposals_client_visible policy uses lowercase values that
     never match the stored title-case, so createClient() would return zero rows). adminClient
     bypasses RLS, so .eq("client_id", ctx.client_id) is the ONLY access-control boundary. -->

Contracts query (verified, RESEARCH Pattern 4):
  adminClient.from("proposals")
    .select("id, contract_pdf_path, services_json, total_price, created_at, sent_at")
    .eq("client_id", ctx.client_id)        // IDOR — not optional
    .eq("status", "Contract Issued")        // title-case
    .not("contract_pdf_path", "is", null)
    .order("created_at", { ascending: false })

Signed URL (verified, proposals/[id]/page.tsx lines 63-68): bucket "proposals", TTL 60*60.
Batch form for a list: adminClient.storage.from("proposals").createSignedUrls(paths, 60*60).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite contracts page with real proposals query + signed-URL download</name>
  <files>app/client/contracts/page.tsx</files>
  <read_first>
    - app/client/contracts/page.tsx (current static stub — REUSE the page-header section and the empty-state card markup verbatim)
    - app/client/proposals/page.tsx (adminClient query + empty-state pattern + status filter)
    - app/client/proposals/[id]/page.tsx (createSignedUrl pattern lines ~63-68, IDOR client_id eq, title-case isSigned comparison, download-link markup)
    - lib/supabase/database.types.ts (confirm proposals columns contract_pdf_path, services_json, total_price, created_at, sent_at, status are real — do not invent columns)
  </read_first>
  <action>
    Rewrite app/client/contracts/page.tsx as a server component. Add export const dynamic = "force-dynamic". Import adminClient from "@/lib/supabase/admin" and getClientContext from "@/lib/auth-helpers". Resolve ctx via getClientContext(); if no ctx.client_id, render the existing no-context fallback (the "No client context found" mono-uppercase block used in proposals/[id]/page.tsx). Query contracts via adminClient.from("proposals").select("id, contract_pdf_path, services_json, total_price, created_at, sent_at") scoped by eq client_id = ctx.client_id, eq status = "Contract Issued", not contract_pdf_path is null, order created_at descending. Use the title-case literal "Contract Issued" — NOT "contract_signed" (D-11). Generate signed URLs by collecting the non-null contract_pdf_path values and calling adminClient.storage.from("proposals").createSignedUrls(paths, 60*60), then building a Map keyed by path to signedUrl. Keep the existing page-header section ("08 · Contracts" / "Service agreements.") verbatim. When the contracts array is empty, render the EXISTING editorial empty-state card verbatim (do not change copy — it is already accurate). When non-empty, render a list of contract rows each with a download affordance: if a signed URL exists, an anchor (target _blank, rel noopener noreferrer, download attribute) styled like the proposals download button; if not, a disabled button. NO mock data, NO mockXxxFor() — real query with the honest empty state only.
  </action>
  <verify>
    <automated>grep -q "Contract Issued" app/client/contracts/page.tsx && grep -q "createSignedUrl" app/client/contracts/page.tsx && grep -q "client_id" app/client/contracts/page.tsx && grep -q "adminClient" app/client/contracts/page.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Page exports const dynamic = "force-dynamic" and uses adminClient (not createClient) for the proposals query.
    - Status filter uses the title-case literal: grep finds "Contract Issued"; grep for "contract_signed" returns NOTHING.
    - Query is scoped by client_id (grep finds "client_id") and filters non-null contract_pdf_path.
    - Signed-URL generation present: grep finds "createSignedUrl" against the "proposals" bucket with a 60*60 TTL.
    - Empty-state branch renders the existing editorial card (the "No contracts yet." / "Service agreements" copy is retained); no mock/fixture data is introduced (grep for "mock" returns nothing).
    - npm run build exits 0.
  </acceptance_criteria>
  <done>Contracts page lists real counter-signed contracts (or the honest empty state), each downloadable via a short-lived signed URL, scoped to the client's org; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service-role query → client org | adminClient bypasses RLS; the only ownership boundary is the server-side `.eq("client_id", ctx.client_id)` derived from the authenticated session. |
| Storage path → download | Raw contract_pdf_path is never exposed; only a short-lived signed URL crosses to the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Elevation of Privilege (Broken Access Control) | adminClient proposals query | mitigate | adminClient bypasses RLS, so the query MUST include `.eq("client_id", ctx.client_id)` (ctx from server-side getClientContext()). This is the sole access-control check; without it, every org's contracts leak. |
| T-19-12 | Information Disclosure | Contract PDF storage path | mitigate | Never expose the raw `contract_pdf_path` or a public URL; serve only a short-lived (TTL 60*60 ≤ 1h) signed Storage URL via createSignedUrl(s). |
| T-19-13 | Information Disclosure | Over-broad status filter | mitigate | Filter strictly to `status = "Contract Issued"` + non-null `contract_pdf_path`, so draft/sent/awaiting-signature proposals never surface as "contracts". |
| T-19-14 | Spoofing | Status-casing mismatch | accept | RLS proposals_client_visible uses lowercase values that don't match stored title-case; this phase intentionally relies on adminClient + manual scoping rather than fixing the RLS taxonomy (deferred per CONTEXT.md). The client_id scope fully compensates. |
</threat_model>

<verification>
- `npm run build` exits 0 (catches hallucinated imports + the rewrite).
- `grep "Contract Issued" app/client/contracts/page.tsx` matches; `grep "contract_signed"` does not.
- Manual (UAT, data-dependent): with a proposals row at status "Contract Issued" + contract_pdf_path, the row renders and downloads; with none, the editorial empty state shows.
</verification>

<success_criteria>
- Contracts derived from proposals at status "Contract Issued" with non-null contract_pdf_path, scoped by client_id via adminClient.
- Each contract downloads via a ≤1h signed Storage URL (proposals bucket).
- Honest editorial empty state when none exist; no mock data.
- Build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/19-client-portal-productionization/19-04-SUMMARY.md`
</output>
