---
phase: 07-ai-report-pipeline
plan: 06
type: execute
wave: 4
depends_on: ["07-04", "07-05"]
files_modified:
  - app/admin/assessments/[id]/review/review-client.tsx
autonomous: true
requirements:
  - REPORT-08
  - REPORT-09
  - REPORT-11

must_haves:
  truths:
    - "ReviewClient props widened to accept schemaJson + audioMedia from Plan 05's page-level fetch"
    - "Collapsible Raw Answers & STT panel sits ABOVE the editable draft (D-04 single-column layout); default open=false when draft exists, default open=true (one-time) when draft was just generated"
    - "Each row in the panel renders [field label] · [value or STT transcript], with audio transcripts pulled from audioMedia where field_id matches"
    - "Empty state respects status: when submission.status === 'ai_draft_failed' the CTA reads 'Retry Draft' and the message points Matt to /admin/month-summary (D-11)"
    - "After finalizeReport returns, the toast surfaces deliveryEmailFailed: ok→'PDF generated and saved!'; failed→'Report saved, email retry queued' (D-08, non-blocking)"
  artifacts:
    - path: "app/admin/assessments/[id]/review/review-client.tsx"
      provides: "Updated ReviewClient with raw-answers panel, retry CTA, deliveryEmailFailed toast"
      contains: "Raw Answers"
  key_links:
    - from: "ReviewClient props"
      to: "page.tsx Server Component fetch"
      via: "schemaJson + audioMedia"
      pattern: "schemaJson.*audioMedia|audioMedia.*schemaJson"
    - from: "handleApprove toast branch"
      to: "finalizeReport return shape"
      via: "result.deliveryEmailFailed"
      pattern: "deliveryEmailFailed"
---

<objective>
Extend `review-client.tsx` to (a) accept the new props from Plan 05, (b) render the D-04 collapsible "Raw Answers & STT" panel above the editable draft with the one-time auto-expand rule, (c) replace the empty-state copy/CTA with the D-11 retry path when `status === 'ai_draft_failed'`, and (d) branch the success toast from `handleApprove` on `result.deliveryEmailFailed` (D-08).

Purpose: This plan closes the user-facing surface for REPORT-08 (raw vs draft comparison), REPORT-09 (Matt can approve/regenerate/edit), and REPORT-11 (no auto-delivery — the toast and CTA make Matt's gate explicit). It also closes the transient prop-type error introduced by Plan 05.

Output: Single-file edit. The bulk of `review-client.tsx` (the editable hazard/summary/compliance form) is unchanged.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-CONTEXT.md
@.planning/phases/07-ai-report-pipeline/07-PATTERNS.md
@app/admin/assessments/[id]/review/review-client.tsx
@app/admin/assessments/[id]/review/page.tsx
@app/admin/assessments/actions.ts

<interfaces>
<!-- After Plan 05, page.tsx passes these props: -->

interface ReviewClientProps {
  submission: any                      // existing — includes draft_report_json, status, answers_json
  schemaJson: Record<string, unknown> | null   // pinned schema; null tolerated
  audioMedia: Array<{ field_id: string; storage_path: string; transcript: string | null }>
}

<!-- After Plan 04, finalizeReport returns: -->
//   { success: boolean, downloadUrl: string | null, deliveryEmailFailed: boolean }

<!-- Canonical status values (D-09): -->
//   in_progress | submitted | draft_ready_for_review | completed | ai_draft_failed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen ReviewClient props + render D-04 raw-answers panel (REPORT-08)</name>
  <files>app/admin/assessments/[id]/review/review-client.tsx</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-04 — collapsible panel rules, single-column layout, one-time auto-expand)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "review-client.tsx — extend with D-04 + D-11" — full code excerpts)
    - app/admin/assessments/[id]/review/review-client.tsx (entire file, 221 lines)
    - app/admin/assessments/[id]/review/page.tsx (Plan 05 output — confirm prop names and shapes)
  </read_first>
  <action>
    Edit `app/admin/assessments/[id]/review/review-client.tsx`:

    1. Widen the function signature on line 32 from `{ submission }: { submission: any }` to:
       ```
       {
         submission,
         schemaJson,
         audioMedia,
       }: {
         submission: any
         schemaJson: Record<string, unknown> | null
         audioMedia: Array<{ field_id: string; storage_path: string; transcript: string | null }>
       }
       ```
       Use the exact prop names from Plan 05 (`schemaJson`, `audioMedia`). Tolerate `null` schemaJson by rendering the panel empty.

    2. Build a flat list of `{ id, label, value }` rows for the raw-answers panel. Algorithm:
       a. Walk `schemaJson?.entities` (Record<string, { type, label?, attributes? }>) — coltorapps shape used elsewhere in this codebase (see `expandRepeatingSections` analog).
       b. For each entity that is a leaf input field (non-section, non-group — anything with `attributes.label` or whose type indicates a field), look up the value:
          - First: `submission.answers_json?.[entityId]` (the typed answer).
          - Fallback: an `audioMedia` row where `field_id === entityId` — use its `transcript ?? "(audio attached, no transcript yet)"`.
          - If neither resolves, show `"—"` (the project's empty-state convention per memory `feedback_no_demo_mocks_in_code.md`).
       c. Display label resolution: prefer `entity.attributes?.label` (string), fall back to the entity id.
       Render rows as `<div class="flex gap-2 py-1.5 border-b border-white/5"><span class="text-white/40 text-xs font-mono w-1/3">{label}</span><span class="text-white/70 text-sm flex-1">{value || "—"}</span></div>`.

    3. Wrap the row list in a `<details>` element (native collapsible — no library dep) with `<summary>` "Raw Answers & STT".
       - Compute `defaultOpen` as a local boolean: `const justGenerated = submission.draft_report_json && !submission.report_storage_path` (heuristic — draft exists but not yet approved; one-time auto-expand on fresh draft per D-04). Open the `<details>` via the `open` attribute when `justGenerated` is true AND `draft` is non-null. Otherwise, render without the `open` attribute so the panel starts collapsed.
       - Style the summary line to match the existing label rhythm: `font-mono uppercase tracking-[0.2em] text-[10px] text-white/40 cursor-pointer hover:text-white/60`.

    4. PLACEMENT: insert the panel between the page Header block (current lines 95-100) and the Executive Summary block (current line 102). Single-column page per D-04 — do NOT introduce a sidebar/grid.

    5. Pass-through behaviour: if `draft === null`, the panel can still render (Matt may want to see raw answers before generating). When `draft === null` the panel auto-expands (so Matt sees the source data immediately).
  </action>
  <verify>
    <automated>grep -n "schemaJson" app/admin/assessments/\[id\]/review/review-client.tsx | head -5; grep -n "audioMedia" app/admin/assessments/\[id\]/review/review-client.tsx | head -5; grep -n "Raw Answers" app/admin/assessments/\[id\]/review/review-client.tsx; grep -n "<details" app/admin/assessments/\[id\]/review/review-client.tsx; grep -n "answers_json" app/admin/assessments/\[id\]/review/review-client.tsx | head -3; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "(review-client|page)\.tsx.*error" || echo "TYPECHECK_OK"</automated>
  </verify>
  <done>
    Component signature accepts `schemaJson` + `audioMedia`. A `<details>` block containing the literal "Raw Answers" is rendered above the Executive Summary block. Row construction reads from `submission.answers_json` and falls back to `audioMedia` transcript by `field_id`. TypeScript clean (incl. closing Plan 05's transient prop error).
  </done>
</task>

<task type="auto">
  <name>Task 2: Retry CTA on ai_draft_failed + deliveryEmailFailed toast branch (REPORT-09, REPORT-11, D-08, D-11)</name>
  <files>app/admin/assessments/[id]/review/review-client.tsx</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-08 toast wording verbatim; D-11 acceptance — retry CTA behaviour)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "D-11 retry CTA" and "Delivery-failed toast" — full code excerpts)
    - app/admin/assessments/[id]/review/review-client.tsx lines 55-90 (existing empty-state branch + handleApprove handler)
  </read_first>
  <action>
    Edit `review-client.tsx`:

    1. Empty-state branch (currently lines 73-90, runs when `!draft`):
       Add `const failed = submission.status === "ai_draft_failed"` just inside the if-block. Then conditionalise the headline, copy, and button label:
       - Headline: `failed ? "AI Draft Failed" : "No AI Draft Yet"`
       - Copy: `failed ? "The previous AI generation failed. See /admin/month-summary for the logged error, then retry." : "The assessment has been submitted. Click below to generate an AI draft from the raw answers."`
       - Button label (existing button at lines 80-87): `generating ? "Generating..." : failed ? "Retry Draft" : "Generate AI Draft"`

       The button still calls the existing `handleGenerate` handler — `generateReportDraft` internally calls `runReportDraftGeneration` which Plan 02 will (a) clear the `ai_draft_failed` state on success, (b) re-log workflow_errors on repeat failure.

    2. handleApprove (currently lines 55-70):
       Change the success branch to inspect `result.deliveryEmailFailed`:
       ```
       const result = await finalizeReport(submission.id, draft)
       if (result.deliveryEmailFailed) {
         toast.warning("Report saved, email retry queued")
       } else {
         toast.success("PDF generated and saved!")
       }
       if (result.downloadUrl) {
         window.open(result.downloadUrl, "_blank")
       }
       router.refresh()
       ```
       Use `toast.warning` (sonner supports it); confirm the import (line 7) covers it — sonner's `toast` is a callable + object with named methods, so the existing import already exposes `.warning`.

       Use the EXACT strings "Report saved, email retry queued" and "PDF generated and saved!" — these match CONTEXT D-08 verbatim. The first one is the user-visible promise that n8n will retry; the second is the existing happy-path copy.

    3. Do NOT introduce a new modal, retry endpoint, or auto-retry timer. D-09 explicitly says rows stay in `ai_draft_failed` until Matt retries from the Review page — manual CTA only.

    REPORT-11 enforcement note: this plan does NOT add any new dispatch path. Matt's explicit Approve click → finalizeReport → email is the ONLY delivery code path. The button label "Approve & Generate PDF →" (existing line 216) already makes the intent explicit; do not change it.
  </action>
  <verify>
    <automated>grep -n "Retry Draft" app/admin/assessments/\[id\]/review/review-client.tsx; grep -n "ai_draft_failed" app/admin/assessments/\[id\]/review/review-client.tsx; grep -n "deliveryEmailFailed" app/admin/assessments/\[id\]/review/review-client.tsx; grep -n "Report saved, email retry queued" app/admin/assessments/\[id\]/review/review-client.tsx; grep -n "PDF generated and saved" app/admin/assessments/\[id\]/review/review-client.tsx; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "review-client\.tsx.*error" || echo "TYPECHECK_OK"</automated>
  </verify>
  <done>
    Empty-state branch checks `submission.status === "ai_draft_failed"` and conditionally renders "Retry Draft" + the month-summary pointer copy. `handleApprove` branches on `result.deliveryEmailFailed` with the two exact toast strings from D-08. No new dispatch/retry path added. TypeScript clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matt's browser → ReviewClient | Authenticated admin surface (existing /admin gate) |
| ReviewClient → finalizeReport Server Action | Already auth-gated server-side (Plan 04) |
| schemaJson + audioMedia → DOM | Admin-only content — surfaced verbatim to Matt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | T (XSS via transcript / answer values) | rendered field row | mitigate | React's default JSX-text rendering escapes — values inserted via `{value}` (text node), not via `dangerouslySetInnerHTML`. AI output is NOT rendered as raw HTML anywhere in this plan (T-07-02-01 cross-ref) |
| T-07-06-02 | T (Spoofed retry click) | Retry Draft button | mitigate | Calls existing `generateReportDraft` Server Action which has its own `auth.getUser()` gate (actions.ts:504-509). No new entry point introduced |
| T-07-06-03 | I (Stale draft displayed after failure) | `submission.status === 'ai_draft_failed'` branch | mitigate | When status flips to `ai_draft_failed`, `draft_report_json` may still hold a prior generation. Empty-state CTA renders ONLY when `!draft`. If a prior draft exists alongside a fresh failure, Matt sees the prior draft + can re-trigger generation via the existing Regenerate button (lines 202-209). Document this nuance in SUMMARY for Plan 07 to confirm via test |
| T-07-06-04 | D (Modal storm on dispatch flap) | deliveryEmailFailed toast | accept | One toast per Approve click; deduped by sonner internal id. Not auto-firing |
| T-07-06-05 | R (Repudiation of Matt's approval) | handleApprove path | mitigate | finalizeReport is the audit source; status='completed' + report_storage_path persisted server-side, not client-side |

</threat_model>

<verification>
- ReviewClient signature widened with no `any`-casts on the prop bag
- Raw Answers & STT collapsible renders above the Executive Summary
- One-time auto-expand: open when draft just generated; collapsed when re-visiting an already-reviewed draft
- ai_draft_failed empty-state renders "Retry Draft" label and points to /admin/month-summary
- handleApprove uses the two D-08 verbatim toast strings
- `npm run build` passes; both `page.tsx` (Plan 05) and `review-client.tsx` (Plan 06) type-check cleanly together
</verification>

<success_criteria>
- D-04 layout shipped: collapsible panel above editable draft, single column
- D-11 retry path shipped: status-driven CTA + copy
- D-08 toast wording shipped verbatim
- REPORT-11 gate preserved: no auto-delivery — Approve click remains the sole trigger
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-06-SUMMARY.md` documenting: signature change, panel placement (line range), retry-CTA logic, toast branch, and a note for Plan 07 on the stale-draft-after-failure nuance (T-07-06-03).
</output>
