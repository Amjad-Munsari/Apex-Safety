---
phase: 07-ai-report-pipeline
plan: 07
type: execute
wave: 4
depends_on: ["07-02", "07-04"]
files_modified:
  - __tests__/phase07/ai-report-pipeline.test.ts
autonomous: true
requirements:
  - REPORT-06
  - REPORT-11
  - REPORT-12

must_haves:
  truths:
    - "Test: runReportDraftGeneration does NOT call dispatchNotification on success (D-06 hard guarantee for REPORT-11)"
    - "Test: runReportDraftGeneration does NOT call dispatchNotification on failure (D-06 cross-check — the email path is exclusively finalizeReport)"
    - "Test: OpenRouter throw → workflow_errors row with workflow_name='ai_report_draft', payload.severity='high', payload.submission_id present (REPORT-12, D-11)"
    - "Test: OpenRouter throw → form_submissions.status flips to 'ai_draft_failed' before the rethrow (D-10 ordering)"
    - "Test: finalizeReport dispatch failure → workflow_errors row with workflow_name='report_delivery_email' AND status stays 'completed' AND return.deliveryEmailFailed === true (D-08, REPORT-06)"
  artifacts:
    - path: "__tests__/phase07/ai-report-pipeline.test.ts"
      provides: "Phase 7 contract tests covering REPORT-06/11/12 + D-06/D-08/D-10/D-11"
      contains: "describe(\"Phase 7"
  key_links:
    - from: "Vitest test file"
      to: "app/admin/assessments/actions.ts"
      via: "module mocks for @ai-sdk/openai, ai, @/lib/notifications/n8n-dispatch, @/lib/supabase/admin"
      pattern: "vi\\.mock"
---

<objective>
Write the contract tests that bind Phase 7's user-invisible guarantees: (REPORT-11/D-06) the draft path is email-free; (REPORT-12/D-10/D-11) AI failures are logged + status flips; (REPORT-06/D-08) email dispatch failures do not roll back the PDF and surface via `deliveryEmailFailed`.

Purpose: These behaviours are easy to regress under future refactor. A single test file using Vitest + module mocks pins them down. The test file lives outside the production paths (`__tests__/phase07/`) so it does not contaminate Plan 02/04/06's file ownership.

Output: Single new test file. No production code changes — Plans 02/04 are the SUTs.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-CONTEXT.md
@.planning/phases/07-ai-report-pipeline/07-PATTERNS.md
@app/admin/assessments/actions.ts
@lib/notifications/n8n-dispatch.ts

<interfaces>
<!-- SUT entry points (after Plans 02 + 04 ship): -->

import { generateReportDraft, finalizeReport } from "@/app/admin/assessments/actions"

<!-- Mocks required: -->
//   vi.mock("ai", () => ({ generateObject: vi.fn() }))
//   vi.mock("@ai-sdk/openai", () => ({ createOpenAI: () => () => ({}) }))
//   vi.mock("@/lib/notifications/n8n-dispatch", () => ({ dispatchNotification: vi.fn() }))
//   vi.mock("@/lib/supabase/admin", () => ({ adminClient: <chainable mock> }))
//   vi.mock("@/lib/supabase/server", () => ({ createClient: async () => ({ auth: { getUser: async () => ({ data: { user: { id: "admin-1" } } }) } }) }))
//   vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))
//   vi.mock("@/lib/pdf/generator", () => ({ generateReportPdfBuffer: vi.fn(async () => Buffer.from("fake-pdf")) }))

<!-- adminClient mock shape: builder pattern returning Promises with { data, error }. -->
//   adminClient.from(table).select(cols).eq(col,val).single() → resolves to { data, error }
//   adminClient.from(table).update(obj).eq(col,val) → resolves to { data, error }
//   adminClient.from(table).insert(obj) → resolves to { data, error }
//   adminClient.storage.from(bucket).upload(...) / .createSignedUrl(...) — similar

<!-- D-11 acceptance translates to test names: -->
//   (a) workflow_errors row inserted
//   (b) status='ai_draft_failed'
//   (c) /admin/month-summary surfaces — covered transitively by (a) since the page reads the same table; not asserted here
//   (d) Review page renders retry CTA — covered by Plan 06's render path; not asserted here
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 7 contract test suite (REPORT-06, REPORT-11, REPORT-12)</name>
  <files>__tests__/phase07/ai-report-pipeline.test.ts</files>
  <behavior>
    Five test cases:
    1. **D-06 / REPORT-11 happy path:** `generateReportDraft("sub-1")` with `generateObject` resolving → adminClient.insert into `workflow_errors` NOT called; adminClient.update on `form_submissions` writes `status: "draft_ready_for_review"`; dispatchNotification mock NOT called (call count === 0).
    2. **D-06 / REPORT-11 failure path:** `generateReportDraft("sub-1")` with `generateObject` rejecting → dispatchNotification mock NOT called (call count === 0) even though the AI failed.
    3. **D-10 / REPORT-12 logging:** Same failure as #2 → adminClient.insert called on `workflow_errors` with exactly `{ workflow_name: "ai_report_draft", error_message: <string>, payload: { submission_id: "sub-1", stack: <any>, severity: "high" } }`; the insert is called BEFORE the status update (assert call order via mock.calls timestamps or sequential mock-call inspection).
    4. **D-10 / D-11 status flip:** Same failure as #2 → adminClient.update on `form_submissions` called with `{ status: "ai_draft_failed" }` and `.eq("id", "sub-1")`.
    5. **D-08 / REPORT-06 dispatch fallback:** `finalizeReport("sub-1", validDraft)` with `dispatchNotification` returning `{ ok: false, error: "n8n down" }` → adminClient.insert called on `workflow_errors` with `workflow_name: "report_delivery_email"`; adminClient.update was ALREADY called with `{ status: "completed", report_storage_path: "<client>/report_sub-1.pdf" }` (status not rolled back); return value `result.deliveryEmailFailed === true` and `result.success === true`.
  </behavior>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-06, D-08, D-10, D-11 — all five tests trace to these)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "D-06 unit-test contract" — declares the dispatchNotification-not-called assertion)
    - app/admin/assessments/actions.ts lines 400-512 (runReportDraftGeneration — confirm catch ordering after Plan 02)
    - app/admin/assessments/actions.ts lines 670-757 (finalizeReport — confirm dispatch shape after Plan 04)
    - lib/notifications/n8n-dispatch.ts (mock target — confirm `dispatchNotification` export name)
    - package.json (confirm Vitest is available; if not, fall back to the project's existing test runner — check `scripts.test`)
    - Any existing test file at `__tests__/` or `**/*.test.ts` (find one analog and mirror its setup: imports, vi.mock placement, top-level describe/it blocks)
  </read_first>
  <action>
    Create `__tests__/phase07/ai-report-pipeline.test.ts`. Structure:

    1. Top-of-file imports + mocks (use Vitest's `vi.mock` calls before any SUT import). Mock the modules listed in the `<interfaces>` block above. The `adminClient` mock must be a chainable proxy where each method returns either a resolvable `{ data, error }` or the next link in the chain — use a small helper that records insert/update/select calls in module-scoped arrays so tests can assert against them.

    2. `describe("Phase 7 — AI report pipeline contract", () => { ... })` with five `it(...)` blocks corresponding to the behaviour bullets above. Test names should embed the relevant decision ID for traceability, e.g. `it("D-06: draft path does NOT call dispatchNotification on success", ...)`.

    3. `beforeEach`: reset all `vi.fn()` mocks AND clear the module-scoped call recorders. Use `vi.resetAllMocks()` then reset per-table recorder arrays.

    4. For test 1 (success path), configure `generateObject` to resolve to a valid `reportSchema` object. Stub the `submission` SELECT to return `{ data: { answers_json: {}, template_version_id: "tv-1" }, error: null }`. Stub the `template_versions` SELECT to return `{ data: { schema_json: { entities: {} } }, error: null }`. Stub the `form_submissions` UPDATE to resolve with `{ error: null }`. Call `await generateReportDraft("sub-1")`. Assert: workflow_errors insert calls.length === 0; dispatchNotification calls.length === 0; one update call with status === "draft_ready_for_review".

    5. For tests 2-4 (failure path), make `generateObject` `mockRejectedValue(new Error("openrouter 500"))`. Call `await expect(generateReportDraft("sub-1")).rejects.toThrow(/Failed to generate report draft/)`. Then assert against the recorder arrays:
       - dispatchNotification calls.length === 0 (test 2)
       - workflow_errors insert was called once with the exact payload shape from behaviour #3 (test 3) — use `expect.objectContaining` for nested payload
       - The order of calls is workflow_errors insert THEN form_submissions update — assert via the order in which they were pushed to a shared call log
       - form_submissions update was called with `{ status: "ai_draft_failed" }` (test 4)

    6. For test 5 (dispatch fallback), configure dispatchNotification to `mockResolvedValue({ ok: false, error: "n8n down" })`. Stub: storage upload `{ error: null }`, two createSignedUrl calls (one for 5-min returning `{ data: { signedUrl: "https://example/short" } }`, one for 7-day returning `{ data: { signedUrl: "https://example/long" } }`), form_submissions UPDATE returning `{ error: null }`. Provide a valid `approvedDraft` object. Call `await finalizeReport("sub-1", approvedDraft)`. Assert:
       - The form_submissions UPDATE was called with `{ draft_report_json: ..., report_storage_path: "<some path>", status: "completed" }` — status is "completed", NOT rolled back
       - The workflow_errors INSERT was called with `workflow_name: "report_delivery_email"`
       - The return value `result.deliveryEmailFailed === true`
       - The return value `result.success === true`
       - The return value `result.downloadUrl === "https://example/short"` (the 5-min URL — NOT the 7-day client URL — must surface to Matt; T-07-04-02 cross-check)

    Run the test file with the project's test command. If Vitest is configured, `npm test -- __tests__/phase07/` should run only this file.

    DO NOT modify production source files in this task — all guarantees must be testable from Plans 02 + 04's outputs as-is. If a test cannot pass because of a real production-code gap, FAIL the verify gate and surface the gap in the SUMMARY so Plan 02 or 04 can be revised — do not paper over with a relaxed assertion.
  </action>
  <verify>
    <automated>npm test -- __tests__/phase07/ai-report-pipeline.test.ts --reporter=verbose 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    Test file exists at `__tests__/phase07/ai-report-pipeline.test.ts`. All five `it(...)` blocks pass. `npm test` exit code 0 for this file. Test names embed the decision IDs (`D-06`, `D-08`, `D-10`). Mock recorder arrays prove call ordering for the workflow_errors-before-status-update invariant. Return-value assertion confirms `downloadUrl === short-url` (5-min) and not the 7-day URL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → production module imports | Tests import real source; mocks intercept side effects (Supabase, AI SDK, n8n) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | T (Test relaxation hiding regression) | Plan 07 test assertions | mitigate | Each assertion ties to a CONTEXT decision ID; SUMMARY records the mapping so any future relaxation requires editing both the test and the SUMMARY |
| T-07-07-02 | I (Real network call) | mock leakage | mitigate | `vi.mock("ai")` and `vi.mock("@/lib/notifications/n8n-dispatch")` ensure no live OpenRouter / n8n call; `OPENROUTER_API_KEY` does not need to be set for the test path because the mocked `createOpenAI` returns a no-op |
| T-07-07-03 | R (Test passes locally, fails CI) | environment drift | accept | Vitest config is established; CI parity assumed |

</threat_model>

<verification>
- File exists at `__tests__/phase07/ai-report-pipeline.test.ts`
- `npm test -- __tests__/phase07/` exit code 0
- All five test names include their decision-ID tag
- No production source modifications introduced by this plan
</verification>

<success_criteria>
- REPORT-06 / REPORT-11 / REPORT-12 each have an explicit failing-then-passing test bound to a CONTEXT decision
- Plans 02 + 04 are now contract-locked — future refactors will break tests if they regress D-06, D-08, D-10, or D-11
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-07-SUMMARY.md` listing: the five test names, which decision IDs each pins, the mock surface, and confirmation that `npm test` exits 0 for the new file.
</output>
