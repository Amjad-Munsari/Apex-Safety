---
phase: 07-ai-report-pipeline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/assessments/[id]/review/page.tsx
autonomous: true
requirements:
  - REPORT-08

must_haves:
  truths:
    - "Review page Server Component fetches the pinned template_versions.schema_json (two-step, never FK-join) and passes it to ReviewClient as a prop"
    - "Review page Server Component fetches field_media audio rows for the submission and passes them to ReviewClient as a prop"
    - "Plan 05 does not touch review-client.tsx — only the page-level fetch (PATTERNS.md correction #2)"
  artifacts:
    - path: "app/admin/assessments/[id]/review/page.tsx"
      provides: "extended Server Component fetch returning schema + audio media"
      contains: "field_media"
  key_links:
    - from: "app/admin/assessments/[id]/review/page.tsx"
      to: "ReviewClient"
      via: "props (schemaJson, audioMedia)"
      pattern: "ReviewClient.*schemaJson|audioMedia"
---

<objective>
Extend the Review page Server Component (`app/admin/assessments/[id]/review/page.tsx`) to fetch the pinned `template_versions.schema_json` and the submission's `field_media` audio rows alongside the existing `form_submissions.*` projection. These props are required by Plan 06's D-04 raw-answers panel and CANNOT be fetched from the client component (Server Component data ownership — PATTERNS.md correction #2).

Purpose: REPORT-08 ("Admin review UI shows the generated draft alongside the raw STT transcript verbatim") needs both the field labels (from the pinned schema) and the audio transcripts (from field_media). The Review page currently only fetches the submission row, so neither is available downstream. Plan 05 fixes the fetch; Plan 06 renders it.

Output: Single-file edit, ~15 lines added. Does NOT touch review-client.tsx (Plan 06's exclusive surface).
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-CONTEXT.md
@.planning/phases/07-ai-report-pipeline/07-PATTERNS.md
@app/admin/assessments/[id]/review/page.tsx
@app/admin/assessments/[id]/review/review-client.tsx
@app/admin/assessments/actions.ts

<interfaces>
<!-- Existing fetch at page.tsx:5-19 (truncated): -->
//   adminClient.from("form_submissions").select(`*`).eq("id", id).single()

<!-- Two-step pinned-template fetch pattern (PATTERNS.md "Shared Patterns"): -->
//   1. Get submission.template_version_id from the existing select
//   2. adminClient.from("template_versions").select("schema_json").eq("id", submission.template_version_id).single()

<!-- field_media schema (used by Phase 14 — see actions.ts:547+ comments): -->
//   field_media { submission_id, field_id, storage_path, transcript, media_type, ... }
//   media_type === 'audio' rows have STT transcripts in the `transcript` column

<!-- Props ReviewClient must receive (Plan 06 implements consumption): -->
//   submission: existing
//   schemaJson: Record<string, unknown> | null   (the pinned schema_json column)
//   audioMedia: Array<{ field_id: string; storage_path: string; transcript: string | null }>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Review page fetch with template_versions schema + field_media audio rows (REPORT-08, D-04)</name>
  <files>app/admin/assessments/[id]/review/page.tsx</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-04 — raw-answers panel data needs)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "review-client.tsx — Server Component data extension required" — full code excerpt; and "Shared Patterns — Two-step pinned-template fetch")
    - app/admin/assessments/[id]/review/page.tsx (entire file, 19 lines)
    - app/admin/assessments/[id]/review/review-client.tsx lines 32-50 (current ReviewClient signature — confirm what new props it will accept; Plan 06 adds the prop wiring)
    - app/admin/assessments/actions.ts lines 412-436 (analog: two-step pinned-schema fetch in runReportDraftGeneration)
  </read_first>
  <action>
    Edit `app/admin/assessments/[id]/review/page.tsx`. Make these changes:

    1. After the existing `submission` fetch (lines 8-12), add the pinned-schema fetch using the two-step pattern:
       ```
       const { data: version } = await adminClient
         .from("template_versions")
         .select("schema_json")
         .eq("id", submission.template_version_id)
         .single()
       ```
       Tolerate missing `version` (Plan 06 renders an empty panel if `schemaJson == null`) — do NOT call `notFound()` on a missing version, only on the submission.

    2. Add the field_media audio fetch:
       ```
       const { data: audioMedia } = await adminClient
         .from("field_media")
         .select("field_id, storage_path, transcript")
         .eq("submission_id", id)
         .eq("media_type", "audio")
       ```
       If `audioMedia` is `null`, coerce to `[]` when passing to the client.

    3. Pass both new values to ReviewClient as props:
       ```
       return <ReviewClient submission={submission} schemaJson={version?.schema_json ?? null} audioMedia={audioMedia ?? []} />
       ```

    DO NOT touch `review-client.tsx` in this plan — Plan 06 owns it. ReviewClient currently accepts only `{ submission }`; Plan 06 will widen the prop interface. To keep Plan 05 type-clean in the meantime, this plan accepts a transient TypeScript error in `page.tsx` of the form `Property 'schemaJson' does not exist on type ...`. Plan 06 (Wave 2, depends on this) closes the error within the same wave-sequenced build. Document the transient error in the SUMMARY.

    Optional mitigation (cleaner if straightforward): pass the new props via a typed wrapper or cast — e.g. `return <ReviewClient {...({ submission, schemaJson: version?.schema_json ?? null, audioMedia: audioMedia ?? [] } as any)} />`. This is acceptable for a one-wave gap and is removed by Plan 06.

    DO NOT change `notFound()` behaviour on missing submission (line 14-16). DO NOT add a third fetch beyond the two specified.
  </action>
  <verify>
    <automated>grep -n "from(\"template_versions\")" app/admin/assessments/\[id\]/review/page.tsx; grep -n "from(\"field_media\")" app/admin/assessments/\[id\]/review/page.tsx; grep -n "media_type" app/admin/assessments/\[id\]/review/page.tsx; grep -n "schemaJson" app/admin/assessments/\[id\]/review/page.tsx; grep -n "audioMedia" app/admin/assessments/\[id\]/review/page.tsx</automated>
  </verify>
  <done>
    `page.tsx` contains: one `from("template_versions")` call selecting `schema_json` keyed on `submission.template_version_id`; one `from("field_media")` call filtering `submission_id == id` AND `media_type == 'audio'`; `ReviewClient` invocation now receives `schemaJson` and `audioMedia` props. `notFound()` still only triggers on missing submission. Plan 06's wave gate (TypeScript build after Plan 06 ships) is what closes any transient type error.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matt's browser → /admin/assessments/[id]/review | Authenticated admin route (existing middleware) |
| Server Component → adminClient | service-role reads to bypass RLS for admin surfaces |
| field_media row → ReviewClient prop | STT transcript content surfaces to Matt only (admin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | I (Cross-org info disclosure) | adminClient reads of field_media | mitigate | Route is admin-only via existing /admin middleware; adminClient is server-side only and never exposed to the client. submission_id filter scopes the field_media query to the single in-context submission |
| T-07-05-02 | I (Schema leakage) | template_versions.schema_json | accept | Schema definitions are reviewed by Matt; not customer secret. Returned to admin client only |
| T-07-05-03 | T (Stale schema mismatch) | unpinned template fetch | mitigate | Two-step pattern (PATTERNS.md "Shared Patterns") keys on `submission.template_version_id` — the PINNED version. Never FK-joins against current published template (Phase 13 RESEARCH Pitfall 2) |
| T-07-05-04 | D | field_media empty / null | mitigate | `?? []` coercion ensures Plan 06's panel renders an empty state instead of throwing |

</threat_model>

<verification>
- `page.tsx` contains the two new fetches with correct column selections
- Props `schemaJson` and `audioMedia` passed to `ReviewClient`
- Two-step fetch (no FK-join) — confirm there is NO `template_versions(` substring inside any `select()` of `form_submissions`
- After Plan 06 ships in Wave 2, `npm run build` is clean. Plan 05 alone may have a transient prop-type error that Plan 06 closes within the same wave gate.
</verification>

<success_criteria>
- Server Component data fetch is the single source of truth for the schema + STT inputs Plan 06 needs
- review-client.tsx untouched (file ownership clean for parallel Wave 2 execution)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-05-SUMMARY.md` documenting: the three queries now executed on page render, the prop names passed to ReviewClient, and any transient TS error to be closed by Plan 06.
</output>
