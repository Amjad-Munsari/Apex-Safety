---
phase: 07-ai-report-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/ai/exemplars/yellow-broom-fra.ts
  - lib/ai/exemplars/site-risk.ts
  - lib/ai/prompt-builder.ts
autonomous: true
requirements:
  - REPORT-03
  - REPORT-04

must_haves:
  truths:
    - "D-02: Importing YELLOW_BROOM_EXEMPLAR returns a non-empty string ≤ 2KB shaped as JSON matching reportSchema"
    - "Importing SITE_RISK_EXEMPLAR resolves to null (stub) until Matt's example arrives"
    - "buildReportPrompt({exemplar, exemplarLabel, expandedAnswers}) returns a string containing the locked persona, locked no-hallucination guard, the labelled exemplar citation, and a JSON-stringified expandedAnswers block — in that order"
  artifacts:
    - path: "lib/ai/exemplars/yellow-broom-fra.ts"
      provides: "YELLOW_BROOM_EXEMPLAR string export"
      contains: "export const YELLOW_BROOM_EXEMPLAR"
    - path: "lib/ai/exemplars/site-risk.ts"
      provides: "SITE_RISK_EXEMPLAR stub export"
      contains: "export const SITE_RISK_EXEMPLAR"
    - path: "lib/ai/prompt-builder.ts"
      provides: "buildReportPrompt pure-function prompt assembler"
      exports: ["buildReportPrompt"]
  key_links:
    - from: "lib/ai/prompt-builder.ts"
      to: "CONTEXT §specifics persona + no-hallucination strings"
      via: "verbatim string constants"
      pattern: "Regulatory Reform.*Fire Safety.*Order 2005"
---

<objective>
Create the `lib/ai/` foundation that Plan 02 consumes — the few-shot exemplar modules (D-02) and the pure `buildReportPrompt` assembler that locks the persona + no-hallucination guard at the top of every prompt.

Purpose: Plan 02 will swap the inline prompt literal at `actions.ts:466-472` for `buildReportPrompt(...)`. That swap is only safe if (a) the exemplar modules exist, (b) the prompt-builder is deterministic and string-bounded (no template lookup, no I/O), and (c) the locked persona / no-hallucination guard strings are exact copies from CONTEXT §specifics. This plan ships those three files as pure modules — no Supabase, no React.

Output: 3 new files under `lib/ai/`. The directory does not yet exist (PATTERNS.md correction #4) so this plan also creates it.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-CONTEXT.md
@.planning/phases/07-ai-report-pipeline/07-AI-SPEC.md
@.planning/phases/07-ai-report-pipeline/07-PATTERNS.md
@lib/form-builder/risk/pas79.ts
@lib/form-builder/expand-repeating-sections.ts

<interfaces>
<!-- Locked text from CONTEXT.md §specifics — copy verbatim, do NOT paraphrase. -->

PERSONA (locked):
"You are a UK Fire Risk Assessor drafting an official report under the Regulatory Reform (Fire Safety) Order 2005. You are assisting Matt Robinson, the competent person, who will review every output before delivery."

NO_HALLUCINATION (locked):
"Every hazard in your output MUST trace to an explicit statement in the input answers. If the data is silent on a topic, omit it — do not infer."

EXEMPLAR_LABEL (when caller uses YELLOW_BROOM_EXEMPLAR):
"YELLOW BROOM 2023 FRA, anonymised"

Target consumer signature (Plan 02 will call this):
buildReportPrompt({
  exemplar: string,
  exemplarLabel: string,
  expandedAnswers: Record<string, unknown>,
}): string

Schema the exemplar JSON must conform to (from actions.ts:455-464):
{
  executiveSummary: string
  hazards: Array<{ location: string; description: string; severity: "Low"|"Medium"|"High"|"Critical"; recommendedAction: string }>
  complianceStatus: "Pass"|"Action Required"|"Fail"
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create yellow-broom-fra exemplar module (REPORT-03, D-02)</name>
  <files>lib/ai/exemplars/yellow-broom-fra.ts</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-02; §specifics — exemplar sanitisation rules)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "lib/ai/exemplars/yellow-broom-fra.ts (NEW — D-02)")
    - lib/form-builder/risk/pas79.ts (analog — header comment style, pure data module pattern)
    - app/admin/assessments/actions.ts lines 455-464 (reportSchema shape the exemplar JSON must match)
  </read_first>
  <action>
    Create the directory `lib/ai/exemplars/` (does not yet exist per PATTERNS.md correction #4) and write `lib/ai/exemplars/yellow-broom-fra.ts`. The file MUST:
    1. Start with the project header comment style from `lib/form-builder/risk/pas79.ts` (path comment + project name + provenance comment citing CONTEXT §D-02).
    2. Export `YELLOW_BROOM_EXEMPLAR` as a `const` string. The string content MUST be a JSON object that matches `reportSchema` (executiveSummary / hazards / complianceStatus) shape verbatim — the LLM uses this as the structural template.
    3. Sanitisation (per CONTEXT §specifics): real client → "Acme Properties Ltd"; site → "12 Example Street"; hazard structure and recommended-action tone preserved. Use 2–4 hazards covering different severities (Low/Medium/High at minimum).
    4. complianceStatus value: "Action Required" (mirrors a real mid-tier FRA outcome).
    5. Total byte size of the exported string ≤ 2KB so we stay well under the gpt-4o-mini context budget.
    6. No imports, no side effects, no functions — pure constant export per D-02 ("loaded once at module init").
    Do NOT include real client identifiers, addresses, or signed signatures. Do NOT use TypeScript-only JSON features (the string itself must be valid JSON.parseable).
  </action>
  <verify>
    <automated>node -e "const m = require('./lib/ai/exemplars/yellow-broom-fra.ts'); /* fallback */" 2>/dev/null; npx tsx -e "import('./lib/ai/exemplars/yellow-broom-fra.ts').then(m => { const s = m.YELLOW_BROOM_EXEMPLAR; if (typeof s !== 'string') throw new Error('not string'); if (Buffer.byteLength(s,'utf8') > 2048) throw new Error('exceeds 2KB: '+Buffer.byteLength(s,'utf8')); const j = JSON.parse(s); if (!j.executiveSummary || !Array.isArray(j.hazards) || !j.complianceStatus) throw new Error('schema mismatch'); for (const h of j.hazards) { if (!['Low','Medium','High','Critical'].includes(h.severity)) throw new Error('bad severity: '+h.severity); } if (!['Pass','Action Required','Fail'].includes(j.complianceStatus)) throw new Error('bad compliance'); console.log('OK', Buffer.byteLength(s,'utf8'), 'bytes,', j.hazards.length, 'hazards'); })"</automated>
  </verify>
  <done>
    File exists; default export-free; named export `YELLOW_BROOM_EXEMPLAR` is a JSON string ≤ 2048 bytes that parses, contains `executiveSummary`/`hazards`/`complianceStatus`, with every hazard severity in the locked enum and complianceStatus in `["Pass","Action Required","Fail"]`. No occurrence of real-client tokens (no `888 Safety`, no `Yellow Broom` real address) inside the JSON content; "Acme Properties Ltd" / "12 Example Street" present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create site-risk stub exemplar (REPORT-04, D-02 deferred)</name>
  <files>lib/ai/exemplars/site-risk.ts</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-02 stub clause; §deferred "Site Risk variant exemplar")
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "lib/ai/exemplars/site-risk.ts (NEW — D-02 stub)")
    - lib/ai/exemplars/yellow-broom-fra.ts (the sibling created in Task 1 — match header comment style)
  </read_first>
  <action>
    Write `lib/ai/exemplars/site-risk.ts` as a stub that keeps the wiring symmetric for REPORT-04 (real content blocked on Matt's example per CONTEXT §deferred). The file MUST:
    1. Start with the same header comment style as Task 1's file, but state explicitly: "STUB — blocked on completed Site Risk example from Matt (07-CONTEXT §deferred). Populate when available; the consumer in lib/ai/prompt-builder.ts will pick the correct exemplar by template type."
    2. Export `SITE_RISK_EXEMPLAR` with TypeScript type `string | null`, initialised to `null`.
    3. No imports, no side effects.
    Document that this is a stub so the next pass (after Matt delivers a Site Risk example) only needs to replace `null` with a string literal — the import surface is already in place.
  </action>
  <verify>
    <automated>npx tsx -e "import('./lib/ai/exemplars/site-risk.ts').then(m => { if (!('SITE_RISK_EXEMPLAR' in m)) throw new Error('missing export'); if (m.SITE_RISK_EXEMPLAR !== null) throw new Error('expected stub null, got: '+typeof m.SITE_RISK_EXEMPLAR); console.log('OK stub'); })"</automated>
  </verify>
  <done>
    File exists with header comment explicitly flagging "STUB" and citing CONTEXT §deferred. Named export `SITE_RISK_EXEMPLAR` resolves to `null` at module load. TypeScript type annotation `string | null` present in the source.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement buildReportPrompt assembler (REPORT-03, D-02 prompt structure)</name>
  <files>lib/ai/prompt-builder.ts</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-02 prompt structure; §specifics — locked persona + no-hallucination strings)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "lib/ai/prompt-builder.ts (NEW)" — full shape sketch)
    - lib/form-builder/expand-repeating-sections.ts (analog — pure function signature pattern)
  </read_first>
  <action>
    Write `lib/ai/prompt-builder.ts`. The file MUST:
    1. Declare two top-level `const` strings at module scope:
       - `PERSONA` — VERBATIM the text from CONTEXT.md §specifics persona line (exactly: "You are a UK Fire Risk Assessor drafting an official report under the Regulatory Reform (Fire Safety) Order 2005. You are assisting Matt Robinson, the competent person, who will review every output before delivery.").
       - `NO_HALLUCINATION` — VERBATIM the text from CONTEXT.md §specifics no-hallucination guard ("Every hazard in your output MUST trace to an explicit statement in the input answers. If the data is silent on a topic, omit it — do not infer.").
       Do NOT paraphrase, normalise punctuation, or substitute em-dashes vs en-dashes. CONTEXT is the source of truth.
    2. Export `buildReportPrompt(args: { exemplar: string; exemplarLabel: string; expandedAnswers: Record<string, unknown> }): string`.
    3. Output order (per D-02 "[system persona] + [no-hallucination rule] + [exemplar block] + [expanded answers JSON]"):
       (a) PERSONA, (b) NO_HALLUCINATION, (c) `Few-shot reference: ${exemplarLabel}` citation line so Matt can audit lineage (CONTEXT §D-02), (d) `${exemplar}`, (e) literal divider line `Now draft a report from these answers:`, (f) `JSON.stringify(expandedAnswers, null, 2)`.
    4. Join blocks with `\n\n`. No template lookup, no env reads, no I/O. Pure function.
    5. No default export.
    Use the analog signature from `lib/form-builder/expand-repeating-sections.ts:19-22` as the function-style reference (typed args object, plain return).
  </action>
  <verify>
    <automated>npx tsx -e "import('./lib/ai/prompt-builder.ts').then(m => { const out = m.buildReportPrompt({ exemplar: '{\"executiveSummary\":\"x\"}', exemplarLabel: 'YELLOW BROOM 2023 FRA, anonymised', expandedAnswers: { door1: { label: 'Front door', condition: 'good' } } }); const checks = [['persona', /Regulatory Reform \(Fire Safety\) Order 2005/], ['no-halluc', /MUST trace to an explicit statement/], ['label', /Few-shot reference: YELLOW BROOM 2023 FRA, anonymised/], ['exemplar', /executiveSummary/], ['answers', /Front door/], ['order-persona-first', /^You are a UK Fire Risk Assessor/]]; for (const [name, re] of checks) { if (!re.test(out)) throw new Error('failed check: '+name); } const personaIdx = out.indexOf('You are a UK Fire Risk Assessor'); const halIdx = out.indexOf('MUST trace'); const labelIdx = out.indexOf('Few-shot reference:'); const answersIdx = out.indexOf('Front door'); if (!(personaIdx < halIdx && halIdx < labelIdx && labelIdx < answersIdx)) throw new Error('ordering wrong'); console.log('OK', out.length, 'chars'); })"</automated>
  </verify>
  <done>
    `buildReportPrompt(...)` returns a string whose substrings appear in order: PERSONA → NO_HALLUCINATION → `Few-shot reference: <label>` → exemplar body → `Now draft a report from these answers:` → JSON-stringified answers. Persona and no-hallucination text match CONTEXT.md §specifics verbatim (regex assertions in verify pass).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static asset → runtime prompt | Exemplar JSON is checked into the repo and never user-mutable; flows into the LLM prompt as a few-shot reference |
| caller → buildReportPrompt | `expandedAnswers` originates from `form_submissions.answers_json` (customer-controlled) and is JSON-stringified into the prompt body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | T (Tampering) | YELLOW_BROOM_EXEMPLAR string | mitigate | Stored as a `const` literal in code; only Matt's reviewed commits can change it; reportSchema check at module-init test guarantees it parses to the locked shape |
| T-07-01-02 | I (Info disclosure) | yellow-broom-fra.ts content | mitigate | Sanitisation contract in task action — real client/site identifiers stripped; verify gate ensures "Acme Properties Ltd" and "12 Example Street" present, not the real source. Header comment cites provenance for audit |
| T-07-01-03 | T (Prompt injection) | expandedAnswers → buildReportPrompt | mitigate | NO_HALLUCINATION guard sits ABOVE the answers block; AI SDK `generateObject` constrains output to Zod schema (Plan 02 enforces this) so injected instructions cannot produce arbitrary text downstream. Answers are JSON.stringified (escaped), not concatenated raw |
| T-07-01-04 | I | PERSONA / NO_HALLUCINATION strings | accept | Public-by-design locked text from CONTEXT; no secret content |
| T-07-01-05 | D (DoS via context bloat) | exemplar size | mitigate | ≤ 2KB byte cap asserted in Task 1 verify; gpt-4o-mini context budget protected |

</threat_model>

<verification>
- `lib/ai/exemplars/yellow-broom-fra.ts`, `lib/ai/exemplars/site-risk.ts`, `lib/ai/prompt-builder.ts` exist
- All three named exports (`YELLOW_BROOM_EXEMPLAR`, `SITE_RISK_EXEMPLAR`, `buildReportPrompt`) are importable
- `YELLOW_BROOM_EXEMPLAR` is a JSON-parseable string ≤ 2048 bytes shaped as `{executiveSummary, hazards[], complianceStatus}` with severity values restricted to the locked enum
- `SITE_RISK_EXEMPLAR === null` (stub contract)
- `buildReportPrompt` output regex-matches PERSONA and NO_HALLUCINATION verbatim, includes the `Few-shot reference: ${label}` citation, and orders persona → guard → exemplar → answers
- `npm run build` succeeds (TypeScript types resolve cleanly across the new files)
</verification>

<success_criteria>
- 3 files created under `lib/ai/`
- All `<automated>` verify commands pass
- No imports from `app/`, `components/`, or Supabase — pure modules suitable for parallel use by Plan 02
- Plan 02 can `import { YELLOW_BROOM_EXEMPLAR } from "@/lib/ai/exemplars/yellow-broom-fra"` and `import { buildReportPrompt } from "@/lib/ai/prompt-builder"` with zero rework
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-01-SUMMARY.md` recording: files created, exemplar byte size, exemplar hazard count, persona/no-hallucination text fidelity (verbatim ✓), and any deviation notes (none expected).
</output>
