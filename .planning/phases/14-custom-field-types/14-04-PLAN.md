---
phase: 14
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - components/form-interpreter/signature-field-renderer.tsx
  - components/form-interpreter/rating-field-renderer.tsx
  - components/form-interpreter/multi-photo-field-renderer.tsx
autonomous: true
tags: [phase-14, renderers, upload-pipeline, signature, rating, multi-photo]
requirements:
  - BUILDER-01
  - BUILDER-02
  - FORM-01
  - FORM-05
  - FORM-06
user_setup: []
must_haves:
  truths:
    - "Signature field draws on a canvas, captures on Done, uploads PNG via uploadMediaAction, stores the storage path in entity.value (D-16)"
    - "Signature field shows uploading spinner overlay while the action is in flight; shows toast on failure; supports Redraw / Remove from preview state"
    - "Rating field renders maxRating stars (default 5) with WCAG 2.5.5 44px touch targets; clicking a star sets the rating; clicking the currently-rated star deselects (returns 0)"
    - "Multi-photo field accepts multiple photos, runs each through useMediaProcessor (HEIC→JPEG + EXIF auto-rotate + compression to 1.2–1.5MB per FORM-06), uploads via uploadMediaAction, stores storage paths array in entity.value (D-17)"
    - "Multi-photo field enforces attrs.maxPhotos cap (default 5; UI-SPEC range 1–20); add cell disappears when at max"
    - "All three renderers correctly accept and propagate the surface prop (dark/cream tokens per Phase 13 pattern) and clientId / submissionId props"
    - "All three renderers reserve a TODO(14-06) marker at the AttachPhotosAffordance integration point — actual rendering lands in Plan 14-06 (D-05, D-06)"
  artifacts:
    - path: "components/form-interpreter/signature-field-renderer.tsx"
      provides: "SignatureFieldRenderer — wraps SignatureField + upload lifecycle + preview/redraw"
      exports: ["SignatureFieldRenderer"]
      min_lines: 80
    - path: "components/form-interpreter/rating-field-renderer.tsx"
      provides: "RatingFieldRenderer — wraps RatingField + WCAG touch targets + score display"
      exports: ["RatingFieldRenderer"]
      min_lines: 60
    - path: "components/form-interpreter/multi-photo-field-renderer.tsx"
      provides: "MultiPhotoFieldRenderer — multi-upload grid + use-media-processor + uploadMediaAction"
      exports: ["MultiPhotoFieldRenderer"]
      min_lines: 100
  key_links:
    - from: "components/form-interpreter/signature-field-renderer.tsx"
      to: "app/admin/assessments/actions.ts uploadMediaAction"
      via: "kind='signature', mediaType='image'"
      pattern: "uploadMediaAction\\([^)]*['\"]signature['\"]"
    - from: "components/form-interpreter/multi-photo-field-renderer.tsx"
      to: "app/admin/assessments/actions.ts uploadMediaAction"
      via: "kind='photo', mediaType='image'"
      pattern: "uploadMediaAction\\([^)]*['\"]photo['\"]"
    - from: "components/form-interpreter/multi-photo-field-renderer.tsx"
      to: "hooks/use-media-processor.ts"
      via: "import { useMediaProcessor }"
      pattern: "useMediaProcessor"
    - from: "All three renderers"
      to: "components/form-interpreter/attach-photos-affordance.tsx (Plan 14-06)"
      via: "conditional render when attrs.attachPhotos === true"
      pattern: "attachPhotos|AttachPhotosAffordance"
---

<objective>
Ship the three "upload-flow" renderers — signature, rating, multi-photo. Each wraps its existing `components/forms/*.tsx` React UI (carried forward from pre-Phase-13) inside the coltorapps `EntityComponentProps` shell, threads the surface / clientId / submissionId props through, calls Plan 14-03's uploadMediaAction for media persistence, and conditionally renders the AttachPhotosAffordance (Plan 14-06 supplies the component; this plan only invokes it).

Purpose: Plan 14-06 (interpreter wiring) needs all six new renderers to exist as importable modules before it can extend the components useMemo map. Splitting the renderer work into 14-04 (these three) and 14-05 (geolocation, computed, repeating-section) keeps each plan under the 50% context target — uploads + grid management is ~30%; computed reactivity + Leaflet + instance state is another ~30%.

Output:
- Three new client component files; each "use client"; each follows the Phase 13 select-field-renderer.tsx scaffolding (surface tokens, EntityComponentProps, label + required + helpText + error).
- Zero new server-side code (uploadMediaAction is already shipped by Plan 14-03).
- Zero modifications to existing renderers or interpreter-renderer.tsx (those land in Plan 14-06).
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-custom-field-types/14-CONTEXT.md
@.planning/phases/14-custom-field-types/14-RESEARCH.md
@.planning/phases/14-custom-field-types/14-PATTERNS.md
@.planning/phases/14-custom-field-types/14-UI-SPEC.md
@.planning/phases/13-form-builder-foundation/13-03-SUMMARY.md
@AGENTS.md
@CLAUDE.md
@components/form-interpreter/select-field-renderer.tsx
@components/form-interpreter/text-field-renderer.tsx
@components/forms/signature-field.tsx
@components/forms/rating-field.tsx
@components/forms/multi-photo-field.tsx
@components/forms/media-field.tsx
@hooks/use-media-processor.ts

<interfaces>
<!-- Existing patterns the executor MUST adapt. -->

From components/form-interpreter/select-field-renderer.tsx (canonical EntityComponentProps + surface tokens scaffold — copy lines 1-46 verbatim, swap entity import + inner content):
```typescript
"use client"
import { cn } from "@/lib/utils"
import type { EntityComponentProps } from "@coltorapps/builder-react"
import type { ENTITY_HERE } from "@/lib/form-builder/entities/X"

type Props = EntityComponentProps<typeof ENTITY_HERE> & { surface?: "dark" | "cream" }

const surfaceTokens = {
  dark:  { label: "text-white/70", required: "text-[#8b2b21]", helpText: "text-white/40", error: "text-[#8b2b21]", /* + per-renderer keys */ },
  cream: { label: "text-[#1a1a1a]", required: "text-[#8b2b21]", helpText: "text-[#6b6560]", error: "text-[#8b2b21]", /* + per-renderer keys */ },
} as const

export function FooRenderer({ entity, setValue, surface = "cream", ...extras }: Props) {
  const t = surfaceTokens[surface]
  const attrs = entity.attributes
  const error = entity.error ? String(entity.error) : undefined
  return (
    <div className="flex flex-col gap-1.5">
      <label className={cn("text-sm font-semibold", t.label)}>
        {attrs.label}
        {attrs.required && <span className={cn("ml-1", t.required)}>*</span>}
      </label>
      {/* INNER UI HERE */}
      {attrs.helpText && <p className={cn("text-xs", t.helpText)}>{attrs.helpText}</p>}
      {error && <p className={cn("text-xs", t.error)}>{error}</p>}
      {attrs.attachPhotos && <AttachPhotosAffordance submissionId={submissionId} entityId={entity.id} fieldLabel={attrs.label as string} surface={surface} clientId={clientId} />}
    </div>
  )
}
```

NEW additional prop pattern for Phase 14 renderers — clientId + submissionId required for upload-flow renderers (signature, multi-photo, attach-photos-affordance). NOT needed for rating (no upload). PATTERNS §"signature-field-renderer.tsx" lines 366-378 documents this.

From components/forms/signature-field.tsx (the existing canvas component — read the file to understand its props):
- It owns the canvas, the Clear / Done buttons, the preview state, and the mouse/touch event handlers
- It exposes a `value` prop and an `onChange(dataUrl: string | undefined)` callback
- The renderer wraps it and converts the dataUrl → server upload → storage path

From components/forms/rating-field.tsx (the existing star UI):
- Renders 5 (or attrs.maxRating) star buttons
- Each button has aria-label="Rate N of MAX"
- onChange(rating: number)
- Already implements click-same-star-to-deselect

From components/forms/multi-photo-field.tsx + components/forms/media-field.tsx (the existing photo grid):
- Receives a `photos: PhotoItem[]` array
- `onAdd(files: FileList)` and `onRemove(index: number)` callbacks
- Uses object URL previews; the renderer is responsible for the upload → storage-path conversion

From hooks/use-media-processor.ts (canonical photo-processing hook):
- Takes a File; returns a Promise<{blob, dataUrl}> after HEIC→JPEG + EXIF auto-rotate + compression to 1.2-1.5MB
- Already wired in components/forms/media-field.tsx

From app/admin/assessments/actions.ts uploadMediaAction (Plan 14-03):
- Signature: `(submissionId: string, fieldId: string, fileDataUrl: string, mediaType: "image"|"audio", clientId: string, kind: "signature"|"photo"): Promise<string>`
- Returns the storage path on success
- Throws on auth/MIME/size/upload failure

AttachPhotosAffordance component (Plan 14-06 ships this; this plan just imports it):
- `<AttachPhotosAffordance submissionId={string} entityId={string} fieldLabel={string} surface={"dark"|"cream"} clientId={string} />`
- Renders the 📎 affordance + thumbnail strip; calls uploadMediaAction internally
- Renderers in this plan add `{attrs.attachPhotos && <AttachPhotosAffordance ... />}` as the last child of their root div

Per UI-SPEC §"signatureFieldRenderer":
- Canvas height hardcoded 180px (already in components/forms/signature-field.tsx)
- States: idle / drawing / has-strokes / signed / uploading / error
- Uploading: Loader2 spinner overlay + "Saving signature…" label
- Error: Sonner toast "Failed to save signature. Try again."

Per UI-SPEC §"ratingFieldRenderer":
- Star size 28px (h-7 w-7) with p-1 padding (36px total)
- WCAG 2.5.5 requires 44px touch target — WRAP each star button in `min-w-[44px] min-h-[44px]` container
- Score display "{current} / {max}" in 14px JetBrains Mono tabular-nums, right of star row
- Click-same-star deselects to 0

Per UI-SPEC §"multiPhotoFieldRenderer":
- Grid: grid-cols-2 mobile, sm:grid-cols-3, gap-3
- Cells: aspect-square, rounded-sm, object-cover
- Remove button: h-6 w-6 destructive, hover visible (desktop), always visible (mobile)
- Add cell visible only when photos.length < maxPhotos
- accept="image/*" capture="environment" on the file input
- Loader2 spinner in add cell while isProcessing
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SignatureFieldRenderer — canvas wrapper + upload lifecycle + preview/redraw/remove</name>
  <files>components/form-interpreter/signature-field-renderer.tsx</files>
  <read_first>
    - components/form-interpreter/select-field-renderer.tsx (CANONICAL EntityComponentProps + surface tokens scaffold)
    - components/forms/signature-field.tsx (the existing canvas UI to wrap; read its prop signature carefully — onChange shape, value shape, internal states)
    - .planning/phases/14-custom-field-types/14-PATTERNS.md §"signature-field-renderer.tsx" lines 357-410 (full wrapper shape)
    - .planning/phases/14-custom-field-types/14-UI-SPEC.md §"signatureFieldRenderer" lines 171-188 (state machine, copy strings, error handling)
    - .planning/phases/14-custom-field-types/14-RESEARCH.md §"Pattern 5" (uploadMediaAction contract — kind='signature' arg)
    - .planning/phases/14-custom-field-types/14-CONTEXT.md D-16 (signature path) and D-05 (attachPhotos affordance)
    - lib/form-builder/entities/signature-field.ts (Plan 14-02 — for the entity type import)
  </read_first>
  <behavior>
    - SignatureFieldRenderer is a "use client" forwardRef? No — same shape as SelectFieldRenderer (plain function component).
    - Accepts `entity, setValue, surface = "cream", clientId, submissionId` props (Props type extends EntityComponentProps<typeof signatureFieldEntity>).
    - Renders the standard label + required asterisk + helpText + error structure from select-field-renderer.tsx.
    - Inner UI: renders <SignatureField> with `value={localPreviewDataUrl}` (local state) and `onChange={async (dataUrl) => { ... }}` callback.
    - onChange flow:
      1. If dataUrl is undefined (user cleared): `setValue(undefined)` and clear localPreview.
      2. Else: setIsUploading(true), call `uploadMediaAction(submissionId, entity.id, dataUrl, "image", clientId, "signature")`. On success: setValue(storagePath); keep localPreview for instant display. On failure: `toast.error("Failed to save signature. Try again.")` (Sonner). Finally: setIsUploading(false).
    - Uploading state: show a Loader2 spinner overlay on the canvas area with "Saving signature…" 12px mono label. Pass `disabled` prop to SignatureField (or wrap in absolutely-positioned overlay with pointer-events-none on the canvas area beneath).
    - Preview state: if entity.value is a string (storage path), show the captured-signature preview. UI-SPEC allows either keeping the base64 local preview OR fetching a signed URL via adminClient.storage.createSignedUrl — the renderer keeps the local base64 preview in state for the current session (no signed-URL round-trip on first render); on a fresh page load with only the storage path in entity.value, the renderer shows a placeholder "Signature captured" pill with the storage path and a Redraw button. Document the trade-off in a JSDoc comment (deferring signed-URL display avoids an extra network round-trip on every render but loses the visual on reload — acceptable per the existing assessment flow which submits within one session).
    - Redraw / Remove from preview state: Redraw clears entity.value (setValue(undefined)) and returns to draw mode. Remove also calls setValue(undefined).
    - {attrs.attachPhotos && <AttachPhotosAffordance ... />} as the last child of the root div. Import AttachPhotosAffordance from "./attach-photos-affordance" — this file does not exist yet but will be shipped by Plan 14-06; Plan 14-06 explicitly declares this dependency. Avoid a TS resolution failure: add a tsconfig path or, simpler, declare a stub interface in this plan and document the cross-plan dependency in the JSDoc. Cleanest approach: ship Plan 14-04 with the import line commented out plus a TODO marker `// TODO(14-06): import { AttachPhotosAffordance } from "./attach-photos-affordance"`, and replace `{attrs.attachPhotos && <AttachPhotosAffordance ... />}` with `{/* TODO(14-06): AttachPhotosAffordance lands in Plan 14-06; rendered here when attrs.attachPhotos */}`. Plan 14-06 will uncomment the import and the JSX. This avoids the cross-plan TS error.
    - Accessibility (UI-SPEC §Accessibility Contract): canvas wrapper carries `role="img"` with `aria-label="Signature pad — draw your signature"`. Clear / Done buttons inherit from SignatureField (no change needed). Preview img alt="Captured signature".
  </behavior>
  <action>
    Create components/form-interpreter/signature-field-renderer.tsx as a "use client" component. Start with select-field-renderer.tsx as the scaffold (copy lines 1-46 — imports, surfaceTokens, Props type, function signature, opening div + label). Swap the entity import to signatureFieldEntity, extend Props with `clientId: string; submissionId: string`.

    Inside the function body:
    - State: `const [localPreview, setLocalPreview] = useState<string | undefined>(undefined)` (base64 data URL during the current session)
    - State: `const [isUploading, setIsUploading] = useState(false)`
    - State: read `value = entity.value` (string storage path) and `error = entity.error ? String(entity.error) : undefined`
    - Compose the SignatureField wrapper. SignatureField's existing props: read components/forms/signature-field.tsx first to get the exact prop names. Pass value={localPreview ?? value}, onChange={handleChange}, surface={surface}. If SignatureField doesn't accept a `disabled` prop, wrap it in `<div className={cn("relative", isUploading && "pointer-events-none")}>` and overlay `{isUploading && <div className="absolute inset-0 flex items-center justify-center bg-black/30"><Loader2 className="animate-spin" /> Saving signature…</div>}`.
    - handleChange: see Behavior section above. Wrap in `async (dataUrl) => { ... }` with try / catch / finally. Import `toast` from "sonner" and `uploadMediaAction` from "@/app/admin/assessments/actions".
    - For attachPhotos affordance: leave the JSX commented per the Behavior note. Plan 14-06 will uncomment.

    Add JSDoc above the export documenting D-05, D-06, D-16, the cross-plan AttachPhotosAffordance dependency, and the local-preview-only display contract.

    There is no dedicated unit test file for this renderer in this task — the renderers are visually-verified components, and a smoke test that asserts <MicButton> presence in the DOM is the wave-level concern (Plan 14-06 owns tests/form-interpreter/renderers.test.ts). For this task, the verify gate is the TypeScript compile + a Vitest smoke test that the file imports cleanly and exports SignatureFieldRenderer.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/form-interpreter/signature-field-renderer" | grep -v "^$" && exit 1 || exit 0</automated>
    <automated>node -e "const s=require('fs').readFileSync('components/form-interpreter/signature-field-renderer.tsx','utf8'); if (!/export function SignatureFieldRenderer/.test(s)) process.exit(1); if (!/uploadMediaAction/.test(s)) process.exit(2); if (!/['\"]signature['\"]/.test(s)) process.exit(3); /* kind arg */ if (!/clientId/.test(s)) process.exit(4); if (!/submissionId/.test(s)) process.exit(5);"</automated>
  </verify>
  <done>SignatureFieldRenderer exists, exports correctly, typechecks, imports uploadMediaAction with kind='signature', threads clientId/submissionId props. AttachPhotosAffordance integration deferred to Plan 14-06 via documented TODO. Renderer is wired into the components map in Plan 14-06 and covered by `tests/form-interpreter/renderers.test.tsx` — this plan ships the renderer as a structurally-sampled importable module; behavioral sampling continuity is owned by 14-06's renderer suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RatingFieldRenderer — star row with WCAG touch targets + score display + deselect-on-same-click</name>
  <files>components/form-interpreter/rating-field-renderer.tsx</files>
  <read_first>
    - components/form-interpreter/select-field-renderer.tsx (CANONICAL scaffold — copy header + token shape)
    - components/forms/rating-field.tsx (existing star UI — confirm prop signature; this renderer wraps it OR re-implements the row directly if the existing component is not a clean fit)
    - .planning/phases/14-custom-field-types/14-PATTERNS.md §"rating-field-renderer.tsx" lines 414-419
    - .planning/phases/14-custom-field-types/14-UI-SPEC.md §"ratingFieldRenderer" lines 190-198 (44px touch target, score display, deselect behaviour)
    - .planning/phases/14-custom-field-types/14-CONTEXT.md D-05 (attachPhotos)
    - lib/form-builder/entities/rating-field.ts (Plan 14-02 — for entity import + attrs.maxRating)
  </read_first>
  <behavior>
    - RatingFieldRenderer is a "use client" function component, EntityComponentProps<typeof ratingFieldEntity> & { surface?: "dark"|"cream" }.
    - Read `const maxRating = (attrs.maxRating as number) ?? 5`.
    - Read current value `const current = (entity.value as number | undefined) ?? 0`.
    - Render an array of maxRating star buttons, each wrapped in `<div className="min-w-[44px] min-h-[44px] flex items-center justify-center">` for WCAG 2.5.5 (UI-SPEC mandates the wrapper approach).
    - Each star: <button type="button" aria-label={`Rate ${n} of ${maxRating}`} onClick={() => onStarClick(n)}> with a Star (or filled Star) icon at h-7 w-7.
    - Filled vs empty: if n <= current, the star is filled (fill="currentColor" + amber accent). Otherwise outline.
    - onStarClick(n): if n === current, setValue(0) (deselect — UI-SPEC contract). Otherwise setValue(n).
    - To right of star row: score display `<span className="font-mono text-sm tabular-nums">{current} / {maxRating}</span>` in the t.label colour.
    - Container: `<div role="group" aria-label={attrs.label as string}>...</div>` (UI-SPEC Accessibility).
    - Standard label + required + helpText + error wrapper from the canonical scaffold.
    - {attrs.attachPhotos && <AttachPhotosAffordance ... />} commented per Task 1 note.
    - NO clientId / submissionId props on this renderer (rating doesn't upload). The interpreter components map (Plan 14-06) will pass them only to renderers that need them.
  </behavior>
  <action>
    Create components/form-interpreter/rating-field-renderer.tsx. Copy the select-field-renderer.tsx scaffold (lines 1-46). Replace entity type import with ratingFieldEntity. Do not extend Props with clientId/submissionId.

    Implement the star row inline (do not wrap the existing components/forms/rating-field.tsx — its prop shape might not match the surface+attrs contract cleanly; an inline implementation is ~40 lines and stays self-contained per PATTERNS § role-match note). Use the Star icon from lucide-react.

    Implementation order:
    1. const maxRating = (attrs.maxRating as number) ?? 5
    2. const current = (entity.value as number | undefined) ?? 0
    3. const handleStarClick = (n: number) => setValue(n === current ? 0 : n)
    4. JSX: outer div with the standard label + required + helpText + error wrapper. Star row as `<div role="group" aria-label={attrs.label as string} className="flex items-center gap-1">{Array.from({length: maxRating}, (_, i) => i+1).map(n => <button ...>...</button>)}<span className="font-mono text-sm tabular-nums ml-3">{current} / {maxRating}</span></div>`.
    5. attachPhotos affordance comment as in Task 1.

    Add JSDoc citing D-05 and UI-SPEC §ratingFieldRenderer.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/form-interpreter/rating-field-renderer" | grep -v "^$" && exit 1 || exit 0</automated>
    <automated>node -e "const s=require('fs').readFileSync('components/form-interpreter/rating-field-renderer.tsx','utf8'); if (!/export function RatingFieldRenderer/.test(s)) process.exit(1); if (!/min-w-\\[44px\\]/.test(s)) process.exit(2); /* WCAG */ if (!/role=['\"]group['\"]/.test(s)) process.exit(3); if (!/maxRating/.test(s)) process.exit(4);"</automated>
  </verify>
  <done>RatingFieldRenderer exists, typechecks, has min-w-[44px] WCAG wrappers, role="group" container, deselect-on-same-click logic, score display. No clientId/submissionId props (correct — no upload). Renderer is wired into the components map in Plan 14-06 and covered by `tests/form-interpreter/renderers.test.tsx` — this plan ships the renderer as a structurally-sampled importable module; behavioral sampling continuity is owned by 14-06's renderer suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MultiPhotoFieldRenderer — grid + useMediaProcessor + per-photo upload + maxPhotos cap</name>
  <files>components/form-interpreter/multi-photo-field-renderer.tsx</files>
  <read_first>
    - components/form-interpreter/select-field-renderer.tsx lines 48-96 (allowMultiple branch — pattern for array-value renderer with per-item state)
    - components/forms/multi-photo-field.tsx (existing photo grid UI; read its prop shape)
    - components/forms/media-field.tsx (existing media field component; the useMediaProcessor wiring lives here)
    - hooks/use-media-processor.ts (the canonical hook — HEIC→JPEG + EXIF + compression to 1.2-1.5MB; FORM-06)
    - .planning/phases/14-custom-field-types/14-PATTERNS.md §"multi-photo-field-renderer.tsx" lines 421-427
    - .planning/phases/14-custom-field-types/14-UI-SPEC.md §"multiPhotoFieldRenderer" lines 201-213
    - .planning/phases/14-custom-field-types/14-CONTEXT.md D-17 (photo storage path) and D-05 (attachPhotos)
    - lib/form-builder/entities/multi-photo-field.ts (Plan 14-02 — entity import + maxPhotos attribute)
    - app/admin/assessments/actions.ts uploadMediaAction (Plan 14-03 — kind='photo' arg)
  </read_first>
  <behavior>
    - MultiPhotoFieldRenderer is "use client". Props: EntityComponentProps<typeof multiPhotoFieldEntity> & { surface, clientId, submissionId }.
    - Read `const maxPhotos = (attrs.maxPhotos as number) ?? 5`.
    - Read `const photos = (entity.value as string[]) ?? []` (array of storage paths).
    - Local state: `const [pendingItems, setPendingItems] = useState<Array<{id: string; previewUrl: string; status: 'processing'|'uploading'|'error'}>>([])` for in-flight uploads (each pending item is staged with an object-URL preview while processing + uploading; on success the storage path is appended to entity.value and the pending item is removed).
    - Grid: `<div className="grid grid-cols-2 sm:grid-cols-3 gap-3">`. Cells: each is `<div className="aspect-square rounded-sm overflow-hidden border relative"><img src={...} className="object-cover" /><button aria-label={`Remove photo ${i+1}`} className="absolute top-1 right-1 h-6 w-6 ...">×</button></div>`.
    - Add cell (only when `photos.length + pendingItems.length < maxPhotos`): `<label className="aspect-square rounded-sm border-2 border-dashed flex items-center justify-center cursor-pointer"><input type="file" accept="image/*" capture="environment" multiple className="sr-only" onChange={handleFiles} /><Plus className="h-6 w-6" /><span>Add Photo</span></label>`. When isProcessing, show Loader2 instead of Plus.
    - handleFiles flow: for each File selected:
      1. Generate a pending id (crypto.randomUUID()), createObjectURL for preview, set status 'processing', push to pendingItems.
      2. Call `useMediaProcessor` (or the equivalent processor function) → returns {blob, dataUrl}.
      3. Update pending item status to 'uploading'.
      4. Call `uploadMediaAction(submissionId, entity.id, dataUrl, "image", clientId, "photo")` → returns storagePath.
      5. On success: append storagePath to a fresh value array and call setValue([...photos, storagePath]). Remove the pending item.
      6. On failure: set pending item status to 'error' (cell shows AlertCircle + "Failed" overlay; tap retries). Toast: "Photo could not be processed. Try a different image." (UI-SPEC §Error States).
    - Remove button on existing photo: setValue(photos.filter((_, i) => i !== removedIndex)). NOTE: this does NOT delete the storage object — that's a Phase 16+ concern (orphan cleanup); document in JSDoc.
    - Count label below grid: `<span className="font-mono text-xs uppercase tracking-wider">{photos.length} / {maxPhotos}</span>`.
    - Standard label + required + helpText + error wrapper.
    - {attrs.attachPhotos && <AttachPhotosAffordance ... />} commented per Task 1 note.
  </behavior>
  <action>
    Create components/form-interpreter/multi-photo-field-renderer.tsx. Copy the select-field-renderer.tsx scaffold (lines 1-46) for the surface tokens + label/error pattern. Replace entity import with multiPhotoFieldEntity. Extend Props with clientId + submissionId.

    Read components/forms/media-field.tsx carefully — its useMediaProcessor wiring is the proven pattern. The renderer in this task either (a) imports useMediaProcessor directly and wires it inline, or (b) wraps an adapted version of MultiPhotoField from components/forms/multi-photo-field.tsx. Prefer (a) — inline implementation; the existing components/forms/*.tsx files have prop shapes designed for the pre-coltorapps era and adapting them adds prop-shimming overhead. The grid + add-cell + remove button code is ~60 lines inline; clearer than a bridge component.

    State management:
    - photos: read from entity.value
    - pendingItems: useState<PendingPhoto[]> where PendingPhoto = { id, previewUrl, status, error? }
    - On unmount or on photo committed, revoke the object URL: `URL.revokeObjectURL(pendingItem.previewUrl)` in a useEffect cleanup or in the success branch.

    For useMediaProcessor: if the hook signature is per-call (returns a process function), call it inside handleFiles. Read hooks/use-media-processor.ts to confirm. If the hook is shaped for an existing component contract that doesn't fit cleanly, extract just the processing function (HEIC→JPEG → EXIF rotate → compress) as a standalone async helper inline in this file — it's no more than 20 lines around browser-image-compression + heic2any, both already installed.

    Wire uploadMediaAction with kind='photo'. Pass mediaType='image'. The action returns the storage path which we append to the entity.value array via setValue.

    Document the orphan-cleanup deferral in a JSDoc block: "Removing a photo from the UI only updates entity.value. The underlying storage object and field_media row are not deleted — orphan cleanup is a Phase 16 concern. The current MVP cost is small (admin-only fill flow, photos remain accessible via Supabase Studio if needed)."

    Accessibility (UI-SPEC §Accessibility Contract): each remove button has `aria-label="Remove photo {N}"`. The file input has `aria-label="Add photo to {field label}"`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/form-interpreter/multi-photo-field-renderer" | grep -v "^$" && exit 1 || exit 0</automated>
    <automated>node -e "const s=require('fs').readFileSync('components/form-interpreter/multi-photo-field-renderer.tsx','utf8'); if (!/export function MultiPhotoFieldRenderer/.test(s)) process.exit(1); if (!/uploadMediaAction/.test(s)) process.exit(2); if (!/['\"]photo['\"]/.test(s)) process.exit(3); if (!/maxPhotos/.test(s)) process.exit(4); if (!/clientId/.test(s) || !/submissionId/.test(s)) process.exit(5); if (!/grid-cols-2/.test(s)) process.exit(6); if (!/aria-label/.test(s)) process.exit(7);"</automated>
  </verify>
  <done>MultiPhotoFieldRenderer exists, typechecks, imports uploadMediaAction with kind='photo', threads clientId/submissionId, implements the grid + add-cell + maxPhotos cap + remove-with-orphan-deferral. Accessibility labels on remove and file input. Renderer is wired into the components map in Plan 14-06 and covered by `tests/form-interpreter/renderers.test.tsx` — this plan ships the renderer as a structurally-sampled importable module; behavioral sampling continuity is owned by 14-06's renderer suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (canvas / file picker) → renderer state | Untrusted media bytes; client-side processing (use-media-processor) is the first filter; server-side uploadMediaAction is the authoritative one |
| Renderer → uploadMediaAction | clientId / submissionId / fieldId are passed in; the action enforces MIME + size + path-prefix (Plan 14-03's threat model covers this) |
| Object URLs (createObjectURL) | Bound to the renderer's lifecycle; must be revoked on cleanup to prevent memory leaks |
| Storage paths in entity.value | Become part of answers_json on submit; same trust as any other text value (Phase 13 entity validation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Information disclosure | URL.createObjectURL leak (memory) if pending items aren't revoked | mitigate | useEffect cleanup that revokes all pendingItems[].previewUrl URLs on unmount; explicit revoke when a pending item transitions to success (its path replaces the preview in the grid). |
| T-14-04-02 | DoS | User selects 100 photos at once via the multi-file input | mitigate | Renderer guards: only enqueue up to (maxPhotos - photos.length) files per selection. Excess files are dropped silently with a toast "Only N photos can be added; the first N were queued." The server-side action additionally rejects oversized payloads per Plan 14-03. |
| T-14-04-03 | Tampering | A user pastes a base64 PNG that contains EXIF GPS metadata into the signature flow (unlikely but possible if they crafted the canvas) | accept | browser-image-compression strips EXIF on processed photos; canvas-rendered signatures never carry EXIF (canvas doesn't add it). Acceptable. |
| T-14-04-04 | XSS | A signature data URL is rendered in the preview state | mitigate | Signature data URLs are output from canvas.toDataURL("image/png") — controlled by the canvas, not user input. When showing as <img src={dataUrl}>, the src is a data URL — modern browsers do not execute scripts in data:image/png URIs. SVG would be a different story; signatures are png only per D-16. Plan 14-03 server-side MIME guard ensures storage path values returned are png. |
| T-14-04-05 | Information disclosure | Signature preview after page reload requires a signed-URL round-trip; we defer this | accept | Documented: preview shown from local state during session; "Signature captured" pill shown on reload. No actual data disclosure; UX trade-off only. Phase 16 may add a signed-URL fetcher when client-surface fill lands. |
| T-14-04-06 | UI redress | Add-cell file input is hidden via sr-only and triggered by a label — could be tricked into accepting drops from unintended sources | accept | Standard file input behaviour; users see and approve via OS dialog. No drag-drop spoofing surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` — zero errors in components/form-interpreter/ paths for the three new files
- Structural greps prove each renderer:
  - Exports correctly
  - Calls uploadMediaAction with the right `kind` argument (signature / photo)
  - Threads surface / clientId / submissionId where required
  - Has WCAG affordances (rating's min-w-[44px], multi-photo's aria-label on remove)
- No existing renderer modifications — only three new files created. `git diff --stat` shows three additions.
- The three renderers are NOT yet wired into interpreter-renderer.tsx — Plan 14-06 owns that. This plan ships them as importable but inactive modules. AttachPhotosAffordance integration deferred to 14-06 via documented TODO comments.
</verification>

<success_criteria>
1. Three new files exist: signature-field-renderer.tsx, rating-field-renderer.tsx, multi-photo-field-renderer.tsx.
2. SignatureFieldRenderer implements the full upload lifecycle (canvas → uploadMediaAction → storagePath in entity.value) with toast-on-error and Loader2 overlay-on-upload.
3. RatingFieldRenderer renders maxRating stars wrapped in min-w-[44px] WCAG containers, deselect-on-same-click, score display, role="group" accessibility.
4. MultiPhotoFieldRenderer implements grid + add-cell + maxPhotos cap, runs each file through useMediaProcessor (HEIC + EXIF + compression), uploads via uploadMediaAction with kind='photo', revokes object URLs on cleanup.
5. All three renderers use the canonical surface-tokens pattern from select-field-renderer.tsx (dark/cream).
6. None of the three renderers modifies an existing file — only new files created.
7. AttachPhotosAffordance integration is a documented TODO in each renderer pointing to Plan 14-06.
8. TypeScript zero errors. Structural greps prove the contracts above.
9. Phase 13 + Plan 14-01 + Plan 14-02 + Plan 14-03 tests still GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/14-custom-field-types/14-04-SUMMARY.md`.
</output>
