---
phase: 14
plan: 05
type: execute
wave: 2
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - components/form-interpreter/geolocation-map.tsx
  - components/form-interpreter/geolocation-field-renderer.tsx
  - components/form-interpreter/computed-field-renderer.tsx
  - components/form-interpreter/repeating-section-renderer.tsx
autonomous: true
tags: [phase-14, renderers, leaflet, pas79, repeating-section, computed]
requirements:
  - BUILDER-01
  - BUILDER-02
  - BUILDER-03
  - FORM-01
user_setup: []
must_haves:
  truths:
    - "Geolocation renderer calls navigator.geolocation.getCurrentPosition on every device (D-11) and never blocks the fill flow"
    - "Geolocation renderer shows the desktop-accuracy badge ('Desktop capture — verify pin on map') when (a) UA does not match mobile pattern OR (b) accuracy > 100m (D-12)"
    - "Geolocation map preview is dynamically imported with { ssr: false } so Leaflet's window usage does not break the Next.js build (RESEARCH Pitfall 1 + 2)"
    - "Geolocation map allows click-to-pin (D-13) — clicking the map updates entity.value to the clicked lat/lng"
    - "Leaflet default marker uses the public/leaflet/*.png assets shipped by Plan 14-01 (RESEARCH Pitfall 1)"
    - "Computed field is read-only and subscribes to its declared input entity values via useInterpreterEntitiesValues — recomputes on every dependency change (D-07, D-10)"
    - "Computed field renders the PAS 79 risk badge with the correct Tailwind class triplet (green/amber/red) per the score band; renders 'Fill in likelihood and consequence' placeholder when either input is unset"
    - "Computed field handles 'inputs not configured' state gracefully — shows 'Computed field not configured — set inputs in the builder.' (UI-SPEC §computedField-specific)"
    - "RepeatingSection renderer produces N instances of the declared template-children entity set with independent value state per instance, owned in local React state and mirrored to entity.value via setValue({instances: [...]}) on every change (D-02, D-04, RESEARCH Pitfall 3)"
    - "RepeatingSection renderer enforces attrs.maxInstances (Add button disabled at max) and surfaces attrs.minInstances violation message on submit attempt"
    - "RepeatingSection each instance card supports collapse/expand and remove (no confirmation dialog, per UI-SPEC §Destructive Actions)"
  artifacts:
    - path: "components/form-interpreter/geolocation-map.tsx"
      provides: "GeoMap default export — Leaflet MapContainer + TileLayer + Marker + click-handler (ssr-safe consumer)"
      min_lines: 50
    - path: "components/form-interpreter/geolocation-field-renderer.tsx"
      provides: "GeolocationFieldRenderer — capture panel + dynamic-imported GeoMap + accuracy badge"
      exports: ["GeolocationFieldRenderer"]
      min_lines: 100
    - path: "components/form-interpreter/computed-field-renderer.tsx"
      provides: "ComputedFieldRenderer — read-only PAS 79 badge with reactive recompute"
      exports: ["ComputedFieldRenderer"]
      min_lines: 70
    - path: "components/form-interpreter/repeating-section-renderer.tsx"
      provides: "RepeatingSectionRenderer — instance state machine + child mini-form rendering"
      exports: ["RepeatingSectionRenderer"]
      min_lines: 150
  key_links:
    - from: "components/form-interpreter/geolocation-field-renderer.tsx"
      to: "components/form-interpreter/geolocation-map.tsx"
      via: "dynamic(() => import('./geolocation-map'), { ssr: false })"
      pattern: "dynamic\\(\\s*\\(\\s*\\)\\s*=>\\s*import\\(['\"]\\./geolocation-map['\"]"
    - from: "components/form-interpreter/computed-field-renderer.tsx"
      to: "lib/form-builder/risk/pas79.ts"
      via: "import { computePAS79RiskLevel }"
      pattern: "computePAS79RiskLevel"
    - from: "components/form-interpreter/computed-field-renderer.tsx"
      to: "@coltorapps/builder-react useInterpreterEntitiesValues"
      via: "selective subscription on [likelihoodId, consequenceId]"
      pattern: "useInterpreterEntitiesValues"
    - from: "components/form-interpreter/repeating-section-renderer.tsx"
      to: "@coltorapps/builder setValue"
      via: "setValue({instances: [...]}) — full array replacement on every change (Pitfall 3)"
      pattern: "setValue\\(\\s*\\{\\s*instances"
---

<objective>
Ship the three remaining specialty renderers — geolocation (with the dynamic-imported Leaflet map and desktop-accuracy badge), computed (read-only PAS 79 badge with reactive recompute), and repeating-section (the most complex renderer, owning instances state locally and mirroring it to the interpreter store).

Purpose: Plan 14-04 shipped the upload-flow renderers; Plan 14-05 ships the three derived/container-flavoured renderers. Splitting renderer work between two parallel plans keeps each under the 50% context budget — geolocation alone (Leaflet integration + UA detection + click-to-pin) is ~25%; computed (useInterpreterEntitiesValues + PAS 79 wiring) is ~15%; repeating-section (instance state + per-child rendering) is ~25-30%.

Output:
- 4 new client component files: GeoMap (sub-component for dynamic import), GeolocationFieldRenderer, ComputedFieldRenderer, RepeatingSectionRenderer.
- Zero modifications to existing renderers or interpreter-renderer.tsx (those land in Plan 14-06).
- AttachPhotosAffordance integration deferred to Plan 14-06 via documented TODO comments (same pattern as Plan 14-04).
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-custom-field-types/14-CONTEXT.md
@.planning/phases/14-custom-field-types/14-RESEARCH.md
@.planning/phases/14-custom-field-types/14-PATTERNS.md
@.planning/phases/14-custom-field-types/14-UI-SPEC.md
@AGENTS.md
@CLAUDE.md
@components/form-interpreter/select-field-renderer.tsx
@components/form-interpreter/section-group-renderer.tsx
@components/forms/geolocation-field.tsx
@lib/form-builder/risk/pas79.ts
@lib/form-builder/entities/geolocation-field.ts
@lib/form-builder/entities/computed-field.ts
@lib/form-builder/entities/repeating-section.ts

<interfaces>
<!-- Existing patterns + Phase 14 Wave 1 dependencies. -->

From components/form-interpreter/select-field-renderer.tsx (CANONICAL scaffold — copy lines 1-46 for surface tokens + label + required + helpText + error wrapper).

From components/form-interpreter/section-group-renderer.tsx (CANONICAL container pattern — repeatingSection outer shell mirrors this; computed field's read-only "no setValue" shape also mirrors this):
- Reads attrs.title / attrs.description and renders the section heading
- For sectionGroup: passes `{children}` JSX prop through directly (InterpreterEntities pre-renders children)
- For repeatingSection: this pattern does NOT work — children inside an instance are NOT registered with the interpreterStore. The renderer must manually render each child entity in each instance using the formBuilder.entities[childType] definition. See RESEARCH Pattern 8 + Pitfall 3 + 14-PATTERNS §"repeating-section-renderer.tsx" for the workaround.

From lib/form-builder/risk/pas79.ts (Plan 14-01):
```typescript
export function computePAS79RiskLevel(likelihood: number | undefined, consequence: number | undefined): RiskResult | null
```
Returns { score, level, colourClass } or null for invalid inputs.

From @coltorapps/builder-react (existing dependency):
- `useInterpreterEntitiesValues(interpreterStore, entityIds: string[])` — selective subscription, only re-renders when the listed entity IDs' values change. See RESEARCH §Pitfall 5.
- The hook requires the interpreterStore reference. Phase 14 components map (Plan 14-06) will pass interpreterStore explicitly to ComputedFieldRenderer.

Leaflet dynamic import pattern (CRITICAL — RESEARCH Pitfall 1+2 + UI-SPEC):
```typescript
import dynamic from "next/dynamic";
const GeoMap = dynamic(() => import("./geolocation-map"), { ssr: false });
```
Inside geolocation-map.tsx ("use client"):
- `import "leaflet/dist/leaflet.css"` — MUST be first import (otherwise tile rendering breaks)
- Marker icon fix:
  ```typescript
  delete (L.Icon.Default.prototype as any)._getIconUrl;
  L.Icon.Default.mergeOptions({
    iconUrl: "/leaflet/marker-icon.png",        // copied by Plan 14-01
    iconRetinaUrl: "/leaflet/marker-icon-2x.png",
    shadowUrl: "/leaflet/marker-shadow.png",
  });
  ```
- OSM tile URL: `https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png` with attribution `© OpenStreetMap contributors`.

Existing components/forms/geolocation-field.tsx provides the capture panel UI (acquiring / success / permission denied / unavailable / error states + Refresh button). The renderer wraps this OR re-implements the capture logic inline — the existing component's prop shape may not fit cleanly with surface + the new value object shape; prefer inline wiring of `navigator.geolocation.getCurrentPosition`.

repeatingSection instance state machine (RESEARCH §Code Examples "Repeating Section Instance Management" lines 855-887):
```typescript
const [instances, setInstances] = useState<Array<Record<string, unknown>>>(() => {
  const stored = entity.value as { instances?: Array<Record<string, unknown>> } | undefined;
  return stored?.instances ?? [];
});

const addInstance = () => {
  const newInstances = [...instances, {}];
  setInstances(newInstances);
  setValue({ instances: newInstances });
};

const removeInstance = (index: number) => {
  const newInstances = instances.filter((_, i) => i !== index);
  setInstances(newInstances);
  setValue({ instances: newInstances });
};

const updateInstance = (index: number, childId: string, value: unknown) => {
  const newInstances = instances.map((inst, i) => i === index ? { ...inst, [childId]: value } : inst);
  setInstances(newInstances);
  setValue({ instances: newInstances });
};
```

Per-instance child rendering (the tricky part — children are NOT registered with the interpreter store; the renderer renders them as standalone controlled inputs):
- For each childId in `schema.entities[entity.id].children`, look up `schema.entities[childId].type` and `schema.entities[childId].attributes`.
- Render a controlled input matching the child entity type (textField → <input>, numberField → <input type="number">, dateField → <input type="date">, selectField → <select>, textareaField → <textarea>, checkboxField → <input type="checkbox">).
- Read schema from a prop — Plan 14-06 passes it via the components map wrapper. The simplest path: ComputedFieldRenderer and RepeatingSectionRenderer both need extra plumbing from interpreter-renderer.tsx (Plan 14-06 covers this).
- DO NOT recursively render specialty entity types inside an instance for Phase 14 — UI-SPEC declares specialty-inside-repeating-section is not supported (CONTEXT §deferred: "Repeating sections nested inside other repeating sections — documented constraint" — extend by analogy: no signature/photo/computed inside an instance for now). Document this as a renderer-side constraint with a clear message: "Specialty fields are not supported inside repeating sections in Phase 14."

UI-SPEC §"repeatingSectionRenderer" lines 253-279 — full layout: section heading + instance count badge + cards with collapse/expand/remove + Add button + min/max enforcement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GeoMap sub-component + GeolocationFieldRenderer with dynamic import + accuracy badge + click-to-pin</name>
  <files>components/form-interpreter/geolocation-map.tsx, components/form-interpreter/geolocation-field-renderer.tsx</files>
  <read_first>
    - components/form-interpreter/select-field-renderer.tsx (scaffold pattern)
    - components/forms/geolocation-field.tsx (existing capture-panel UI; read its state machine for copywriting carry-forward)
    - .planning/phases/14-custom-field-types/14-RESEARCH.md §"Pattern 6" (lines 453-497) — full GeoMap template
    - .planning/phases/14-custom-field-types/14-RESEARCH.md §"Common Pitfalls / Pitfall 1, 2" (lines 765-790) — Leaflet marker + CSS gotchas
    - .planning/phases/14-custom-field-types/14-UI-SPEC.md §"geolocationFieldRenderer" lines 215-237 (layout, states, accuracy badge, click-to-pin instruction)
    - .planning/phases/14-custom-field-types/14-CONTEXT.md D-11, D-12, D-13 (capture-always, accuracy threshold, click-to-pin)
    - lib/form-builder/entities/geolocation-field.ts (Plan 14-02 — value shape {lat, lng, accuracy, capturedAt})
    - public/leaflet/ (Plan 14-01 — marker PNGs)
  </read_first>
  <behavior>
    - geolocation-map.tsx: "use client", default-exports a `GeoMap` function. Props: { lat: number; lng: number; onClickPin?: (lat: number, lng: number) => void; readOnly?: boolean }. Imports leaflet/dist/leaflet.css as the first import. Performs the marker-icon-path fix (RESEARCH Pitfall 1) — delete `_getIconUrl`, mergeOptions with /leaflet/marker-icon.png etc. Renders MapContainer at zoom=15 centred on [lat, lng], a TileLayer with OSM URL + attribution, and a single Marker at [lat, lng]. When onClickPin is provided AND !readOnly, mount a ClickHandler component that uses useMapEvents to fire onClickPin on map click. role="application" aria-label="Location map — click to adjust pin position" on the outer wrapper div (UI-SPEC Accessibility).
    - geolocation-field-renderer.tsx: "use client" function component. Props: EntityComponentProps<typeof geolocationFieldEntity> & { surface?: "dark"|"cream" }. NO clientId/submissionId — geolocation does not upload anything to storage. (Plan 14-06 wires components map accordingly.)
    - State: `const [isCapturing, setIsCapturing] = useState(false)`; `const [captureError, setCaptureError] = useState<string | null>(null)`.
    - Read `value = entity.value as { lat: number; lng: number; accuracy: number; capturedAt: string } | undefined`.
    - Capture button (Refresh) triggers `navigator.geolocation.getCurrentPosition(success, error)` (D-11). On success: setValue({ lat, lng, accuracy, capturedAt: new Date().toISOString() }). On error: setCaptureError(humanMessage) per UI-SPEC §Error States (Permission denied / Position unavailable / Timeout) — use the exact copy strings.
    - On mount: if no value, auto-trigger capture once (D-11 — never blocks the fill flow; runs in background).
    - Capture panel (UI-SPEC §"Capture panel states"): renders MapPin + lat/lng + "±{accuracy}m · {time}" when success; AlertCircle + error message when error; Loader2 + "Acquiring GPS coordinates…" when capturing. Refresh button (RefreshCw icon) on the right.
    - **Accuracy badge (D-12)**: render `<div role="alert" className="...bg-amber-100 text-amber-900 border border-amber-300...">⚠ Desktop capture — verify pin on map</div>` when value exists AND (value.accuracy > 100 OR `/Mobi|Android/i.test(navigator.userAgent) === false`). Place above the map preview. role="alert" so screen readers announce on render.
    - Map preview: shown ONLY when value exists. Dynamic import: `const GeoMap = dynamic(() => import("./geolocation-map"), { ssr: false })` at module scope (above the component). Render `<GeoMap lat={value.lat} lng={value.lng} onClickPin={(lat, lng) => setValue({ ...value, lat, lng, capturedAt: new Date().toISOString() })} />` in a `<div className="rounded-sm overflow-hidden" style={{ height: 200 }}>` wrapper.
    - "Click to adjust pin" instruction (UI-SPEC): below the map, render `<p className="text-xs text-muted">Click the map to move the pin to the correct location.</p>` when value exists.
    - Standard label + required + helpText + error wrapper.
    - attachPhotos affordance comment per Plan 14-04 Task 1 note.
  </behavior>
  <action>
    Create components/form-interpreter/geolocation-map.tsx using RESEARCH §Pattern 6 lines 453-497 verbatim. Use react-leaflet v5 imports (MapContainer, TileLayer, Marker, useMapEvents). Default-export the GeoMap component.

    Create components/form-interpreter/geolocation-field-renderer.tsx. Use select-field-renderer.tsx scaffold (lines 1-46 minus the entity-specific inner JSX). Replace entity import with geolocationFieldEntity. Implement the state machine + capture flow + accuracy badge + dynamic GeoMap import as specified above.

    For UA detection: a one-liner `const isMobile = typeof navigator !== "undefined" && /Mobi|Android/i.test(navigator.userAgent)` — accept the false-positive risk (e.g., iPad in desktop mode). The accuracy threshold (>100m) is the secondary signal per D-12, so the UA check doesn't need to be perfect.

    For the capture flow: wrap navigator.geolocation.getCurrentPosition in a Promise so the success/error branches are clean. Set isCapturing(true) before the call; reset in both handlers. Use the exact error copy from UI-SPEC §Error States.

    Document in JSDoc: the auto-capture-on-mount behaviour (D-11 — never blocks the fill flow), the accuracy threshold trigger (D-12), the click-to-pin contract (D-13), the dynamic-import gotcha (Leaflet uses window). Cite RESEARCH Pitfall 1 + 2.

    NO test file for the renderers in this task — visual verification is owned by Plan 14-08 (UAT) and Plan 14-06 ships the smoke test for the components map. Verify gate: TS compile + structural greps.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/form-interpreter/(geolocation-map|geolocation-field-renderer)" | grep -v "^$" && exit 1 || exit 0</automated>
    <automated>node -e "const s=require('fs').readFileSync('components/form-interpreter/geolocation-map.tsx','utf8'); if (!/leaflet\\/dist\\/leaflet.css/.test(s)) process.exit(1); if (!/_getIconUrl/.test(s)) process.exit(2); /* marker fix */ if (!/\\/leaflet\\/marker-icon.png/.test(s)) process.exit(3); if (!/openstreetmap/i.test(s)) process.exit(4);"</automated>
    <automated>node -e "const s=require('fs').readFileSync('components/form-interpreter/geolocation-field-renderer.tsx','utf8'); if (!/export function GeolocationFieldRenderer/.test(s)) process.exit(1); if (!/dynamic\\(/.test(s) || !/ssr:\\s*false/.test(s)) process.exit(2); if (!/getCurrentPosition/.test(s)) process.exit(3); if (!/accuracy/.test(s)) process.exit(4); if (!/Desktop capture/.test(s)) process.exit(5); /* D-12 badge */"</automated>
  </verify>
  <done>geolocation-map.tsx exists with leaflet.css import + marker-icon fix + OSM tiles + click handler. geolocation-field-renderer.tsx exists with dynamic GeoMap import + getCurrentPosition capture + D-12 accuracy badge. TS clean. Renderer is wired into the components map in Plan 14-06 and covered by `tests/form-interpreter/renderers.test.tsx` — this plan ships the renderer as a structurally-sampled importable module; behavioral sampling continuity is owned by 14-06's renderer suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ComputedFieldRenderer — PAS 79 badge with useInterpreterEntitiesValues reactivity</name>
  <files>components/form-interpreter/computed-field-renderer.tsx</files>
  <read_first>
    - components/form-interpreter/section-group-renderer.tsx (CANONICAL read-only / no-setValue scaffold)
    - lib/form-builder/risk/pas79.ts (Plan 14-01 — the function being consumed; read exports + types)
    - lib/form-builder/entities/computed-field.ts (Plan 14-02 — attrs.formula, attrs.computedInputs.{likelihood, consequence})
    - .planning/phases/14-custom-field-types/14-RESEARCH.md §"Pattern 3" (lines 336-376) — full ComputedFieldRenderer template + interpreterStore prop plumbing
    - .planning/phases/14-custom-field-types/14-RESEARCH.md §"Pitfall 5" (lines 808-815) — interpreterStore prop requirement
    - .planning/phases/14-custom-field-types/14-UI-SPEC.md §"computedFieldRenderer" lines 239-251 (layout, pending state, configuration error state, no animation)
    - .planning/phases/14-custom-field-types/14-CONTEXT.md D-07, D-08, D-09, D-10
  </read_first>
  <behavior>
    - "use client", function component. Props: EntityComponentProps<typeof computedFieldEntity> & { surface?: "dark"|"cream"; interpreterStore: ReturnType<typeof useInterpreterStore<typeof formBuilder>> }.
    - The interpreterStore prop is mandatory and is plumbed in by Plan 14-06 (interpreter-renderer.tsx components map wrapper). Document this prop with a JSDoc explaining why (Pitfall 5 — useInterpreterEntitiesValues requires the store reference).
    - Read attrs.formula (always "pas79" for Phase 14 per D-08); read `inputs = attrs.computedInputs as { likelihood?: string; consequence?: string } | undefined`.
    - **Configuration error state**: if `!inputs?.likelihood || !inputs?.consequence` (empty entity IDs), render the "Computed field not configured — set inputs in the builder." pill (UI-SPEC styling: `border border-dashed text-muted px-4 py-3 rounded-sm`). Return early.
    - **Subscribe to dependency values**: `const values = useInterpreterEntitiesValues(interpreterStore, [inputs.likelihood, inputs.consequence])`. This hook re-renders the component only when those two specific entity values change (selective subscription per RESEARCH Pattern 3).
    - **Pending state**: if either dependency value is undefined / null / NaN, render the "Fill in likelihood and consequence fields above to compute risk." pill (UI-SPEC styling: same dashed pending look).
    - **Computed badge**: call `computePAS79RiskLevel(Number(values[inputs.likelihood]), Number(values[inputs.consequence]))`. If null → pending state (input out of range). Otherwise render `<div role="status" aria-live="polite" className={cn("px-4 py-3 rounded-sm text-sm font-semibold border", result.colourClass)}>{result.level} — Score: {result.score}</div>`. role="status" aria-live="polite" so screen readers announce updates (UI-SPEC Accessibility).
    - Standard label + helpText wrapper (no required asterisk — computedField has no requiredAttribute per Plan 14-02). No error display (read-only).
    - attachPhotos affordance comment per Plan 14-04 Task 1 note.
    - NO animation on badge update — the colour change IS the signal (UI-SPEC §"Reactivity").
  </behavior>
  <action>
    Create components/form-interpreter/computed-field-renderer.tsx. Use section-group-renderer.tsx as the scaffold (read-only pattern, no setValue calls). Use RESEARCH §Pattern 3 template lines 336-376 as the source.

    Imports:
    - "use client" at top
    - cn, EntityComponentProps, computedFieldEntity, computePAS79RiskLevel, useInterpreterEntitiesValues, useInterpreterStore (from @coltorapps/builder-react — useInterpreterStore is imported for its return-type signature on the interpreterStore prop typing)

    Implementation order:
    1. Destructure props including the new interpreterStore prop
    2. Read attrs + inputs
    3. Configuration-error early return
    4. useInterpreterEntitiesValues subscription (always called — React hooks rules; the early returns must come before any hook call). Restructure: call the hook with an empty array if inputs not configured, then branch on the result. OR call the hook unconditionally with `[inputs?.likelihood || '', inputs?.consequence || ''].filter(Boolean)` and branch on inputs configuration AFTER the hook. Pick whichever satisfies the rules-of-hooks lint without re-renders on empty.
    5. Coerce values to numbers, call computePAS79RiskLevel
    6. Render: outer div with label + (badge OR pending pill OR config-error pill) based on state

    Surface tokens: use the same surfaceTokens shape as section-group-renderer.tsx. Add a `placeholder` key for the dashed pending pill text colour.

    Document the cross-plan dependency in JSDoc: "interpreterStore is plumbed in by InterpreterRenderer (Plan 14-06's components map wrapper for computedField). Phase 14 RESEARCH Pitfall 5 documents why this is required — the other renderers do not need interpreterStore."

    No test file in this task (Plan 14-06 ships the renderer smoke test). PAS 79 math is already covered by Plan 14-01's pas79.test.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/form-interpreter/computed-field-renderer" | grep -v "^$" && exit 1 || exit 0</automated>
    <automated>node -e "const s=require('fs').readFileSync('components/form-interpreter/computed-field-renderer.tsx','utf8'); if (!/export function ComputedFieldRenderer/.test(s)) process.exit(1); if (!/computePAS79RiskLevel/.test(s)) process.exit(2); if (!/useInterpreterEntitiesValues/.test(s)) process.exit(3); if (!/interpreterStore/.test(s)) process.exit(4); if (!/role=['\"]status['\"]/.test(s)) process.exit(5); if (!/aria-live=['\"]polite['\"]/.test(s)) process.exit(6);"</automated>
  </verify>
  <done>ComputedFieldRenderer exists, typechecks, consumes computePAS79RiskLevel + useInterpreterEntitiesValues + interpreterStore prop. role="status" aria-live="polite" on the badge for accessibility. Handles configuration-error / pending / computed states. Renderer is wired into the components map in Plan 14-06 and covered by `tests/form-interpreter/renderers.test.tsx` — this plan ships the renderer as a structurally-sampled importable module; behavioral sampling continuity is owned by 14-06's renderer suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RepeatingSectionRenderer — instance state machine + per-instance child rendering + min/max enforcement</name>
  <files>components/form-interpreter/repeating-section-renderer.tsx</files>
  <read_first>
    - components/form-interpreter/section-group-renderer.tsx (outer container shell pattern — heading, divider, gap-4 child stack)
    - .planning/phases/14-custom-field-types/14-RESEARCH.md §"Code Examples — Repeating Section Instance Management" (lines 855-887) — the canonical state machine
    - .planning/phases/14-custom-field-types/14-RESEARCH.md §"Pitfall 3" (lines 791-799) — value shape MUST be {instances: [...]} on every setValue call
    - .planning/phases/14-custom-field-types/14-PATTERNS.md §"repeating-section-renderer.tsx" lines 459-481 (full implementation contract)
    - .planning/phases/14-custom-field-types/14-UI-SPEC.md §"repeatingSectionRenderer" lines 253-279 (full layout: heading + instance cards + collapse + remove + Add button + min/max)
    - .planning/phases/14-custom-field-types/14-CONTEXT.md D-01, D-02, D-03, D-04 (architecture, template children, min/max, value shape)
    - lib/form-builder/entities/repeating-section.ts (Plan 14-02 — attrs.title, description, minInstances, maxInstances; childrenAllowed: true)
  </read_first>
  <behavior>
    - "use client" function component. Props: EntityComponentProps<typeof repeatingSectionEntity> & { surface?: "dark"|"cream"; schema: FormBuilderSchema }. The schema prop is required so the renderer can look up child entity types — Plan 14-06's components map wrapper provides it.
    - Read attrs.title, attrs.description, attrs.minInstances (default 0), attrs.maxInstances (default undefined). Read entity.id (the repeatingSection ID).
    - Look up child template IDs: `const childIds = (schema.entities[entity.id] as any).children as string[] ?? []`. These are the template entity IDs that each instance contains.
    - State: instances and instance state machine per RESEARCH lines 855-887. setInstances + setValue({instances}) MUST be called together (Pitfall 3 — partial setValue loses instances).
    - State: `const [collapsedIndices, setCollapsedIndices] = useState<Set<number>>(new Set())` — track which instance cards are collapsed.
    - State: `const [showMinError, setShowMinError] = useState(false)` — surfaces min-instance violation on submit attempt. (Submit-attempt detection is harder than it sounds; Phase 14 simpler approach: show the min error pill whenever `instances.length < minInstances && instances.length > 0` — i.e., once the user has at least one instance, they see the requirement. Document this trade-off; the alternative is plumbing submit-attempt events from InterpreterRenderer which adds complexity for marginal UX gain.)
    - Section heading row: Newsreader 18px title + description below + instance count badge "{N} instance" / "{N} instances" in teal mono right-aligned (UI-SPEC).
    - Instance cards: vertical stack with gap-3. Each card has a header (`#${idx+1}` left + collapse button + remove button right) and a collapsible body. Body renders the child template inline: for each childId in childIds, look up `const childEntity = schema.entities[childId]; const childType = childEntity.type; const childAttrs = childEntity.attributes` and render a controlled input matching the child type. Read `value = instances[idx][childId]` and onChange = `(v) => updateInstance(idx, childId, v)`.
    - Child input mapping (Phase 14 supports basic types only inside instances — UI-SPEC scope):
      - textField → <input type="text"> with attrs.label, attrs.placeholder, attrs.required (asterisk shown but not blocking — server validates after submit)
      - numberField → <input type="number">
      - dateField → <input type="date">
      - selectField → <select> with attrs.options (single-select only; allowMultiple inside instances is out of scope for Phase 14 — surface a small "Multi-select not supported inside repeating sections" placeholder if encountered)
      - textareaField → <textarea>
      - checkboxField → <input type="checkbox">
      - Specialty types (signatureField, ratingField, multiPhotoField, geolocationField, computedField, repeatingSection) → render `<div className="text-xs text-destructive">Specialty fields are not supported inside repeating sections in Phase 14.</div>` (constraint per CONTEXT §deferred + UI-SPEC scope).
    - Add Instance button: full-width dashed border, h-10, "Add {attrs.title || 'instance'}" + Plus icon. Disabled when `maxInstances && instances.length >= maxInstances`. Disabled state: opacity-50 + "Maximum {maxInstances} instances reached." text below.
    - Min violation message: when showMinError is true, render `<p className="text-xs text-destructive">{attrs.title || 'This section'} requires at least {minInstances} {minInstances === 1 ? 'instance' : 'instances'}.</p>` below the Add button.
    - No confirmation on Remove (UI-SPEC §Destructive Actions).
    - Accessibility (UI-SPEC §Accessibility Contract):
      - Container: role="group" aria-label={attrs.title as string}
      - Each instance card: role="group" aria-label={`Instance ${idx + 1}`}
      - Add button: aria-label={`Add ${attrs.title || 'instance'}`}
      - Remove button: aria-label={`Remove instance ${idx + 1}`}
      - Collapse button: aria-label={`Collapse instance ${idx + 1}`} aria-expanded={!collapsed}
    - NO attachPhotos affordance — repeatingSection is a container per D-05.
  </behavior>
  <action>
    Create components/form-interpreter/repeating-section-renderer.tsx. Start with the section-group-renderer.tsx outer shell as a scaffold, then diverge significantly because instance management requires custom state.

    Imports:
    - "use client"
    - useState from react
    - cn from @/lib/utils
    - EntityComponentProps from @coltorapps/builder-react
    - repeatingSectionEntity type from "@/lib/form-builder/entities/repeating-section"
    - FormBuilderSchema type from "@/lib/form-builder"
    - Plus, X, ChevronDown, ChevronUp icons from lucide-react

    Implementation order:
    1. Type Props with schema: FormBuilderSchema prop addition
    2. Read attrs + childIds + initial instances from entity.value (defaulting to [])
    3. Implement addInstance / removeInstance / updateInstance / toggleCollapse helpers — every state-mutator must call setValue({instances: newInstances}) too
    4. Render outer section heading + instance count badge
    5. Map instances to instance cards. Within each card body, map childIds and switch on schema.entities[childId].type to render the right control. Specialty types render the "not supported" placeholder.
    6. Render Add button + min violation pill below

    Surface tokens: same pattern as section-group-renderer.tsx. Add tokens for `instanceCard`, `addButton`, `addButtonDisabled`, `badge` (teal mono accent), `divider`.

    Document the per-instance validation gap in a JSDoc header: "Per-instance child validation is enforced ONLY in this renderer at user-interaction time (e.g., required asterisks shown but not blocking). coltorapps validateEntitiesValues does NOT recurse into the instances array (RESEARCH Open Question #1). For Phase 14, the server-side gap is accepted; the admin trust boundary limits exposure. Phase 15 or a follow-up plan should add a server-side per-instance validation loop in submitAssessmentAction."

    Document the specialty-inside-repeating constraint in JSDoc: "Specialty fields (signature, rating, multi-photo, geolocation, computed, repeatingSection) are not supported inside repeating sections in Phase 14 — surfaced inline as a destructive text message. CONTEXT §deferred explicitly defers this. Reason: each specialty renderer has its own interpreterStore plumbing requirements (e.g., computed needs the store; signature needs upload action wiring); replicating that plumbing inside instances doubles the complexity for marginal value (FRA-doors scenario uses only basic-type children — text + select + checkbox)."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/form-interpreter/repeating-section-renderer" | grep -v "^$" && exit 1 || exit 0</automated>
    <automated>node -e "const s=require('fs').readFileSync('components/form-interpreter/repeating-section-renderer.tsx','utf8'); if (!/export function RepeatingSectionRenderer/.test(s)) process.exit(1); if (!/setValue\\(\\s*\\{\\s*instances/.test(s)) process.exit(2); /* Pitfall 3 contract */ if (!/maxInstances/.test(s)) process.exit(3); if (!/minInstances/.test(s)) process.exit(4); if (!/schema\\s*:?\\s*FormBuilderSchema/.test(s) && !/schema:\\s*FormBuilderSchema/.test(s)) process.exit(5); /* schema prop */ if (!/role=['\"]group['\"]/.test(s)) process.exit(6); if (!/aria-label.*[Ii]nstance/.test(s)) process.exit(7); if (!/Specialty fields are not supported/.test(s)) process.exit(8); /* documented constraint */"</automated>
  </verify>
  <done>RepeatingSectionRenderer exists, typechecks, accepts schema prop, implements instance state machine with full setValue({instances}) on every mutation (Pitfall 3), enforces min/max, renders basic-type child inputs inline, blocks specialty types with documented message. Accessibility labels present. Renderer is wired into the components map in Plan 14-06 and covered by `tests/form-interpreter/renderers.test.tsx` — this plan ships the renderer as a structurally-sampled importable module; behavioral sampling continuity is owned by 14-06's renderer suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigator.geolocation → renderer state | Browser API; the user grants permission; the lat/lng is sensitive PII |
| OSM tile server requests | Each map view sends the map centre coords to OSM via the tile URL (which encodes z/x/y); same data as what getCurrentPosition returned — acceptable per RESEARCH §Security Domain |
| computedFieldRenderer → DOM | The PAS 79 result string is rendered as text — no XSS surface |
| repeatingSectionRenderer instances → setValue | Local React state, then immediately mirrored to interpreterStore via setValue; the value shape contract (Pitfall 3) is the load-bearing invariant |
| Per-instance child inputs → instances[idx] | Bypasses coltorapps validateEntityValue (which only validates top-level entity values); server-side validation gap accepted (T-14-02-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Information disclosure | Geolocation lat/lng logged to console / Sentry / analytics | mitigate | The renderer NEVER calls console.log / console.error with the value object. Errors from getCurrentPosition are logged with their `error.code` (a small integer enum) only, not coords. Document this in JSDoc. |
| T-14-05-02 | Information disclosure | OSM tile server learns the assessment site location | accept | The map centre (the captured lat/lng) is part of the tile URL — same information as what the GPS captured. RESEARCH §Security Domain accepts this for the assessment context. No additional control available without paying for a private tile provider. |
| T-14-05-03 | Tampering | A user with admin access manipulates the inputs.likelihood / inputs.consequence schema to point at a non-numeric entity | mitigate | computePAS79RiskLevel returns null for non-numeric inputs (Plan 14-01 tested). Renderer shows the "Fill in likelihood and consequence" pending pill. No crash, no incorrect badge. |
| T-14-05-04 | Tampering | Client submits a repeatingSection value with instances containing arbitrary child entity IDs not in the template | mitigate | Plan 14-03's expandRepeatingSections only iterates over schema.entities[id].children — extra keys are ignored at AI prompt time. Plan 14-02's repeatingSectionEntity.validate checks the container shape. The remaining gap (per-instance child VALUES not server-validated) is documented as T-14-02-03 (accepted, admin trust boundary). |
| T-14-05-05 | DoS | repeatingSection with thousands of instances would render thousands of inputs | mitigate | maxInstancesAttribute is capped at 50 in UI-SPEC. Plan 14-07 enforces this in the builder properties panel. Even at 50 instances × 5 children = 250 inputs, render time is bounded. |
| T-14-05-06 | UI redress | Specialty-inside-repeating shows an error message instead of rendering | accept | This is the intended Phase 14 scope constraint per CONTEXT §deferred. Surfaced inline so the admin knows the limitation; the builder UI in Plan 14-07 will discourage this configuration but does not hard-block it. |
| T-14-05-07 | Tampering | The user agent string for D-12 desktop detection is trivially spoofable | accept | The accuracy threshold (>100m) is the secondary signal; the badge is informational, not authoritative. Even if UA detection is bypassed, the badge still triggers on poor accuracy. No security impact. |
</threat_model>

<verification>
- `npx tsc --noEmit` — zero errors in the four new files
- Structural greps prove each renderer:
  - Geolocation: dynamic import with ssr:false, getCurrentPosition usage, accuracy badge text, Leaflet asset references
  - Computed: useInterpreterEntitiesValues + interpreterStore prop + role="status" aria-live="polite"
  - RepeatingSection: setValue({instances:...}) contract on every mutation, schema prop, accessibility labels, specialty constraint message
- No existing renderer modifications — only four new files created.
- The four renderers are NOT wired into interpreter-renderer.tsx — Plan 14-06 owns that.
- AttachPhotosAffordance integration deferred to Plan 14-06 (geolocation gets it; computed and repeating-section do NOT per D-05's "non-section" qualifier — geolocation is a non-section entity, repeating-section is a container).
</verification>

<success_criteria>
1. geolocation-map.tsx exists with leaflet.css import, marker icon path fix using public/leaflet/ assets, OSM tile URL + attribution, click-to-pin handler, role="application" wrapper.
2. GeolocationFieldRenderer dynamically imports GeoMap with { ssr: false }, calls navigator.geolocation.getCurrentPosition (auto on mount per D-11), shows the desktop-accuracy badge (D-12) with role="alert", supports click-to-pin (D-13).
3. ComputedFieldRenderer subscribes via useInterpreterEntitiesValues to the inputs.likelihood + inputs.consequence entity values, calls computePAS79RiskLevel, renders the badge with the correct colour class + role="status" aria-live="polite". Handles configuration-error + pending + computed states.
4. RepeatingSectionRenderer owns instances state locally, mirrors to entity.value via setValue({instances: [...]}) on every change (Pitfall 3 contract), enforces minInstances / maxInstances, renders basic-type children inline, blocks specialty-inside-instance with documented message. Accessibility labels present per UI-SPEC §Accessibility Contract.
5. All four files typecheck cleanly. Phase 13 + Plan 14-01 + 14-02 + 14-03 test suites still GREEN.
6. AttachPhotosAffordance integration is a documented TODO in geolocation-field-renderer.tsx (the only of the three that gets it — computed and repeating-section don't carry attachPhotos per D-05).
7. interpreterStore + schema cross-plan prop dependencies are documented in JSDoc; Plan 14-06 will plumb them via the components map wrapper.
</success_criteria>

<output>
After completion, create `.planning/phases/14-custom-field-types/14-05-SUMMARY.md`.
</output>
