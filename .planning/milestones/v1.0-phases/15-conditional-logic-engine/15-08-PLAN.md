---
phase: 15
plan: 08
type: execute
wave: 4
depends_on: [15-01, 15-02, 15-03, 15-04, 15-05, 15-06, 15-07]
files_modified:
  - supabase/migrations/012_phase15_conditional_smoke_test.sql
  - tests/e2e/phase15-smoke.spec.ts
  - tests/e2e/helpers/answers-json.ts
  - .planning/phases/15-conditional-logic-engine/15-UAT.md
autonomous: false
requirements: [COND-01, COND-02, COND-03, COND-04, BUILDER-02]
requirements_addressed: [COND-01, COND-02, COND-03, COND-04, BUILDER-02]

must_haves:
  truths:
    - "Migration 012 seeds a `Phase 15 Conditional Smoke Test` published template version containing the three D-01/D-02/D-03 canonical patterns: PAS 79 risk → mitigation show-rule; FRA-doors repeatingSection with per-instance require-on-Poor; root-scope section hide-on-Site-type"
    - "The migration follows migration 011's CTE + DO $$ + `gen_random_uuid()` pattern verbatim (no hardcoded UUIDs; parent/child invariant honoured)"
    - "`supabase db push` applies migration 012 to the LIVE Supabase DB via the supabase-888 MCP (NON-TTY workaround per Phase 14 precedent); a row appears in `supabase_migrations.schema_migrations`"
    - "Playwright e2e spec exercises both canonical patterns end-to-end: build (or open) the smoke template, fill required source values, assert dependent visibility/require state, submit, query `form_submissions.answers_json` to confirm scrub"
    - "15-UAT.md provides the manual walkthrough script for the human verification checkpoint"
  artifacts:
    - path: supabase/migrations/012_phase15_conditional_smoke_test.sql
      provides: "Smoke-test template insert + published version (the canonical Phase 15 acceptance fixture)"
    - path: tests/e2e/phase15-smoke.spec.ts
      provides: "Playwright e2e: PAS 79 mitigation flow + FRA-doors per-instance flow"
    - path: .planning/phases/15-conditional-logic-engine/15-UAT.md
      provides: "Human verification script for the [checkpoint:human-verify] task"
  key_links:
    - from: supabase/migrations/012_phase15_conditional_smoke_test.sql
      to: lib/form-builder/attributes/visibility-rules.ts
      via: "schema_json contains visibilityRules objects matching the attribute factory's expected shape"
      pattern: "visibilityRules.*sourceEntityId"
    - from: tests/e2e/phase15-smoke.spec.ts
      to: app/admin/assessments/actions.ts (submitAssessmentAction)
      via: "Submit flow asserts the scrubbed answers_json contents post-write"
      pattern: "answers_json"
---

<objective>
Ship the canonical Phase 15 smoke fixture and the end-to-end smoke spec. Migration 012 follows migration 011's pattern verbatim (CTE + DO $$ + `gen_random_uuid()`) and seeds three rule patterns that together exercise D-01 (cascade strip on hidden parent), D-02 (computedField as rule source), and D-03 (ancestor-scope and per-instance child rules). The migration MUST be pushed to the live Supabase DB via the supabase-888 MCP in the same wave (per Phase 14's 14-08 precedent) so the human-verify checkpoint can walk through the template against the actual published version, not a local-only fixture. The Playwright spec is the automated counterpart; 15-UAT.md is the human walkthrough.

Purpose: All five phase requirements (COND-01 .. COND-04 + BUILDER-02) — final acceptance fixture proving the phase is complete and shippable.
Output: 1 migration + 1 Playwright spec + 1 UAT script + 1 manual verification.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-conditional-logic-engine/15-CONTEXT.md
@.planning/phases/15-conditional-logic-engine/15-RESEARCH.md
@.planning/phases/15-conditional-logic-engine/15-PATTERNS.md
@.planning/phases/15-conditional-logic-engine/15-VALIDATION.md
@.planning/phases/14-custom-field-types/14-08-PLAN.md
@.planning/phases/14-custom-field-types/14-UAT.md
@supabase/migrations/011_specialty_smoke_test_template.sql
@lib/form-builder/attributes/visibility-rules.ts
@lib/form-builder/risk/pas79.ts
@playwright.config.ts
</context>

<task type="auto" tdd="true">
  <name>Task 1: Migration 012 — smoke-test template with three rule patterns</name>
  <files>
    supabase/migrations/012_phase15_conditional_smoke_test.sql
  </files>
  <read_first>
    supabase/migrations/011_specialty_smoke_test_template.sql (FULL FILE — the verbatim CTE + DO $$ + gen_random_uuid() pattern + the parent/child invariant note at lines 43-46),
    .planning/phases/15-conditional-logic-engine/15-PATTERNS.md §"supabase/migrations/012_phase15_conditional_smoke_test.sql" (the three entity layouts),
    .planning/phases/15-conditional-logic-engine/15-CONTEXT.md <specifics> (the PAS 79 + FRA-doors + root scope canonical tests),
    lib/form-builder/attributes/visibility-rules.ts (the expected visibilityRules JSON shape),
    lib/form-builder/risk/pas79.ts (the PAS 79 output strings — "Intolerable", "Substantial", etc., needed for the rule value),
    lib/form-builder/entities/computed-field.ts (the formula="pas79" + inputs map shape)
  </read_first>
  <behavior>
    Migration 012 contains a single DO $$ block that:
    - Looks up an admin user id (same pattern as migration 011 line 50).
    - Declares all entity UUIDs via `:= gen_random_uuid()` per migration 011 lines 53-60. NO hardcoded UUIDs.
    - INSERTs one row into `form_templates` (owner_type='admin', name='Phase 15 Conditional Smoke Test', description tells the human walker what to test).
    - INSERTs one row into `template_versions` (status='published', published_at=now(), schema_json containing the three rule patterns):
      - Pattern A (PAS 79 → Mitigation, D-02): two numberField entities (likelihood + consequence), one computedField (formula='pas79', computedInputs maps likelihood→numberA-id, consequence→numberB-id), one textField "Mitigation" with `visibilityRules: { rules: [{ sourceEntityId: <computedField-id>, operator: "equals", value: "Intolerable", action: "show" }], logic: "and" }`.
      - Pattern B (FRA-doors per-instance, D-03): one repeatingSection "Fire doors register" with two child fields: a selectField "Door condition" (options: Good, Fair, Poor) and a selectField "Repair urgency" (options: Low, Medium, High) with `visibilityRules: { rules: [{ sourceEntityId: <doorCondition-child-id>, operator: "equals", value: "Poor", action: "require" }], logic: "and" }`.
      - Pattern C (root-scope cascade, D-01): one selectField "Site type" at root (options: Commercial, Residential, Industrial); one sectionGroup "Fire doors register section" with `visibilityRules: { rules: [{ sourceEntityId: <siteType-id>, operator: "equals", value: "Commercial", action: "show" }], logic: "and" }`. Place the FRA-doors repeatingSection from Pattern B as a child of this sectionGroup so D-01 cascade is exercised.
    - The `schema_json` follows coltorapps shape: top-level `{ entities: {...}, root: [...] }` where every container's `children` array matches every child's `parentId` per the migration 011 invariant.
    - File header comment block documents: purpose, three patterns + their decision references (D-01/D-02/D-03), parent/child invariant reminder.
  </behavior>
  <action>
    Create `supabase/migrations/012_phase15_conditional_smoke_test.sql` following migration 011's pattern verbatim. File-top comment block per the behavior block. Single DO $$ DECLARE ... BEGIN ... END $$ block. Declare all entity UUIDs via gen_random_uuid(). Use `format()` / string concatenation to build the `schema_json` jsonb literal (same approach as migration 011 — see its formatting). Confirm the three rule patterns embed exactly the JSON shape expected by `visibilityRulesAttribute.validate` from plan 15-01. Confirm the FRA-doors repeatingSection sits inside the "Fire doors register section" sectionGroup so the D-01 cascade is observable. Do NOT push to DB yet — Task 3 handles the apply.
  </action>
  <verify>
    <automated>test -f supabase/migrations/012_phase15_conditional_smoke_test.sql &amp;&amp; head -50 supabase/migrations/012_phase15_conditional_smoke_test.sql | grep -E "DO \$\$|gen_random_uuid|owner_type|status.*published"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/012_phase15_conditional_smoke_test.sql` exists.
    - File contains a single DO $$ block with DECLARE + gen_random_uuid() per migration 011 pattern.
    - File contains all three rule patterns (grep for `equals.*Intolerable`, `equals.*Poor`, `equals.*Commercial` each returns ≥1 match).
    - No hardcoded UUIDs (search for fixed-pattern UUIDs returns no matches outside gen_random_uuid declarations).
    - File-top comment documents the three patterns and their D-NN references.
    - Lints cleanly via `npx supabase migration lint` if that command exists; otherwise visual review per migration 011 standards.
  </acceptance_criteria>
  <done>Migration 012 is authored and ready to push.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Playwright e2e smoke spec</name>
  <files>
    tests/e2e/phase15-smoke.spec.ts
  </files>
  <read_first>
    playwright.config.ts (existing — for the testDir, baseURL, projects shape),
    .planning/phases/15-conditional-logic-engine/15-VALIDATION.md §Manual-Only Verifications + §Phase Requirements → Test Map (the e2e rows),
    .planning/phases/15-conditional-logic-engine/15-RESEARCH.md §Validation Architecture (the Playwright command),
    supabase/migrations/012_phase15_conditional_smoke_test.sql (the smoke template the spec opens — needs the template name "Phase 15 Conditional Smoke Test")
  </read_first>
  <behavior>
    Single Playwright spec file with two test() blocks:
    Test A — PAS 79 mitigation flow:
    - Log in as admin (use existing auth helper if present; otherwise document the auth bootstrap step needed by the harness).
    - Navigate to the form-fill route for the smoke template (admin can open any template via the on-site flow per existing routes).
    - Fill likelihood=5, consequence=5 (yields "Intolerable" per PAS 79 matrix).
    - Assert that the Mitigation textField BECOMES visible (was hidden before the computed evaluated to Intolerable). Use a visibility assertion (`expect(page.getByLabel('Mitigation')).toBeVisible()`).
    - Fill Mitigation with a real value.
    - Submit.
    - Query the resulting `form_submissions.answers_json` (via Supabase MCP read or via a server-action helper exposed for tests) and assert the Mitigation key IS present in the scrubbed payload (because it was visible at submit time).
    - Repeat the flow with likelihood=1, consequence=1 (yields a low risk band): Mitigation does NOT appear; submit; verify `answers_json` does NOT contain the Mitigation key (server scrub).
    Test B — FRA-doors per-instance require flow:
    - Same login.
    - Open the smoke template (Site type = Commercial so the section is visible).
    - Add 2 instances to the Fire doors register repeatingSection.
    - Instance 0: Door condition = "Good"; Repair urgency NOT required; can submit empty.
    - Instance 1: Door condition = "Poor"; Repair urgency SHOWS required asterisk (per UI-SPEC §4 dynamic required). Submitting without Repair urgency fails per-instance validation; filling it succeeds.
    - Assert the resulting `answers_json` has both instances persisted with the correct child values.
    Optional test C — D-01 cascade smoke:
    - Set Site type = "Residential". The Fire doors register section + all its instances unmount. Submit.
    - Assert `answers_json` does NOT contain the repeatingSection key (cascade strip — D-01).
  </behavior>
  <action>
    Create `tests/e2e/phase15-smoke.spec.ts` with the two/three test() blocks per the behavior block. Use Playwright `test()` from `@playwright/test`. For the Supabase read after submit, use the supabase-888 MCP `execute_sql` tool via a small `tests/e2e/helpers/answers-json.ts` helper that wraps the MCP call (REQUIRED — no fallback gap, no `OR document the gap` hedge per checker Warning 6). The helper signature: `async function readAnswersJson(submissionId: string): Promise<Record<string, unknown>>`. Setup/teardown blocks use the same helper to seed/clean state. Every test that submits MUST end with a `readAnswersJson(submissionId)` call followed by `expect(Object.keys(json))` assertions: Tests A + C assert hidden entity IDs are ABSENT from the keys; Test B asserts both instances appear with the correct per-instance child values. Configure the spec to use the existing playwright.config.ts baseURL (`localhost:3000` per Phase 14 precedent if not otherwise set).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/phase15-smoke.spec.ts --reporter=list 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/e2e/phase15-smoke.spec.ts` exists with ≥2 `test()` blocks (PAS 79 + FRA-doors).
    - Each block contains explicit `expect()` calls for visibility AND for required UX state changes.
    - **EVERY submitting test ends with a `readAnswersJson(submissionId)` call** + at least one `expect(Object.keys(json))` assertion verifying the scrub contract. No test may skip the answers_json check.
    - Helper file `tests/e2e/helpers/answers-json.ts` exists and uses the supabase-888 MCP `execute_sql` tool.
    - Spec runs without TypeScript errors (`npx tsc --noEmit` clean).
    - If a Playwright run is performed against the dev server, the tests either pass OR the failures are limited to environment-only issues (e.g., dev server not running) — not logic bugs. Failures of the smoke patterns themselves indicate Wave 1-3 regression and MUST be fixed before this task is marked done.
  </acceptance_criteria>
  <done>Automated end-to-end coverage for both canonical Phase 15 flows is shipped; manual UAT covers the gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [BLOCKING] Schema push migration 012 via Supabase MCP</name>
  <files>
    .planning/phases/15-conditional-logic-engine/15-PUSH-LOG.md
  </files>
  <read_first>
    .planning/phases/14-custom-field-types/14-08-PLAN.md (the 14-08 [BLOCKING] supabase db push precedent — non-TTY workaround via the supabase-888 MCP),
    supabase/migrations/012_phase15_conditional_smoke_test.sql (just authored)
  </read_first>
  <behavior>
    Use the supabase-888 MCP to apply migration 012 to the LIVE remote Supabase project (the same project Phase 14's migration 011 was applied to). Non-TTY workaround: use the MCP `apply_migration` tool (per migration 011 precedent) — pass the migration name and the SQL body. After apply, query `supabase_migrations.schema_migrations` to confirm version `20260526...` appears (the timestamp prefix matches the file). Record the version and applied-at timestamp in `.planning/phases/15-conditional-logic-engine/15-PUSH-LOG.md` for audit. If the apply fails (parse error, FK violation, etc.), DO NOT mark the task done — fix the migration in Task 1 and retry. This task is BLOCKING the next checkpoint.
  </behavior>
  <action>
    Invoke the supabase-888 MCP `apply_migration` tool with `name="phase15_conditional_smoke_test"` and the body read from `supabase/migrations/012_phase15_conditional_smoke_test.sql`. On success, immediately call `execute_sql` with `SELECT version, name, statements FROM supabase_migrations.schema_migrations WHERE name = 'phase15_conditional_smoke_test' ORDER BY version DESC LIMIT 1;`. Write the resulting version + timestamp to `.planning/phases/15-conditional-logic-engine/15-PUSH-LOG.md`. Also call `execute_sql` with `SELECT id, name FROM form_templates WHERE name = 'Phase 15 Conditional Smoke Test' LIMIT 1;` to confirm the template row exists.
  </action>
  <verify>
    <automated>cat .planning/phases/15-conditional-logic-engine/15-PUSH-LOG.md | grep -E "version|Phase 15 Conditional"</automated>
  </verify>
  <acceptance_criteria>
    - supabase-888 MCP apply_migration call succeeded (no error response).
    - schema_migrations query returns a row matching `name = 'phase15_conditional_smoke_test'`.
    - form_templates query returns a row with `name = 'Phase 15 Conditional Smoke Test'`.
    - `15-PUSH-LOG.md` records the version + template ID + timestamp.
    - If apply fails: do NOT mark task done; document the failure mode and return to Task 1 to fix.
  </acceptance_criteria>
  <done>Migration 012 lives in the production DB; the smoke template is queryable; the audit log is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Author 15-UAT.md manual walkthrough script</name>
  <files>
    .planning/phases/15-conditional-logic-engine/15-UAT.md
  </files>
  <read_first>
    .planning/phases/14-custom-field-types/14-UAT.md (the format precedent — sections A–H per Phase 14's manual walkthrough),
    .planning/phases/15-conditional-logic-engine/15-UI-SPEC.md (all sections — the manual walker needs to verify each interaction contract),
    .planning/phases/15-conditional-logic-engine/15-VALIDATION.md §Manual-Only Verifications
  </read_first>
  <behavior>
    15-UAT.md follows 14-UAT.md's section-based format. Sections:
    A — Open the smoke template as admin. Verify the PropertiesPanel renders a `CONDITIONAL LOGIC` collapsible at the bottom for a text/select/number entity, and does NOT render it for the Fire doors register sectionGroup.
    B — Expand the section on the Mitigation text field. Confirm the AND/OR toggle, the existing rule row (computedField source → equals Intolerable → show), the trash button, the + Add condition dashed button.
    C — Open the FRA-doors template-fill flow. Set Site type = Commercial. Verify the Fire doors register section appears. Add 2 instances. Confirm per-instance scope: Repair urgency in instance 0 is NOT required when Door condition = Good; instance 1's Repair urgency becomes required (asterisk shown) when Door condition = Poor.
    D — Set Site type = Residential. Verify the entire section + instances unmount (D-01 cascade).
    E — Build a deliberate cycle in the builder: add a rule on field X referencing field Y; add a rule on field Y referencing field X. Save. Verify the Sonner toast `Circular rule detected` appears; the PropertiesPanel for X (or Y) shows the inline `CycleErrorBanner` with both labels; the Publish button is disabled with tooltip `Fix circular rules before publishing`.
    F — Remove one rule of the cycle. Verify the cycleState clears (banner disappears, publish unblocks).
    G — Fill the PAS 79 flow with likelihood=5, consequence=5. Mitigation appears. Submit. Verify the submission row exists.
    H — (Optional, DB-side) Query `form_submissions.answers_json` for the last submission. Confirm hidden-then-shown cases land correctly + hidden-on-submit cases are scrubbed.
    Each section has a check-box list per the 14-UAT.md format.
  </behavior>
  <action>
    Create `.planning/phases/15-conditional-logic-engine/15-UAT.md` with the 8 sections per the behavior block. Use 14-UAT.md's markdown format. Include short reproduction steps + expected outcomes + check-box list per section. Footer notes the supabase-888 MCP can be used to inspect `answers_json` during section H.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-conditional-logic-engine/15-UAT.md &amp;&amp; wc -l .planning/phases/15-conditional-logic-engine/15-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/15-conditional-logic-engine/15-UAT.md` exists.
    - Contains 8 sections A–H per the behavior block.
    - Each section has a check-box list.
    - Section H references the supabase-888 MCP for DB inspection (or documents an in-app surface if one exists).
  </acceptance_criteria>
  <done>Manual walkthrough script is ready for the next checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [checkpoint:human-verify] Phase 15 acceptance walkthrough</name>
  <what-built>
    All Phase 15 deliverables: visibilityRules attribute on 13 entities; pure-logic visibility/scrub/cycle modules; coltorapps shouldBeProcessed integration in interpreter; computeFormProgress visibility-awareness; server-side scrub in submitAssessmentAction; validateRuleGraph at all 4 save/publish call sites (admin + customer); ConditionalLogicSection + RuleRow + CycleErrorBanner builder UI; migration 012 applied to the live DB with the canonical PAS 79 + FRA-doors + root-cascade smoke template.
  </what-built>
  <how-to-verify>
    Follow `.planning/phases/15-conditional-logic-engine/15-UAT.md` sections A–H end-to-end against the dev server (`npm run dev` → http://localhost:3000) with the user logged in as admin. Tick off each section's check-box list. Note any deviations. For section H, optionally query `form_submissions.answers_json` via the supabase-888 MCP `execute_sql` tool to confirm the scrub contract.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 15 complete, or describe issues found.</resume-signal>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration 012 → live Supabase DB | applied via supabase-888 MCP; same trust boundary as migration 011. |
| Playwright spec → dev server | Test-only; no security-relevant boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08-01 | Tampering | Migration 012 SQL | mitigate | Migration is reviewable SQL; follows migration 011's vetted pattern; gen_random_uuid() prevents UUID collisions; no untrusted input. The supabase-888 MCP apply path is the same one used by migration 011 (vetted in Phase 14). |
| T-15-08-02 | Tampering | Smoke template schema | accept | Template is a fixture for testing — even if its rule wiring is "deliberately fragile" (e.g., the cycle in UAT section E), the cycle is built only as a test artefact and removed (UAT section F) before any real submission. validateRuleGraph (plan 15-03) prevents accidental publication of cycles via the normal save path. |
| T-15-08-03 | Information Disclosure | answers_json query in UAT section H | accept | The supabase-888 MCP requires authenticated access (service-role key); same access boundary as any other production read. Documented and reviewed as part of the manual walkthrough. |
| T-15-08-04 | Denial of Service | E2E spec runtime | accept | Playwright spec runs on CI or dev — no production impact. |
| T-15-08-05 | Repudiation | DB migration audit | mitigate | 15-PUSH-LOG.md records the version + timestamp + template ID. `supabase_migrations.schema_migrations` is the DB-side audit trail. |
</threat_model>

<verification>
- Migration 012 exists, follows migration 011 pattern, contains all three rule patterns.
- Migration 012 applied to live DB (15-PUSH-LOG.md confirms).
- Playwright spec exists with ≥2 test blocks.
- 15-UAT.md exists with 8 sections A–H.
- Human walkthrough completed and signed off via the checkpoint resume signal.
- Full Vitest suite: `npx vitest run` reports 0 failures.
</verification>

<success_criteria>
Phase 15 ships: every COND-01..04 + BUILDER-02 requirement is implemented and acceptance-tested via the canonical smoke template; the conditional-logic engine is live in the production DB; human verification confirms the UX matches UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/15-conditional-logic-engine/15-08-SUMMARY.md` documenting: the migration 012 contents (entity layout overview), the push outcome (version + timestamp from 15-PUSH-LOG.md), the e2e spec coverage, the UAT outcome (any deviations or follow-ups). Then update `.planning/STATE.md` to mark Phase 15 as code-complete + DB-applied; phase-completion sweep via the standard `/gsd-verify-work` + `/gsd-phase-complete 15` cadence.
</output>
