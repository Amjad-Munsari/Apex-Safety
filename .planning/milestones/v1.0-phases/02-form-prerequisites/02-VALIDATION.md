# Phase 2: Form Prerequisites - Validation Strategy

**Phase:** 02
**Slug:** form-prerequisites
**Date:** 2026-04-29

## 1. Acceptance Criteria (UAT)

Matt (Assessor) must be able to:
- [ ] Open the form and see the hardcoded schema fields (FRA Type 3 mockup).
- [ ] Record voice notes for text fields; transcription appears in the field within 5 seconds.
- [ ] Attach HEIC photos; they are converted to JPEG automatically.
- [ ] Confirm uploaded photo size is between 1.0MB and 1.5MB.
- [ ] Close the browser, reopen, and see his data still present (Cloud Draft Sync).

## 2. Automated Verification

### Component Tests (Vitest)
- **Form Renderer:** Verify that passing a JSON schema correctly generates the corresponding UI components.
- **STT Proxy:** Mock OpenRouter response and verify the `/api/proxy/stt` endpoint returns correct text.

### E2E Tests (Playwright)
- **Media Pipeline:**
  1. Upload `tests/fixtures/test-image.heic`.
  2. Intercept upload request.
  3. Assert payload is `image/jpeg`.
  4. Assert payload size < 1.5MB.
- **Persistence:**
  1. Login as Client A.
  2. Input data.
  3. Wait for sync.
  4. Clear Local Storage.
  5. Refresh page.
  6. Assert data is restored from Supabase.

## 3. Security & Isolation
- [ ] Verify that drafts are isolated by `client_id` (Client B cannot see Client A's drafts).
- [ ] Verify that the OpenRouter API key is NOT exposed in the client bundle.

---
*Generated by GSD Validation-Strategy*
