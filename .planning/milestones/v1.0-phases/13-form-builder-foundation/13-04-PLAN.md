---
phase: 13-form-builder-foundation
plan: 04
type: execute
wave: 3
depends_on: ["13-02", "13-03"]
files_modified:
  - supabase/migrations/010_form_builder_foundation_reseed.sql
  - lib/supabase/templates.ts
  - components/templates/template-builder.tsx
  - components/templates/field-palette.tsx
  - components/templates/field-config.tsx
  - components/templates/sortable-field.tsx
  - components/forms/form-renderer.tsx
  - lib/types/form-builder.ts
  - app/admin/templates/[id]/editor-client.tsx
autonomous: false
requirements: [BUILDER-03]

must_haves:
  decisions_covered:
    - "D-03: Big-bang cutover (data + dead-code side) — delete the pre-coltorapps builder/renderer/types files and push the migration so no old-shape state remains"
    - "D-05: Drop & reseed — truncate the existing form_submissions / form_assignments / template_versions / form_templates rows; no FormSchema converter is built"
    - "D-06: Reseed only a minimal basic-types smoke-test template; the full FRA reseed is deferred to Phase 18"
  truths:
    - "The four form tables (form_submissions, form_assignments, template_versions, form_templates) are truncated of the old custom-shape data"
    - "A 'Basic Types Smoke Test' template with all 7 basic entity types is seeded as a coltorapps-shape template_versions row"
    - "Migration 010 is applied to the live database via supabase db push"
    - "The dead pre-coltorapps builder, renderer, and types files no longer exist in the tree"
    - "The project builds clean with no imports referencing the deleted custom FormSchema types"
    - "A human has verified the build, save, version, fill, submit loop end to end"
  artifacts:
    - path: "supabase/migrations/010_form_builder_foundation_reseed.sql"
      provides: "Drop + reseed migration with the 7-basic-types smoke-test template"
      contains: "TRUNCATE"
  key_links:
    - from: "supabase/migrations/010_form_builder_foundation_reseed.sql"
      to: "template_versions"
      via: "INSERT smoke-test template with coltorapps schema_json"
      pattern: "template_versions"
    - from: "lib/supabase/templates.ts"
      to: "lib/form-builder"
      via: "FormBuilderSchema type for schema_json"
      pattern: "FormBuilderSchema"
---

<objective>
Complete the big-bang cutover (D-03/D-05): apply the drop-and-reseed migration that wipes the old custom-`FormSchema` data and seeds a coltorapps-shape smoke-test template, delete the now-dead pre-coltorapps builder/renderer/types files, clean up `lib/supabase/templates.ts`, push the migration to the live database, and gate the phase on a human end-to-end verification.

Purpose: Delivers the data side of the cutover and removes the dead code so the tree is clean. This plan runs LAST because the new builder (Plan 02) and interpreter (Plan 03) must exist and build green before the old files are deleted — deleting earlier would break the tree. The `supabase db push` is the mandatory `[BLOCKING]` schema task: without it the live DB still has old-shape rows and verification is a false positive (build/type checks pass without the push because Supabase types come from config, not the live DB).

Output: `supabase/migrations/010_form_builder_foundation_reseed.sql` applied to the live DB; 6 dead files deleted; `lib/supabase/templates.ts` cleaned (`updateTemplateDraft` removed, types updated); a passing human-verify checkpoint.
</objective>

<decisions_covered>
CONTEXT.md decisions implemented by this plan:
- **D-03:** Big-bang cutover (data + dead-code side) — Task 2 deletes the pre-coltorapps builder, renderer, and `lib/types/form-builder.ts` files; Task 3 pushes the migration so no parallel/old-shape state remains.
- **D-05:** Drop & reseed — Task 1's migration 010 truncates the existing `form_submissions` / `form_assignments` / `template_versions` / `form_templates` rows (disposable dev/demo data); no `FormSchema`→coltorapps converter is built.
- **D-06:** Reseed only a minimal basic-types smoke-test template — Task 1 seeds one 7-basic-types coltorapps-shape template to exercise the build→save→version→fill→submit loop; the full FRA reseed is deferred to Phase 18.
</decisions_covered>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-form-builder-foundation/13-CONTEXT.md
@.planning/phases/13-form-builder-foundation/13-RESEARCH.md
@.planning/phases/13-form-builder-foundation/13-PATTERNS.md
@.planning/phases/13-form-builder-foundation/13-02-SUMMARY.md
@.planning/phases/13-form-builder-foundation/13-03-SUMMARY.md
@AGENTS.md
@supabase/migrations/001_initial_schema.sql
@supabase/migrations/009_clients_contact_columns.sql
@lib/supabase/templates.ts
@lib/types/form-builder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the drop-and-reseed migration 010</name>
  <read_first>
    - supabase/migrations/009_clients_contact_columns.sql (analog — header comment convention, file numbering)
    - supabase/migrations/001_initial_schema.sql (form_templates / template_versions / form_submissions / form_assignments column definitions + FK directions, to get the TRUNCATE order right)
    - .planning/phases/13-form-builder-foundation/13-RESEARCH.md § "Runtime State Inventory" (truncate the four tables; D-06 pre-condition — confirm no production submissions first), § "Migration Reconciliation" (next number is 010)
    - .planning/phases/13-form-builder-foundation/13-PATTERNS.md § "supabase/migrations/010_form_builder_foundation_reseed.sql" (truncation order, smoke-test seed CTE pattern, real-UUID requirement)
    - .planning/phases/13-form-builder-foundation/13-01-SUMMARY.md (the 7 entity type names + their attribute keys — the seed schema_json must use real entity types)
    - AGENTS.md (owner_type is 'admin'/'customer'; no hardcoded catalogs that diverge from prod)
  </read_first>
  <action>
    First, the D-06 pre-condition: run `SELECT count(*), status FROM form_submissions GROUP BY status` against the live DB and confirm there is no production assessment data before writing the truncation. Current scale is ~7-8 clients, pre-launch — expected safe. If the count shows real submitted/reviewed data, STOP and escalate before proceeding.

    Create `supabase/migrations/010_form_builder_foundation_reseed.sql` with the standard header comment block (mirror `009`). The migration:

    1. `TRUNCATE TABLE form_submissions CASCADE; TRUNCATE TABLE form_assignments CASCADE; TRUNCATE TABLE template_versions CASCADE; TRUNCATE TABLE form_templates CASCADE;` — child tables first to respect FK order.

    2. Seed one `form_templates` row named `'Basic Types Smoke Test'` with `template_type = 'fra'`, `owner_type = 'admin'`, `owner_id = (SELECT id FROM admin_users LIMIT 1)`, `is_published = false`.

    3. Seed one `template_versions` row (`version_number = 1`, `created_by` = the same admin id) whose `schema_json` is a valid coltorapps `{ entities, root }` object exercising ALL 7 basic entity types — one `textField`, one `numberField`, one `dateField`, one `selectField` (with at least 2 options), one `textareaField`, one `checkboxField`, and one `sectionGroup` containing at least one child field. Entity UUIDs in the JSON must be real UUIDs generated at migration time via `gen_random_uuid()` (use a `DO` block or CTE that generates them and assembles the `jsonb` — do NOT hardcode placeholder strings). Each entity's `attributes` must use the exact attribute keys from the Plan 01 entity definitions (e.g. `label`, `required`, `options`, `prefillSource`, `title`).

    Per AGENTS.md "DB as source of truth" — this seed is the smoke-test fixture (D-06), not a production catalog; the real FRA reseed is Phase 18. Do NOT add hardcoded mock data anywhere in TypeScript.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sql=fs.readFileSync('supabase/migrations/010_form_builder_foundation_reseed.sql','utf8'); const t=(sql.match(/TRUNCATE TABLE/g)||[]).length; const types=['textField','numberField','dateField','selectField','textareaField','checkboxField','sectionGroup'].filter(x=>sql.includes(x)); if(t<4) throw new Error('expected 4 TRUNCATEs, got '+t); if(types.length<7) throw new Error('missing entity types: '+types.length); console.log('OK: '+t+' truncates, '+types.length+'/7 entity types in seed');"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/010_form_builder_foundation_reseed.sql` exists with the standard header comment block
    - The migration contains exactly 4 `TRUNCATE TABLE` statements in child-first order (form_submissions, form_assignments, template_versions, form_templates)
    - The seeded `schema_json` references all 7 entity type names and the `sectionGroup` has at least one nested child entity
    - The seed uses `gen_random_uuid()` for entity IDs — `grep -c "gen_random_uuid" supabase/migrations/010_form_builder_foundation_reseed.sql` ≥ 1, and no placeholder strings like `<uuid1>` remain
    - `owner_type` in the seed insert is `'admin'` (never `'client'`)
  </acceptance_criteria>
  <done>Migration 010 truncates the four form tables and seeds a coltorapps-shape smoke-test template covering all 7 basic entity types with real UUIDs.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead pre-coltorapps code and clean lib/supabase/templates.ts</name>
  <read_first>
    - lib/supabase/templates.ts (self — keep getFormTemplates/getFormTemplate/getLatestPublishedVersion lines 6-31; delete updateTemplateDraft lines 53-61; update type imports)
    - lib/types/form-builder.ts (the custom FormSchema/FormField/FormTemplate/TemplateVersion types being deleted)
    - .planning/phases/13-form-builder-foundation/13-RESEARCH.md § "State of the Art" (the deprecated/delete list), § "Common Pitfalls" Pitfall 5 (updateTemplateDraft / current_draft_json)
    - .planning/phases/13-form-builder-foundation/13-PATTERNS.md § "lib/supabase/templates.ts"
    - .planning/phases/13-form-builder-foundation/13-02-SUMMARY.md, 13-03-SUMMARY.md (confirm Plans 02/03 already removed their FormSchema imports)
  </read_first>
  <action>
    Delete these 6 dead files (replaced by Plans 02/03):
    - `components/templates/template-builder.tsx`
    - `components/templates/field-palette.tsx`
    - `components/templates/field-config.tsx`
    - `components/templates/sortable-field.tsx`
    - `components/forms/form-renderer.tsx`
    - `lib/types/form-builder.ts`

    Also delete `app/admin/templates/[id]/editor-client.tsx` (the hand-rolled editor superseded by Plan 02's `builder-client.tsx`).

    Clean `lib/supabase/templates.ts`: keep `getFormTemplates()`, `getFormTemplate()`, `getLatestPublishedVersion()` unchanged; delete `updateTemplateDraft()` entirely (writes to the non-existent `form_templates.current_draft_json` column — RESEARCH.md Pitfall 5). Update any type imports — replace references to the deleted `FormSchema`/`TemplateVersion` types with `FormBuilderSchema` from `@/lib/form-builder`.

    After deletion, grep the whole tree for any remaining import of the deleted modules (`@/lib/types/form-builder`, `@/components/templates/template-builder`, `@/components/forms/form-renderer`, `updateTemplateDraft`) and fix every dangling reference. The build MUST be clean — a dangling import is a hard failure.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20 && echo "---dangling refs---" && grep -rl "lib/types/form-builder\|components/templates/template-builder\|components/forms/form-renderer\|updateTemplateDraft" app components lib --include="*.ts" --include="*.tsx" 2>/dev/null || echo "no dangling references"</automated>
  </verify>
  <acceptance_criteria>
    - The 6 dead files no longer exist: `components/templates/{template-builder,field-palette,field-config,sortable-field}.tsx`, `components/forms/form-renderer.tsx`, `lib/types/form-builder.ts`
    - `app/admin/templates/[id]/editor-client.tsx` no longer exists
    - `lib/supabase/templates.ts` no longer contains `updateTemplateDraft` — `grep -c "updateTemplateDraft" lib/supabase/templates.ts` returns 0
    - No file in `app/`, `components/`, `lib/` imports `@/lib/types/form-builder`, `@/components/templates/template-builder`, or `@/components/forms/form-renderer`
    - `npx tsc --noEmit` exits 0 (no type errors)
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>All pre-coltorapps dead code is removed, `templates.ts` is cleaned, and the project builds with zero dangling imports.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3 [BLOCKING]: Apply migration 010 to the live database via supabase db push</name>
  <read_first>
    - supabase/migrations/010_form_builder_foundation_reseed.sql (the migration produced in Task 1)
    - .env.local (confirm a Supabase access token / DB connection is available; the project is live)
    - .planning/phases/13-form-builder-foundation/13-RESEARCH.md § "Migration Reconciliation" (010 is the next number)
    - AGENTS.md (project conventions)
  </read_first>
  <action>
    Apply migration 010 to the live Supabase database. This task is MANDATORY and BLOCKING — without it the live DB still holds old custom-`FormSchema` rows and the phase verification is a false positive (the Next.js build and `tsc` pass regardless of DB state because Supabase types come from `supabase/` config files, not the live database).

    Run `supabase db push`. For non-interactive execution, set the `SUPABASE_ACCESS_TOKEN` environment variable so the CLI authenticates without a prompt (read it from `.env.local` or the project secret store; do NOT hardcode it in any committed file). If `supabase db push` requires an interactive confirmation prompt that cannot be suppressed with a flag, pause and surface the exact command to the user for manual execution.

    After the push, verify migration 010 is recorded as applied (`supabase migration list` shows 010 in the remote column) and that the smoke-test template exists: `SELECT name FROM form_templates` returns `Basic Types Smoke Test` and `SELECT version_number FROM template_versions` returns `1`.
  </action>
  <verify>
    <automated>npx supabase migration list 2>&1 | grep -E "010" || echo "MANUAL: confirm migration 010 applied to remote DB"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completed successfully (or the user confirmed manual application)
    - `supabase migration list` shows `010_form_builder_foundation_reseed` applied to the remote database
    - The live DB has exactly one `form_templates` row named `Basic Types Smoke Test` and one `template_versions` row at `version_number = 1`
    - The old custom-`FormSchema` rows are gone (post-truncation)
  </acceptance_criteria>
  <done>Migration 010 is applied to the live database; the four form tables are reseeded; the smoke-test template is queryable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the full build→fill→submit loop</name>
  <what-built>
    The complete Phase 13 cutover: coltorapps form builder (three-panel UI on `/admin/templates`), save/publish creating immutable `template_versions` rows, the coltorapps interpreter renderer on the assessment fill flow, and migration 010 applied with a 7-basic-types smoke-test template seeded. All Vitest suites under `tests/form-builder/` are green and the project builds clean.
  </what-built>
  <how-to-verify>
    1. Run the dev server (`npm run dev`) and sign in as admin.
    2. Visit `/admin/templates` — confirm the "Basic Types Smoke Test" template appears in the list.
    3. Open it in the builder. Confirm the three-panel layout renders (palette left, canvas centre, properties right) and the canvas shows all 7 seeded entity types including the sectionGroup with its nested child.
    4. Click each of the 7 palette buttons — confirm a new field of that type is added to the canvas.
    5. Drag a field card to reorder it; drag a field into the sectionGroup — confirm reorder and reparent both work.
    6. Select a field, edit its label in the properties panel — confirm the canvas card updates immediately.
    7. Click "Save draft" — confirm the "Saved" tag appears. Click "Save draft" again — this should create version 2 (verify in Supabase: `SELECT version_number FROM template_versions ORDER BY version_number` shows 1 AND 2, and version 1's `schema_json` is unchanged).
    8. Click "Publish Template" — confirm the publish confirm dialog, then the "LIVE v{N}" badge.
    9. Open an assessment fill flow for the smoke-test template, fill the required fields, and submit — confirm the submission succeeds and `form_submissions` has a row with `status = 'submitted'` and a `template_version_id` pointing at the version you filled against.
    10. Confirm a submission filled against version 1 still renders version 1's fields even after version 2 exists (version pinning).
  </how-to-verify>
  <resume-signal>Type "approved" if the full loop works, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → live database | The migration truncates production-region tables; an incorrect TRUNCATE order or scope is destructive |
| Local CLI → remote Supabase project | `supabase db push` mutates the live `eu-west-2` database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Denial of Service | `TRUNCATE` in migration 010 | mitigate | D-06 pre-condition: the executor runs `SELECT count(*), status FROM form_submissions` and confirms no production assessment data before applying; truncation order is child-first to avoid FK errors; pre-launch scale (~7-8 clients) bounds the blast radius |
| T-13-15 | Tampering | Seeded `schema_json` | mitigate | The seed schema must pass `validateSchema` (it uses only the 7 registered entity types and real UUIDs); a malformed seed would be caught the first time the builder hydrates it — the human-verify checkpoint step 3 exercises this |
| T-13-16 | Information Disclosure | `SUPABASE_ACCESS_TOKEN` for `db push` | mitigate | The token is read from `.env.local` / secret store and passed as an environment variable only; it is never written to a committed file or the migration |
| T-13-17 | Elevation of Privilege | Migration RLS posture | accept | Migration 010 only truncates + seeds data; it does not alter RLS policies. The `owner_type`/RLS contract from migrations 003/004 remains in force unchanged. |
</threat_model>

<verification>
- Migration 010 exists, truncates the 4 tables child-first, seeds a 7-type coltorapps template with real UUIDs
- The 6 dead files + `editor-client.tsx` are deleted; `npx tsc --noEmit` and `npx next build` both pass with zero dangling imports
- `supabase migration list` shows 010 applied to the remote DB
- `npx vitest run tests/form-builder/` exits 0 (full suite green)
- Human checkpoint approved — the build→save→version→fill→submit loop works end to end including version pinning
</verification>

<success_criteria>
- D-05/D-06: old custom-shape data wiped; coltorapps-shape smoke-test template seeded and live
- BUILDER-03: re-save creates an immutable version 2 without mutating version 1 (verified by the human in step 7)
- D-03: the pre-coltorapps builder, renderer, and types are fully deleted — the cutover is complete
- The phase verification is real, not a false positive — migration 010 is applied to the live database
</success_criteria>

<output>
After completion, create `.planning/phases/13-form-builder-foundation/13-04-SUMMARY.md`.
</output>
