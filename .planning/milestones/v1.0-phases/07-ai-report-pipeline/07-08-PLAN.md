---
phase: 07-ai-report-pipeline
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - package-lock.json
autonomous: true
gap_closure: true
requirements: [REPORT-05, REPORT-06]
must_haves:
  truths:
    - "@react-pdf/renderer is resolvable from node_modules at runtime and build-time"
    - "`npm run build` completes without `Module not found: Can't resolve '@react-pdf/renderer'`"
    - "finalizeReport's dynamic `import('@/lib/pdf/generator')` no longer throws ERR_MODULE_NOT_FOUND"
  artifacts:
    - path: "node_modules/@react-pdf/renderer/package.json"
      provides: "Installed @react-pdf/renderer package metadata proving the dep is materialised on disk"
      contains: "\"name\": \"@react-pdf/renderer\""
    - path: "node_modules/@react-pdf/renderer/lib/index.js"
      provides: "CJS entry point that Node/Next resolves when finalizeReport calls the dynamic import"
  key_links:
    - from: "lib/pdf/generator.tsx (line 2)"
      to: "@react-pdf/renderer"
      via: "static ES import"
      pattern: "from\\s+['\"]@react-pdf/renderer['\"]"
    - from: "app/admin/assessments/actions.ts:finalizeReport"
      to: "lib/pdf/generator"
      via: "dynamic import → generateReportPdfBuffer"
      pattern: "await\\s+import\\(['\"]@/lib/pdf/generator['\"]\\)"
---

<objective>
Restore `@react-pdf/renderer` to `node_modules` so finalizeReport's PDF render path is reachable at runtime and `npm run build` completes cleanly. This closes the REPORT-05/REPORT-06 BLOCKER documented in `07-VERIFICATION.md` (gap #2) and in `deferred-items.md` (the `@react-pdf/renderer` entry).

Purpose: Without a resolvable `@react-pdf/renderer`, every path that produces a client-deliverable PDF crashes: finalizeReport cannot generate the buffer, cannot upload to the `reports` bucket, cannot flip status to `completed`, and cannot dispatch the n8n `report_ready` notification. The entire delivery side of the Phase 7 goal ("produces a branded PDF report") is non-functional in the current worktree state. The dep is declared in `package.json` (`"@react-pdf/renderer": "^4.5.1"`) but `node_modules/@react-pdf/renderer/package.json` is absent, indicating a partial/broken install — `npm install` is the canonical fix per `deferred-items.md`.
Output: A correctly populated `node_modules/@react-pdf/renderer/` tree, an updated `package-lock.json` if npm decides to lock new transitive versions, and a green `npm run build` (the same command that currently fails per `deferred-items.md`).
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-VERIFICATION.md
@.planning/phases/07-ai-report-pipeline/deferred-items.md
@.planning/phases/07-ai-report-pipeline/07-06-SUMMARY.md
@./CLAUDE.md
@./AGENTS.md

<interfaces>
<!-- The dynamic-import path finalizeReport uses. Executor does NOT modify this file - the point of this plan is to make the dep that this file imports resolvable. -->

From lib/pdf/generator.tsx (line 2):
```
import { renderToBuffer } from '@react-pdf/renderer'
```

From package.json (line 20):
```
"@react-pdf/renderer": "^4.5.1",
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reinstall dependencies to materialise @react-pdf/renderer</name>
  <files>package-lock.json</files>
  <read_first>
    - package.json (confirm `"@react-pdf/renderer": "^4.5.1"` is still present at line 20)
    - .planning/phases/07-ai-report-pipeline/deferred-items.md (the `@react-pdf/renderer` entry — explicitly recommends `npm install` as the fix)
    - .planning/phases/07-ai-report-pipeline/07-VERIFICATION.md gap #2 (truth wording: "Branded PDF rendered and stored in `reports` bucket")
  </read_first>
  <action>
    Step 1: Verify the pre-condition matches VERIFICATION.md — run `ls node_modules/@react-pdf/renderer/package.json`. Expected: file not found (broken install). If the file IS present and contains `"name": "@react-pdf/renderer"`, skip to Step 4 (the gap has already self-resolved between verification and execution; proceed directly to build verification).

    Step 2: Run `npm install` at the repo root. Do NOT pass `--force`, `--legacy-peer-deps`, or any other override flag — package.json already pins `^4.5.1` and the lockfile should resolve cleanly. If npm reports peer-dependency conflicts, STOP and surface the exact npm output in the SUMMARY (do not silently work around them).

    Step 3: Confirm the install actually populated the renderer module — `ls node_modules/@react-pdf/renderer/package.json` must now succeed AND `cat node_modules/@react-pdf/renderer/package.json | head -3` must show `"name": "@react-pdf/renderer"`.

    Step 4: Run `npm run build`. This is the same command that fails per `deferred-items.md` with `Module not found: Can't resolve '@react-pdf/renderer'`. A successful build proves the dep is now resolvable through Next.js's module resolution (not just present on disk).

    Notes & constraints:
    - Do NOT modify package.json (the `^4.5.1` pin is correct per VERIFICATION.md).
    - Do NOT add `@react-pdf/renderer` via `npm install @react-pdf/renderer` as a separate add — that would bump the version specifier. A plain `npm install` rehydrates from the existing lockfile.
    - Do NOT delete `node_modules` first. If `npm install` does not converge on its own, surface the failure rather than escalating to `rm -rf node_modules && npm install` without approval.
    - `npm run build` may surface pre-existing `SUPABASE_URL is required` failure documented in the OTHER `deferred-items.md` entry (the env-var one); that failure is OUT OF SCOPE for this plan. If it appears, set the build env via `SUPABASE_URL=https://stub.invalid SUPABASE_SERVICE_ROLE_KEY=stub npm run build` (any non-empty value satisfies the runtime guard at build collection time), document the workaround in the SUMMARY, and confirm the relevant assertion: ZERO occurrences of `Can't resolve '@react-pdf/renderer'` in the build output. The env-var deferral remains in deferred-items.md untouched.
    - `package-lock.json` may legitimately change if npm refreshes transitive metadata; commit the change. If the lockfile does NOT change, the only "modified" artifact will be `node_modules/` (untracked by git) — note this in the SUMMARY.
  </action>
  <verify>
    <automated>test -f "node_modules/@react-pdf/renderer/package.json" &amp;&amp; node -e "console.log(require('@react-pdf/renderer/package.json').name)" | grep -x "@react-pdf/renderer"</automated>
    <automated>SUPABASE_URL=https://stub.invalid SUPABASE_SERVICE_ROLE_KEY=stub npm run build 2&gt;&amp;1 | tee /tmp/build.log; ! grep -q "Can't resolve '@react-pdf/renderer'" /tmp/build.log</automated>
    <automated>node -e "const m = require('@react-pdf/renderer'); if (typeof m.renderToBuffer !== 'function') { console.error('renderToBuffer is not a function'); process.exit(1); } console.log('renderToBuffer resolved');"</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/@react-pdf/renderer/package.json` exists and its `name` field is `@react-pdf/renderer`
    - `require('@react-pdf/renderer').renderToBuffer` is a callable function (the same export used by `lib/pdf/generator.tsx`)
    - `npm run build` does NOT emit the string `Can't resolve '@react-pdf/renderer'` in its output
    - No edits to `package.json` (`grep -c "@react-pdf/renderer" package.json` still returns 1, version specifier unchanged)
    - `deferred-items.md` `@react-pdf/renderer` entry is updated in-place with a closure note pointing at this plan's SUMMARY — the env-var entry above it is left untouched
  </acceptance_criteria>
  <done>
    `@react-pdf/renderer` is resolvable from Node and Next.js. The next time `finalizeReport` runs against a real submission (manually or via UAT), the dynamic `import("@/lib/pdf/generator")` will load, `generateReportPdfBuffer` will execute, and the PDF will be uploadable to the `reports` bucket — unblocking REPORT-05, REPORT-06, REPORT-09 runtime, and REPORT-10 dispatch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Package contents are downloaded over HTTPS; integrity is verified by the lockfile's SHA-512 hashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | npm registry supply-chain | accept | `package-lock.json` already pins SHA-512 integrity hashes for every transitive dep; `npm install` rejects mismatches. No new direct deps introduced — this is a re-hydration from an existing lockfile entry. |
| T-07-08-02 | Denial of Service | `npm install` blocking on a wedged registry/cache | accept | If install hangs/fails, executor surfaces the failure rather than working around it (per action notes). User can re-run in a fresh shell. |
| T-07-08-03 | Information Disclosure | Build-time env stubs in SUMMARY | mitigate | Use literal `https://stub.invalid` and `stub` strings only (documented in action). Do NOT paste any real env value from `.env.local` into commands, logs, or the SUMMARY. |
</threat_model>

<verification>
- `node_modules/@react-pdf/renderer/package.json` is on disk and has `"name": "@react-pdf/renderer"` (currently absent per VERIFICATION.md spot-check "@react-pdf/renderer resolves: FAIL")
- `npm run build` no longer emits `Module not found: Can't resolve '@react-pdf/renderer'` (currently emits this per `deferred-items.md`)
- Dynamic `require('@react-pdf/renderer')` returns an object whose `renderToBuffer` is a function (the API surface used by `lib/pdf/generator.tsx:2`)
- `deferred-items.md` `@react-pdf/renderer` entry annotated with closure pointer to `07-08-SUMMARY.md`
</verification>

<success_criteria>
- All three `<automated>` gates in Task 1 green
- VERIFICATION.md gap #2 (`@react-pdf/renderer` not present) is observably closed by the directory check
- No version churn: `package.json` `@react-pdf/renderer` specifier remains `^4.5.1`
- Phase 7 PDF render contract is reachable end-to-end (gated only on REPORT-08 transcript closure for the full happy-path; this plan unblocks the render half)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-08-SUMMARY.md` covering:
- Whether `npm install` alone resolved it, or any additional intervention was required
- The exact `package-lock.json` diff (if any) — paths and lines changed
- Confirmation of `renderToBuffer` resolvability via the Node one-liner
- Build-log snippet showing the `Can't resolve '@react-pdf/renderer'` line is GONE (and the env-stub workaround that was used, if any)
- In-place edit to `deferred-items.md` marking the `@react-pdf/renderer` deferral CLOSED with a back-reference to this SUMMARY
</output>
