---
phase: 07-ai-report-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/notifications/n8n-dispatch.ts
autonomous: true
requirements:
  - REPORT-10

must_haves:
  truths:
    - "NotificationPayload now includes a `report_ready` variant with the exact field set from CONTEXT D-07"
    - "dispatchNotification function body is unchanged — variant is accepted via discriminated union, posted verbatim to n8n"
  artifacts:
    - path: "lib/notifications/n8n-dispatch.ts"
      provides: "extended NotificationPayload union including report_ready"
      contains: "type: \"report_ready\""
  key_links:
    - from: "Plan 04 finalizeReport"
      to: "lib/notifications/n8n-dispatch.ts NotificationPayload.report_ready"
      via: "type-discriminated payload"
      pattern: "type: \"report_ready\""
---

<objective>
Extend the `NotificationPayload` discriminated union in `lib/notifications/n8n-dispatch.ts` with the `report_ready` variant locked by CONTEXT D-07. No runtime change — only a TypeScript type extension.

Purpose: Plan 04 (`finalizeReport` extension) is the consumer; it cannot type-check until this variant exists. Splitting it into its own plan keeps Wave 1 wide (Plans 01 and 03 ship in parallel, neither blocks the other) and lets `dispatchNotification` ship a clean 1-commit diff.

Output: 1 file modified, ~10 lines added.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-CONTEXT.md
@.planning/phases/07-ai-report-pipeline/07-PATTERNS.md
@lib/notifications/n8n-dispatch.ts

<interfaces>
<!-- Existing union pattern (lines 3-29). Plan 03 appends one new variant. -->

export type NotificationPayload =
  | { type: "expiry_alert"; ... }
  | { type: "document_uploaded"; ... }
  | { type: "assignment_reminder"; ... }
  | {
      type: "report_ready"
      client_email: string
      client_name: string
      report_url: string         // 7-day signed URL — Plan 04 supplies this
      assessment_date: string    // en-GB formatted, matches PDF header
      report_storage_path: string // for n8n logging / dedup
    }

<!-- Email subject (n8n side, per CONTEXT §specifics — informational only): -->
//   "Your Fire Risk Assessment is ready — {client_name}"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend NotificationPayload union with report_ready variant (REPORT-10, D-07)</name>
  <files>lib/notifications/n8n-dispatch.ts</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-07 — full payload shape)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "lib/notifications/n8n-dispatch.ts" — existing union pattern)
    - lib/notifications/n8n-dispatch.ts (entire file — only 67 lines)
  </read_first>
  <action>
    Add a new variant to the `NotificationPayload` discriminated union (currently lines 3-29). Append after the `assignment_reminder` arm. Field set, in order, must match CONTEXT §D-07 verbatim:
    - `type: "report_ready"`
    - `client_email: string`
    - `client_name: string`
    - `report_url: string` (7-day signed URL, supplied by Plan 04 — preserve the comment "7-day signed URL")
    - `assessment_date: string` (preserve comment "en-GB formatted, matches PDF header")
    - `report_storage_path: string` (preserve comment "for n8n logging / dedup")

    DO NOT touch the `dispatchNotification` function body (lines 37-67) — it is provider-agnostic and forwards the union verbatim. DO NOT add helper functions, builders, or wrappers. Pure type-union extension.
  </action>
  <verify>
    <automated>grep -c "type: \"report_ready\"" lib/notifications/n8n-dispatch.ts; grep -E "report_url|assessment_date|report_storage_path" lib/notifications/n8n-dispatch.ts | wc -l; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "n8n-dispatch\.ts.*error" || echo "TYPECHECK_OK"</automated>
  </verify>
  <done>
    File contains exactly one `type: "report_ready"` discriminator (grep == 1); the three new field comments (`report_url`, `assessment_date`, `report_storage_path`) all present (wc -l == 3 minimum); `tsc --noEmit` clean; `dispatchNotification` body unchanged (line count of the function unchanged ± 0 lines).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → n8n webhook | Outbound POST carrying signed URL + customer email; HMAC-ish secret in `X-Webhook-Secret` header |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | I (Info disclosure) | NotificationPayload.report_url in transit | mitigate | TLS via n8n endpoint (existing `N8N_WEBHOOK_URL` enforced HTTPS in prod); `dispatchNotification` already gates on `N8N_WEBHOOK_URL` + `N8N_WEBHOOK_SECRET` presence (lines 41-49) |
| T-07-03-02 | T (Tampering) | webhook body in flight | mitigate | `X-Webhook-Secret` header (line 56) — n8n side validates; out-of-scope for this plan |
| T-07-03-03 | I | report_storage_path in payload | accept | Path is `{client_id}/report_{submissionId}.pdf` — already a Supabase-internal identifier, not customer-readable as-is; n8n uses it for dedup logging only |
| T-07-03-04 | S (Spoofing) | injected NotificationPayload at call site | mitigate | Discriminated union ensures TypeScript callers cannot omit required fields; Plan 04 supplies all six |

</threat_model>

<verification>
- `lib/notifications/n8n-dispatch.ts` contains the new `report_ready` arm
- TypeScript build clean
- `dispatchNotification` function body byte-identical to pre-edit state (line 37-67 unchanged)
- Plan 04 can construct `{ type: "report_ready" as const, ... }` and pass to `dispatchNotification` without TS errors
</verification>

<success_criteria>
- 1 file modified, single semantic edit
- Plan 04 unblocked to wire dispatch
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-03-SUMMARY.md` listing the union arm added, the unchanged dispatcher body, and confirming TS build green.
</output>
