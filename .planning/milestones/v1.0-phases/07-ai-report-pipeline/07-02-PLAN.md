---
phase: 07-ai-report-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/admin/assessments/actions.ts
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-07
  - REPORT-12

must_haves:
  truths:
    - "D-01/D-02: runReportDraftGeneration builds its prompt via buildReportPrompt(...) using YELLOW_BROOM_EXEMPLAR — the inline template literal at actions.ts:466-472 is gone; provider/schema lock from AI-SPEC §2 preserved (no change to generateObject call or reportSchema shape)"
    - "On any throw inside generateObject, a workflow_errors row is inserted with workflow_name='ai_report_draft' before the function rethrows"
    - "On AI failure, form_submissions.status flips to 'ai_draft_failed' BEFORE the rethrow so the Review page can render the retry CTA"
    - "D-03: Repeating-section expansion (actions.ts:443) is preserved — Plan 02 does NOT change it"
    - "submitAssessmentAction's auto-trigger remains email-free: only the existing draft path is invoked, no dispatchNotification call is added (D-06)"
  artifacts:
    - path: "app/admin/assessments/actions.ts"
      provides: "runReportDraftGeneration with exemplar-injected prompt + workflow_errors wrap + ai_draft_failed status flip"
      contains: "buildReportPrompt"
  key_links:
    - from: "app/admin/assessments/actions.ts runReportDraftGeneration"
      to: "lib/ai/prompt-builder.ts buildReportPrompt"
      via: "named import"
      pattern: "from \"@/lib/ai/prompt-builder\""
    - from: "app/admin/assessments/actions.ts catch block"
      to: "supabase workflow_errors table"
      via: "adminClient.from(\"workflow_errors\").insert"
      pattern: "workflow_name: \"ai_report_draft\""
---

<objective>
Extend `runReportDraftGeneration` (`app/admin/assessments/actions.ts:405-512`) so it (a) builds the prompt via Plan 01's `buildReportPrompt(...)` with the YELLOW BROOM exemplar (D-02), and (b) wraps the `generateObject` call in a workflow_errors-logging catch that flips the submission to `status='ai_draft_failed'` (D-10) before rethrowing.

Purpose: This is the AI draft pipeline core. REPORT-01 (trigger → format prompt), REPORT-02 (structured output via Zod schema-bounded `generateObject`), REPORT-07 (atomic status transitions — including the new failure state), REPORT-12 (workflow_errors visibility) all collapse onto this one function. The status-transition contract for `draft_ready_for_review` and `ai_draft_failed` becomes load-bearing for Plan 05/06's Review UI work.

Output: Single-file edit. ≤ 60 lines of changes inside one function. No new files, no migration.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-CONTEXT.md
@.planning/phases/07-ai-report-pipeline/07-AI-SPEC.md
@.planning/phases/07-ai-report-pipeline/07-PATTERNS.md
@app/admin/assessments/actions.ts
@app/api/cron/expiry/route.ts
@app/admin/compliance/actions.ts

<interfaces>
<!-- New imports Plan 02 introduces. -->

import { YELLOW_BROOM_EXEMPLAR } from "@/lib/ai/exemplars/yellow-broom-fra"
import { buildReportPrompt } from "@/lib/ai/prompt-builder"

<!-- workflow_errors table input columns (live schema — see PATTERNS.md correction #1). -->

workflow_errors {
  workflow_name TEXT NOT NULL          // 'ai_report_draft' — NOT 'workflow_type'
  error_message TEXT NOT NULL
  payload       JSONB                  // embed { submission_id, stack, severity: 'high' } here
}

<!-- Locked status taxonomy (D-09). -->

form_submissions.status canonical values:
- in_progress
- submitted
- draft_ready_for_review     // existing happy path
- completed
- ai_draft_failed            // NEW — set by Plan 02 catch block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline prompt with buildReportPrompt + YELLOW_BROOM_EXEMPLAR (REPORT-03 wiring, D-02)</name>
  <files>app/admin/assessments/actions.ts</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-02; §code_context — runReportDraftGeneration preserved repeating-section expansion)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (section "Exemplar injection (D-02)" code excerpt)
    - app/admin/assessments/actions.ts lines 1-50 (existing imports — where to add the new two)
    - app/admin/assessments/actions.ts lines 405-512 (runReportDraftGeneration — full body to understand the call site)
    - lib/ai/prompt-builder.ts (Plan 01 output — argument shape)
    - lib/ai/exemplars/yellow-broom-fra.ts (Plan 01 output — exported constant name)
  </read_first>
  <action>
    Edit `app/admin/assessments/actions.ts`:
    1. Add two named imports near the top of the file (after the existing `@ai-sdk/openai` / `ai` imports):
       - `import { YELLOW_BROOM_EXEMPLAR } from "@/lib/ai/exemplars/yellow-broom-fra"`
       - `import { buildReportPrompt } from "@/lib/ai/prompt-builder"`
    2. Inside `runReportDraftGeneration` (lines 405-496), replace the inline `prompt: \`Act as a Fire Risk Assessor...\`` template literal at lines 466-472 with:
       ```
       prompt: buildReportPrompt({
         exemplar: YELLOW_BROOM_EXEMPLAR,
         exemplarLabel: "YELLOW BROOM 2023 FRA, anonymised",
         expandedAnswers,
       }),
       ```
    3. Do NOT touch the surrounding `generateObject` config (`model`, `schema`) or the repeating-section expansion (`expandedAnswers` computation at line 443). Those stay verbatim — D-03 locks the expansion behaviour.
    4. Do NOT remove the existing console.error / rethrow in the existing catch block in this task — Task 2 will overhaul that block.
    Preserve all existing behaviour outside the prompt swap; verify the function still type-checks against the existing `reportSchema` (lines 455-464).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "actions\.ts.*error" || echo "TYPECHECK_OK"; grep -n "buildReportPrompt" app/admin/assessments/actions.ts | head -5; grep -n "YELLOW_BROOM_EXEMPLAR" app/admin/assessments/actions.ts | head -5; grep -c "Act as a Fire Risk Assessor" app/admin/assessments/actions.ts</automated>
  </verify>
  <done>
    `actions.ts` contains exactly one `buildReportPrompt(` call inside `runReportDraftGeneration`; the literal string `"Act as a Fire Risk Assessor"` no longer appears in the file (grep count == 0); `YELLOW_BROOM_EXEMPLAR` import line present; `npx tsc --noEmit` reports no new errors in this file (TYPECHECK_OK emitted).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap generateObject in workflow_errors + ai_draft_failed catch (REPORT-01, REPORT-02, REPORT-07, REPORT-12, D-10)</name>
  <files>app/admin/assessments/actions.ts</files>
  <behavior>
    - Happy path: `generateObject` returns object → `draft_report_json` updated → `status='draft_ready_for_review'` (existing happy path, unchanged)
    - AI failure path (e.g. OpenRouter 500 / Zod schema violation): catch block fires → insert workflow_errors row with `workflow_name='ai_report_draft'`, `error_message=err.message`, `payload={ submission_id, stack, severity: 'high' }` → update `form_submissions.status='ai_draft_failed'` → `revalidatePath` Review URL → rethrow so the calling Server Action surfaces the error to the client
    - Order: workflow_errors INSERT must happen BEFORE the status update, both BEFORE the rethrow (D-10 explicit ordering)
  </behavior>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-09 status taxonomy; D-10 wrap order; D-11 acceptance criteria — these drive the test in Plan 07)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (sections "Workflow-errors wrap pattern" and "SCHEMA NOTE" — confirms workflow_name/error_message/payload columns, NOT workflow_type/severity)
    - app/admin/assessments/actions.ts lines 467-495 (existing try/catch to overhaul)
    - app/api/cron/expiry/route.ts lines 100-125 (analog workflow_errors insert)
    - app/admin/compliance/actions.ts lines 70-90 (second analog — same column names)
  </read_first>
  <action>
    Edit the existing catch block inside `runReportDraftGeneration` (currently at `app/admin/assessments/actions.ts:492-495`). Replace its body with:
    1. Keep the `console.error("generateReportDraft failed:", err)` line for dev visibility.
    2. INSERT a `workflow_errors` row via `adminClient` (NOT the user-scoped `supabase` client — adminClient bypasses RLS so the insert succeeds even when the Server Action was triggered from a non-admin context like the after-submit callback). Columns to write (use exact live schema names per PATTERNS.md correction #1):
       - `workflow_name: "ai_report_draft"` (snake_case literal — matches D-10)
       - `error_message: err?.message ?? String(err)`
       - `payload: { submission_id: submissionId, stack: err?.stack ?? null, severity: "high" }`
       DO NOT add a `workflow_type` or `severity` top-level column — they do not exist. Severity nests under `payload` per PATTERNS.md.
    3. UPDATE `form_submissions` setting `status: "ai_draft_failed"` where `id == submissionId` via the same `adminClient`.
    4. Call `revalidatePath(\`/admin/assessments/${submissionId}/review\`)` so the Review page re-renders with the retry CTA from Plan 06.
    5. Rethrow `new Error(\`Failed to generate report draft via AI: ${err.message || String(err)}\`)` (preserves the existing error message shape used by the manual Server Action wrapper at line 503-512).
    Do this in this exact order — workflow_errors insert FIRST (so the audit row exists even if the status update later fails), THEN status update, THEN revalidate, THEN throw. Awaits on both Supabase calls — do not fire-and-forget.

    Also tighten the success branch: confirm the happy path still calls `revalidatePath("/admin/assessments")` and `revalidatePath(\`/admin/assessments/${submissionId}/review\`)` (existing lines 488-489) — do NOT remove or duplicate these.

    D-06 cross-check (no email from draft path): scan the entire `runReportDraftGeneration` body after the edit and confirm there is NO call to `dispatchNotification`, no import of it inside the function, and no `n8n` literal. The draft path must remain delivery-side-effect-free. Plan 07 will codify this as a test; this task just preserves the contract.
  </action>
  <verify>
    <automated>grep -n "workflow_name: \"ai_report_draft\"" app/admin/assessments/actions.ts; grep -n "status: \"ai_draft_failed\"" app/admin/assessments/actions.ts; grep -c "dispatchNotification" app/admin/assessments/actions.ts; awk '/async function runReportDraftGeneration/,/^}/' app/admin/assessments/actions.ts | grep -c "dispatchNotification"; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "actions\.ts.*error" || echo "TYPECHECK_OK"</automated>
  </verify>
  <done>
    Catch block in `runReportDraftGeneration` contains, in order: console.error → adminClient.from("workflow_errors").insert with `workflow_name: "ai_report_draft"` literal → adminClient.from("form_submissions").update({status: "ai_draft_failed"}) → revalidatePath → throw. The substring `dispatchNotification` does NOT appear anywhere inside the `runReportDraftGeneration` function body (D-06 hard guarantee — the awk grep count == 0; the file-level count may be >0 because Plan 04 will add it inside `finalizeReport`). TypeScript still compiles clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer answers_json → AI prompt | Submission content is customer-controlled; flows through `expandRepeatingSections` and `buildReportPrompt` into the LLM |
| AI response → form_submissions.draft_report_json | LLM output (Zod-validated by `generateObject`) lands in DB and renders into Matt's Review UI |
| OPENROUTER_API_KEY → server-side env | Secret material, must never reach the client bundle |
| catch block → workflow_errors | Failure observability surface for /admin/month-summary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T (Prompt injection) | answers_json → prompt | mitigate | NO_HALLUCINATION guard precedes answers in `buildReportPrompt`; `generateObject` Zod-schema constrains output to `reportSchema` so injected instructions cannot escape the structured-output channel into raw HTML / Markdown rendering |
| T-07-02-02 | I (Info disclosure) | OPENROUTER_API_KEY | mitigate | Key only read server-side via `process.env.OPENROUTER_API_KEY` inside `runReportDraftGeneration` (line 451); function is not exported as a Server Action — only the wrapper `generateReportDraft` is, which does not echo the key |
| T-07-02-03 | T | workflow_errors row content | mitigate | `payload` jsonb stores `err.stack` which may contain repo paths — accepted (admin-only surface at /admin/month-summary). No customer PII is intentionally placed in `payload` beyond `submission_id` |
| T-07-02-04 | D (Denial via repeated failures) | OpenRouter outage | accept | Per D-09 the row stays in `ai_draft_failed` until Matt retries from the Review page; no auto-retry loop introduced (avoids cost amplification under sustained outage) |
| T-07-02-05 | R (Repudiation) | who triggered the failed draft | mitigate | `adminClient` insert into workflow_errors captures submission_id; auth context preserved at the calling Server Action layer (`generateReportDraft` line 504-509) — wrap is internal only |
| T-07-02-06 | I (Cross-org access) | adminClient bypass of RLS | accept | Service-role client is necessary for workflow_errors writes from non-admin contexts (after-submit callback). Existing pattern documented in PATTERNS.md "Auth gate" section; callers of `runReportDraftGeneration` already gate on auth |

</threat_model>

<verification>
- `buildReportPrompt` import + call site present in `runReportDraftGeneration`
- Old inline prompt string ("Act as a Fire Risk Assessor") removed (grep count == 0)
- Catch block writes `workflow_errors` row with literal `workflow_name: "ai_report_draft"`
- Catch block flips status to `ai_draft_failed` BEFORE rethrow
- No `dispatchNotification` call inside `runReportDraftGeneration` body (D-06 contract)
- `npm run build` passes
- `tsc --noEmit` clean for this file
</verification>

<success_criteria>
- Single-file diff in `app/admin/assessments/actions.ts`
- Plan 01 imports cleanly into Plan 02
- Plan 07 can mock OpenRouter to throw, expect a `workflow_errors` row + `ai_draft_failed` status, and the test scaffolding does not require additional production-code changes
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-02-SUMMARY.md` documenting: lines changed in `actions.ts`, before/after snippet of the prompt swap, before/after catch block, confirmation that `dispatchNotification` is absent from `runReportDraftGeneration`, and the exact workflow_errors row shape now written.
</output>
