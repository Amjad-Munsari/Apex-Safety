---
phase: 07-ai-report-pipeline
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/month-summary/page.tsx
autonomous: true
gap_closure: true
requirements: [REPORT-12]
must_haves:
  truths:
    - "/admin/month-summary lists individual workflow_errors rows (workflow_name + error_message + submission_id + created_at), not only a count"
    - "Matt can read the failure reason for an `ai_report_draft` or `report_delivery_email` row without leaving the dashboard"
    - "statusLabel and statusColor maps render the D-09 canonical taxonomy (`completed`, `ai_draft_failed`, `submitted`, `draft_ready_for_review`) — legacy `draft`/`delivered` keys retained only as backward-compat fallback for pre-D-09 rows"
  artifacts:
    - path: "app/admin/month-summary/page.tsx"
      provides: "Workflow Errors list section between the stat cards row and the Assessments table"
      contains: "from(\"workflow_errors\")"
    - path: "app/admin/month-summary/page.tsx"
      provides: "Updated statusLabel/statusColor maps with D-09 canonical keys"
      contains: "ai_draft_failed"
  key_links:
    - from: "MonthSummaryPage (server component)"
      to: "workflow_errors table"
      via: "adminClient.from('workflow_errors').select('id, workflow_name, error_message, submission_id, severity, created_at').gte('created_at', startOfMonth).order(...).limit(...)"
      pattern: "workflow_errors[\\s\\S]*workflow_name[\\s\\S]*error_message"
    - from: "MonthSummaryPage Workflow Errors section"
      to: "/admin/assessments/{submission_id}"
      via: "Next Link wrapping each row when submission_id is non-null"
      pattern: "Link[^>]*assessments"
---

<objective>
Add a row-level Workflow Errors list section to `/admin/month-summary` and update the status-display maps to the D-09 canonical taxonomy, closing the REPORT-12 / CONTEXT D-11(c) gap documented in `07-VERIFICATION.md` (gap #3). Currently the page shows only `errorsRes.count` ("Workflow Errors: 1") — D-11 acceptance explicitly requires "`/admin/month-summary` shows the error", which a count badge does not satisfy. The page also still maps legacy `draft`/`delivered` status tokens; D-09 canonical statuses (`completed`, `ai_draft_failed`) render as raw lowercase strings today.

Purpose: D-11 (`07-CONTEXT.md` lines 54-55) is the acceptance gate for REPORT-12. It says: "trigger an AI failure (mocked OpenRouter 500) and verify (a) `workflow_errors` row inserted, (b) `status='ai_draft_failed'`, (c) `/admin/month-summary` shows the error, (d) Review page renders a retry CTA." Items (a), (b), (d) are verified green; (c) is observably partial — the page renders a count of errors but no row content, so Matt cannot tell which workflow failed, why, or for which submission. D-10 reinforces: "Row is visible in `/admin/month-summary`". The fix is additive: keep the existing count card; insert an errors list section below the stat cards row; align the existing status maps with the D-09 taxonomy that `runReportDraftGeneration` and `finalizeReport` already write.
Output: An updated `app/admin/month-summary/page.tsx` that fetches the row-level workflow_errors data alongside the existing count, renders a list section with severity styling, and uses D-09 canonical status keys in the assessments table.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-CONTEXT.md
@.planning/phases/07-ai-report-pipeline/07-VERIFICATION.md
@app/admin/month-summary/page.tsx
@./CLAUDE.md
@./AGENTS.md

<interfaces>
<!-- Current month-summary structure — adapt these patterns, do NOT rewrite the page. -->

From app/admin/month-summary/page.tsx (lines 13-30) — existing fetch pattern:
```
const [assessmentsRes, documentsRes, proposalsRes, errorsRes] = await Promise.all([
  adminClient.from("form_submissions").select("*", { count: "exact", head: true }).gte("created_at", startOfMonth),
  adminClient.from("documents").select("*", { count: "exact", head: true }).gte("uploaded_at", startOfMonth),
  adminClient.from("proposals").select("*", { count: "exact", head: true }).gte("created_at", startOfMonth),
  adminClient.from("workflow_errors").select("*", { count: "exact", head: true }).gte("created_at", startOfMonth),
])
```

From app/admin/month-summary/page.tsx (lines 77-89) — existing status maps (legacy keys; D-09 keys missing):
```
const statusLabel: Record<string, string> = {
  draft: "Draft",
  draft_ready_for_review: "Awaiting Review",
  delivered: "Delivered",
  submitted: "Submitted",
}
const statusColor: Record<string, string> = {
  draft: "text-[#555]",
  draft_ready_for_review: "text-gold",
  delivered: "text-[#3b8273]",
  submitted: "text-white/60",
}
```

From app/admin/month-summary/page.tsx (lines 122-164) — the "RECENT ASSESSMENTS" Card section is the visual template for the new errors list section (same Card wrapper, same border styling, same table layout language).

From CONTEXT D-09 (locked status taxonomy — `07-CONTEXT.md` lines 44-52):
```
| in_progress | Customer is filling the form, autosave only |
| submitted | Customer hit Submit; AI draft not yet generated |
| draft_ready_for_review | draft_report_json populated, awaiting Matt |
| completed | Matt approved, PDF stored, delivery dispatched |
| ai_draft_failed | AI generation threw; row stays here until Matt retries |
```

From CONTEXT D-10 (locked row-visibility requirement — `07-CONTEXT.md` line 54):
```
"Row is visible in `/admin/month-summary`"
```

Workflow_errors columns the new section consumes (canonical names used by actions.ts:508-516 INSERT):
```
id            UUID
workflow_name TEXT  (values: 'ai_report_draft', 'report_delivery_email', plus pre-existing tags from other phases)
error_message TEXT
submission_id UUID NULL  (nullable because some workflow_errors are not submission-scoped)
severity      TEXT  (values: 'high', 'medium', 'low')
payload       JSONB NULL
created_at    TIMESTAMPTZ
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add row-level workflow_errors list section and update D-09 status maps in /admin/month-summary</name>
  <files>app/admin/month-summary/page.tsx</files>
  <read_first>
    - app/admin/month-summary/page.tsx (entire file — 167 lines; you are modifying the only file in this plan, READ IT IN FULL)
    - .planning/phases/07-ai-report-pipeline/07-VERIFICATION.md gap #3 (truth wording + the four `artifacts.issue` items)
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md §D-09 (status taxonomy) and §D-10/D-11 (row-visibility requirement)
    - app/admin/assessments/actions.ts lines 507-525 (the INSERT path for `workflow_name='ai_report_draft'` — confirms the column names you fetch)
    - app/admin/assessments/actions.ts lines 822-832 (the INSERT path for `workflow_name='report_delivery_email'` — same)
  </read_first>
  <action>
    Apply ALL of the following edits in a single commit. The file is small (167 lines); do not refactor unrelated code.

    Edit 1 — Add a fifth fetch to the existing Promise.all (lines 13-30). Append a new entry that retrieves up to 25 recent rows with the columns the list section will render. Use the EXACT same `adminClient.from("workflow_errors")` builder pattern, NOT a separate await. Final 5-element array:
    ```
    const [assessmentsRes, documentsRes, proposalsRes, errorsRes, errorRowsRes] = await Promise.all([
      // ... existing 4 unchanged ...
      adminClient
        .from("workflow_errors")
        .select("id, workflow_name, error_message, submission_id, severity, created_at")
        .gte("created_at", startOfMonth)
        .order("created_at", { ascending: false })
        .limit(25),
    ])
    const recentErrors = errorRowsRes.data ?? []
    ```
    Notes:
    - Reuse `startOfMonth` already computed at line 10. Do not introduce a separate time window.
    - 25-row cap is a reasonable bound for one month of admin viewing — small dashboards, not pagination. Document this in the SUMMARY.
    - Do NOT delete the existing `errorsRes` count fetch. The stat card at line 67-75 still shows the total count; the new section shows the rows. Both surfaces are required.

    Edit 2 — Update the status maps at lines 77-89 to include the D-09 canonical keys WITHOUT removing the legacy keys (backward compat for any pre-D-09 rows in the DB):
    ```
    const statusLabel: Record<string, string> = {
      // D-09 canonical taxonomy (per 07-CONTEXT.md)
      in_progress: "In Progress",
      submitted: "Submitted",
      draft_ready_for_review: "Awaiting Review",
      completed: "Completed",
      ai_draft_failed: "AI Draft Failed",
      // Legacy keys retained for pre-D-09 rows
      draft: "Draft",
      delivered: "Delivered",
    }
    const statusColor: Record<string, string> = {
      in_progress: "text-white/40",
      submitted: "text-white/60",
      draft_ready_for_review: "text-gold",
      completed: "text-[#3b8273]",
      ai_draft_failed: "text-danger",
      // Legacy
      draft: "text-[#555]",
      delivered: "text-[#3b8273]",
    }
    ```
    Notes:
    - `text-danger` matches the same token already used by the Workflow Errors stat card (line 72 `color: errorsRes.count && errorsRes.count > 0 ? "danger" : "[#555]"`). If `text-danger` is not in the existing Tailwind config, fall back to `text-red-400` and document the choice in the SUMMARY.
    - The fallback `statusLabel[a.status] || a.status` at line 152 already handles unknown statuses by rendering the raw string — leave that fallback alone.

    Edit 3 — Insert a new Card section between the stat cards grid (closing `</div>` at line 120) and the "RECENT ASSESSMENTS" Card opening (line 123). Pattern-match the visual language of the existing assessments Card (same `bg-[#1c1c1c] border-white/5 rounded-sm overflow-hidden` wrapper, same header bar style, same table-or-empty-state branching):

    Structure:
    1. `<Card>` wrapper with the same Tailwind classes as the assessments Card
    2. Header row: `<div className="px-6 py-4 border-b border-white/5 flex items-center gap-3">` with `<span className="font-mono text-[10px] uppercase tracking-widest text-[#555]">Workflow Errors This Month</span>` and a count-on-the-right `<span className="font-mono text-[10px] text-white/20 ml-auto">{recentErrors.length} records</span>`
    3. Branch on `recentErrors.length > 0`:
       a. List path: a `<table>` with `<thead>` containing columns `WORKFLOW`, `MESSAGE`, `SEVERITY`, `SUBMISSION`, `WHEN`. Each `<tr>` renders:
          - WORKFLOW: `<td>{e.workflow_name}</td>` with `font-mono text-xs text-white/80`
          - MESSAGE: `<td>{e.error_message}</td>` — render plain text (React's default JSX text-node escaping protects against XSS); cap with `max-w-md truncate` so a long stack-trace one-liner does not blow out the row. NO `dangerouslySetInnerHTML`.
          - SEVERITY: `<span>` colored by severity — `high` → `text-danger` (or `text-red-400` fallback); `medium` → `text-gold`; `low`/null → `text-white/40`. Uppercase, font-mono, tracking-widest.
          - SUBMISSION: if `e.submission_id` is non-null, render a `<Link href={\`/admin/assessments/${e.submission_id}/review\`}>` whose text is the FIRST 8 CHARS of the UUID (e.g. `a1b2c3d4`) styled `font-mono text-[10px] text-white/60 hover:text-white`. If `submission_id` is null, render `"—"` (the project no-mocks empty-state convention).
          - WHEN: `<td>{new Date(e.created_at).toLocaleString("en-GB", { day: "numeric", month: "short", hour: "2-digit", minute: "2-digit" })}</td>` — same locale as the assessments column.
       b. Empty path: `<div className="px-6 py-12 text-center text-white/20 font-mono text-xs uppercase tracking-widest">No workflow errors this month</div>` (mirror the assessments empty state at line 160).

    Notes:
    - The `Link` import at line 3 is already present — reuse it; do not add a second import.
    - Do NOT add an external library (no `date-fns`, no `sonner`, no shadcn Table — the existing page uses raw `<table>` and that is the established pattern).
    - Section header copy is `"Workflow Errors This Month"` — match the title-case rhythm of `"Assessments This Month"` already in use.
    - The stat card at line 67-75 ("Workflow Errors" count, num `04`) stays UNCHANGED. The new section is a separate row-level surface below the cards.
    - Do NOT add a "View all" / pagination link in v1. If `recentErrors.length === 25` (the cap was hit), append a small hint line below the table: `<div className="px-6 py-2 text-[10px] font-mono text-white/30">Showing 25 most recent — see direct DB query for full month</div>`. Do not invent a `/admin/workflow-errors` route.

    Edit 4 — No other changes. Specifically:
    - Do NOT rename the existing `errorsRes` variable
    - Do NOT change the stat card grid (lines 109-120)
    - Do NOT modify the assessments fetch (lines 33-44) or assessments table (lines 122-164)
    - Do NOT add `revalidate` / `noStore` directives — `export const dynamic = "force-dynamic"` at line 6 already drives freshness
    - Do NOT add the proposals workflow tag or other phase tags as filter chips — v1 surface is unscoped (all workflow_errors this month, all tags inline)
  </action>
  <verify>
    <automated>grep -n "workflow_errors" app/admin/month-summary/page.tsx | grep -v '^\s*//\|^\s*#' | wc -l | awk '$1 &gt;= 2 { exit 0 } { exit 1 }'</automated>
    <automated>grep -c "ai_draft_failed" app/admin/month-summary/page.tsx | awk '$1 &gt;= 2 { exit 0 } { exit 1 }'</automated>
    <automated>grep -c "completed" app/admin/month-summary/page.tsx | awk '$1 &gt;= 2 { exit 0 } { exit 1 }'</automated>
    <automated>grep -E "workflow_name|error_message|submission_id|severity" app/admin/month-summary/page.tsx | grep -v "^\s*//" | wc -l | awk '$1 &gt;= 4 { exit 0 } { exit 1 }'</automated>
    <automated>grep -q "Workflow Errors This Month" app/admin/month-summary/page.tsx</automated>
    <automated>grep -q "No workflow errors this month" app/admin/month-summary/page.tsx</automated>
    <automated>! grep -q "dangerouslySetInnerHTML" app/admin/month-summary/page.tsx</automated>
    <automated>npx tsc --noEmit 2&gt;&amp;1 | grep -E "app/admin/month-summary/page\.tsx" ; test $? -ne 0  # PASSES when no TS errors are emitted for this file</automated>
  </verify>
  <acceptance_criteria>
    - All eight `<automated>` gates green
    - `workflow_errors` is referenced in at least 2 non-comment locations (existing count fetch + new row-level fetch)
    - `ai_draft_failed` and `completed` D-09 keys each appear at least 2x (statusLabel + statusColor entries)
    - At least 4 of the columns `workflow_name`, `error_message`, `submission_id`, `severity` are referenced in JSX (one per `<td>` rendering)
    - Section header `"Workflow Errors This Month"` and empty-state copy `"No workflow errors this month"` present verbatim
    - ZERO occurrences of `dangerouslySetInnerHTML` (T-07-10-01 mitigation — error_message is rendered as JSX text only)
    - `tsc --noEmit` emits no errors for `app/admin/month-summary/page.tsx`
    - Stat card (num `04`, label `Workflow Errors`) UNCHANGED at lines 67-75 — both the count badge AND the new list section are present
    - Legacy `draft`/`delivered` status keys retained (backward compat) — `grep -c "draft:" app/admin/month-summary/page.tsx` returns at least 2 hits (statusLabel + statusColor for `draft`)
  </acceptance_criteria>
  <done>
    Matt loads `/admin/month-summary` and sees, immediately below the four stat cards: a "Workflow Errors This Month" section listing each error row with `workflow_name`, truncated `error_message`, severity pill, deep-link to the assessment (when scoped), and timestamp. Status pills in the Assessments table render `Completed` / `AI Draft Failed` / `Awaiting Review` / `Submitted` for D-09 canonical values (instead of the raw lowercase strings that previously showed for `completed` and `ai_draft_failed`). CONTEXT D-10/D-11(c) acceptance ("`/admin/month-summary` shows the error") observably satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow_errors table → admin browser | Error messages may contain stack traces or user-supplied input that flowed into the failure path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Tampering / XSS | error_message rendering | mitigate | Render via JSX text node `{e.error_message}` only — React's default text-node escaping applies. ZERO `dangerouslySetInnerHTML` (verified by `<automated>` gate). `max-w-md truncate` caps display width without bypassing escaping. |
| T-07-10-02 | Information Disclosure | workflow_errors rows may surface customer PII captured in error payloads | accept | Page is admin-only (`/admin/*` middleware-gated, `adminClient` server-side fetch). Same trust surface as the existing assessments fetch already on this page. v1 list shows `error_message` only — `payload` JSONB column is NOT rendered (defensive minimisation). |
| T-07-10-03 | Repudiation | Cap at 25 rows may hide an error from Matt's view | mitigate | Cap-hit hint line ("Showing 25 most recent — see direct DB query for full month") instructs the operator to drop into DB tooling for the full audit when needed. Stat card retains the EXACT month-total count, so total is still visible. |
| T-07-10-04 | Denial of Service | Long error_message dumps blowing out the table layout | mitigate | `max-w-md truncate` Tailwind utility caps per-row width; severity column + workflow_name column are unaffected. |
| T-07-10-05 | Elevation of Privilege | Deep link to `/admin/assessments/{submission_id}/review` for a row whose submission_id belongs to a different org | accept | The target route already enforces admin-only auth + its own RLS — a stale or cross-org submission_id click resolves to the route's own 404 / authz response, not to a privilege bypass on this page. |
</threat_model>

<verification>
- Eight `<automated>` gates in Task 1 all green
- `recentErrors` array is fetched via the existing Promise.all (not a separate await — single round-trip preserved)
- Stat card row UNCHANGED (lines 109-120 remain untouched)
- D-09 canonical keys present in both `statusLabel` and `statusColor`; legacy keys retained for backward compat
- Section uses native `<table>` + Card wrapper consistent with assessments table — no new dep
- No `dangerouslySetInnerHTML` introduced (XSS guard)
</verification>

<success_criteria>
- VERIFICATION.md gap #3 truth observably satisfied: row-level rendering of workflow_name, error_message, submission_id, created_at, with severity styling
- CONTEXT D-10 / D-11(c) acceptance unblocked: `/admin/month-summary` shows the error, not just its count
- D-09 status taxonomy update applied to the assessments table render — `completed` / `ai_draft_failed` no longer render as raw lowercase strings
- No regression to the stat card count, the assessments table, or unrelated dashboard state
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-10-SUMMARY.md` covering:
- Diff scope (line ranges added/changed in `page.tsx`)
- Confirmation that the stat card (`04 Workflow Errors`) is unchanged
- The Tailwind severity color tokens actually used (`text-danger` vs `text-red-400` fallback) — note which path was taken and why
- The behaviour when zero workflow_errors exist this month (empty-state copy renders)
- The behaviour when 25+ workflow_errors exist (cap-hit hint visible)
- Confirmation the new section is purely additive (no removed code) and the legacy `draft`/`delivered` status keys are still present
</output>
