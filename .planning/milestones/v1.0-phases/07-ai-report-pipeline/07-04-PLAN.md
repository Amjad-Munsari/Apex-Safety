---
phase: 07-ai-report-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - app/admin/assessments/actions.ts
autonomous: true
requirements:
  - REPORT-05
  - REPORT-06
  - REPORT-07
  - REPORT-09
  - REPORT-10

must_haves:
  truths:
    - "D-05/D-09: finalizeReport (the sole client-deliverable PDF path) persists report_storage_path AND flips status='completed' atomically (single update call, both columns) — 'completed' is the canonical post-approve state from the D-09 status taxonomy {in_progress, submitted, draft_ready_for_review, completed, ai_draft_failed}"
    - "After the status flip succeeds, finalizeReport mints a 7-day signed URL via createSignedUrl(fileName, 60*60*24*7) — separate call from the existing 5-min Matt URL"
    - "finalizeReport dispatches a `report_ready` payload to n8n; dispatch failure inserts workflow_errors row (workflow_name='report_delivery_email') but does NOT roll back the status flip (D-08)"
    - "finalizeReport returns { success, downloadUrl, deliveryEmailFailed } so review-client.tsx can render the D-08 non-blocking toast"
    - "Branded PDF still rendered via existing generateReportPdfBuffer — REPORT-05 brand text contract preserved (Plan 04 does not touch components/pdf/report-document.tsx)"
  artifacts:
    - path: "app/admin/assessments/actions.ts"
      provides: "finalizeReport with 7-day URL + n8n dispatch + workflow_errors fallback"
      contains: "report_delivery_email"
  key_links:
    - from: "app/admin/assessments/actions.ts finalizeReport"
      to: "lib/notifications/n8n-dispatch.ts dispatchNotification"
      via: "report_ready NotificationPayload"
      pattern: "type: \"report_ready\""
    - from: "app/admin/assessments/actions.ts finalizeReport"
      to: "supabase storage reports bucket"
      via: "createSignedUrl(fileName, 60*60*24*7)"
      pattern: "createSignedUrl.*60 \\* 60 \\* 24 \\* 7"
---

<objective>
Extend `finalizeReport` (`app/admin/assessments/actions.ts:673-757`) so the Approve path (D-05) now: (a) persists `report_storage_path` alongside the `status='completed'` flip in the same `update()` call (already present — verify), (b) mints a 7-day signed URL for the client email separately from the existing 5-min Matt URL, (c) dispatches via the new `report_ready` n8n variant from Plan 03, and (d) writes a `workflow_errors` row on dispatch failure without rolling back the PDF artefact, surfacing a `deliveryEmailFailed` flag to the caller.

Purpose: This is the delivery side of REPORT-10. REPORT-06 / REPORT-07 (storage + atomic status) are already mostly in place; the change locks `report_storage_path` and `status='completed'` into a single statement so the truth is atomic per D-09. REPORT-09 (Matt can approve) was wired in Phase 6; Plan 04 augments it with the email side-effect contract from D-07/D-08.

Output: Single-file edit. ~40 lines added inside one function. No new files, no migration.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-ai-report-pipeline/07-CONTEXT.md
@.planning/phases/07-ai-report-pipeline/07-PATTERNS.md
@app/admin/assessments/actions.ts
@lib/notifications/n8n-dispatch.ts
@app/admin/compliance/actions.ts
@app/api/cron/expiry/route.ts

<interfaces>
<!-- After Plan 03, this variant exists in NotificationPayload: -->

{ type: "report_ready"
  client_email: string
  client_name: string
  report_url: string         // 7-day signed URL
  assessment_date: string    // en-GB formatted
  report_storage_path: string
}

<!-- workflow_errors columns (live schema — see PATTERNS.md correction #1): -->

workflow_errors {
  workflow_name TEXT NOT NULL          // 'report_delivery_email' for the D-08 fallback
  error_message TEXT NOT NULL
  payload       JSONB                  // embed full payload + severity: 'high'
}

<!-- Contact email lookup — copy pattern from cron/expiry/route.ts:87-100: -->
//   adminClient.from("client_users").select("name, email").eq("client_id", submission.client_id).limit(1)

<!-- finalizeReport return shape (NEW per PATTERNS.md correction #4): -->
//   { success: boolean, downloadUrl: string | null, deliveryEmailFailed: boolean }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7-day client URL + n8n dispatch + workflow_errors fallback to finalizeReport (REPORT-10, D-07, D-08)</name>
  <files>app/admin/assessments/actions.ts</files>
  <read_first>
    - .planning/phases/07-ai-report-pipeline/07-CONTEXT.md (§decisions D-05/D-07/D-08 — verbatim contract for dispatch order + workflow_errors row)
    - .planning/phases/07-ai-report-pipeline/07-PATTERNS.md (sections "extend finalizeReport" and "Contact email lookup pattern")
    - app/admin/assessments/actions.ts lines 670-757 (current finalizeReport body)
    - app/admin/compliance/actions.ts lines 30-100 (analog — dispatchNotification with workflow_errors fallback)
    - app/api/cron/expiry/route.ts lines 80-125 (analog — client_users contact lookup + workflow_errors insert)
    - lib/notifications/n8n-dispatch.ts (Plan 03 output — report_ready variant)
  </read_first>
  <action>
    Edit `finalizeReport` in `app/admin/assessments/actions.ts`. Make these changes IN ORDER:

    1. Import `dispatchNotification` from `@/lib/notifications/n8n-dispatch` at the top of the file (if not already present — check existing imports first).

    2. Extend the `submission` SELECT at lines 689-693 to also project the client id (already selected) — additionally fetch `client_users` to resolve the contact email. After the existing `submission` fetch (line 695), add a second query (two-step pattern):
       ```
       const { data: clientUsers } = await adminClient
         .from("client_users")
         .select("name, email")
         .eq("client_id", submission.client_id)
         .limit(1)
       const contact = clientUsers?.[0]
       const contactEmail = contact?.email
       ```
       If `contactEmail` is missing, do NOT throw — set it to an empty string. n8n will be told via the dispatch error path; the PDF artefact is still saved.

    3. Confirm the existing `update()` at lines 734-741 already writes BOTH `report_storage_path: fileName` AND `status: "completed"` in a single statement (REPORT-07 atomicity — D-09). It does. Do not split this into two updates.

    4. AFTER the status update succeeds (line 745) and AFTER the existing `revalidatePath` calls (lines 747-748), BEFORE the existing 5-min signed URL block at lines 750-754:
       a. Mint the client URL with 7-day TTL (D-07):
          ```
          const { data: clientSigned } = await adminClient
            .storage
            .from("reports")
            .createSignedUrl(fileName, 60 * 60 * 24 * 7)
          ```
       b. Build the payload (use `as const` on `type` per discriminated-union pattern in PATTERNS.md):
          ```
          const payload = {
            type: "report_ready" as const,
            client_email: contactEmail ?? "",
            client_name: client?.name ?? "there",
            report_url: clientSigned?.signedUrl ?? "",
            assessment_date: assessmentDate,
            report_storage_path: fileName,
          }
          ```
          (`assessmentDate` is already computed on line 700 using `toLocaleDateString("en-GB", ...)` — reuse that variable, do not re-derive. This guarantees the email date matches the PDF header per D-07.)
       c. Dispatch + fallback (D-08):
          ```
          const dispatchResult = await dispatchNotification(payload)
          let deliveryEmailFailed = false
          if (!dispatchResult.ok) {
            deliveryEmailFailed = true
            await adminClient.from("workflow_errors").insert({
              workflow_name: "report_delivery_email",
              error_message: dispatchResult.error ?? "unknown dispatch failure",
              payload: { ...payload, severity: "high" },
            })
            // do NOT throw — PDF is the artefact of record (D-08)
          }
          ```

    5. Keep the existing 5-min `createSignedUrl(fileName, 60 * 5)` call for Matt's immediate download (D-05 — the existing behaviour stays). DO NOT reuse the 7-day URL for Matt's UI; the two URLs serve different threat models (T-07-04-02 below).

    6. Change the return statement (line 756) from `{ success: true, downloadUrl: signedUrlData?.signedUrl ?? null }` to:
       ```
       return { success: true, downloadUrl: signedUrlData?.signedUrl ?? null, deliveryEmailFailed }
       ```

    DO NOT touch the PDF render call (line 707-716), the upload (line 721-727), or the update statement (line 734-741). DO NOT pre-throw if the contact lookup fails — the dispatch arm will log and `deliveryEmailFailed=true` will surface in the toast (Plan 06 wires that).

    Compatibility cross-check: at the end of the edit, scan the file and confirm `dispatchNotification` is called EXACTLY once (inside finalizeReport). Plan 02 already established it does NOT appear in `runReportDraftGeneration` (D-06).
  </action>
  <verify>
    <automated>grep -n "createSignedUrl(fileName, 60 \* 60 \* 24 \* 7)" app/admin/assessments/actions.ts; grep -n "workflow_name: \"report_delivery_email\"" app/admin/assessments/actions.ts; grep -n "type: \"report_ready\" as const" app/admin/assessments/actions.ts; grep -n "deliveryEmailFailed" app/admin/assessments/actions.ts | wc -l; grep -c "dispatchNotification" app/admin/assessments/actions.ts; awk '/async function runReportDraftGeneration/,/^}/' app/admin/assessments/actions.ts | grep -c "dispatchNotification"; awk '/export async function finalizeReport/,/^}/' app/admin/assessments/actions.ts | grep -c "createSignedUrl"; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E "actions\.ts.*error" || echo "TYPECHECK_OK"</automated>
  </verify>
  <done>
    Inside `finalizeReport`: one `createSignedUrl(fileName, 60 * 60 * 24 * 7)` for the client URL, one existing `createSignedUrl(fileName, 60 * 5)` for Matt (awk-scoped grep count == 2); one literal `type: "report_ready" as const`; one workflow_errors insert with `workflow_name: "report_delivery_email"`; `deliveryEmailFailed` appears ≥ 3 times (declaration + assignment + return). `dispatchNotification` count inside `runReportDraftGeneration` body == 0 (D-06). Return shape now ends with `, deliveryEmailFailed }`. TypeScript clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Matt's browser → finalizeReport Server Action | Authenticated admin call (existing `requireActor` gate via `supabase.auth.getUser()` at line 682-686) |
| finalizeReport → Supabase Storage `reports` bucket | adminClient (service role) writes PDF + mints signed URLs |
| finalizeReport → n8n webhook | Outbound POST containing 7-day signed URL + customer email |
| 7-day URL ↔ 5-min URL | TWO separately scoped signed URLs; must not be conflated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | E (Elevation of privilege) | finalizeReport entry | mitigate | Existing `createClient().auth.getUser()` gate at lines 681-686; non-authenticated callers throw |
| T-07-04-02 | I (URL leakage across audiences) | 7-day client URL vs 5-min Matt URL | mitigate | Two separate `createSignedUrl` calls keep the long-lived URL out of Matt's browser context; the 7-day URL is only ever placed inside the n8n payload and never returned to the client component. Verify gate `awk` count == 2 enforces both calls exist; Plan 04 must NOT replace the 5-min URL with the 7-day one |
| T-07-04-03 | I (Cross-org access to report PDF) | storage path `{client_id}/report_{submissionId}.pdf` | mitigate | Auth gate at line 682 guarantees only authenticated admin (Matt) can call `finalizeReport`. The signed URL itself encodes the storage path; URL-bearer is the trust model. 7-day TTL chosen per D-07 — accepted leakage window |
| T-07-04-04 | D (Email side-effect blocks PDF) | n8n outage | mitigate | D-08 ordering: PDF render + upload + status flip happen BEFORE dispatch; dispatch failure inserts workflow_errors and sets `deliveryEmailFailed=true` but DOES NOT throw. PDF stays as artefact of record |
| T-07-04-05 | R (Repudiation of delivery) | n8n dispatch outcome | mitigate | workflow_errors row captures `payload.report_storage_path` for dedup + replay; n8n side will re-attempt via that path. Plan 04 surfaces failure to Matt via toast (Plan 06) |
| T-07-04-06 | I (Email-to-wrong-client) | client_users contact lookup | mitigate | `.eq("client_id", submission.client_id).limit(1)` scopes to the submission's client_id only; submission was fetched at line 689-693 with admin auth gate already satisfied. No user-supplied email field path |
| T-07-04-07 | T (Tampering with status) | atomic update | mitigate | `report_storage_path` and `status: "completed"` written in the SAME `update()` call (existing lines 734-741) — REPORT-07 atomicity preserved |
| T-07-04-08 | S (Webhook secret leakage) | n8n call | mitigate | Secret is sent via `X-Webhook-Secret` by `dispatchNotification` (existing line 56 of n8n-dispatch.ts); never logged. Plan 04 does not echo `process.env.N8N_WEBHOOK_SECRET` |

</threat_model>

<verification>
- `finalizeReport` contains exactly two `createSignedUrl` calls scoped to `fileName`: one with `60 * 5` (Matt) and one with `60 * 60 * 24 * 7` (client)
- `dispatchNotification` invoked exactly once inside `finalizeReport`; count remains zero inside `runReportDraftGeneration`
- workflow_errors fallback writes `workflow_name: "report_delivery_email"` only when `!dispatchResult.ok`
- Return shape `{ success, downloadUrl, deliveryEmailFailed }`
- `npm run build` passes
- The `update()` writing `report_storage_path` + `status: 'completed'` remains a single statement (no split)
</verification>

<success_criteria>
- finalizeReport now wires the client-delivery email side-effect per D-07/D-08
- The PDF is never blocked by an email failure
- Plan 06 has a stable `deliveryEmailFailed: boolean` in the return shape to render the non-blocking toast
- Plan 07 can mock `dispatchNotification` to return `{ ok: false }` and assert the workflow_errors row + flag
</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-report-pipeline/07-04-SUMMARY.md` documenting: line count delta, both signed-URL calls with their TTLs, the dispatch fallback shape, the new return signature, and confirmation that the atomic update statement is unchanged.
</output>
