---
phase: 16-multi-tenancy-fork-on-fill
plan: 04
plan_id: 16-04
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - app/client/assignments/page.tsx
  - app/client/assignments/[id]/page.tsx
  - app/client/assignments/[id]/fill/page.tsx
  - app/client/assignments/actions.ts
  - app/client/assignments/_components/assignment-card.tsx
  - app/client/assignments/[id]/fill-as-is-button.tsx
  - tests/form-builder/assignment-status-transitions.test.ts
  - tests/form-builder/assignments-query.test.ts
autonomous: true
requirements:
  - BUILDER-01
  - BUILDER-02
  - BUILDER-03
tags: [client, assignments, lifecycle, interpreter, rsc]
must_haves:
  truths:
    - "/client/assignments shows Active + Completed tabs; default Active; filters .is(deleted_at, null) and .eq(client_id, ctx.client_id) (D-09, D-10)."
    - "/client/assignments/[id] landing page shows template name + due date + instructions block (if set) + Fill as-is / Customise first buttons (D-07, D-04)."
    - "/client/assignments/[id]/fill renders the interpreter against the assignment's pinned template_version_id (D-05)."
    - "Status transitions pending → in_progress fire on first draft create OR Fill-as-is click; in_progress → completed fires on submit (D-11)."
    - "Status transition uses optimistic guard .eq('status', previous_state) preventing backwards transitions (D-11, T-16-05)."
    - "Customer self-fill (assignment_id IS NULL) writes pull client_id from server context (D-16, T-16-04 partial — completed in Plan 06)."
    - "Wave-0 test scaffolds for assignment-status-transitions and assignments-query are filled with real assertions."
  artifacts:
    - path: app/client/assignments/page.tsx
      provides: "Active + Completed tabs RSC; defense-in-depth client_id filter; deleted_at filter"
      contains: "TabsTrigger"
    - path: app/client/assignments/[id]/page.tsx
      provides: "Landing page with Fill / Customise buttons + instructions block"
      contains: "Fill as-is"
    - path: app/client/assignments/[id]/fill/page.tsx
      provides: "Interpreter mount against pinned version_id"
      contains: "template_version_id"
    - path: app/client/assignments/actions.ts
      provides: "transitionAssignmentStatus helper + startAssignmentFill server action + submitAssignedFillAction (assigned-fill submission server action)"
      exports: ["transitionAssignmentStatus", "startAssignmentFill", "submitAssignedFillAction"]
    - path: app/client/assignments/_components/assignment-card.tsx
      provides: "Cream-surface assignment card primitive (template name, due, status pill, instructions clamp)"
      exports: ["AssignmentCard"]
  key_links:
    - from: app/client/assignments/page.tsx
      to: form_assignments via RLS-aware createClient
      via: ".eq('client_id', ctx.client_id).is('deleted_at', null)"
      pattern: "is\\(\\\"deleted_at\\\", null\\)"
    - from: app/client/assignments/[id]/fill/page.tsx
      to: app/admin/assessments/[id] interpreter pattern
      via: "two-step fetch — assignment row, then template_versions by .eq('id', assignment.template_version_id).single()"
      pattern: "template_version_id"
    - from: app/client/assignments/actions.ts
      to: form_assignments.status update
      via: ".update({ status: next }).eq('id', id).eq('status', previous)"
      pattern: "eq\\(\\\"status\\\", previous"
---

<objective>
Wire the client-side lifecycle UI (D-09, D-10, D-11). Deliver:

1. `/client/assignments/page.tsx` — Active + Completed tabs RSC with defense-in-depth client_id + deleted_at filters.
2. `/client/assignments/[id]/page.tsx` — landing page with Fill as-is / Customise first buttons + instructions block.
3. `/client/assignments/[id]/fill/page.tsx` — interpreter against the assignment's pinned version.
4. `app/client/assignments/actions.ts` — `transitionAssignmentStatus` helper + `startAssignmentFill` (for the Fill-as-is click that flips status from pending → in_progress before navigation).
5. `app/client/assignments/_components/assignment-card.tsx` — shared cream-surface card primitive.
6. `app/client/assignments/[id]/fill-as-is-button.tsx` — client island that calls startAssignmentFill then navigates.
7. Fill in Wave-0 scaffolds: `tests/form-builder/assignment-status-transitions.test.ts` + `tests/form-builder/assignments-query.test.ts`.

Note: the Customise-first button + fork action live in Plan 05 (parallel to this plan in Wave 2). The interpreter fill route reuses the existing Phase 13/14/15 InterpreterRenderer; the `in_progress → completed` transition on submit plugs into the existing customer submission server action (which lives at `app/client/templates/actions.ts:saveClientDraftAction` / submission flow — extend it to also call `transitionAssignmentStatus` when the submission has a non-null `assignment_id`).

Purpose: Customers can see what's assigned, open it, and fill it. The interpreter is unchanged from Phase 14/15 — only the host wrapper page is new.

Output: Three new routes, one new actions module, one shared card component, one client button, two filled test scaffolds.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md
@.planning/phases/16-01-PLAN.md
@.planning/phases/16-02-PLAN.md

<interfaces>
From lib/supabase/server.ts:
- export async function createClient(): Promise<SupabaseClient>  // RLS-aware via @supabase/ssr cookies

From lib/auth-helpers.ts:
- export async function getClientContext(): Promise<{ user_id: string; client_id: string; role: string } | null>
- export async function requireActorUserId(actorType: "admin" | "client"): Promise<string | null>

Existing patterns to read for analogs:
- app/client/templates/page.tsx (cream surface header pattern, mono index, serif h2, RLS-aware fetch, client-template-card grid)
- app/client/templates/_components/client-template-card.tsx (lines 39-114 — useTransition + AlertDialog + toast pattern; lines 56-80 — card markup with hover border, status pill colours)
- app/client/templates/actions.ts (customer server actions: requireClientContext lines 9-13, requireOwnedTemplate lines 23-35, fork pattern lines 214-283)
- app/admin/assessments/[id]/page.tsx (lines 1-58 — two-step fetch UUID guard + template_version_id pinning; assessment-client.tsx mount with InterpreterRenderer)
- app/admin/assessments/[id]/assessment-client.tsx (InterpreterRenderer mount + submitAction flow — analog for fill/page.tsx client component)
- app/admin/assessments/actions.ts (lines 232-309 — submitAssessmentAction with .eq("status", "draft") optimistic guard; the transitionAssignmentStatus pattern mirrors this)
- components/ui/tabs.tsx
- components/ui/badge.tsx
- components/ui/card.tsx

UI-SPEC Route D, E, F (the client-side routes built in this plan):
- Route D — page heading 28px Newsreader, Tabs default="active", assignment card 18px Newsreader title, 14px Inter instructions clamp-2
- Route E — single centred column max-w-xl py-12 px-6, mono back link 10px, template name 28px Newsreader, instructions block bg-[#f5f3ee] border-l-2 border-[#c0a66d]
- Route F — surface swap from admin assessment fill route; cream end-to-end; Submit button "Submit form" black fill full-width
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build /client/assignments tabs page + AssignmentCard shared component + fill in assignments-query.test.ts scaffold</name>
  <read_first>
    - app/client/templates/page.tsx (full file — page header pattern, RLS-aware createClient + getClientContext + ctx?.client_id filter, mono index 06 · Templates → adapt to 05 · Assigned Forms; card grid)
    - app/client/templates/_components/client-template-card.tsx (lines 56-80 — card markup; reuse classes for AssignmentCard)
    - components/ui/tabs.tsx (Tabs / TabsList / TabsTrigger / TabsContent API)
    - lib/supabase/server.ts (createClient signature)
    - lib/auth-helpers.ts (getClientContext shape)
    - tests/form-builder/assignments-query.test.ts (scaffold from Plan 01 Task 3 — replace it.todo with real assertions)
    - tests/form-builder/save-draft.test.ts (Vitest mock skeleton — vi.mock(@/lib/supabase/server) and vi.mock(@/lib/auth-helpers) patterns)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route D — /client/assignments" (Active + Completed tabs, card spec, empty states, copywriting)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "app/client/assignments/page.tsx" [File Classification row 3: app/client/assignments/page.tsx]
  </read_first>
  <behavior>
    - app/client/assignments/page.tsx is an async server component. Uses RLS-aware `createClient()` from `lib/supabase/server.ts`. Calls `getClientContext()`; if null, renders an empty state without throwing (the layout's auth gate handles the actual redirect — match `app/client/templates/page.tsx`).
    - Fetch: `supabase.from("form_assignments").select("id, template_id, due_date, status, instructions, created_at, template:form_templates(name)").eq("client_id", ctx.client_id).is("deleted_at", null).order("due_date", { ascending: true, nullsFirst: false }).order("created_at", { ascending: false })`.
    - Split into `active = rows.filter(r => r.status !== "completed")` and `completed = rows.filter(r => r.status === "completed")`.
    - Render shadcn `<Tabs defaultValue="active">` with TabsTrigger "active" labelled "Active" and TabsTrigger "completed" labelled "Completed". TabsContent for each renders a vertical stack of `<AssignmentCard ...>`.
    - Card click target wraps the entire card in `<Link href={`/client/assignments/${a.id}`}>` for Active tab; Completed tab links to `/client/assignments/${a.id}/submission` (a route built in a future phase — link still rendered for the spec; if the submission viewer doesn't exist yet, link to `/client/assignments/${a.id}` instead and document in summary).
    - Empty states (locked copy per UI-SPEC):
      - Active empty: heading "No forms assigned yet" + body "When Matt assigns a form to your organisation, it'll appear here."
      - Completed empty: heading "No completed forms yet" + body "Submitted assignments will appear here for reference."
    - Page header per UI-SPEC: mono `05 · Assigned Forms` in teal `#3b8273`, serif h2 "Forms assigned to you." at **28px** Newsreader.
    - AssignmentCard component is `"use client"` only if it needs interactivity; otherwise pure server-rendered. Make it server-only — just JSX + tailwind. Props: `assignment: { id; template: { name }; due_date; status; instructions }; variant: "active" | "completed"`.
    - assignments-query.test.ts (filling Plan 01 scaffold): mock the supabase chain and assert that calling the page logic (or a query helper extracted from the page) invokes `.eq("client_id", expectedClientId)` AND `.is("deleted_at", null)`. Use the spy-on-from pattern from `tests/form-builder/version-pin.test.ts`.
  </behavior>
  <files>
    - app/client/assignments/page.tsx (NEW)
    - app/client/assignments/_components/assignment-card.tsx (NEW)
    - tests/form-builder/assignments-query.test.ts (REPLACE scaffold from Plan 01 with real assertions)
  </files>
  <action>
    Read app/client/templates/page.tsx end-to-end so the new page mirrors the header layout (mono index, serif h2 at 28px, optional subtitle) and the cream surface classes (`text-[#1a1a1a]`, `bg-[#faf9f6]` via parent layout).

    Author app/client/assignments/_components/assignment-card.tsx as a server component. Export `function AssignmentCard({ assignment, variant }: AssignmentCardProps)`. Markup per UI-SPEC: outer `<article className="bg-white border border-[#e5e1d8] rounded-sm p-5 flex flex-col gap-4 hover:border-[#1a1a1a]/30 transition-colors">`. Row 1: serif `text-[18px] text-[#1a1a1a]` template name. Row 2: mono `text-[9px] uppercase tracking-[0.2em] text-[#8a857f]` showing `DUE · {formatDate(due_date) || "no due date"}` + the status pill inline. Row 3 (Active tab only, when instructions present): `<p className="text-sm font-sans text-[#6b6560] italic line-clamp-2 before:content-['—_'] before:text-[#c0a66d]">{instructions}</p>` — em dash prefix.

    Status pill uses inline ternary per UI-SPEC colour map: pending → `text-[#666] bg-[#555]/10`, in_progress → `text-[#c0a66d] bg-[#c0a66d]/10`, completed → `text-[#3b8273] bg-[#3b8273]/10`. Pill classes: `inline-flex items-center px-2 py-0.5 rounded-sm font-mono text-[9px] uppercase tracking-[0.25em] [colour class]`.

    Author app/client/assignments/page.tsx. Imports: `import Link from "next/link";`, `import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";`, `import { createClient } from "@/lib/supabase/server";`, `import { getClientContext } from "@/lib/auth-helpers";`, `import { AssignmentCard } from "./_components/assignment-card";`. `export const dynamic = "force-dynamic"`.

    Page function: get ctx, get supabase, run the fetch with the .eq/.is filters and the order chain. Split rows by status. Render container (matches `app/client/templates/page.tsx` outer layout), header section (mono index + serif h2), then `<Tabs defaultValue="active">` with the two TabsContent stacks. Empty states use the locked copy strings.

    Now fill tests/form-builder/assignments-query.test.ts. Top: standard Vitest mock skeleton from save-draft.test.ts (vi.mock @/lib/supabase/server, vi.mock @/lib/auth-helpers with getClientContext returning `{ client_id: "client-org-001", user_id: "user-001", role: "client" }`, vi.mock next/cache, next/navigation).

    The page itself is a default-exported async function. Either: (a) extract the query construction into a small named export `buildAssignmentsQuery(supabase, clientId)` in the page file (then assert against the export), OR (b) test the query shape by spying on the supabase mock chain methods. Pick (b) — less code churn. Mock pattern:

    ```
    const eqSpy = vi.fn().mockReturnThis();
    const isSpy = vi.fn().mockReturnThis();
    vi.mock("@/lib/supabase/server", () => ({ createClient: vi.fn().mockResolvedValue({ from: vi.fn().mockReturnValue({ select: vi.fn().mockReturnThis(), eq: eqSpy, is: isSpy, order: vi.fn().mockResolvedValue({ data: [] }) }) }) }));
    ```

    Then invoke the page (`const Page = (await import("@/app/client/assignments/page")).default; await Page({});`). Assert `eqSpy` was called with `("client_id", "client-org-001")` and `isSpy` was called with `("deleted_at", null)`. Use `expect(eqSpy).toHaveBeenCalledWith("client_id", "client-org-001")` and similar for isSpy.

    Verify the test passes with `npm test -- tests/form-builder/assignments-query.test.ts --run`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for (const f of ['app/client/assignments/page.tsx','app/client/assignments/_components/assignment-card.tsx']) if(!fs.existsSync(f)){console.error('Missing:',f);process.exit(1);} const p=fs.readFileSync('app/client/assignments/page.tsx','utf8');for (const s of ['Tabs','defaultValue=\"active\"','TabsTrigger','TabsContent','from(\"form_assignments\")','eq(\"client_id\"','is(\"deleted_at\", null)','No forms assigned yet','Forms assigned to you','text-[28px]']) {if(!p.includes(s)){console.error('Page missing:',s);process.exit(2);}} console.log('OK')" && npm test -- tests/form-builder/assignments-query.test.ts --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File app/client/assignments/page.tsx exists.
    - Page contains `defaultValue="active"`.
    - Page contains `from("form_assignments")`.
    - Page contains `eq("client_id"` (T-16-01 defense-in-depth).
    - Page contains `is("deleted_at", null)` (T-16-08).
    - Page contains literal `Forms assigned to you` (UI-SPEC heading).
    - Page contains literal `text-[28px]` (UI-SPEC unified client heading size).
    - Page contains literal `No forms assigned yet` AND `No completed forms yet` (locked empty states).
    - File app/client/assignments/_components/assignment-card.tsx exists with `export function AssignmentCard`.
    - AssignmentCard contains the three pill colour literals `#666`, `#c0a66d`, `#3b8273`.
    - tests/form-builder/assignments-query.test.ts has at least one `expect(` call referencing `client_id` AND one referencing `deleted_at`.
    - `npm test -- tests/form-builder/assignments-query.test.ts --run` exits 0 with passing tests (not just todos).
  </acceptance_criteria>
  <done>
    `/client/assignments` renders Active/Completed tabs with the seeded assignments visible. Empty states show locked copy. The assignments-query test asserts both defense-in-depth filters at the mock layer.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: actions.ts skeleton (requireClientContext, requireOwnedAssignment, transitionAssignmentStatus, startAssignmentFill) + landing page + FillAsIsButton</name>
  <read_first>
    - app/admin/assessments/[id]/page.tsx (lines 1-58 — UUID guard + two-step fetch pattern; mirror for the landing route)
    - app/admin/assessments/actions.ts (lines 17-73 — startAssessment redirect-outside-catch pattern; lines 232-309 — .eq("status", "draft") optimistic guard, analog for transitionAssignmentStatus)
    - app/client/templates/actions.ts (lines 9-13 — requireClientContext; lines 23-35 — requireOwnedTemplate, the analog for requireOwnedAssignment)
    - lib/supabase/server.ts
    - lib/auth-helpers.ts
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route E — /client/assignments/[id]"
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "app/client/assignments/[id]/page.tsx" + "app/client/assignments/actions.ts" [File Classification rows 4+6: landing route + client-assignments actions]
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pattern 4: Status Transition Without Scattering Writes" lines 559-591
  </read_first>
  <behavior>
    - app/client/assignments/actions.ts begins with `"use server"`. Exports introduced in this task: `requireClientContext`, `requireOwnedAssignment`, `transitionAssignmentStatus`, `startAssignmentFill`. (submitAssignedFillAction is added in Task 2b.)
    - `requireClientContext()` — internal helper mirroring `app/client/templates/actions.ts` lines 9-13. Resolves the signed-in client_user's `{ user_id, client_id, role }`; throws "Not a client user" if absent.
    - `requireOwnedAssignment(assignmentId, clientId)` — reads `form_assignments` by id; throws "Assignment not found" when null; throws "Forbidden: not your assignment" when `client_id !== clientId`; throws "Cannot use a revoked assignment" when `deleted_at !== null`. Returns the row (including `template_version_id`, used in Task 2b).
    - `transitionAssignmentStatus(supabase, assignmentId, next)` — exported for testing. Computes `previous = next === "in_progress" ? "pending" : "in_progress"`. Calls `supabase.from("form_assignments").update({ status: next }).eq("id", assignmentId).eq("status", previous)`. On error, `console.error("Status transition failed", error)` and return (DO NOT throw — per RESEARCH Pattern 4 the submission write should not be reverted by a status glitch).
    - `startAssignmentFill(assignmentId)` — server action invoked by FillAsIsButton. Body: ctx → supabase → requireOwnedAssignment → transitionAssignmentStatus(supabase, id, "in_progress") → revalidatePath("/client/assignments") → redirect(`/client/assignments/${assignmentId}/fill`). Redirect is LAST, outside try/catch.
    - app/client/assignments/[id]/page.tsx is RSC. UUID guard, RLS-aware fetch via createClient, joined template name. If `assignment.status === "completed"`, redirect to `/client/assignments/${id}` (or render an "already completed" card). Otherwise renders the landing UI per UI-SPEC Route E: mono back link, template name @ 28px Newsreader, due date mono line (overdue red), instructions block (cream surface bg-[#f5f3ee], gold accent border-left, "From your assessor" mono label) when instructions non-null, CTA row with `<FillAsIsButton assignmentId={id} />` + a Plan-05-placeholder Customise button.
    - The Customise button is a disabled placeholder in this task; Plan 05 replaces it with the real CustomiseFirstButton. Code comment: `// Plan 05 wires CustomiseFirstButton here`.
    - app/client/assignments/[id]/fill-as-is-button.tsx is `"use client"`. Renders a black-fill primary Button "Fill as-is". onClick wraps `startAssignmentFill(assignmentId)` inside startTransition; bubbles NEXT_REDIRECT errors so Next handles them, toasts on real errors.
  </behavior>
  <files>
    - app/client/assignments/actions.ts (NEW — partial: requireClientContext, requireOwnedAssignment, transitionAssignmentStatus, startAssignmentFill; submitAssignedFillAction follows in Task 2b)
    - app/client/assignments/[id]/page.tsx (NEW)
    - app/client/assignments/[id]/fill-as-is-button.tsx (NEW)
  </files>
  <action>
    Author app/client/assignments/actions.ts with `"use server"`, the four exports listed in `<behavior>`. Imports: `createClient` from `@/lib/supabase/server`, `getClientContext` + `requireActorUserId` from `@/lib/auth-helpers`, `revalidatePath` from `next/cache`, `redirect` from `next/navigation`. Implement requireClientContext as a thin wrapper around getClientContext + null-throw. Implement requireOwnedAssignment per the `<behavior>` spec.

    transitionAssignmentStatus body — the exact .update().eq().eq() chain with optimistic guard. Log via console.error on failure; DO NOT throw.

    startAssignmentFill body: `const ctx = await requireClientContext(); const supabase = await createClient(); const assignment = await requireOwnedAssignment(assignmentId, ctx.client_id); await transitionAssignmentStatus(supabase, assignmentId, "in_progress"); revalidatePath("/client/assignments"); redirect(`/client/assignments/${assignmentId}/fill`);`. Redirect is the LAST line, outside any try/catch.

    Author app/client/assignments/[id]/page.tsx as RSC per the `<behavior>` block. UUID guard pattern from `app/admin/assessments/[id]/page.tsx`. Use `notFound()` from `next/navigation` when assignment is null or `deleted_at` is set. Mount the FillAsIsButton client island plus the disabled Customise placeholder.

    Author app/client/assignments/[id]/fill-as-is-button.tsx. `"use client"`, useTransition + toast + the startAssignmentFill import. Handler — let NEXT_REDIRECT-shaped errors re-throw; toast.error other errors with `err.message || "Could not start fill"`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for (const f of ['app/client/assignments/actions.ts','app/client/assignments/[id]/page.tsx','app/client/assignments/[id]/fill-as-is-button.tsx']) if(!fs.existsSync(f)){console.error('Missing:',f);process.exit(1);} const a=fs.readFileSync('app/client/assignments/actions.ts','utf8');for (const s of ['use server','requireClientContext','requireOwnedAssignment','transitionAssignmentStatus','startAssignmentFill','eq(\"status\", previous','redirect(']) {if(!a.includes(s)){console.error('Actions missing:',s);process.exit(2);}} const lp=fs.readFileSync('app/client/assignments/[id]/page.tsx','utf8');for (const s of ['Fill as-is','Customise first','From your assessor','#c0a66d','text-[28px]']) {if(!lp.includes(s)){console.error('Landing missing:',s);process.exit(3);}} const b=fs.readFileSync('app/client/assignments/[id]/fill-as-is-button.tsx','utf8');if(!b.includes('use client')){console.error('Button missing use client');process.exit(4);} if(!b.includes('useTransition')){console.error('Button missing useTransition');process.exit(5);} console.log('OK')" && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist: actions.ts, [id]/page.tsx, [id]/fill-as-is-button.tsx.
    - app/client/assignments/actions.ts contains `"use server"`, `requireClientContext`, `requireOwnedAssignment`, `transitionAssignmentStatus`, `startAssignmentFill`, the optimistic guard substring `eq("status", previous`, and `redirect(`.
    - app/client/assignments/actions.ts has `redirect(` AFTER `revalidatePath(` (ordering check via grep line numbers).
    - app/client/assignments/[id]/page.tsx contains literals `Fill as-is`, `Customise first`, `From your assessor`, `#c0a66d`, `text-[28px]`.
    - app/client/assignments/[id]/fill-as-is-button.tsx contains `"use client"` and `useTransition`.
    - `npm run build` exits 0 (no TypeScript errors in any of the three new files).
  </acceptance_criteria>
  <done>
    Landing page + actions skeleton ship. Fill-as-is click flips status pending → in_progress and redirects to the fill route. The fill route itself is built in Task 2b.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Fill route + FillAssignmentClient + submitAssignedFillAction + assignment-status-transitions test</name>
  <read_first>
    - app/client/assignments/actions.ts (created in Task 2a — append submitAssignedFillAction)
    - app/admin/assessments/[id]/assessment-client.tsx (analog for FillAssignmentClient — InterpreterRenderer mount + submit handler)
    - app/admin/assessments/[id]/page.tsx (two-step fetch pattern for the fill route)
    - app/client/templates/actions.ts (lines 9-13 — requireClientContext pattern)
    - lib/supabase/server.ts
    - lib/auth-helpers.ts
    - tests/form-builder/assignment-status-transitions.test.ts (Wave-0 scaffold — replace todos with real assertions, including the Task 4 submitAssignedFillAction test from B1 coverage)
    - tests/form-builder/save-draft.test.ts (Vitest mock skeleton — vi.mock for @/lib/supabase/server, @/lib/auth-helpers, next/cache, next/navigation)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route F — /client/assignments/[id]/fill"
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "app/client/assignments/[id]/fill/page.tsx" [File Classification row 5: app/client/assignments/[id]/fill/page.tsx]
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pattern 4: Status Transition Without Scattering Writes" lines 559-591
  </read_first>
  <behavior>
    - app/client/assignments/[id]/fill/page.tsx is RSC. UUID guard, fetch assignment via createClient (RLS), verify `status !== "completed"` and `deleted_at IS NULL` (else redirect to landing). Two-step fetch the pinned version: `supabase.from("template_versions").select("schema_json, template_id").eq("id", assignment.template_version_id).single()`. Mount `<FillAssignmentClient schemaJson={version.schema_json} assignmentId={id} clientId={ctx.client_id} templateVersionId={assignment.template_version_id} />`.
    - app/client/assignments/[id]/fill/fill-assignment-client.tsx is `"use client"`. Mirrors app/admin/assessments/[id]/assessment-client.tsx but with the cream surface and `submitAssignedFillAction` as the submit handler. Submit button "Submit form" black-fill full-width per UI-SPEC Route F.
    - submitAssignedFillAction is REQUIRED — signature `(assignmentId: string, answers: Record<string, unknown>): Promise<void>` per Task 2 `<behavior>`:
      1. `const ctx = await requireClientContext();`
      2. `const supabase = await createClient();`
      3. `const assignment = await requireOwnedAssignment(assignmentId, ctx.client_id);`
      4. INSERT `form_submissions` with `{ assignment_id: assignmentId, client_id: ctx.client_id, template_version_id: assignment.template_version_id, status: "submitted", answers_json: answers }` — `client_id` from `ctx.client_id` ONLY (T-16-04 mitigation).
      5. `await transitionAssignmentStatus(supabase, assignmentId, "completed");` AFTER the insert.
      6. `revalidatePath("/client/assignments");` + `revalidatePath(`/client/assignments/${assignmentId}`);`
      7. `redirect(`/client/assignments/${assignmentId}`);` as the LAST statement, outside any try/catch.
    - tests/form-builder/assignment-status-transitions.test.ts is filled with four tests:
      - Test 1: `transitionAssignmentStatus(s, id, "in_progress")` issues `.update({ status: "in_progress" }).eq("id", id).eq("status", "pending")`.
      - Test 2: same for "completed" with previous="in_progress".
      - Test 3: when `.update()` returns `{ error: { message: "boom" } }`, the function logs (console.error spy fires) and does NOT throw.
      - Test 4 (submitAssignedFillAction INSERT shape — B1 coverage): per the Task 2 detailed spec — assert INSERT payload keys + values, the follow-up status-transition update chain, and the redirect call site. (Split into a sibling spec `tests/form-builder/submit-assigned-fill.test.ts` if the file exceeds ~150 lines.)
  </behavior>
  <files>
    - app/client/assignments/actions.ts (MODIFY — append submitAssignedFillAction)
    - app/client/assignments/[id]/fill/page.tsx (NEW)
    - app/client/assignments/[id]/fill/fill-assignment-client.tsx (NEW)
    - tests/form-builder/assignment-status-transitions.test.ts (REPLACE Wave-0 scaffold from Plan 01)
    - (OPTIONAL) tests/form-builder/submit-assigned-fill.test.ts — only if assignment-status-transitions.test.ts grows past ~150 lines and the submitAssignedFillAction Test 4 needs its own file
  </files>
  <action>
    Open app/client/assignments/actions.ts (created in Task 2a). Append submitAssignedFillAction per the `<behavior>` spec. The redirect is the LAST statement, outside any try/catch.

    Author app/client/assignments/[id]/fill/page.tsx as RSC. UUID guard. Fetch assignment + pinned version (two-step). Redirect to landing when assignment.status === "completed" or deleted_at is set. Mount `<FillAssignmentClient ...>`.

    Author app/client/assignments/[id]/fill/fill-assignment-client.tsx as a client component. Read `app/admin/assessments/[id]/assessment-client.tsx` first to understand InterpreterRenderer wiring + submit handling. Mirror the structure but on the cream surface (UI-SPEC Route F). The submit handler calls `submitAssignedFillAction(assignmentId, answers)`. Toast on error; let NEXT_REDIRECT bubble.

    Now fill tests/form-builder/assignment-status-transitions.test.ts. Vitest mock skeleton (vi.mock("@/lib/supabase/server"), vi.mock("@/lib/auth-helpers") with getClientContext returning `{ client_id: "client-org-001", user_id: "u-1", role: "client" }`, vi.mock("next/cache") with revalidatePath spy, vi.mock("next/navigation") with redirect spy).

    Build a fake supabase whose `.from()` returns different chain builders keyed by table name. Default `.from("form_assignments").update(...).eq().eq()` resolves `{ error: null }`. Default `.from("form_assignments").select(...).eq("id", "asg-1").single()` resolves `{ data: { id: "asg-1", client_id: "client-org-001", template_version_id: "ver-1", deleted_at: null }, error: null }`. Default `.from("form_submissions").insert(payload)` returns `{ error: null }` and records payload via spy.

    Tests 1 + 2 + 3: as previously described (transitionAssignmentStatus chain shape + log-on-error).

    Test 4: per the Task 2 spec — call `await submitAssignedFillAction("asg-1", { q1: "answer" })`. Assert form_submissions insert spy received `{ assignment_id: "asg-1", client_id: "client-org-001", template_version_id: "ver-1", status: "submitted", answers_json: { q1: "answer" } }`. Assert form_assignments update spy chain has `update({ status: "completed" })` with `.eq("id", "asg-1")` and `.eq("status", "in_progress")`. Assert redirect spy received `/client/assignments/asg-1`.

    Run `npm test -- tests/form-builder/assignment-status-transitions.test.ts --run` and confirm green.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for (const f of ['app/client/assignments/[id]/fill/page.tsx','app/client/assignments/[id]/fill/fill-assignment-client.tsx']) if(!fs.existsSync(f)){console.error('Missing:',f);process.exit(1);} const a=fs.readFileSync('app/client/assignments/actions.ts','utf8');for (const s of ['submitAssignedFillAction','assignment_id: assignmentId','client_id: ctx.client_id','status: \"submitted\"','transitionAssignmentStatus(supabase, assignmentId, \"completed\"']) {if(!a.includes(s)){console.error('Actions missing:',s);process.exit(2);}} const fp=fs.readFileSync('app/client/assignments/[id]/fill/page.tsx','utf8');if(!fp.includes('template_version_id')){console.error('Fill page missing two-step fetch');process.exit(3);} if(!fp.includes('form_assignments')||!fp.includes('template_versions')){console.error('Fill page missing both table references');process.exit(4);} const t=fs.readFileSync('tests/form-builder/assignment-status-transitions.test.ts','utf8');if(t.match(/it\.todo\(/g)){console.error('Test scaffolds remain — should be filled');process.exit(5);} for (const s of ['toHaveBeenCalledWith','assignment_id','client_id','submitAssignedFillAction']) {if(!t.includes(s)){console.error('Test missing:',s);process.exit(6);}} console.log('OK')" && npm test -- tests/form-builder/assignment-status-transitions.test.ts --run 2>&1 | tail -20 && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - app/client/assignments/[id]/fill/page.tsx exists and references both `form_assignments` and `template_versions` (two-step fetch).
    - app/client/assignments/[id]/fill/fill-assignment-client.tsx exists with `"use client"`.
    - app/client/assignments/actions.ts contains `export async function submitAssignedFillAction(` with the parameter shape `(assignmentId: string, answers: Record<string, unknown>)`.
    - submitAssignedFillAction body contains: `assignment_id: assignmentId`, `client_id: ctx.client_id` (T-16-04), `status: "submitted"`, `transitionAssignmentStatus(supabase, assignmentId, "completed"`, and a `redirect(` call as the LAST statement.
    - Grep gate: `grep -nE "client_id: ctx\\.client_id" app/client/assignments/actions.ts` returns at least one match.
    - Grep gate: `grep -nE "assignment_id: assignmentId" app/client/assignments/actions.ts` returns at least one match.
    - tests/form-builder/assignment-status-transitions.test.ts contains zero `it.todo(` lines and contains `toHaveBeenCalledWith` calls (real spy assertions).
    - tests/form-builder/assignment-status-transitions.test.ts (or sibling tests/form-builder/submit-assigned-fill.test.ts) contains Test 4 — asserts the submitAssignedFillAction INSERT shape, the follow-up status-transition update, and the redirect target.
    - `npm test -- tests/form-builder/assignment-status-transitions.test.ts --run` exits 0.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    The full assigned-fill loop ships: customer opens the assignment, clicks Fill as-is (status flips → in_progress, redirects to fill), the interpreter renders the pinned version, submit writes form_submissions with the correct shape AND transitions status → completed AND redirects back to the assignment page. Status-transition + submission-shape tests are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /client/* RSC | Untrusted JWT; RLS is the gate via @supabase/ssr cookies |
| client component (FillAsIsButton) → server action startAssignmentFill | Same trust boundary; server action validates via requireClientContext + requireOwnedAssignment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | Cross-tenant assignment read on /client/assignments | mitigate | Defense-in-depth `.eq("client_id", ctx.client_id)` on every fetch, PLUS RLS gate from migration 001 `form_assignments_client_own`. Verified by grep in acceptance criteria + the Plan 01 RLS test. |
| T-16-05 | Tampering | Forged status transition completed → in_progress (or any backwards step) | mitigate | transitionAssignmentStatus uses `.eq("status", previous)` optimistic guard — DB rejects the update when status has already advanced. Asserted by tests/form-builder/assignment-status-transitions.test.ts. |
| T-16-08 | Information Disclosure | Soft-deleted assignment leaks via missing deleted_at filter | mitigate | Every client read filters `.is("deleted_at", null)`. requireOwnedAssignment throws on `deleted_at !== null`. |
| T-16-06 | Elevation of Privilege | Service-role import on customer surface | mitigate | This plan uses `lib/supabase/server.ts createClient` (RLS-aware) everywhere on `app/client/*`. No `lib/supabase/admin.ts` import is added — verifiable via grep gate. |
| T-16-07 | Tampering | Direct POST to startAssignmentFill without client claim | mitigate | requireClientContext (which wraps getClientContext) throws "Not a client user" before any DB write. requireOwnedAssignment confirms ownership. |
</threat_model>

<verification>
After all two tasks:
- `npm run build` succeeds.
- `npm test -- tests/form-builder/assignments-query.test.ts tests/form-builder/assignment-status-transitions.test.ts --run` exits 0.
- Manual smoke (dev): sign in as a client_user whose org has ≥ 1 assignment seeded; visit `/client/assignments` and see the assignment under Active; click into landing; click Fill as-is and observe redirect to the fill route with the interpreter loaded; submit; observe status flips to completed and the row moves to Completed tab.
- grep gate: `grep -r "from \"@/lib/supabase/admin\"" app/client/assignments/ | wc -l` must be `0` (T-16-06 mitigation).
</verification>

<success_criteria>
- /client/assignments page renders Active + Completed tabs with locked empty-state copy.
- /client/assignments/[id] landing page renders Fill / Customise buttons + instructions block when present.
- /client/assignments/[id]/fill page mounts the interpreter against the pinned version.
- app/client/assignments/actions.ts exports the four functions with the documented signatures and the optimistic status guard.
- Two filled test scaffolds (assignment-status-transitions, assignments-query) pass.
- No service-role client is imported under app/client/assignments/.
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-tenancy-fork-on-fill/16-04-SUMMARY.md` recording: the new route paths, the actions.ts exports + their signatures, the test names that now pass, and any deviation from the UI-SPEC card markup.
</output>
