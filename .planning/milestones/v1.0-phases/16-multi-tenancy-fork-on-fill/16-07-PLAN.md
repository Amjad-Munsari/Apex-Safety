---
phase: 16-multi-tenancy-fork-on-fill
plan: 07
plan_id: 16-07
type: execute
wave: 4
depends_on: [16-01, 16-02, 16-03, 16-04, 16-05, 16-06]
files_modified:
  - lib/supabase/database.types.ts
autonomous: false
requirements:
  - BUILDER-01
  - BUILDER-02
  - BUILDER-03
  - BUILDER-04
  - BUILDER-05
tags: [supabase, db-push, types-regen, verification, BLOCKING]
must_haves:
  truths:
    - "Migrations 013 + 014 are pushed to the live Supabase project (form_assignments.instructions exists; form_submissions.assignment_id is nullable)."
    - "lib/supabase/database.types.ts is regenerated and committed."
    - "Full Vitest sweep (`npm test`) green including new tests/rls/ + four new tests/form-builder/ specs."
    - "TypeScript compile (`npm run build`) green against the regenerated types."
    - "Cross-org RLS spec runs and all 5 assertions pass (4 negatives + 1 positive control) when env vars are present."
  artifacts:
    - path: lib/supabase/database.types.ts
      provides: "Regenerated Supabase types reflecting migrations 013 + 014"
      contains: "instructions"
  key_links:
    - from: live Supabase DB
      to: supabase/migrations/013, 014
      via: "supabase db push"
      pattern: "instructions TEXT"
    - from: lib/supabase/database.types.ts
      to: live Supabase schema
      via: "supabase gen types typescript"
      pattern: "form_assignments"
---

<objective>
[BLOCKING] Push migrations 013 + 014 to the live Supabase project, regenerate TypeScript types, and run the full Phase 16 verification sweep.

This plan is gated on completion of plans 01-06 because:
1. Schema files (013, 014) authored in Plan 01 must not be pushed before the application code (Plans 02-06) is in place — otherwise the live DB is in a state the deployed app can't yet use.
2. The RLS test (Plan 01) and the four form-builder unit tests (filled across Plans 04, 05, 06) require the live schema to actually push, since they hit the live Supabase project for end-to-end assertions (RLS suite) or against database.types.ts (compile-time).

This plan is split into four tasks: Task 1a (human runs `supabase db push` + `gen types typescript` — manual-only), Task 1b (Claude programmatically verifies the regenerated types contain the new column shapes AND runs `npm test --run` + `npm run build`), Task 1c (BUILDER-04 dependency-pin regression check), and Task 2 (UAT.md + ROADMAP.md authoring). Because `supabase db push` requires production-DB credentials and may prompt interactively, the plan is `autonomous: false`.

Output: Live DB updated; types regenerated; phase verification green; BUILDER-04 stack pin confirmed intact; 16-UAT.md + ROADMAP.md ready for phase close-out.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-VALIDATION.md

<interfaces>
Supabase CLI commands:
- `supabase db push` — applies pending migrations from supabase/migrations/ to the linked project. Requires either an interactive TTY or `SUPABASE_ACCESS_TOKEN` env var for non-TTY auth.
- `supabase gen types typescript --linked > lib/supabase/database.types.ts` — regenerates types from the linked project's schema.

Existing migration push precedent:
- Phase 14 used migration 011 + supabase db push (see .planning/phases/14-custom-field-types/14-08-PLAN.md if needed)
- Phase 15 used migration 012 + supabase db push (see STATE.md mention of "migration 012 live")

Tests that exercise the live DB:
- tests/rls/multi-tenancy.spec.ts (Plan 01) — requires the live DB to have migrations 013+014 applied so the seed harness can insert rows that compile against the new nullable assignment_id
- tests/security.spec.ts (existing) — should still pass after the push (unrelated to Phase 16 changes)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1a [BLOCKING]: Human runs `supabase db push` + `gen types typescript` (manual-only)</name>
  <read_first>
    - supabase/migrations/013_phase16_assignments_instructions.sql (Plan 01 Task 1 — confirm content before pushing)
    - supabase/migrations/014_phase16_customer_submissions.sql (Plan 01 Task 1 — confirm content before pushing)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md § "<schema_push_requirement>" (the BLOCKING gate)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-VALIDATION.md § "Sampling Rate" + § "Phase gate"
  </read_first>
  <what-built>
    Plans 01-06 are complete. Migrations 013 + 014 exist as files but are NOT yet pushed. The application code in Plans 02-06 was written against the expected post-push schema; TypeScript types (`lib/supabase/database.types.ts`) still reflect the pre-push schema. This task pushes the migrations and regenerates the types. Claude cannot run `supabase db push` autonomously (interactive prompt + production-DB side effect), so a human runs the two commands and reports outputs.
  </what-built>
  <how-to-verify>
    Run the following commands locally, in order, and paste the exit codes + relevant output back:

    1. **Verify migration files are present:**
       ```
       ls supabase/migrations/013_phase16_assignments_instructions.sql supabase/migrations/014_phase16_customer_submissions.sql
       ```
       Both files must exist.

    2. **Push migrations to the linked Supabase project:**
       ```
       supabase db push
       ```
       Expected CLI output mentions applying `013_phase16_assignments_instructions.sql` and `014_phase16_customer_submissions.sql`, followed by "Finished supabase db push." If the CLI prompts for confirmation, type `y` (013 is an ADD COLUMN; 014 is a DROP NOT NULL — both non-destructive, no data loss). If non-interactive (CI / pipeline), ensure `SUPABASE_ACCESS_TOKEN` is exported. Capture the full stdout/stderr and the exit code.

    3. **Regenerate TypeScript types:**
       ```
       npx supabase gen types typescript --linked > lib/supabase/database.types.ts
       ```
       Capture the exit code. File should be rewritten in-place.

    4. **(Optional but recommended) Smoke the live DB via Supabase Studio or psql:**
       - `\d form_assignments` should show column `instructions text` (no NOT NULL).
       - `\d form_submissions` should show `assignment_id uuid` with NO `not null` on its line.

    Acceptance gate for this manual task:
    - `supabase db push` exit code is `0`.
    - `npx supabase gen types typescript --linked > lib/supabase/database.types.ts` exit code is `0`.
    - `lib/supabase/database.types.ts` was modified (file mtime updated; non-empty).

    Paste the exit codes + the last ~20 lines of `supabase db push` output back. If either command failed, do NOT mark this task verified — share the failure mode and we will diagnose before continuing to Task 1b.
  </how-to-verify>
  <resume-signal>
    Type `verified` and paste the captured exit codes + tail of `supabase db push` output. If either command failed, paste the full error instead — do NOT mark verified.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1b: Programmatic verification — types reflect migration, build green, suite green</name>
  <read_first>
    - lib/supabase/database.types.ts (just regenerated in Task 1a — confirm the file exists and was modified)
    - Plan 01 Task 3 (the RLS spec and 4 form-builder scaffolds — they must all pass under `npm test`)
  </read_first>
  <action>
    Claude runs the following commands and asserts their exit codes / output. Do NOT proceed if any assertion fails — report back to the user and stop.

    1. Assert `lib/supabase/database.types.ts` reflects migration 013 (`instructions` column on `form_assignments`):
       `grep -nE "instructions" lib/supabase/database.types.ts` must return at least one match within a `form_assignments` table block. A defensive multi-line grep:
       `node -e "const c=require('fs').readFileSync('lib/supabase/database.types.ts','utf8'); const idx=c.indexOf('form_assignments'); if(idx<0){console.error('form_assignments table missing from types');process.exit(1);} const region=c.slice(idx, idx+4000); if(!/instructions\s*:\s*string\s*\|\s*null/.test(region)){console.error('instructions column not present on form_assignments in types');process.exit(2);} console.log('OK: instructions column present on form_assignments')"`.

    2. Assert `lib/supabase/database.types.ts` reflects migration 014 (`assignment_id: string | null` on `form_submissions`):
       `node -e "const c=require('fs').readFileSync('lib/supabase/database.types.ts','utf8'); const idx=c.indexOf('form_submissions'); if(idx<0){console.error('form_submissions table missing from types');process.exit(1);} const region=c.slice(idx, idx+4000); if(!/assignment_id\s*:\s*string\s*\|\s*null/.test(region)){console.error('assignment_id is not nullable on form_submissions in types');process.exit(2);} console.log('OK: assignment_id nullable on form_submissions')"`.

    3. Run the full Vitest sweep: `npm test --run`. Assert exit code 0. Tail the output; verify the RLS spec (`tests/rls/multi-tenancy.spec.ts`) either ran with 5 passing assertions OR skipped cleanly when env vars are absent (the describe.skipIf path). Verify the 4 filled form-builder specs (`fork-assigned-template`, `assignment-status-transitions`, `customer-self-fill-submission`, `assignments-query`) all reported as passing.

    4. Run the production build: `npm run build`. Assert exit code 0.

    If all four assertions pass, proceed to Task 2. If any assertion fails, STOP and report the failing assertion + the full output back to the user. Do not attempt to "fix" a types-mismatch by hand-editing `database.types.ts` — regenerate via Task 1a instead.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('lib/supabase/database.types.ts','utf8'); const i1=c.indexOf('form_assignments'); const i2=c.indexOf('form_submissions'); if(i1<0||i2<0){console.error('missing table block');process.exit(1);} const r1=c.slice(i1,i1+4000); const r2=c.slice(i2,i2+4000); if(!/instructions\s*:\s*string\s*\|\s*null/.test(r1)){console.error('instructions col not present');process.exit(2);} if(!/assignment_id\s*:\s*string\s*\|\s*null/.test(r2)){console.error('assignment_id not nullable');process.exit(3);} console.log('OK')" && npm test --run 2>&1 | tail -40 && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --run` exits 0.
    - `npm run build` exits 0.
    - `lib/supabase/database.types.ts` contains `instructions: string | null` within the `form_assignments` table block (regex match `/instructions\s*:\s*string\s*\|\s*null/` against the 4000 chars following the `form_assignments` token).
    - `lib/supabase/database.types.ts` contains `assignment_id: string | null` within the `form_submissions` table block (regex match `/assignment_id\s*:\s*string\s*\|\s*null/` against the 4000 chars following the `form_submissions` token).
    - All five RLS spec assertions in `tests/rls/multi-tenancy.spec.ts` pass when env vars are present (verify via Vitest reporter output); otherwise the suite is reported as skipped (describe.skipIf), which is acceptable for environments without `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` set.
    - All four filled form-builder specs (`fork-assigned-template`, `assignment-status-transitions` (incl. submitAssignedFillAction test), `customer-self-fill-submission`, `assignments-query`) report as passing in the Vitest output.
  </acceptance_criteria>
  <done>
    Live schema, regenerated types, application code, and test suite are all aligned. The phase-level gates (Vitest, build, RLS suite when env present) are green programmatically.
  </done>
</task>

<task type="auto">
  <name>Task 1c: BUILDER-04 carry-forward regression check (Phase 13 stack pin)</name>
  <read_first>
    - package.json (verify the @coltorapps/builder + react versions Phase 13 locked in)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Library Resolution" (if present) — confirms the version expectations
    - .planning/ROADMAP.md Phase 16 Requirements line — BUILDER-04 is listed
  </read_first>
  <action>
    BUILDER-04 is a carry-forward requirement from Phase 13 (the form builder dependency pin: `@coltorapps/builder@^0.2.4` + `react@^19`). Phase 16 does not change the builder dependency, but the requirement is listed in this plan's `requirements` frontmatter, so a regression check is needed: confirm the dependencies have not silently drifted.

    Run two grep gates against `package.json`:
    1. `grep -E '"@coltorapps/builder":\s*"\^?0\.2\.4"' package.json` — must exit 0 (one or more matches).
    2. `grep -E '"react":\s*"\^?19' package.json` — must exit 0 (one or more matches).

    If either grep returns no matches, STOP and report — a dependency drift in Phase 16 would break the form-builder surface across all Phase 13/14/15/16 routes. Otherwise the requirement is regression-clear.
  </action>
  <verify>
    <automated>grep -E '"@coltorapps/builder":\s*"\^?0\.2\.4"' package.json && grep -E '"react":\s*"\^?19' package.json && echo "BUILDER-04 OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"@coltorapps/builder":\s*"\^?0\.2\.4"' package.json` exits 0.
    - `grep -E '"react":\s*"\^?19' package.json` exits 0.
  </acceptance_criteria>
  <done>
    BUILDER-04 dependency pin is intact post-Phase-16. Phase 13's builder stack is still on the version Phase 13 / 14 / 15 / 16 all assume.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author phase summary + UAT note + ROADMAP update</name>
  <read_first>
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-01-SUMMARY.md (through Plan 06 summaries — they should all exist by the time Task 2 runs)
    - .planning/ROADMAP.md § "Phase 16: Multi-Tenancy + Fork-on-Fill" — the success criteria block
    - .planning/STATE.md — current position
    - .planning/phases/14-custom-field-types/14-UAT.md (analog for UAT.md file shape)
    - .planning/phases/15-conditional-logic-engine/15-UAT.md (more recent analog)
  </read_first>
  <behavior>
    - A new file `.planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md` exists containing manual walkthrough steps for the three manual-only verifications declared in 16-VALIDATION.md:
      - §A — Customise first → fork → redirect (D-07 e2e flow).
      - §B — Admin clients list shows active-assignment counter pill (D-12 admin pill).
      - §C — /client/templates no longer shows admin masters (D-09 cleanup verification).
    - Each UAT section includes: prerequisites (test users, seed data), step-by-step actions, expected observations, pass/fail checkboxes.
    - ROADMAP.md's Phase 16 entry has its `Plans: 7 plans` line set correctly and the plans list under Phase 16 enumerates 16-01 through 16-07 with their objectives.
    - STATE.md is updated to reflect Phase 16 plan-creation completion (the orchestrator may handle this — if not, this task adds the necessary increment).
  </behavior>
  <files>
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md (NEW)
    - .planning/ROADMAP.md (MODIFY)
  </files>
  <action>
    Read .planning/phases/15-conditional-logic-engine/15-UAT.md as a structural analog (it's the most recent UAT file, so its layout matches current conventions).

    Author .planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md with three sections:

    Section A — "Customise first → fork → builder redirect (D-07)":
    1. Prereqs: admin signed in to one window; client_user A signed in to a private/incognito window; at least one published admin master template; an assignment from admin to client A's org pointing at that template.
    2. As client A: navigate to /client/assignments, see the assignment under Active, click into it.
    3. On the landing page, verify: template name displayed at the top; due date shown; instructions block visible IF set on the assignment.
    4. Click "Customise first" — AlertDialog opens with the locked title "Create your own copy?".
    5. Click "Create my copy" — observe the redirect to /client/templates/<some-uuid>/edit (builder opens with the assignment's fields visible).
    6. In Supabase Studio: verify the form_assignments row now points at the fork's template_id (template_id changed) and the fork has parent_template_id = the master's id.
    Expected outcomes (checkbox list): redirect happened; builder shows the same fields as the master; assignment row's template_id is rewritten.

    Section B — "Admin clients list counter pill (D-12)":
    1. As admin: assign the same template to 2 different clients via the AssignTemplateModal (one of them being client A).
    2. Navigate to /admin/clients — observe the counter pill (earth amber) on the rows for both clients. Click hover to verify the tooltip "2 active assignments" appears (or "1 active assignment" depending on prior state).
    3. Client with zero active assignments shows NO pill (the absence is the affordance).
    Expected outcomes: pill renders only on rows with > 0 active assignments; hover tooltip pluralisation correct.

    Section C — "/client/templates simplification (D-09)":
    1. As client A: navigate to /client/templates.
    2. Verify the page shows only "My Templates" (no "Available admin masters" / "Available Templates" section).
    3. Verify the page heading uses the 28px Newsreader size (visually consistent with /client/assignments heading).
    4. If client A has zero customer templates, verify the empty state shows "No templates yet" + the locked body copy.
    Expected outcomes: no admin-masters section; unified heading size; empty-state copy correct.

    Then modify .planning/ROADMAP.md:
    - Locate the "Phase 16: Multi-Tenancy + Fork-on-Fill" section.
    - Update `**Plans:**` line from "Not yet planned" to `7 plans`.
    - Replace the "Plans: Not yet planned — run `/gsd:plan-phase 16`." line with an enumeration:

    ```
    Plans: 7 plans

    Plans:

    **Wave 0** *(schema + test infrastructure)*
    - [ ] 16-01-PLAN.md — Migrations 013 + 014, vitest.config extension, RLS helpers, RLS spec, form-builder Wave-0 scaffolds (wave 0)

    **Wave 1** *(admin server-action surface + shared modal)*
    - [ ] 16-02-PLAN.md — createAssignments + AssignTemplateModal + mount on /admin/templates/[id] (wave 1)

    **Wave 2** *(admin viewing surfaces + client lifecycle scaffolding — parallel after Wave 1)*
    - [ ] 16-03-PLAN.md — /admin/assignments queue + /admin/clients/[id] Assigned Forms tab + counter pill (wave 2)
    - [ ] 16-04-PLAN.md — /client/assignments tabs + landing page + interpreter fill + status transitions (wave 2)

    **Wave 3** *(fork + self-fill — parallel after Wave 2)*
    - [ ] 16-05-PLAN.md — forkAssignedTemplate + CustomiseFirstButton + delete forkOnFill dead code (wave 3)
    - [ ] 16-06-PLAN.md — /client/templates simplification + /client/templates/[id]/fill + submitCustomerTemplateFillAction (wave 3)

    **Wave 4** *(BLOCKING: db push + verification — sequential)*
    - [ ] 16-07-PLAN.md — supabase db push + types regen + full sweep + UAT + ROADMAP update (wave 4)
    ```

    Do NOT modify any other section of ROADMAP.md. Preserve the existing Goal, Depends on, Requirements, and Success Criteria for Phase 16.
  </action>
  <verify>
    <automated>ls .planning/phases/16-multi-tenancy-fork-on-fill/16-01-SUMMARY.md .planning/phases/16-multi-tenancy-fork-on-fill/16-02-SUMMARY.md .planning/phases/16-multi-tenancy-fork-on-fill/16-03-SUMMARY.md .planning/phases/16-multi-tenancy-fork-on-fill/16-04-SUMMARY.md .planning/phases/16-multi-tenancy-fork-on-fill/16-05-SUMMARY.md .planning/phases/16-multi-tenancy-fork-on-fill/16-06-SUMMARY.md && node -e "const fs=require('fs');if(!fs.existsSync('.planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md')){console.error('UAT missing');process.exit(1);} const uat=fs.readFileSync('.planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md','utf8');for (const s of ['Customise first','counter pill','admin masters','Create your own copy?']) {if(!uat.includes(s)){console.error('UAT missing:',s);process.exit(2);}} const r=fs.readFileSync('.planning/ROADMAP.md','utf8');for (const s of ['16-01-PLAN.md','16-07-PLAN.md','7 plans']) {if(!r.includes(s)){console.error('ROADMAP missing:',s);process.exit(3);}} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/16-multi-tenancy-fork-on-fill/16-UAT.md exists.
    - UAT contains the three section headings for D-07, D-12, D-09 (verifiable by literal substring matching: "Customise first", "counter pill", "admin masters").
    - UAT contains the locked AlertDialog string "Create your own copy?".
    - ROADMAP.md contains references to 16-01-PLAN.md through 16-07-PLAN.md.
    - ROADMAP.md Phase 16 plans line reads `7 plans` (replacing the prior "Not yet planned").
    - ROADMAP.md retains its existing Phase 16 Goal, Depends on, Requirements, and Success Criteria (no unintended deletions — verifiable by grep for the existing success-criteria phrases like "no cross-org template or submission visibility").
    - All six prior-plan SUMMARY files exist: `.planning/phases/16-multi-tenancy-fork-on-fill/16-01-SUMMARY.md` through `16-06-SUMMARY.md` (verifiable via the `ls` prefix on the verify block — failure means at least one upstream plan did not complete its output step, blocking phase close-out).
  </acceptance_criteria>
  <done>
    16-UAT.md is ready for the user to walk through. ROADMAP.md reflects the plan structure. Phase 16 close-out artifacts (per-plan SUMMARYs from Plans 01-06 + this UAT + this ROADMAP update) are complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → live Supabase project | `supabase db push` writes DDL via the linked-project auth. Project is locked to eu-west-2 per FOUND-01. |
| regenerated types → application compile | Compile gate confirms TypeScript and live schema agree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | RLS suite false-passes after schema push (e.g. policies dropped) | mitigate | The 5-spec suite runs as part of `npm test` in Task 1's verification step — any RLS regression caused by the push would surface here. |
| Schema mismatch | Tampering | database.types.ts stale vs live DB after push | mitigate | Task 1 step 3 regenerates the types from the linked project. Task 1 step 6 (`npm run build`) catches any mismatch as a TypeScript error. |
| Data loss | Tampering | Push misfires and a table is dropped | accept | Migrations 013 + 014 are additive/permissive only (ADD COLUMN; DROP NOT NULL). Neither deletes data. Backup is the standard Supabase nightly. |
</threat_model>

<verification>
After both tasks:
- Live Supabase schema reflects 013 + 014.
- `lib/supabase/database.types.ts` regenerated.
- `npm test` and `npm run build` are green.
- 16-UAT.md exists with the three manual walkthrough sections.
- ROADMAP.md Phase 16 entry shows 7 plans enumerated.
</verification>

<success_criteria>
- Migrations applied to live DB (verified via Studio or psql).
- TypeScript types regenerated and committed at phase close-out.
- All four phase-level gates (Vitest sweep, build, RLS suite when env present, manual UAT prepared) are green.
- ROADMAP + UAT + per-plan SUMMARYs all in place.
- Phase 16 ready for the user to mark complete via `/gsd:verify-work` or equivalent gate.
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-tenancy-fork-on-fill/16-07-SUMMARY.md` recording: which migrations pushed, the types-regen output diff signature (e.g. line counts before/after), the full-suite test results, the build result, and the UAT-readiness checklist.
</output>
