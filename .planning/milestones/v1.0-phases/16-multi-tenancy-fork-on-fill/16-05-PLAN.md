---
phase: 16-multi-tenancy-fork-on-fill
plan: 05
plan_id: 16-05
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-04]
files_modified:
  - app/client/assignments/actions.ts
  - app/client/assignments/[id]/page.tsx
  - app/client/assignments/[id]/customise-first-button.tsx
  - tests/form-builder/fork-assigned-template.test.ts
autonomous: true
requirements:
  - BUILDER-01
  - BUILDER-03
tags: [client, fork, redirect, server-action]
must_haves:
  truths:
    - "Customer can click Customise first on an assignment landing page; an AlertDialog confirms; on confirm forkAssignedTemplate runs and redirects to /client/templates/[fork_id]/edit (D-07)."
    - "forkAssignedTemplate copies the assignment's pinned template_version_id (NOT the master's latest) into a new customer-owned form_templates row with owner_type='customer', owner_id=ctx.client_id, parent_template_id=master.id, is_published=true (D-05, D-08)."
    - "Fork's v1 template_versions row is published (published_at=now()) and contains the exact schema_json from the pinned source (D-05, D-08)."
    - "On fork success, originating form_assignments.template_id and template_version_id are rewritten to point at the fork (D-06)."
    - "redirect() to /client/templates/[fork_id]/edit is the LAST statement of forkAssignedTemplate, outside any try/catch (RESEARCH Pitfall 1)."
    - "fork-assigned-template.test.ts scaffold replaced with real query-shape assertions (D-05, D-06, D-08)."
  artifacts:
    - path: app/client/assignments/actions.ts
      provides: "forkAssignedTemplate(assignmentId) server action — multi-step JS write ending in redirect"
      exports: ["forkAssignedTemplate"]
    - path: app/client/assignments/[id]/customise-first-button.tsx
      provides: "Client component AlertDialog + useTransition wrapper around forkAssignedTemplate"
      exports: ["CustomiseFirstButton"]
  key_links:
    - from: app/client/assignments/[id]/customise-first-button.tsx
      to: app/client/assignments/actions.ts (forkAssignedTemplate)
      via: "await forkAssignedTemplate(assignmentId) inside startTransition"
      pattern: "forkAssignedTemplate"
    - from: app/client/assignments/actions.ts (forkAssignedTemplate)
      to: form_templates + template_versions + form_assignments (3 writes, 1 update)
      via: "ordered inserts then update then redirect"
      pattern: "parent_template_id"
    - from: app/client/assignments/[id]/page.tsx
      to: customise-first-button.tsx
      via: "<CustomiseFirstButton assignmentId={id} />"
      pattern: "CustomiseFirstButton"
---

<objective>
Build the explicit-button fork flow (D-07).

1. `forkAssignedTemplate(assignmentId)` server action in `app/client/assignments/actions.ts` — verifies org ownership, reads pinned version, inserts customer-owned fork + v1 published version, rewrites the originating assignment row, redirects to the builder.
2. `app/client/assignments/[id]/customise-first-button.tsx` — client component AlertDialog confirmation wrapping the action call.
3. Replace the placeholder button in `app/client/assignments/[id]/page.tsx` (left by Plan 04) with the real CustomiseFirstButton mount.
4. Fill `tests/form-builder/fork-assigned-template.test.ts` scaffold with query-shape assertions per D-05, D-06, D-08.

Note: The deletion of the predecessor `forkOnFill` from `app/client/templates/actions.ts` (and any orphan dead-code cleanup, including `lib/forms/schema-diff.ts` if it becomes unused) has been moved to Plan 06 to avoid two parallel wave-3 plans writing to the same file. Plan 06's final task owns that cleanup.

Purpose: Customers must be able to fork an assignment into their own org's template, edit it, and submit against the fork. The master template is never mutated.

Output: One new server action, one new client component, one button mount, one filled test scaffold.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md
@.planning/phases/16-04-PLAN.md

<interfaces>
From lib/supabase/server.ts:
- export async function createClient(): Promise<SupabaseClient>  // RLS-aware

From lib/auth-helpers.ts:
- export async function getClientContext(): Promise<{ user_id: string; client_id: string; role: string } | null>
- export async function requireActorUserId(actorType: "admin" | "client"): Promise<string | null>

Existing fork pattern source (the function being decommissioned in Plan 06, but its SHAPE is the analog for the new one):
- app/client/templates/actions.ts forkOnFill at lines 214-283 — uses .eq("template_id", masterId).order("version_number", desc).limit(1) to find LATEST published — the new function reads the EXACT pinned version_id instead

Columns to write on the fork insert (per AGENTS.md polymorphic contract):
- form_templates: name, template_type, owner_type='customer', owner_id=ctx.client_id (the ORG, never client_users.id), parent_template_id=master_template_id, is_published=true
- template_versions: template_id (fork.id), version_number=1, schema_json (copied from pinned), published_at=new Date().toISOString(), created_by=ctx.user_id

UI-SPEC Route E "Customise first" AlertDialog (locked copy):
- Title: "Create your own copy?"
- Body: "This creates your organisation's version of this form. Changes to the original won't carry over to your copy."
- Cancel: "Keep original"
- Confirm: "Create my copy" (black-fill button — primary variant, NOT destructive)

UI-SPEC error toast copy:
- "Could not create your copy. Please try again."

shadcn primitives (already installed):
- AlertDialog (components/ui/alert-dialog.tsx) — Title, Description, Cancel, Action, Trigger
- Button (components/ui/button.tsx)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add forkAssignedTemplate to app/client/assignments/actions.ts + fill test scaffold</name>
  <read_first>
    - app/client/assignments/actions.ts (created in Plan 04 — append the new export; do NOT regenerate the file from scratch)
    - app/client/templates/actions.ts (lines 214-283 — forkOnFill prior art; structure to copy + the version-fetch line to CHANGE per D-05)
    - app/admin/assessments/actions.ts (lines 17-73 — startAssessment redirect-outside-catch pattern)
    - supabase/migrations/001_initial_schema.sql (form_templates + template_versions + form_assignments column shapes)
    - supabase/migrations/003_form_template_customer_ownership.sql (owner_type='customer' contract)
    - supabase/migrations/005_template_versions_polymorphic_created_by.sql (template_versions.created_by polymorphism — accepts admin or client user UUIDs)
    - tests/form-builder/fork-assigned-template.test.ts (Plan 01 scaffold — replace todos with real assertions)
    - tests/form-builder/version-pin.test.ts (spy-on-from query-shape assertion pattern)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pattern 1: Transactional Fork Server Action" lines 275-402
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "app/client/assignments/actions.ts" → "Existing fork prior art" [File Classification row 6: app/client/assignments/actions.ts; row 21 prior art: MODIFY app/client/templates/actions.ts]
  </read_first>
  <behavior>
    - forkAssignedTemplate is a `"use server"` export with signature `(assignmentId: string): Promise<never>`.
    - Step 1: `const ctx = await requireClientContext();` (helper introduced in Plan 04).
    - Step 2: `const userId = await requireActorUserId("client");` for the version's `created_by`.
    - Step 3: `const supabase = await createClient();` (RLS-aware).
    - Step 4: Read the assignment row by id: `select("id, client_id, template_id, template_version_id, deleted_at")`. Throw "Assignment not found" if missing. Throw "Forbidden: not your assignment" if `client_id !== ctx.client_id`. Throw "Cannot fork a revoked assignment" if `deleted_at !== null`.
    - Step 5: Read the pinned template_versions row BY ITS ID (NOT by template_id + latest): `supabase.from("template_versions").select("schema_json").eq("id", assignment.template_version_id).single()`. Throw "Pinned version missing" on error.
    - Step 6: Read the master template metadata for name/type carry-over: `supabase.from("form_templates").select("name, template_type").eq("id", assignment.template_id).single()`.
    - Step 7: Insert fork form_templates: `.insert({ name: master.name, template_type: master.template_type, owner_type: "customer", owner_id: ctx.client_id, parent_template_id: assignment.template_id, is_published: true }).select("id").single()`. Throw on error.
    - Step 8: Insert fork v1 template_versions: `.insert({ template_id: fork.id, version_number: 1, schema_json: pinned.schema_json, published_at: new Date().toISOString(), created_by: userId }).select("id").single()`. Throw on error.
    - Step 9: Update the originating assignment to point at the fork: `.update({ template_id: fork.id, template_version_id: v1.id }).eq("id", assignmentId)`. Throw on error.
    - Step 10: `revalidatePath("/client/assignments"); revalidatePath("/client/templates");` (both lists must refresh).
    - Step 11: `redirect(\`/client/templates/${fork.id}/edit\`);` — the LAST statement, outside any try/catch.
    - The function MUST NOT wrap any of steps 1-10 in a try/catch that could swallow the redirect's NEXT_REDIRECT.
    - The function MUST NOT call .rpc() (codebase has zero RPC pattern — RESEARCH Alternative Considered, rejected).
    - tests/form-builder/fork-assigned-template.test.ts mocks supabase via vi.mock("@/lib/supabase/server"), mocks getClientContext + requireActorUserId, mocks next/cache + next/navigation. Asserts:
      - (a) The function reads `template_versions` by `.eq("id", "<assignment.template_version_id-value>")` — NOT by template_id + latest (D-05 fidelity).
      - (b) The fork form_templates insert payload has `owner_type: "customer"`, `owner_id: "client-org-001"` (the org), `parent_template_id: "<master-template-id>"`, `is_published: true` (D-08).
      - (c) The v1 template_versions insert payload has `version_number: 1`, `published_at` set to a non-null string, `schema_json` referentially equal to the pinned source (D-05 fidelity).
      - (d) After the version insert, an UPDATE on form_assignments is issued with `template_id: <fork.id>, template_version_id: <v1.id>` (D-06).
      - (e) redirect was called with the path matching `/client/templates/<fork.id>/edit`.
      - (f) revalidatePath was called with both `/client/assignments` and `/client/templates`.
  </behavior>
  <files>
    - app/client/assignments/actions.ts (MODIFY — append new export forkAssignedTemplate)
    - tests/form-builder/fork-assigned-template.test.ts (REPLACE scaffold from Plan 01)
  </files>
  <action>
    Open app/client/assignments/actions.ts (already exists from Plan 04). Append `forkAssignedTemplate` per the `<behavior>` block. Imports — confirm `redirect` from `next/navigation` is already imported (Plan 04 imported it for startAssignmentFill); if not, add it. Confirm `revalidatePath` from `next/cache` is imported.

    The implementation is a top-level async server function (no IIFE, no try/catch wrap around the whole body). Each step throws on error so the failure surfaces to the client component's catch and produces a toast. The redirect is the LAST statement.

    Author tests/form-builder/fork-assigned-template.test.ts. Top of file: Vitest mock skeleton from tests/form-builder/save-draft.test.ts adapted for this scope:

    ```
    vi.mock("@/lib/supabase/server", () => ({ createClient: vi.fn() }));
    vi.mock("@/lib/auth-helpers", () => ({ requireActorUserId: vi.fn().mockResolvedValue("user-001"), getClientContext: vi.fn().mockResolvedValue({ user_id: "user-001", client_id: "client-org-001", role: "client" }) }));
    vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }));
    vi.mock("next/navigation", () => ({ redirect: vi.fn() }));
    ```

    In each test, build a fake supabase with a query-builder chain that records arguments. The from() spy returns different responses keyed by table name (form_assignments, template_versions, form_templates). Use vi.fn().mockImplementation((table) => { ... }).

    Sample seed (use real-looking UUIDs):
    - assignment row: `{ id: "asg-1", client_id: "client-org-001", template_id: "master-1", template_version_id: "ver-master-1", deleted_at: null }`
    - pinned version: `{ schema_json: { entities: { e1: { type: "text" } }, root: { children: ["e1"] } } }`
    - master template: `{ name: "FRA Type 3", template_type: "fra" }`
    - fork insert response: `{ id: "fork-1" }`
    - v1 insert response: `{ id: "ver-fork-1" }`

    Wire the chain so:
    - .from("form_assignments").select(...).eq("id", "asg-1").single() resolves with `{ data: assignmentRow, error: null }`.
    - .from("template_versions").select(...).eq("id", "ver-master-1").single() resolves with `{ data: pinnedVersion, error: null }`.
    - .from("form_templates").select(...).eq("id", "master-1").single() resolves with `{ data: masterTemplate, error: null }`.
    - .from("form_templates").insert(<forkInsertPayload>).select("id").single() resolves with `{ data: { id: "fork-1" }, error: null }`.
    - .from("template_versions").insert(<v1Payload>).select("id").single() resolves with `{ data: { id: "ver-fork-1" }, error: null }`.
    - .from("form_assignments").update(<assignmentUpdate>).eq("id", "asg-1") resolves with `{ error: null }`.

    Then `await forkAssignedTemplate("asg-1")` and assert:
    - .eq was called with `("id", "ver-master-1")` (D-05 pin fidelity).
    - Some insert call was made with payload containing `owner_type: "customer"`, `owner_id: "client-org-001"`, `parent_template_id: "master-1"`, `is_published: true` (D-08).
    - Some insert call was made with payload containing `version_number: 1` and `published_at` non-null and `schema_json` === pinnedVersion.schema_json (D-05 + D-08).
    - An update call was made on `form_assignments` with `template_id: "fork-1", template_version_id: "ver-fork-1"` (D-06).
    - The `redirect` mock was called with the string `/client/templates/fork-1/edit`.
    - The `revalidatePath` mock was called with both `/client/assignments` and `/client/templates`.

    Run `npm test -- tests/form-builder/fork-assigned-template.test.ts --run` to confirm green.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('app/client/assignments/actions.ts','utf8');for (const s of ['forkAssignedTemplate','owner_type: \"customer\"','parent_template_id','is_published: true','version_number: 1','published_at: new Date().toISOString()','redirect(`/client/templates/']) {if(!a.includes(s)){console.error('Actions missing:',s);process.exit(1);}} const t=fs.readFileSync('tests/form-builder/fork-assigned-template.test.ts','utf8');for (const s of ['forkAssignedTemplate','owner_type','parent_template_id','redirect','toHaveBeenCalledWith']) {if(!t.includes(s)){console.error('Test missing:',s);process.exit(2);}} if(t.match(/it\\.todo\\(/g)) {console.error('Scaffold todos remain — should be filled');process.exit(3);} console.log('OK')" && npm test -- tests/form-builder/fork-assigned-template.test.ts --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/client/assignments/actions.ts contains `forkAssignedTemplate`.
    - app/client/assignments/actions.ts contains `owner_type: "customer"`.
    - app/client/assignments/actions.ts contains `parent_template_id` (the link from fork to master).
    - app/client/assignments/actions.ts contains `is_published: true` (D-08).
    - app/client/assignments/actions.ts contains `version_number: 1`.
    - app/client/assignments/actions.ts contains `redirect(\`/client/templates/` (template literal — D-07 navigation target).
    - app/client/assignments/actions.ts does NOT wrap the redirect inside a try/catch — verifiable by inspecting lines around the redirect call.
    - tests/form-builder/fork-assigned-template.test.ts contains zero `it.todo(` calls (all filled with real assertions).
    - tests/form-builder/fork-assigned-template.test.ts contains `toHaveBeenCalledWith` calls (proves real spy assertions).
    - `npm test -- tests/form-builder/fork-assigned-template.test.ts --run` exits 0.
  </acceptance_criteria>
  <done>
    forkAssignedTemplate is implemented, tested at query-shape level, and ready to be invoked by the client component built in Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build CustomiseFirstButton client component + mount it on the landing page</name>
  <read_first>
    - app/client/templates/_components/client-template-card.tsx (lines 39-114 — the canonical AlertDialog + useTransition + toast pattern for client-surface server-action triggers)
    - components/ui/alert-dialog.tsx (the primitive API used by the analog)
    - components/ui/button.tsx
    - app/client/assignments/actions.ts (the forkAssignedTemplate export to call)
    - app/client/assignments/[id]/page.tsx (the placeholder location from Plan 04 — replace it)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route E — Customise first AlertDialog" + § "Copywriting Contract" (locked AlertDialog strings)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pattern 1: Transactional Fork Server Action with redirect()" → Caller pattern (lines 375-402)
  </read_first>
  <behavior>
    - CustomiseFirstButton is `"use client"` exporting `CustomiseFirstButton({ assignmentId }: { assignmentId: string })`.
    - Renders a shadcn outline Button labelled "Customise first" (UI-SPEC Route E — outline variant, black text on cream).
    - Clicking the button opens an `<AlertDialog>` with:
      - Title: `Create your own copy?`
      - Description: `This creates your organisation's version of this form. Changes to the original won't carry over to your copy.`
      - Cancel: `Keep original`
      - Action (NOT destructive variant — this is a create action per UI-SPEC § Destructive Actions table): `Create my copy` with black-fill primary-button classes.
    - On Action click: `startTransition(async () => { try { await forkAssignedTemplate(assignmentId); } catch (err) { if (err is NEXT_REDIRECT) re-throw; else toast.error("Could not create your copy. Please try again."); } });`.
    - While pending, the Action button's label becomes "Creating copy…" and is disabled.
    - The component does NOT render a success toast — the redirect is the success signal (RESEARCH Pattern 1 caller pattern).
    - app/client/assignments/[id]/page.tsx (modified in Plan 04 with a disabled placeholder) is updated: import CustomiseFirstButton and replace the placeholder JSX with `<CustomiseFirstButton assignmentId={id} />`.
  </behavior>
  <files>
    - app/client/assignments/[id]/customise-first-button.tsx (NEW)
    - app/client/assignments/[id]/page.tsx (MODIFY — replace Plan 04 placeholder)
  </files>
  <action>
    Author app/client/assignments/[id]/customise-first-button.tsx. `"use client"`. Imports: `import { useState, useTransition } from "react";`, `import { toast } from "sonner";`, `import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from "@/components/ui/alert-dialog";`, `import { Button } from "@/components/ui/button";`, `import { forkAssignedTemplate } from "@/app/client/assignments/actions";`.

    Inside the component: `const [open, setOpen] = useState(false); const [pending, startTransition] = useTransition();`. Render `<AlertDialog open={open} onOpenChange={setOpen}>`. Trigger: `<AlertDialogTrigger asChild><Button variant="outline" className="...black-outline-cream-surface-classes...">Customise first</Button></AlertDialogTrigger>`. Content: header with locked title + description, footer with Cancel and Action.

    Handler: `function handleConfirm() { startTransition(async () => { try { await forkAssignedTemplate(assignmentId); /* unreachable — server action redirects */ } catch (err) { const msg = err instanceof Error ? err.message : ""; if (msg.includes("NEXT_REDIRECT")) throw err; toast.error("Could not create your copy. Please try again."); } }); }`.

    Action button uses `<AlertDialogAction onClick={handleConfirm} disabled={pending} className="bg-[#1a1a1a] text-white hover:bg-[#1a1a1a]/90">{pending ? "Creating copy…" : "Create my copy"}</AlertDialogAction>`.

    Then modify app/client/assignments/[id]/page.tsx — import `CustomiseFirstButton from "./customise-first-button"` and replace the disabled placeholder rendered by Plan 04 with `<CustomiseFirstButton assignmentId={id} />`. Side-by-side with `<FillAsIsButton assignmentId={id} />` in the CTA row (UI-SPEC Route E layout: two buttons side-by-side desktop, stacked mobile).

    Do not change any other landing-page behavior.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const b=fs.readFileSync('app/client/assignments/[id]/customise-first-button.tsx','utf8');for (const s of ['use client','useTransition','AlertDialog','Create your own copy?','Keep original','Create my copy','forkAssignedTemplate','Could not create your copy. Please try again.','Creating copy']) {if(!b.includes(s)){console.error('Button missing:',s);process.exit(1);}} const p=fs.readFileSync('app/client/assignments/[id]/page.tsx','utf8');if(!p.includes('CustomiseFirstButton')){console.error('Landing page does not import CustomiseFirstButton');process.exit(2);} if(p.includes('Plan 05 wires')){console.error('Plan 04 placeholder comment still present — must be removed');process.exit(3);} console.log('OK')" && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File app/client/assignments/[id]/customise-first-button.tsx exists with `"use client"`.
    - Component contains `forkAssignedTemplate` import and call inside startTransition.
    - Component contains all four locked AlertDialog copy strings: `Create your own copy?`, `This creates your organisation's version of this form. Changes to the original won't carry over to your copy.`, `Keep original`, `Create my copy`.
    - Component contains the error toast string `Could not create your copy. Please try again.`.
    - Component contains the pending label `Creating copy…`.
    - app/client/assignments/[id]/page.tsx imports CustomiseFirstButton and renders it.
    - app/client/assignments/[id]/page.tsx no longer contains the Plan-04 placeholder comment `Plan 05 wires`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>
    Customer clicks "Customise first" → AlertDialog opens → confirm → fork created → redirect to `/client/templates/[fork.id]/edit`. The originating assignment now points at the fork (verifiable in Studio).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → forkAssignedTemplate server action | Untrusted invocation; requireOwnedAssignment is the org-ownership gate |
| RLS @supabase/ssr → form_templates / template_versions / form_assignments | Server uses RLS-aware createClient; admin trust boundary is NOT crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Elevation of Privilege | Forking another org's assignment (client A submits assignmentId=B's) | mitigate | forkAssignedTemplate verifies `assignment.client_id === ctx.client_id` BEFORE any write (explicit check; defense-in-depth alongside RLS). Throws "Forbidden: not your assignment". |
| T-16-06 | Elevation of Privilege | Service-role client imported on customer surface | mitigate | forkAssignedTemplate uses `createClient` from `lib/supabase/server.ts` (RLS-aware). NO import of `lib/supabase/admin.ts`. Grep gate: `grep -rn "supabase/admin" app/client/assignments/` must return 0 hits. |
| T-16-07 | Tampering | Direct POST to forkAssignedTemplate without client claim | mitigate | requireClientContext throws "Not a client user" first; requireActorUserId("client") supplies the version's created_by. |
| T-16-01 | Information Disclosure | Reading pinned schema_json across orgs | mitigate | RLS on template_versions + form_assignments (migrations 001/003). The pinned-version fetch only succeeds because the assignment row is owned by ctx.client_id. |
</threat_model>

<verification>
After both tasks:
- `npm run build` succeeds.
- `npm test -- tests/form-builder/fork-assigned-template.test.ts --run` exits 0.
- grep gate: `grep -rn "from \"@/lib/supabase/admin\"" app/client/assignments/` returns 0 hits (T-16-06).
- Manual smoke (dev, optional): sign in as customer; open active assignment; click Customise first; confirm; observe redirect to `/client/templates/[fork-id]/edit`; verify in Studio that form_assignments row now references the fork's template_id and v1 version id.
- Note: the `forkOnFill` dead-code removal grep gate (`grep -rn "forkOnFill" app/ lib/` returns 0 hits) is verified by Plan 06's final task, not here — Plan 06 now owns the deletion to avoid wave-3 file-overlap.
</verification>

<success_criteria>
- forkAssignedTemplate exists in app/client/assignments/actions.ts with the documented multi-step shape ending in redirect-outside-catch.
- CustomiseFirstButton client component is mounted on the landing page in place of Plan 04's placeholder.
- fork-assigned-template.test.ts asserts query shapes for D-05 (pinned read), D-06 (assignment rewire), D-08 (auto-publish v1).
- No regressions in existing Phase 13/14/15 tests.
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-tenancy-fork-on-fill/16-05-SUMMARY.md` recording: the forkAssignedTemplate signature, the step-by-step write order, the customised AlertDialog locked-copy strings shipped, and the test assertions added. Note that the forkOnFill deletion + schema-diff orphan cleanup is owned by Plan 06.
</output>
