---
phase: 16-multi-tenancy-fork-on-fill
plan: 02
plan_id: 16-02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - app/admin/assignments/actions.ts
  - components/admin/assign-template-modal.tsx
  - app/admin/templates/[id]/page.tsx
  - components/ui/checkbox.tsx
autonomous: true
requirements:
  - BUILDER-01
tags: [admin, server-action, modal, shadcn, assignment]
must_haves:
  truths:
    - "Admin can assign a published template to one or more clients with optional due_date + instructions from /admin/templates/[id] (D-01)."
    - "createAssignments writes N form_assignments rows in a single .insert() call, one per selected client (D-02)."
    - "Assignments are mutable via updateAssignment until status='completed'; revoke is a soft-delete on form_assignments.deleted_at (D-03)."
    - "Server action calls requireActorUserId('admin') as its first statement (T-16-02, T-16-07)."
    - "Modal is a single shared component reusable on both admin entry points (D-01)."
    - "Due-date input defaults to today + 7 days (Specific Ideas)."
    - "instructions column from migration 013 is populated when provided."
  artifacts:
    - path: app/admin/assignments/actions.ts
      provides: "createAssignments, updateAssignment, revokeAssignment server actions"
      exports: ["createAssignments", "updateAssignment", "revokeAssignment"]
    - path: components/admin/assign-template-modal.tsx
      provides: "Shared shadcn Dialog modal with template picker + multi-select clients + due_date + instructions; submits to createAssignments"
      exports: ["AssignTemplateModal"]
    - path: components/ui/checkbox.tsx
      provides: "shadcn checkbox primitive (npx shadcn add checkbox)"
      exports: ["Checkbox"]
  key_links:
    - from: components/admin/assign-template-modal.tsx
      to: app/admin/assignments/actions.ts
      via: "import { createAssignments } and call inside startTransition"
      pattern: "createAssignments"
    - from: app/admin/templates/[id]/page.tsx
      to: components/admin/assign-template-modal.tsx
      via: "<AssignTemplateModal templateId={id} clients={...} />"
      pattern: "AssignTemplateModal"
    - from: app/admin/assignments/actions.ts
      to: lib/auth-helpers.requireActorUserId
      via: "first statement of every export"
      pattern: "requireActorUserId\\(\\\"admin\\\"\\)"
---

<objective>
Wire the admin-side "Assign template to clients" capability (D-01, D-02, D-04). Deliver:

1. `app/admin/assignments/actions.ts` — server actions `createAssignments`, `updateAssignment`, `revokeAssignment`.
2. `components/admin/assign-template-modal.tsx` — shared multi-select Dialog modal reusable on both admin entry points.
3. Mount `<AssignTemplateModal templateId={id} clients={...} />` on `/admin/templates/[id]` (Route C per UI-SPEC).
4. Install `components/ui/checkbox.tsx` via `npx shadcn add checkbox`.

Purpose: Phase 16's primary admin capability. The second mount (per-client tab on `/admin/clients/[id]`) is delivered in Plan 03; this plan provides the shared component it consumes.

Output: A working modal at Route C that writes N form_assignments rows with `status='pending'`, optional due_date and instructions, pinned to the template's latest published version.
</objective>

<execution_context>
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/Antigravity/888 Safety/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-CONTEXT.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md
@.planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md
@.planning/phases/16-01-PLAN.md

<interfaces>
From lib/supabase/admin.ts:
- export const adminClient: SupabaseClient  // service-role, server-only

From lib/auth-helpers.ts:
- export async function requireActorUserId(actorType: "admin" | "client"): Promise<string | null>

From the form_assignments table (migration 001 lines 72-83, plus migration 013 column):
- columns: id (uuid pk), client_id (uuid fk), template_id (uuid fk), template_version_id (uuid fk), assigned_by (uuid), status (text default 'pending'), due_date (date null), instructions (text null) — added by migration 013, deleted_at (timestamptz null), created_at (timestamptz default now)

From form_templates / template_versions (migration 001):
- template_versions.published_at (timestamptz null) — version is published iff NOT NULL
- template_versions.version_number (int)

shadcn primitives already installed:
- Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger (components/ui/dialog.tsx)
- Button (components/ui/button.tsx)
- Input (components/ui/input.tsx)
- Textarea (components/ui/textarea.tsx)
- Select primitives (components/ui/select.tsx)
- Label (components/ui/label.tsx)

shadcn primitive to install (per UI-SPEC Registry Safety):
- npx shadcn add checkbox → creates components/ui/checkbox.tsx exporting Checkbox

Toast:
- import { toast } from "sonner"

Pattern source for shadcn dialog modal (already verified live):
- components/admin/upload-document-modal.tsx (lines 86-220) — Dialog + DialogContent dark surface, form submit, native date input with colorScheme: dark
- components/clients/new-client-dialog.tsx (lines 37-59) — useState submitting + try/catch + toast pattern

Pattern source for admin server action (already verified live):
- app/admin/templates/actions.ts lines 9-36 — "use server", requireActorUserId first, RLS-aware insert, revalidatePath
- RESEARCH § "Code Examples" → "Assign action — server action shape" lines 774-875
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install shadcn Checkbox + author admin server actions in app/admin/assignments/actions.ts</name>
  <read_first>
    - components.json (confirms shadcn style/baseColor; required by `npx shadcn add`)
    - components/ui/dialog.tsx (existing primitive — confirms add path)
    - app/admin/templates/actions.ts (lines 1-50 — analog for server-action header, requireActorUserId-first, .insert + revalidatePath)
    - app/admin/assessments/actions.ts (lines 232-309 — submitAssessmentAction pattern showing .eq("status", previous) optimistic guard for state-machine writes; mirror for `updateAssignment` D-03 immutability check)
    - lib/supabase/admin.ts (the adminClient export shape — admin server actions use adminClient per the architectural responsibility map; the RLS-aware createClient is for app/client/ routes)
    - lib/auth-helpers.ts (lines 50-56 — requireActorUserId signature)
    - supabase/migrations/001_initial_schema.sql (lines 72-83 — form_assignments columns the insert must populate)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Code Examples" — createAssignments / updateAssignment / revokeAssignment example bodies lines 775-875
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "app/admin/assignments/actions.ts (server actions, CRUD)" [File Classification row 2: app/admin/assignments/actions.ts]
  </read_first>
  <behavior>
    - `npx shadcn add checkbox` runs successfully and creates components/ui/checkbox.tsx exporting `Checkbox`.
    - app/admin/assignments/actions.ts begins with `"use server"`; every exported function calls `await requireActorUserId("admin")` as the first statement after parameter unpacking (T-16-07 mitigation).
    - createAssignments: signature `(templateId: string, clientIds: string[], opts?: { dueDate?: string; instructions?: string }) => Promise<{ created: number }>`. Resolves the latest published template_versions row for templateId (newest version_number where published_at IS NOT NULL); throws if none. Builds `clientIds.map(c => ({ client_id: c, template_id, template_version_id: pubVersion.id, assigned_by: adminUserId, due_date: opts?.dueDate ?? null, instructions: opts?.instructions ?? null, status: "pending" }))` and `.insert(rows)` via adminClient. Throws if clientIds is empty. Revalidates `/admin/assignments`, `/admin/clients`, `/admin/clients/${id}` for each clientId, and `/admin/templates/${templateId}`.
    - updateAssignment: signature `(assignmentId: string, patch: { dueDate?: string | null; instructions?: string | null }) => Promise<void>`. Reads current row to fetch `status` + `client_id`; throws if not found; throws if `status === "completed"` (D-03 immutability). Builds a sparse update object only for keys present in `patch` (undefined fields are skipped, but explicit null is honoured so admin can clear due_date / instructions). Revalidates `/admin/assignments` and `/admin/clients/${client_id}`.
    - revokeAssignment: signature `(assignmentId: string) => Promise<void>`. Reads current row for `client_id`; calls `.update({ deleted_at: new Date().toISOString() }).eq("id", assignmentId)`. Revalidates same paths as updateAssignment. Never uses `.delete()` (soft-delete convention).
    - No function uses `redirect()` (only fork-on-click in Plan 05 redirects).
    - File contains no `console.log`. Errors propagate via thrown Error with the supabase error message.
  </behavior>
  <files>
    - components/ui/checkbox.tsx (NEW via shadcn CLI)
    - app/admin/assignments/actions.ts (NEW)
  </files>
  <action>
    Run `npx shadcn@latest add checkbox -y` from the project root. Verify components/ui/checkbox.tsx exists and exports `Checkbox`. If the CLI prompts interactively despite `-y`, fall back to inspecting `npx shadcn info checkbox` and writing the file manually per the Radix `@radix-ui/react-checkbox` pattern used by other shadcn components in this project (Dialog already imports from `@radix-ui/react-dialog` — the package set is on the same scope; `npm install @radix-ui/react-checkbox` may be required if the CLI does not auto-install).

    Author app/admin/assignments/actions.ts. Header: `"use server";` then imports — `import { adminClient } from "@/lib/supabase/admin";`, `import { requireActorUserId } from "@/lib/auth-helpers";`, `import { revalidatePath } from "next/cache";`. Export interface `CreateAssignmentsInput { dueDate?: string; instructions?: string; }`. Export the three functions per the `<behavior>` block.

    For createAssignments: after `requireActorUserId`, validate `clientIds.length > 0` with throw "Select at least one client". Resolve the published version via `adminClient.from("template_versions").select("id").eq("template_id", templateId).not("published_at", "is", null).order("version_number", { ascending: false }).limit(1).maybeSingle()`. Throw "Template has no published version" if maybeSingle returns null. Build rows array and call `adminClient.from("form_assignments").insert(rows)`. Revalidate all paths per behavior block.

    For updateAssignment: after `requireActorUserId`, `adminClient.from("form_assignments").select("status, client_id").eq("id", assignmentId).single()`. If error or null, throw "Assignment not found". If `status === "completed"`, throw "Cannot edit a completed assignment" (D-03). Build update object — only set `due_date` if `patch.dueDate !== undefined`; only set `instructions` if `patch.instructions !== undefined`. Issue `.update(update).eq("id", assignmentId)`. Revalidate.

    For revokeAssignment: after `requireActorUserId`, read row for client_id (single), then `.update({ deleted_at: new Date().toISOString() }).eq("id", assignmentId)`. Throw on update error. Revalidate.

    Do NOT add a redirect anywhere in this file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');if(!fs.existsSync('components/ui/checkbox.tsx')){console.error('checkbox primitive missing');process.exit(1);}const c=fs.readFileSync('app/admin/assignments/actions.ts','utf8');for (const s of ['use server','createAssignments','updateAssignment','revokeAssignment','requireActorUserId(\"admin\")','revalidatePath','from(\"form_assignments\")','published_at']) {if(!c.includes(s)){console.error('Missing token:',s);process.exit(2);}} if(c.includes('redirect(')) {console.error('redirect not allowed here');process.exit(3);} if(c.match(/\\.delete\\(/)) {console.error('hard delete found');process.exit(4);} console.log('OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File components/ui/checkbox.tsx exists with `export { Checkbox }` (matches shadcn convention used by other primitives).
    - File app/admin/assignments/actions.ts exists.
    - File contains literal `"use server"` directive on line 1.
    - File contains `export async function createAssignments(`.
    - File contains `export async function updateAssignment(`.
    - File contains `export async function revokeAssignment(`.
    - File contains string `requireActorUserId("admin")` (T-16-07 mitigation).
    - File contains string `.not("published_at", "is", null)` (pin to published version).
    - File contains string `status: "completed"` checked in updateAssignment (D-03 immutability gate).
    - File contains string `deleted_at: new Date().toISOString()` in revokeAssignment.
    - File contains zero occurrences of `.delete(` (soft-delete convention).
    - File contains zero occurrences of `redirect(` (no navigation from this module).
    - `npx tsc --noEmit` reports no errors that reference app/admin/assignments/actions.ts.
  </acceptance_criteria>
  <done>
    Admin server actions are callable from any admin server component / client component. Checkbox primitive is available for Task 2's modal.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the shared AssignTemplateModal client component</name>
  <read_first>
    - components/admin/upload-document-modal.tsx (lines 1-220 — Dialog + DialogContent dark surface; native date input with colorScheme: dark on lines 163-174; form submit + setSubmitting pattern lines 39-83)
    - components/clients/new-client-dialog.tsx (lines 37-59 — try/catch + toast.success/error pattern)
    - components/ui/dialog.tsx (verifies the imported primitives are Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger)
    - components/ui/checkbox.tsx (created in Task 1 — confirms the import path and prop shape; shadcn Checkbox accepts `checked` and `onCheckedChange`)
    - components/ui/select.tsx (Select primitives for the template picker)
    - components/ui/label.tsx
    - components/ui/textarea.tsx
    - components/ui/input.tsx
    - app/admin/assignments/actions.ts (created in Task 1 — the modal imports createAssignments)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Component Inventory" + § "Per-Route Visual Contracts" → Route C → modal section + § "Copywriting Contract" (every user-facing string is locked)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-RESEARCH.md § "Pattern 3: Multi-Select Assign Modal" lines 497-557
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "components/admin/assign-template-modal.tsx" [File Classification row 8: components/admin/assign-template-modal.tsx]
  </read_first>
  <behavior>
    - Module is a `"use client"` component exporting `AssignTemplateModal`.
    - Props: `templateId?: string` (pre-fill template picker when launched from template page), `preselectClientId?: string` (pre-check one client when launched from client detail page), `templates?: Array<{ id: string; name: string }>` (picker options — required if templateId is not provided), `clients: Array<{ id: string; name: string }>`, `triggerLabel?: string` (defaults to "Assign to clients" if launched without preselectClientId, else "Assign template" — matches UI-SPEC copy contract).
    - Renders shadcn `<Dialog>` with a `<Button>` trigger using `triggerLabel`. DialogContent uses dark-surface classes from upload-document-modal.tsx (`bg-[#1c1c1c] border-white/10 text-white sm:max-w-[480px]`).
    - DialogTitle: `Assign template` (Newsreader 2xl per upload-document-modal.tsx pattern).
    - Form body has four sections (in this order): (a) Template `<Select>` picker — hidden when `templateId` prop is set, otherwise renders Select listing `templates`; (b) Clients checkbox grid wrapped in `max-h-48 overflow-y-auto`, one row per client showing `<Checkbox checked={selected.has(c.id)} onCheckedChange={(v) => toggle(c.id, v)} />` + `<Label>{c.name}</Label>`; preselectClientId pre-fills the Set; (c) Due date input: `<Input type="date" className="bg-black/50 border-white/10 text-white" style={{ colorScheme: "dark" }}>` initialised to today + 7 days as `YYYY-MM-DD`; (d) Instructions: `<Textarea placeholder="Optional instructions for the client…" rows={3}>`.
    - Submit button uses gold accent classes per UI-SPEC ("Assign to clients" primary CTA is gold on admin surface). Label: `Assign template` (UI-SPEC Copywriting Contract). Disabled when `selected.size === 0` OR no template selected OR pending.
    - Submit handler uses `useTransition` (NOT a `<form action>`). Inside startTransition: try `await createAssignments(selectedTemplate, Array.from(selected), { dueDate: dueDate || undefined, instructions: instructions.trim() || undefined })`, then `toast.success(\`Assigned to ${selected.size} client${selected.size > 1 ? "s" : ""}\`)` and `setOpen(false)`. Catch: `toast.error(err instanceof Error ? err.message : "Assignment failed — please try again or contact support")`. The validation-only "Select at least one client to assign" toast fires from a pre-submit guard (when the disabled button somehow gets activated, e.g. keyboard); when user clicks the disabled button (not possible but defensive), no-op.
    - On the validation path: if user removes all checkboxes after having selected some, the button becomes disabled — no toast needed because the disabled state is the affordance. The validation toast (`Select at least one client to assign`) fires only if the form is submitted programmatically with empty selection (e.g. via keyboard Enter when no clients are selected).
    - The modal resets form state on close.
  </behavior>
  <files>
    - components/admin/assign-template-modal.tsx (NEW)
  </files>
  <action>
    Create components/admin/assign-template-modal.tsx. Header line: `"use client";`. Imports: `import { useState, useTransition } from "react";`, `import { toast } from "sonner";`, `import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";`, `import { Button } from "@/components/ui/button";`, `import { Checkbox } from "@/components/ui/checkbox";`, `import { Label } from "@/components/ui/label";`, `import { Input } from "@/components/ui/input";`, `import { Textarea } from "@/components/ui/textarea";`, `import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";`, `import { createAssignments } from "@/app/admin/assignments/actions";`.

    Define the `interface AssignTemplateModalProps` per `<behavior>`. Compute default due_date inside a useState initialiser: `const d = new Date(); d.setDate(d.getDate() + 7); return d.toISOString().slice(0, 10);` (yields YYYY-MM-DD).

    Render structure: outer `<Dialog open={open} onOpenChange={setOpen}>`. Trigger uses `<DialogTrigger asChild><Button variant="outline" className="gap-2">...triggerLabel...</Button></DialogTrigger>` — variant outline per UI-SPEC Route C. DialogContent classes from upload-document-modal.tsx (dark surface). DialogHeader with DialogTitle "Assign template" using `font-serif text-2xl font-normal`.

    Below the header, render the four input sections. Use shadcn Label above each input with the mono `text-white/70 font-mono text-xs uppercase tracking-widest` style. Date input uses `style={{ colorScheme: "dark" }}` exactly as upload-document-modal.tsx line 173.

    Footer: `<div className="flex justify-end gap-2 mt-6">` with a "Cancel" outline Button + an Assign Button. Disabled state: `disabled={pending || !selectedTemplate || selectedClients.size === 0}`. When pending: render text "Assigning…"; otherwise "Assign template" per copywriting contract.

    onSubmit handler — guard, startTransition, await createAssignments, toast.success with the pluralised template string, setOpen(false). On catch, toast.error with err.message OR the locked "Assignment failed — please try again or contact support" string.

    Reset state on close: in `onOpenChange={(next) => { setOpen(next); if (!next) { setSelectedTemplate(templateId ?? ""); setSelectedClients(new Set(preselectClientId ? [preselectClientId] : [])); setDueDate(defaultDate); setInstructions(""); } }}`.

    Do NOT inline implementation as a fenced code block — author the component file directly.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('components/admin/assign-template-modal.tsx','utf8');for (const s of ['use client','useTransition','export function AssignTemplateModal','createAssignments(','toast.error','Assigned to ','colorScheme: \"dark\"','from \"@/components/ui/checkbox\"','from \"@/components/ui/dialog\"']) {if(!c.includes(s)){console.error('Missing:',s);process.exit(1);}} if(!c.match(/Assignment failed/i)){console.error('Locked error copy missing');process.exit(2);} console.log('OK')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File components/admin/assign-template-modal.tsx exists.
    - File begins with `"use client"`.
    - File exports `AssignTemplateModal`.
    - File contains `useTransition` (per RESEARCH "Don't Hand-Roll" — useTransition over `<form action>`).
    - File imports `createAssignments` from `@/app/admin/assignments/actions`.
    - File contains string `Assigned to ` (success toast prefix per UI-SPEC Copywriting Contract).
    - File contains string `Assignment failed — please try again or contact support` (server error toast per UI-SPEC).
    - File contains `colorScheme: "dark"` (matches upload-document-modal.tsx date-picker pattern).
    - File imports from `@/components/ui/checkbox`.
    - File contains `Cannot edit` only IF it implements an edit path (it does not in this plan — verify it does NOT contain that string; that lives in a future updateAssignment UI plan).
    - `npx tsc --noEmit` reports no new errors referencing assign-template-modal.tsx.
  </acceptance_criteria>
  <done>
    The shared modal is mountable from any admin route. Defaults: due_date = today + 7 days; instructions optional; multi-select clients via shadcn Checkbox.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount AssignTemplateModal on /admin/templates/[id] (Route C)</name>
  <read_first>
    - app/admin/templates/[id]/page.tsx (current page — read end-to-end to understand the header toolbar position and the existing Edit/Publish buttons that the new "Assign to clients" button slots beside)
    - app/admin/clients/page.tsx OR adminClient usage in any page.tsx (the clients list fetch pattern — Route C must pass `clients` prop to the modal so the picker has options)
    - lib/supabase/admin.ts
    - components/admin/assign-template-modal.tsx (created in Task 2)
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-UI-SPEC.md § "Route C — /admin/templates/[id]"
    - .planning/phases/16-multi-tenancy-fork-on-fill/16-PATTERNS.md § "Modify app/admin/templates/[id]/page.tsx" [File Classification row 17: MODIFY app/admin/templates/[id]/page.tsx]
  </read_first>
  <behavior>
    - app/admin/templates/[id]/page.tsx fetches `clients` (id, name) via `adminClient.from("clients").select("id, name").is("deleted_at", null).order("name")`.
    - The page renders `<AssignTemplateModal templateId={id} clients={clients ?? []} triggerLabel="Assign to clients" />` in the existing page header toolbar, alongside the existing Edit/Publish buttons.
    - No other behavior on the page changes (the template edit/publish flow is untouched).
    - The Assign button uses outline variant with lucide-react `<Share2 className="w-4 h-4" />` icon, per UI-SPEC Route C.
  </behavior>
  <files>
    - app/admin/templates/[id]/page.tsx (MODIFY)
  </files>
  <action>
    Read app/admin/templates/[id]/page.tsx in full. Identify the existing header toolbar (the JSX region rendering Edit + Publish buttons in the page header — likely inside a flex container near the top). Add a clients fetch alongside the existing template + version fetches at the top of the page function: `const { data: clients } = await adminClient.from("clients").select("id, name").is("deleted_at", null).order("name");`.

    Import the modal: `import { AssignTemplateModal } from "@/components/admin/assign-template-modal";`. Import `Share2` from `lucide-react` if the modal trigger Button does not already supply its own icon (the modal in Task 2 does not — pass the icon via children if the Button trigger accepts arbitrary content, else add an icon prop to the modal in a follow-up; for this task render the icon as a child of the DialogTrigger Button defined inside the modal — adjust Task 2's component if needed, but prefer to render icon inside the modal's Button so this page only writes `<AssignTemplateModal templateId={id} clients={clients ?? []} triggerLabel="Assign to clients" />`).

    Inside the existing header toolbar JSX, after the existing Edit/Publish buttons, add `<AssignTemplateModal templateId={id} clients={clients ?? []} triggerLabel="Assign to clients" />`. Use the same gap/flex container so the new button aligns visually with the others.

    Do NOT change any existing logic on the page (template fetch, version fetch, builder mount). The diff is additive only.

    If the page is currently a Server Component and the modal is `"use client"`, the modal renders as a client island — no other changes required.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('app/admin/templates/[id]/page.tsx','utf8');for (const s of ['AssignTemplateModal','templateId={id}','Assign to clients','from(\"clients\")']) {if(!c.includes(s)){console.error('Missing:',s);process.exit(1);}} console.log('OK')" && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File app/admin/templates/[id]/page.tsx contains `AssignTemplateModal`.
    - File contains `templateId={id}` (the prop wiring).
    - File contains `Assign to clients` literal (UI-SPEC Route C trigger copy).
    - File contains `from("clients")` (the clients fetch).
    - File still contains the prior Edit/Publish trigger references (do NOT delete existing functionality — verify by grepping for any pre-existing identifier you observed during Read).
    - `npm run build` completes successfully (no TypeScript errors, no missing-import errors on the modified page).
  </acceptance_criteria>
  <done>
    Visiting `/admin/templates/[some-published-template-id]` in dev shows an "Assign to clients" button next to Edit/Publish. Clicking it opens the modal with the template pre-selected and the full clients list available for checkbox multi-select.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server action | Untrusted POST; auth gate is `requireActorUserId('admin')` |
| client component → server action | Direct invocation via React; same auth gate enforced server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | createAssignments invoked with forged client_ids by a non-admin | mitigate | `requireActorUserId("admin")` is the first statement of every exported server action. RLS policy `form_assignments_admin_all` is the secondary gate at the DB level. |
| T-16-07 | Tampering | Server action invocable via direct POST | mitigate | Same — every exported function begins with `await requireActorUserId("admin")`. Without admin claim, the call throws Unauthorized before any DB write. Verified by grep gate in Task 1 acceptance criteria. |
| T-16-05 | Tampering | Editing a completed assignment to revert it back to in_progress | mitigate | updateAssignment reads current status; throws "Cannot edit a completed assignment" when status === "completed" (D-03 immutability). |
| T-16-08 | Information Disclosure | Revoked assignments leaking to admin queue/tab via missing deleted_at filter | mitigate (deferred to plans 03+) | revokeAssignment writes `deleted_at`; consumer queries in Plan 03 must apply `.is("deleted_at", null)`. |
</threat_model>

<verification>
After all three tasks:
- `npm run build` completes successfully.
- Manual smoke (dev only): visit `/admin/templates/[id]` where id is a published template; observe the new outline button; click it; observe the modal with the template name pre-shown, full clients list, due_date defaulting to today+7, instructions textarea. Submit selects 1+ clients → toast "Assigned to N client(s)" → row(s) visible in DB via Supabase studio.
</verification>

<success_criteria>
- components/ui/checkbox.tsx exists (shadcn primitive).
- app/admin/assignments/actions.ts exports createAssignments / updateAssignment / revokeAssignment with `requireActorUserId("admin")` as the first statement of each.
- components/admin/assign-template-modal.tsx exports AssignTemplateModal with the prop shape declared in `<behavior>`.
- app/admin/templates/[id]/page.tsx mounts the modal in the header toolbar.
- `npm run build` succeeds.
- No existing admin route functionality is regressed.
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-tenancy-fork-on-fill/16-02-SUMMARY.md` recording: the three exported server-action signatures, the modal's prop interface, the mounting site on `/admin/templates/[id]`, and the locked UI copywriting strings actually shipped.
</output>
