import { describe, it } from "vitest";

describe("Field Palette (BUILDER-01)", () => {
  it.todo(
    "clicking a palette button adds an entity to the builder store with the correct type"
  );
  it.todo(
    "adding a textField entity populates default attribute values (label empty string, required false)"
  );
  it.todo(
    "adding a sectionGroup entity adds it to root and its children list is empty"
  );
});
