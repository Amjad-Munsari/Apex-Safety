import { describe, it } from "vitest";

describe("validateEntitiesValues", () => {
  it.todo(
    "validateEntitiesValues rejects a value for a required textField when the value is empty"
  );
  it.todo(
    "validateEntitiesValues accepts valid values for all 7 entity types"
  );
  it.todo(
    "validateEntitiesValues rejects values whose entity IDs are not present in the schema"
  );
  it.todo(
    "validateEntitiesValues server action re-validates after a client submission"
  );
});
