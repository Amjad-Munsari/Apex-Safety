// Phase 15 Wave-0 stub — populated by Wave 1+
// Plan 15-02 Task 2 fills in the real assertions.
import { describe, it } from "vitest";

// NOTE: Do NOT statically import production modules — Wave 1 tasks add
// the `await import("@/lib/form-builder/visibility/...")` calls when they implement.

describe("cascadeVisibility — parent-child hide cascade", () => {
  it.todo("hidden parent causes all children to be hidden (sectionGroup cascade)");
  it.todo("hidden sectionGroup causes grandchildren (nested fields) to be hidden");
  it.todo("repeatingSection per-instance cascade: hiding the container hides all instance children");
});
