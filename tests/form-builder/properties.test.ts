import { describe, it } from "vitest";

describe("Properties Panel (BUILDER-02)", () => {
  it.todo(
    "setEntityAttribute updates the attribute value in the builder store"
  );
  it.todo(
    "setting label to empty string triggers attribute validation error"
  );
  it.todo(
    "setting required to true is reflected in the entity attributes"
  );
  it.todo(
    "properties panel renders the correct attribute inputs for each entity type"
  );
});
